# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008772.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008772

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 조건 문장이 있고, 가운데에는 물을 붓는 상황을 나타내는 간단한 그림이 배치되어 있습니다.
- 오른쪽 아래에는 괄호로 된 선택지 형태의 텍스트가 있으며, 하단 왼쪽에는 `(정답)` 표시와 `(해설)` 문장이 따로 있습니다.
- 전체적으로 글 + 삽화 + 정답/해설이 분리된 초등 수학·과학형 문제 레이아웃입니다.

[보이는 텍스트]
- `□ 82.`
- `가 물병에 물을 가득 채운 후 나 물병에 옮겨 담았습니다. 그림과 같이`
- `물을 채웠을 때에 들이가 더 작은 물병을 선택해 보세요.`
- 그림 속 표기: `가`, `나`
- 오른쪽 아래 선택지 표기: `( 가 물병, 나 물병 )`
- 하단 왼쪽: `(정답) 가 물병`
- 하단 왼쪽 해설: `(해설) 가 물병의 물이 나 물병에 다 들어가므로 가 물병의 들이가 더 적습니다.`

[반복되는 시각 패턴]
- 원 안에 들어간 한글 표기 `가`, `나`가 문제 설명, 그림, 선택지, 해설에서 반복됩니다.
- `가 물병`, `나 물병`이라는 명칭이 여러 위치에 반복되어, 그림 속 대상과 문장 속 대상이 연결되도록 되어 있습니다.
- 선택지 표기는 괄호 안에 두 항목이 쉼표로 구분되어 제시되는 형식입니다.

[수식 단계]
- 해당 없음: 이 문제는 등호로 이어지는 수식 변형이 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음: 수식 등호 단계가 없습니다.

[세부 내용]
- 그림은 두 개의 물병을 보여 줍니다.
  - 위쪽에는 비스듬히 기울어진 물병이 있고, 그 안/옆에 `가` 표시가 있습니다.
  - 아래쪽에는 세워진 물병이 있고, 내부에 물이 채워진 모습과 함께 `나` 표시가 있습니다.
- 위쪽 물병에서 아래쪽 물병으로 물이 붓는 상황을 선으로 표현한 듯한 연결 구도가 있습니다.
- 아래쪽 물병은 입구가 좁고 몸통이 길며, 내부에 물 높이를 나타내는 파란 음영이 보입니다.
- 위쪽 물병은 비교적 더 길고, 기울어진 상태로 입구 쪽이 아래를 향하고 있습니다.
- 문제의 핵심은 “그림과 같이 물을 채웠을 때 더 작은 들이의 물병”을 고르는 것이지만, 여기서는 답을 추론하지 않고 선택지에 제시된 두 물병 이름만 확인합니다.

[Python DSL 작성 힌트]
- 문제 텍스트는 상단 문장 2줄로 나누어 배치하면 좋습니다.
- 그림은 중앙에 크게 배치하고, 두 물병을 각각 별도 도형/이미지 요소로 분리해 표현할 수 있습니다.
- `가`, `나` 표시는 원형 라벨처럼 보이므로, 텍스트를 원형 배지 위에 올리는 방식이 적절합니다.
- 오른쪽 아래의 선택지는 괄호와 쉼표를 포함한 한 줄 텍스트로 처리하면 됩니다.
- 하단의 `(정답)`, `(해설)`은 별도 텍스트 블록으로 분리해 배치하면 원본 구조를 잘 반영할 수 있습니다.
- 색상은 주로 검은 글자와 하늘색/파란색 물병 선, 물의 채움 표현으로 구성됩니다.

[주의할 점]
- 정답을 보고서 안에서 직접 확정적으로 활용하지 말고, 분석 초안에는 선택지만 기록해야 합니다.
- `가`와 `나`의 위치를 바꾸어 해석하지 않도록 주의해야 합니다. 그림 속 위쪽 물병이 `가`, 아래쪽 물병이 `나`로 보입니다.
- 해설 문장은 보이지만, 그 내용을 근거로 답을 다시 계산하거나 서술하지 마세요.
- 그림의 물 높이, 병의 형태, 기울기 등은 시각적으로 보이는 것만 기술하고 수치화하지 마세요.

[불확실한 부분]
- 문제 왼쪽 맨 앞의 작은 네모 표시는 체크박스인지 인쇄용 기호인지 확실하지 않습니다.
- 물병의 정확한 윤곽선 세부는 이미지 해상도상 다소 흐릿하여, 병목과 몸통의 경계가 아주 정밀하게 구분되지는 않습니다.
- 아래쪽 물병 내부의 물 채움 범위는 그림의 스타일상 일부 장식처럼 보일 수 있어, 실제 물 높이를 엄밀히 수치화하기는 어렵습니다.

--- RAW VISION DRAFT END ---
