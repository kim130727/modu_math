# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008968.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008968

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 좌측 상단에 문제 번호가 있고, 그 뒤로 본문 지시문이 2줄 정도로 배치되어 있습니다.
- 본문 아래에는 연한 초록색 계열의 표가 가운데에 놓여 있으며, 나무 종류와 높이(cm)를 비교하는 형태입니다.
- 표 아래쪽 오른편에는 선택지처럼 보이는 괄호 안 텍스트가 따로 배치되어 있습니다.
- 그 아래에는 `정답`, `풀이`, 여러 개의 계산식과 결론 문장이 순서대로 세로 배치되어 있습니다.
- 전체적으로 “문제 제시 → 표 제시 → 선택/풀이 제시” 구조입니다.

[보이는 텍스트]
- `2. 현주는 집 주변 나무들의 높이를 조사했습니다. 두 나무의 높이 차가 100 cm에 가장 가까운 나무는 어떤 나무와 어떤 나무인지 알맞은 것을 선택하세요.`
- 표의 열 제목:
  - `나무 종류`
  - `소나무`
  - `전나무`
  - `벚나무`
- 표의 행 제목:
  - `높이(cm)`
- 각 높이 수치:
  - `247`
  - `356`
  - `115`
- 표 오른쪽 아래에 괄호로 묶인 텍스트:
  - `( 소나무, 전나무, 벚나무 )`
- 아래 풀이 부분의 보이는 텍스트:
  - `(정답) 소나무, 전나무`
  - `(풀이)`
  - `(전나무) - (소나무) = 356 - 247 = 109(cm)`
  - `(소나무) - (벚나무) = 247 - 115 = 132(cm)`
  - `(전나무) - (벚나무) = 356 - 115 = 241(cm)`
  - `두 나무의 높이의 차가 100 cm에 가장 가까운 나무는 소나무와 전나무입니다.`

[반복되는 시각 패턴]
- 표 안에서 나무 종류가 가로 방향으로 3개 나란히 배치되어 있고, 각 나무에 대응하는 높이 숫자도 바로 아래 같은 열에 정렬되어 있습니다.
- 풀이에서는 두 나무를 괄호로 묶어 비교하는 식이 3번 반복됩니다.
  - `A - B = 수치`
  - `A - C = 수치`
  - `B - C = 수치`
- 각 식은 “큰 수에서 작은 수를 빼는 형태”로 보이며, 괄호로 나무 이름을 먼저 표시한 뒤 실제 숫자를 대입한 구조입니다.
- 마지막 결론 문장은 정답을 한 문장으로 다시 요약하는 반복 패턴입니다.

[수식 단계]
- 표 자체에는 수식 변형 단계가 없고, 아래 풀이 부분에만 계산식이 있습니다.
- 첫 번째 식:
  - `(전나무) - (소나무) = 356 - 247 = 109(cm)`
- 두 번째 식:
  - `(소나무) - (벚나무) = 247 - 115 = 132(cm)`
- 세 번째 식:
  - `(전나무) - (벚나무) = 356 - 115 = 241(cm)`
- 각 식은 하나의 등호로 연결된 연산 결과를 연속해서 보여주는 형태입니다.
- 등호 앞쪽은 나무 이름 비교식, 가운데는 숫자 대입식, 마지막은 계산 결과로 보입니다.

[등호별 변환 의미]
- `356 - 247 = 109(cm)`
  - 전나무 높이에서 소나무 높이를 뺀 계산 과정을 나타냅니다.
  - 왼쪽의 식 기호와 오른쪽의 숫자 대입이 대응됩니다.
- `247 - 115 = 132(cm)`
  - 소나무와 벚나무의 높이 차를 계산한 것으로 보입니다.
- `356 - 115 = 241(cm)`
  - 전나무와 벚나무의 높이 차를 계산한 것으로 보입니다.
- 각 등호는 “같다”는 관계를 단계적으로 이어서 보여주는 것이 아니라, 식을 숫자로 바꾼 뒤 결과를 적는 구조로 보입니다.

[세부 내용]
- 문제는 나무 3종의 높이를 표로 제시하고, 높이 차가 `100 cm`에 가장 가까운 두 나무를 고르는 형식입니다.
- 표는 2행 4열처럼 보이며, 맨 왼쪽 첫 칸에는 항목명, 나머지 3칸에는 나무 이름과 높이가 들어 있습니다.
- 표의 위아래 테두리와 칸 구분선이 연한 초록색으로 표시되어 있습니다.
- 나무 이름과 높이는 각 열 아래로 정확히 대응되도록 배치되어 있습니다.
- 풀이 부분은 표 아래 왼쪽에 텍스트로 정렬되어 있고, 정답은 풀이보다 위쪽에 따로 표시되어 있습니다.
- 마지막 문장은 선택 결과를 자연어로 설명하는 문장입니다.

[Python DSL 작성 힌트]
- 표를 먼저 구성한 뒤, 각 열에 `나무 종류`와 `높이(cm)` 정보를 넣는 방식이 적절해 보입니다.
- 표의 셀은 가운데 정렬에 가깝게 배치되어 있으며, 숫자 셀은 굵게 보이거나 진하게 표시된 것처럼 보입니다.
- 문제 문장은 표 위에 두 줄로 배치하고, 표 아래에는 정답/풀이 텍스트 블록을 별도로 배치하면 좋습니다.
- 풀이의 계산식은 세 줄을 세로로 쌓아 배치하면 화면 구조를 잘 반영할 수 있습니다.
- 선택지처럼 보이는 `( 소나무, 전나무, 벚나무 )`는 별도 텍스트 요소로 오른쪽 아래에 배치하는 것이 자연스럽습니다.
- 나무 이름과 숫자 사이의 대응 관계가 중요하므로, 표의 열 순서를 그대로 유지하는 것이 좋습니다.

[주의할 점]
- 정답을 다시 계산하거나 새로 추론하여 서술하지 마세요.
- 빈칸은 보이지 않으며, 선택지 형식의 괄호 텍스트만 보입니다.
- 표의 숫자를 다른 값으로 바꾸거나, 나무 순서를 임의로 재배열하지 마세요.
- 풀이의 계산 결과가 화면에 이미 적혀 있으므로, 이를 다른 방식으로 바꾸지 마세요.
- “가장 가까운”이라는 표현은 문제의 요구사항일 뿐, 여기서는 해결 과정을 추가로 설명하지 않는 것이 좋습니다.

[불확실한 부분]
- 표의 정확한 행 수/열 경계는 이미지 해상도 때문에 약간 흐릿하게 보일 수 있습니다.
- `( 소나무, 전나무, 벚나무 )` 텍스트는 선택지인지 안내 문구인지 화면상 완전히 구분되지는 않지만, 오른쪽 아래에 별도 표시된 텍스트로는 분명히 보입니다.
- `정답`과 `풀이`의 글씨 크기 및 강조 정도는 이미지 품질상 완전히 단정하기 어렵습니다.

--- RAW VISION DRAFT END ---
