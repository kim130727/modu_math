[전체적인 구성]
- 화면 상단에 문제 번호와 본문 지시문이 있고, 그 아래에 저울 그림 2개가 좌우로 배치되어 있습니다.
- 그림 아래에는 비교 과정을 설명하는 문장이 가운데 정렬로 2줄 정도 배치되어 있습니다.
- 그 아래쪽에는 `(정답)` 표시가 붙은 짧은 답안 줄이 있고, 이어서 `(해설)` 문장이 더 길게 제시되어 있습니다.
- 전체적으로 “그림을 보고 가장 가벼운 물건을 찾는 비교 문제” 형태입니다.

[보이는 텍스트]
- `□ 67. 저울을 사용하여 포크, 국자, 숟가락 중에서 가장 가벼운 물건은 무엇인지 알아보려고 할 때 알맞은 말을 선택하세요.`
- 그림 위 라벨:
  - `포크`
  - `국자`
  - `국자`
  - `숟가락`
- 그림 아래 설명문:
  - `포크와 국자 중 ( 포크 , 국자 )가 더 가볍고,`
  - `국자와 숟가락 중 ( 국자 , 숟가락 )이/가 더 가벼우므로`
  - `포크와 ( 국자 , 숟가락 )의 무게를 비교해야 합니다.`
- 하단:
  - `(정답)포크, 숟가락, 숟가락`
  - `(해설)포크와 국자 중 포크가 더 가볍고, 국자와 숟가락 중 숟가락이 더 가벼우므로 포크와 숟가락의 무게를 비교해야 합니다.`

[반복되는 시각 패턴]
- 저울 그림이 2개 반복됩니다.
  - 왼쪽 저울: 왼쪽 접시에 `포크`, 오른쪽 접시에 `국자`
  - 오른쪽 저울: 왼쪽 접시에 `국자`, 오른쪽 접시에 `숟가락`
- 각 저울 위에는 물건 이름이 작은 연한 배경의 라벨 상자로 표시되어 있습니다.
- 저울은 중앙 받침과 양쪽 접시가 있는 동일한 형태이며, 비교 대상만 바뀌어 있습니다.
- 문장 안에는 괄호 `( )` 형태의 선택 슬롯이 3곳 보입니다.
  - 첫 번째는 `( 포크 , 국자 )`
  - 두 번째는 `( 국자 , 숟가락 )`
  - 세 번째는 `( 국자 , 숟가락 )`처럼 보이는 선택 대상 위치
- 이 문제는 “더 가벼운 것 선택” 패턴이 반복되는 비교형 문제입니다.

[수식 단계]
- 해당 없음
- 등호로 이어지는 계산식이나 변형식은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음
- 수식의 좌변/우변 변화, 등호 연쇄, 항의 이동 같은 표현은 없습니다.

[세부 내용]
- 왼쪽 그림의 저울은 왼쪽 접시가 내려가 있고, 오른쪽 접시가 올라가 보입니다.  
  - 다만 이 시각적 경사만으로 정확한 비교 방향을 단정할 때는 주의가 필요합니다.
- 오른쪽 그림도 한쪽이 내려가 보이는 형태이며, 물건 배치가 서로 반대 방향으로 되어 있습니다.
- 두 그림 모두 물건 이름 라벨이 각 접시 위쪽에 따로 붙어 있어, 어떤 물건이 어느 쪽 접시에 있는지 시각적으로 구분할 수 있습니다.
- 본문 설명문은 괄호 안의 말을 고르는 방식으로 되어 있어, 문장 완성형 선택 문제입니다.
- 하단 `(정답)`과 `(해설)`은 화면에 인쇄되어 있지만, Python DSL 작성 시에는 보통 정답 영역을 별도로 처리할지 여부를 확인해야 합니다.
- 그림과 문장 사이의 간격이 비교적 넓고, 설명문은 중앙 정렬에 가깝게 배치되어 있습니다.

[Python DSL 작성 힌트]
- 먼저 문제 본문을 텍스트 블록으로 배치하고, 그 아래에 저울 그림 2개를 가로로 나란히 배치하는 구조가 적절합니다.
- 각 저울은 하나의 도형 그룹으로 묶고,
  - 저울 본체
  - 왼쪽 접시
  - 오른쪽 접시
  - 각 접시에 놓인 물건 그림
  - 물건 위 라벨 상자
  를 별도 요소로 분리하면 좋습니다.
- 설명문은 줄바꿈을 포함한 문장 블록으로 처리하고, 괄호 선택지 부분은 텍스트 그대로 유지하는 방식이 적합합니다.
- `(정답)`과 `(해설)`은 하단 보조 텍스트로 배치할 수 있습니다.
- 저울 기울기, 접시 위치, 물건의 배치는 그림 요소의 상대좌표를 활용해 표현하는 것이 좋습니다.
- 선택형 빈칸은 실제로 비어 있는 슬롯이 아니라, 괄호 안 선택지 중 하나를 고르는 구조이므로 슬롯 처리 여부를 구분해야 합니다.

[주의할 점]
- 문제를 풀어서 답을 새로 계산하지 마세요.
- `(포크, 국자)` 같은 괄호 안 표현을 임의로 바꾸거나 빈칸 답으로 확정하지 마세요.
- 저울의 기울기 방향을 텍스트 해설과 혼동하지 않도록 주의하세요.
- 하단의 `(정답)`은 시각 정보로는 보이지만, 원문 문제의 핵심 지시문과 별개이므로 DSL에서 포함/제외 기준을 정해야 합니다.
- 그림 속 물건 이름은 모두 한글로 표시되어 있으며, 동일한 물건명 라벨이 두 저울에서 중복 사용됩니다.
- “가볍다/무겁다”의 판단을 시각 분석 초안에서 단정적으로 서술하지 말고, 문제에 적힌 설명문 그대로 옮겨 적는 수준으로 유지하세요.

[불확실한 부분]
- 저울 접시가 실제로 어느 쪽이 더 내려갔는지, 이미지 해상도상 약간 애매한 부분이 있습니다.
- 본문 문장 중 `( 국자 , 숟가락 )이/가`처럼 조사 선택이 포함된 부분은 인쇄 상태에 따라 띄어쓰기나 괄호 간격이 다르게 보일 수 있습니다.
- `(정답)`과 `(해설)`이 원래 학생용 문제지에 항상 포함되는지, 혹은 해설 포함 이미지인지 여부는 확정할 수 없습니다.
- 저울 그림의 정확한 비교 결과를 이 초안에서 단정하지 않는 것이 안전합니다.
