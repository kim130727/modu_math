# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008794.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008794

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 화면 왼쪽 위에 문제 번호와 지시문이 있고, 오른쪽 상단에는 주사기 그림이 배치되어 있습니다.
- 중앙 오른쪽에는 보라색 테두리의 가로로 긴 선택 박스가 있으며, 그 안에 단위 표기가 좌우로 나뉘어 놓여 있습니다.
- 하단 왼쪽에는 정답 표시와 해설 문장이 별도로 인쇄되어 있습니다.
- 전체적으로 “그림을 보고 알맞은 단위를 고르는 객관식형 선택”처럼 보이는 구성입니다.

# [보이는 텍스트]
- `□ 19. 알맞은 단위를 선택하세요.`
- 선택 박스 안:
  - `mL`
  - `L`
- 하단:
  - `(정답)mL`
  - `(해설)주사기의 들이 단위는 mL입니다.`

# [반복되는 시각 패턴]
- 단위 선택용으로 보이는 큰 사각형 박스가 1개 있고, 그 안에 단위 두 개가 좌우로 분리되어 배치되어 있습니다.
- 텍스트 `mL`와 `L`은 서로 같은 형식의 단위 표기이며, 글자 크기와 위치가 균형 있게 배치되어 있습니다.
- 문제 번호 앞의 작은 네모 표시는 체크박스처럼 보이는 요소입니다.
- 하단의 `(정답)`, `(해설)`은 괄호가 포함된 동일한 스타일의 안내 문구 패턴입니다.

# [수식 단계]
- 해당 없음.  
- 보이는 것은 수식 전개가 아니라 단위 선택 문제입니다.

# [등호별 변환 의미]
- 해당 없음.  
- 등호로 연결된 변형식이나 계산 과정은 보이지 않습니다.

# [세부 내용]
- 오른쪽 위의 그림은 주사기(의료용 주입기)로 보이며, 바늘과 원통형 몸체가 세밀하게 그려져 있습니다.
- 그림은 선택 박스 위쪽에 떠 있는 듯 배치되어 있어, 그림을 보고 단위를 판단하게 하는 구조로 보입니다.
- 선택 박스는 연보라색 계열의 테두리로 둘러싸인 긴 직사각형이며, 내부는 두 칸처럼 보이도록 `mL`가 왼쪽, `L`이 오른쪽에 배치되어 있습니다.
- 두 단위 사이에는 별도의 선이나 화살표는 없고, 같은 박스 안에서 단지 위치만 나뉘어 있습니다.
- 하단의 정답/해설 문구는 문제 본문과는 구분되는 설명 영역처럼 보입니다.
- `(해설)주사기의 들이 단위는 mL입니다.`라는 문장은 단위를 직접 설명하지만, 이는 화면에 인쇄된 해설로만 처리해야 합니다.

# [Python DSL 작성 힌트]
- 문제 본문 영역, 그림 영역, 선택 박스 영역, 하단 해설 영역을 서로 분리해서 배치하는 방식이 적절합니다.
- 선택 박스는 하나의 큰 직사각형으로 그리고, 그 내부에 좌우 두 텍스트 `mL`, `L`을 정렬하면 좋습니다.
- 주사기 그림은 선택지 위쪽 중앙 부근에 배치하면 원본 구성과 유사합니다.
- 하단의 `(정답)`과 `(해설)`은 본문과 다른 텍스트 블록으로 분리해 넣는 것이 좋습니다.
- 정답값을 DSL에서 자동 채우지 말고, 화면에 보이는 텍스트로만 반영하세요.

# [주의할 점]
- 정답을 추론해서 답안 칸을 채우지 마세요.
- 문제 풀이 과정으로 오해될 만한 설명을 추가하지 마세요.
- 주사기 그림의 세부 구조를 과도하게 단정하지 말고, 보이는 범위에서만 기술하세요.
- 선택 박스 내부의 정확한 선 분할 여부는 이미지에서 명확히 보이는 범위까지만 서술하세요.
- 하단의 `(정답)mL`는 화면에 이미 표시된 정보이므로, 이를 새로운 추론 결과처럼 쓰지 마세요.

# [불확실한 부분]
- 주사기 그림이 정확히 몇 개의 세부 부품으로 그려졌는지는 이미지 해상도상 일부 불명확합니다.
- 선택 박스 내부가 실제로 두 개의 칸으로 나뉜 것인지, 아니면 단순히 한 박스 안에 두 단위가 떨어져 배치된 것인지 경계선이 뚜렷하지 않습니다.
- 문제 제목 앞의 작은 네모(□)가 체크박스인지 편집 표시인지 확정하기 어렵습니다.

--- RAW VISION DRAFT END ---
