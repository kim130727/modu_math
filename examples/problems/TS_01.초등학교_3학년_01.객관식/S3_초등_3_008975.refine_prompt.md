# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008975.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008975

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]

- 한 문제짜리 선택형 문항입니다.
- 좌측 상단에 `□ 10.` 형태의 번호가 있고, 그 오른쪽에 문제 지시문 `반직선을 선택하세요.`가 크게 인쇄되어 있습니다.
- 본문에는 여러 개의 선 그림이 흩어져 배치되어 있으며, 그중 하나는 아래쪽 왼편에 `(정답)` 표시와 함께 따로 제시되어 있습니다.
- 하단에는 `(해설)` 표시와 함께 반직선의 정의를 설명하는 문장이 한 줄로 들어 있습니다.
- 전체적으로 “문제 문장 + 그림 선택지들 + 정답 표시 + 해설 문장” 구조입니다.

[보이는 텍스트]

- `□ 10.`
- `반직선을 선택하세요.`
- `(정답)`
- `(해설)한 점에서 한쪽으로 끝없이 늘인 곧은 선을 찾습니다.`

[반복되는 시각 패턴]

- 여러 개의 선분/선 모양이 비슷한 굵기로 제시되어 있습니다.
- 각 그림은 검은색 가는 선으로 표현되어 있고, 끝점 또는 기준점처럼 보이는 작은 점이 함께 표시되어 있습니다.
- 위쪽에는 서로 다른 유형의 선 그림들이 가로로 분산 배치되어 있습니다.
- 아래쪽 왼편에는 `(정답)` 텍스트 옆에 하나의 선 그림이 별도로 배치되어 있어, 다른 그림과 구분됩니다.
- 전체적으로 도형 선택 문제에서 흔한 “보기 그림 여러 개 + 정답 그림 강조” 패턴입니다.

[수식 단계]

- 해당 없음.
- 이 문항은 수식 전개나 등호 변형이 아니라 도형/개념 선택 문제입니다.

[등호별 변환 의미]

- 해당 없음.
- 등호를 포함한 변형식이 보이지 않습니다.

[세부 내용]

- 상단 문제 번호는 `10.`으로 보이며, 앞에 작은 체크 박스처럼 보이는 빈 사각형이 있습니다.
- 문제 문장 `반직선을 선택하세요.`는 왼쪽 상단에 비교적 큰 글씨로 제시되어 있습니다.
- 그림은 총 4개 정도로 보입니다.
  - 왼쪽 위: 곡선처럼 휘어진 선이 점 2개 근처를 연결한 형태로 보입니다.
  - 가운데 위: 꺾인 선처럼 보이는 두 구간이 만나는 형태가 있습니다. 한 점에서 방향이 바뀌는 모양입니다.
  - 오른쪽 위: 직선에 가까운 길쭉한 선이 비스듬히 놓여 있고, 양 끝에 점이 있는 듯 보입니다.
  - 왼쪽 아래: `(정답)` 표시 옆에 비스듬한 직선 모양이 있으며, 한쪽 끝에 점이 있고 반대쪽은 더 길게 뻗은 형태처럼 보입니다.
- 해설 문장은 아래쪽에 가로로 길게 배치되어 있고, 괄호 `(`, `)`로 시작하는 라벨이 붙어 있습니다.
- 해설의 핵심 표현은 `한 점에서 한쪽으로 끝없이 늘인 곧은 선`입니다.  
  - 이는 문제를 푸는 설명 문장으로 보이지만, 여기서는 문장 자체만 시각적으로 확인할 수 있습니다.

[Python DSL 작성 힌트]

- 문제 영역을 상단 텍스트, 중앙 그림 영역, 하단 해설 영역으로 나누어 배치하면 좋습니다.
- 그림들은 각각 독립된 객체로 두고, 좌표를 달리하여 좌우로 흩어지게 배치하는 방식이 어울립니다.
- 각 선은 굵은 검은 선으로, 끝점에는 작은 점(원형 마커)이 포함된 형태로 표현할 수 있습니다.
- `(정답)` 라벨은 아래쪽 왼편 그림 바로 옆에 붙여 배치합니다.
- 해설 문장은 작은 글씨로 하단에 한 줄 배치하고, `(해설)` 라벨과 본문을 이어서 적습니다.
- 선택형 문항이므로 정답 칸을 채우는 구조보다는 “보기 그림 중 하나를 고르는” 형태로 구현하는 것이 자연스럽습니다.

[주의할 점]

- 반직선을 직접 풀어서 설명하거나 정답을 단정하지 마세요.
- 보이는 도형의 정확한 종류는 일부 그림이 선분인지 반직선인지, 또는 곡선인지 애매할 수 있으므로 단정적인 명칭 사용에 주의해야 합니다.
- `(정답)` 표시가 특정 그림 옆에 있으나, 이것을 문제 풀이 결과로 확장해 서술하지 마세요.
- 해설 문장에 있는 개념 설명을 정답 확정 근거처럼 과도하게 해석하지 마세요.
- 화면 하단 문장은 일부 잘려 보이지 않게 인쇄된 형태일 수 있으니, 보이는 범위 안에서만 기술하세요.

[불확실한 부분]

- 위쪽 왼쪽 그림은 곡선인지, 단순한 굽은 선 표기인지 화면 해상도상 약간 애매합니다.
- 위쪽 가운데 그림은 선분인지 반직선인지, 혹은 꺾인 선의 일부인지 정확히 판별이 어려운 부분이 있습니다.
- 오른쪽 위 그림의 양 끝 점 표시는 끝점인지 단순 표시점인지 다소 अस्प명합니다.
- 아래쪽 `(정답)` 옆 도형이 실제로 어떤 성질의 선인지, 이미지 해상도만으로는 세부 판별이 완전하지 않습니다.

--- RAW VISION DRAFT END ---
