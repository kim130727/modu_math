# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008828.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008828

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면은 1문항형 구성으로 보이며, 좌상단에 문제 번호와 지시문이 있고, 중앙 오른쪽에는 요거트병 그림이 배치되어 있습니다.
- 본문에는 단위 선택형 문장이 크게 제시되고, 아래쪽에는 정답 표시와 해설 문장이 따로 있습니다.
- 전체적으로 “알맞은 단위 선택” 문제 형식이며, 괄호 안의 단위 후보 중 하나를 고르는 구조입니다.

[보이는 텍스트]
- `□ 61. 알맞은 단위를 선택하세요.`
- 그림 위/오른쪽에 요거트병 일러스트
- 가운데 문장:
  - `요구르트병의 들이는 약 80( mL , L )입니다.`
- 아래쪽:
  - `(정답)mL`
  - `(해설)요구르트병의 들이는 80 L보다 더 적으므로 80 mL가 알맞습니다.`
- 문장 내에 괄호로 둘러싸인 단위 선택지가 보이며, `mL`와 `L`가 쉼표로 나열되어 있습니다.

[반복되는 시각 패턴]
- 괄호 형태의 선택지 패턴: `80( mL , L )`
- 문제 지시문과 해설이 분리되어 있는 패턴
- 본문 문장과 아래 정답/해설 문장이 세로로 구분되어 배치된 패턴
- 단위 선택 문제에서 흔히 보이는 “숫자 + 괄호 안 단위 후보” 형식

[수식 단계]
- 해당 없음
- 등호나 계산식이 보이지 않으며, 수식 변형 단계도 없습니다.

[등호별 변환 의미]
- 해당 없음
- 등호 기호가 사용되지 않았습니다.

[세부 내용]
- 좌측 상단에 작은 네모 기호 `□`가 먼저 있고, 그 뒤에 문제 번호 `61.`이 이어집니다.
- 지시문 `알맞은 단위를 선택하세요.`는 문제 번호 뒤에 한 줄로 이어져 있습니다.
- 본문 문장은 화면 중앙보다 약간 아래에 크게 배치되어 있으며, 문장 중간에 숫자 `80`과 단위 후보 괄호가 삽입된 형태입니다.
- 괄호 안의 단위는 `mL`와 `L` 두 가지이며, 둘 사이에 쉼표가 있습니다.
- 오른쪽 위쪽에는 작은 요거트병 그림이 있어, 문장 속 대상이 무엇인지 시각적으로 보조합니다.
- 하단의 `(정답)mL`는 정답 표시로 보이며, 그 아래 `(해설)` 뒤에 설명 문장이 이어집니다.
- 해설 문장에서는 `80 L보다 더 적으므로`라는 표현이 보이지만, 이는 설명 텍스트일 뿐 문제 풀이 과정 자체는 아닙니다.

[Python DSL 작성 힌트]
- 문제 영역을 상단 지시문, 중앙 본문, 우측 그림, 하단 정답/해설의 4개 블록으로 나누어 배치하면 좋습니다.
- 본문은 `80`과 괄호 안 선택지 `mL, L`을 한 줄에 포함한 텍스트 오브젝트로 처리하면 됩니다.
- 선택지 부분은 텍스트 박스 안에서 괄호와 쉼표 위치를 그대로 유지하는 것이 중요합니다.
- 그림은 텍스트와 분리된 독립 오브젝트로 두고, 본문 문장과 시각적으로 대응되도록 오른쪽 상단에 놓으면 유사합니다.
- 하단의 정답/해설은 별도 설명 박스로 분리하는 것이 좋습니다.
- `□ 61.` 같은 시작 기호와 번호는 좌상단 정렬로 배치하는 것을 권장합니다.

[주의할 점]
- 정답을 추론하여 본문 빈칸처럼 채워 넣지 마세요. 화면에는 이미 `(정답)mL`가 보이지만, 이는 시각 자료의 일부로만 다뤄야 합니다.
- `80` 뒤의 단위 선택지는 빈칸이 아니라 괄호형 선택지입니다.
- 해설 문장의 내용은 보이지만, 이를 바탕으로 추가 계산이나 다른 단정은 하지 마세요.
- 그림의 정확한 세부 형태나 라벨 크기는 이미지 해상도상 일부 흐릴 수 있으므로 과도하게 단정하지 않는 것이 좋습니다.

[불확실한 부분]
- 요거트병 그림의 세부 외곽선, 뚜껑 색상 경계, 라벨 글자의 정확한 폰트는 다소 작아서 세밀하게는 불확실합니다.
- `□`가 체크박스인지 문제 표식인지 화면만으로는 확정하기 어렵지만, 문제 번호 앞의 작은 네모 기호로는 분명히 보입니다.
- `(정답)mL`의 정확한 띄어쓰기나 줄 간격은 화면 해상도에 따라 약간 달리 인식될 수 있습니다.

--- RAW VISION DRAFT END ---
