[전체적인 구성]
- 한 장의 문제 이미지로 보이며, 좌측 상단에 문제 번호와 지시문이 있고, 중앙 오른쪽에 연분홍색 테두리의 큰 사각형 박스가 있습니다.
- 박스 안에는 각의 예시로 보이는 선분/곡선 그림 3개가 가로로 나란히 배치되어 있습니다.
- 박스 아래 왼쪽에는 `(정답)` 표기가 있고, 그 오른쪽에 각의 그림 1개가 따로 제시되어 있습니다.
- 하단에는 `(해설)` 문구와 함께 설명 문장이 두 줄로 적혀 있습니다.
- 전체적으로 “각을 찾아 선택하세요”라는 선택형 문제 형식입니다.

[보이는 텍스트]
- `95. 각을 찾아 선택하세요.`
- `(정답)`
- `(해설) 첫째는 곡은 선과 곧은 선으로 이루어져 있습니다.`
- `둘째는 두 반직선이 한 점에서 만나지 않습니다.`

[반복되는 시각 패턴]
- 연분홍색 테두리 박스 안에 서로 다른 도형 예시 3개가 일정한 간격으로 배열되어 있습니다.
- 각 예시 모두 선분/선 형태가 중심이며, 하나는 곡선이 포함되고, 하나는 두 선이 벌어져 있고, 하나는 각처럼 보이는 두 선과 아래쪽 가로선이 만나는 구조로 보입니다.
- 아래 `(정답)` 영역에는 선택 결과를 보여주듯 단독 도형이 한 번 더 제시되어 있습니다.
- 해설 문장은 `첫째`, `둘째`처럼 순서를 나누어 설명하는 반복 구조입니다.

[수식 단계]
- 해당 없음. 수식 변형이나 등호가 나타나지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호가 포함된 식이 보이지 않습니다.

[세부 내용]
- 문제 번호 `95.`는 좌측 상단에 작게 표시되어 있습니다.
- 문제 문장은 번호 오른쪽에 이어서 배치되어 있으며, 글자 크기가 비교적 큽니다.
- 중앙의 연분홍색 큰 사각형은 선택지 영역처럼 보이고, 그 안은 흰 배경입니다.
- 선택지 1:
  - 왼쪽 아래에서 시작해 오른쪽 위로 휘어 올라가는 곡선이 보입니다.
  - 아래에는 수평에 가까운 곧은 선이 있고, 곡선의 끝과 아래 선의 끝이 가까이 배치되어 있습니다.
- 선택지 2:
  - 한 점에서 시작한 듯 보이는 두 개의 짧은 직선이 좌우로 벌어져 있습니다.
  - 두 선은 아래쪽에서는 서로 가까워지지만, 실제로는 만나지 않는 형태로 보입니다.
- 선택지 3:
  - 왼쪽 위에서 오른쪽 아래로 내려오는 직선과, 아래쪽에서 왼쪽에서 오른쪽으로 뻗는 직선이 만나는 듯한 구조입니다.
  - 전체적으로 각의 한 예시처럼 보이는 도형입니다.
- `(정답)` 아래 도형은 오른쪽 아래로 향하는 사선과 왼쪽 아래에 있는 가로선이 한 점에서 만나는 형태로 그려져 있습니다.
- 해설은 첫 번째 그림과 두 번째 그림이 각이 아닌 이유를 설명하고, 세 번째 그림이 각에 해당함을 암시하는 문장 구조로 보입니다.

[Python DSL 작성 힌트]
- 문제 상단의 텍스트, 선택지 박스, 정답 영역, 해설 영역을 각각 독립된 블록으로 나누어 배치하면 좋습니다.
- 선택지 박스는 연분홍색 테두리의 둥근 사각형으로 표현하는 것이 적절해 보입니다.
- 박스 내부의 3개 도형은 가로 방향 정렬로 배치하고, 각 도형 사이에 충분한 간격을 둡니다.
- 도형은 선분과 곡선을 섞어 그려야 하며, 각 도형의 상대적 위치와 방향을 맞추는 것이 중요합니다.
- `(정답)` 아래의 도형은 별도 영역에 두고, 문제 상단 선택지와 시각적으로 구분되게 처리합니다.
- 해설 문장은 텍스트 박스로 두 줄에 걸쳐 배치하면 원본과 유사합니다.
- 도형의 정확한 기울기, 곡선의 휨 정도, 선 끝점의 길이는 이미지 보고 가능한 범위에서만 반영하세요.

[주의할 점]
- 각 도형이 정확히 어떤 이름인지 단정하지 말고, 보이는 선의 형태만 묘사하세요.
- 정답 도형이 무엇을 뜻하는지는 분석 초안에서 확정적으로 서술하지 않는 것이 안전합니다.
- 해설 문장이 문제의 정답을 직접 설명하는 것처럼 보이더라도, 답 자체를 채워 넣지는 마세요.
- `각`, `반직선`, `곡은 선`, `곧은 선` 같은 용어는 화면에 보이는 텍스트로만 다루세요.
- 선택지의 도형이 일부 잘려 보이거나 얇아서 अस्पष्ट한 부분이 있으므로, 너무 단정적으로 좌표를 확정하지 마세요.

[불확실한 부분]
- 선택지 1의 곡선과 아래 곧은 선이 실제로 어떤 점에서 이어지는지 이미지 해상도상 다소 불명확합니다.
- 선택지 2의 두 선이 정확히 한 점에서 시작하는지, 아니면 매우 가까이만 있는지는 확대가 필요할 수 있습니다.
- 선택지 3의 오른쪽 끝 교차 지점이 정확히 접점인지, 단순히 끝점이 겹쳐 보이는지 다소 अस्पष्ट합니다.
- `(정답)` 아래 도형의 정확한 각도와 선 길이는 이미지에서 완전히 선명하지 않습니다.
