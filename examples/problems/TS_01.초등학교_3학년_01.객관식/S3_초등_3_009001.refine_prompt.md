# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009001.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009001

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호 `39.`가 있고, 그 오른쪽에 문제 문장이 한 줄로 배치되어 있습니다.
- 문제 문장 아래쪽에는 연한 파란색 격자 배경 위에 사각형/사다리꼴 도형 4개가 가로로 나열되어 있습니다.
- 각 도형 안에는 한글 글자 `가`, `나`, `다`, `라`가 각각 1개씩 들어 있습니다.
- 도형들 아래 중앙에는 보기처럼 `( 가, 나, 다, 라 )`가 크게 표시되어 있습니다.
- 더 아래 왼쪽에는 `(정답)나`가 따로 적혀 있고, 그 아래에는 `(해설)`로 시작하는 설명 문장이 이어집니다.

[보이는 텍스트]
- `39. 정사각형을 찾아 기호를 선택하세요.`
- 도형 안 글자: `가`, `나`, `다`, `라`
- 아래 보기: `( 가, 나, 다, 라 )`
- `정답)나`
- `해설)정사각형은 네 각이 모두 직각이고 네 변의 길이가 모두 같은 사각형입니다. 따라서 나입니다.`
- 해설 문장 끝은 화면에서 일부 줄바꿈되어 보이며, 문장 전체는 “따라서 나입니다.”로 읽힙니다.

[반복되는 시각 패턴]
- 연한 파란색 격자무늬가 도형 배경 전체에 반복됩니다.
- 도형은 모두 사각형 계열이며, 각각 하나의 문자 라벨을 중앙에 배치한 형태입니다.
- `가`, `나`, `다`, `라`처럼 선택지에 해당하는 기호가 각 도형에 하나씩 대응되어 있습니다.
- 아래의 `( 가, 나, 다, 라 )`는 위 도형들과 같은 순서의 선택지 목록처럼 보입니다.

[수식 단계]
- 해당 없음. 수식이나 등호 변형 과정은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호로 이어지는 변형식이 보이지 않습니다.

[세부 내용]
- 첫 번째 도형 `가`:
  - 왼쪽에 배치된 가로로 긴 직사각형처럼 보입니다.
  - 내부에 `가`가 중앙에 적혀 있습니다.
  - 파란 격자선 위에 놓여 있어 크기 비교가 가능해 보이지만, 답은 이 초안에서 판단하지 않습니다.
- 두 번째 도형 `나`:
  - 화면 중앙 쪽에 위치한 정사각형처럼 보이는 도형입니다.
  - 내부에 `나`가 중앙에 적혀 있습니다.
  - 위, 아래, 왼쪽, 오른쪽 변이 모두 같은 길이로 보이는 형태입니다.
- 세 번째 도형 `다`:
  - `나`의 오른쪽에 있는 세로로 긴 사각형입니다.
  - 윗변 오른쪽 꼭짓점 쪽이 약간 기울어진 선처럼 보이는데, 실제 도형 일부인지 격자와 겹친 것인지 시각적으로 확인이 필요합니다.
  - 내부에 `다`가 중앙에 적혀 있습니다.
- 네 번째 도형 `라`:
  - 가장 오른쪽에 있는 사다리꼴 형태입니다.
  - 윗변보다 아랫변이 더 길어 보이며, 좌우 변이 기울어져 있습니다.
  - 내부에 `라`가 중앙에 적혀 있습니다.
- 해설 영역:
  - 정답 표시와 해설 문장이 문제 아래쪽에 별도로 인쇄되어 있습니다.
  - 해설은 정사각형의 성질을 글로 설명한 뒤, 해당 도형이 `나`라고 적고 있습니다.
  - 다만 이 초안에서는 해설의 결론을 추론에 의존해 서술하지 않고, 화면에 보이는 문구 자체만 기록합니다.

[Python DSL 작성 힌트]
- 최상단에 문제 번호와 지시문을 텍스트로 배치합니다.
- 그 아래에 연한 파란색 격자 배경 사각형 영역을 크게 두고, 그 위에 4개의 도형을 가로 방향으로 정렬합니다.
- 각 도형은 다음처럼 분리해서 그리면 좋습니다:
  - `가`: 가로 직사각형
  - `나`: 정사각형
  - `다`: 세로 직사각형
  - `라`: 사다리꼴
- 각 도형의 중앙에는 한 글자 라벨을 넣습니다.
- 도형 아래에는 가운데 정렬된 보기 `( 가, 나, 다, 라 )`를 추가합니다.
- 더 아래 왼쪽에는 `(정답)나`, `(해설)` 텍스트를 순서대로 배치합니다.
- 격자 배경은 도형과 독립된 큰 사각형 컨테이너로 처리하면 재현하기 쉽습니다.
- 도형 위치는 좌에서 우로 `가 - 나 - 다 - 라` 순서로 배치하면 화면 구조와 유사합니다.

[주의할 점]
- 정답을 직접 계산하거나 다시 판단하지 마세요.
- `가`, `나`, `다`, `라`의 실제 정답 선택을 초안에서 새로 추론해 쓰지 마세요.
- 도형의 정확한 길이비나 각도는 이미지 해상도 때문에 오차가 있을 수 있으므로 단정하지 마세요.
- `다` 도형의 윗부분은 선이 기울어진 것처럼 보이지만, 정확히 어떤 편집 요소인지는 조심해서 표현하세요.
- 아래의 `(정답)나`는 화면에 이미 보이는 정보이므로, 이를 문제 풀이 결과로 새로 계산한 것처럼 서술하지 마세요.

[불확실한 부분]
- `가` 도형과 `다` 도형의 정확한 변 길이와 직각 여부는 이미지 상 다소 흐릿하여 완전히 확정하기 어렵습니다.
- `다` 도형의 윗부분 경계선은 격자선과 겹쳐 보여 실제 외곽선 해석에 약간의 불확실성이 있습니다.
- 격자선의 정확한 칸 수와 각 도형의 정밀한 위치는 이미지 해상도 때문에 오차가 있을 수 있습니다.

--- RAW VISION DRAFT END ---
