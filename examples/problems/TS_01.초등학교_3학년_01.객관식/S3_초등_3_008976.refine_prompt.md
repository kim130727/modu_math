# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008976.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008976

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 흰 배경의 학습지 화면이며, 왼쪽 상단에 문제 번호와 지시문이 있고, 그 아래와 오른쪽에 여러 개의 선 그림이 선택지처럼 배치되어 있습니다.
- 문제는 “직선을 선택하세요.”라는 요구를 주고, 아래쪽에 “(정답)” 표시가 있는 선 1개와 “(해설)” 문장이 함께 보입니다.
- 전체적으로 도형 판별 문제 형태이며, 글자 영역과 그림 영역이 위아래로 나뉘어 있습니다.

[보이는 텍스트]
- “11.”
- “직선을 선택하세요.”
- “(정답)”
- “(해설) 양쪽으로 끝없이 늘이게 곧은 선을 찾습니다.”
- 그림 안에는 별도의 숫자나 기호가 더 보이지 않습니다.

[반복되는 시각 패턴]
- 선 그림이 여러 개 제시되어 있으며, 각 선은 서로 다른 형태입니다.
  - 위쪽 왼쪽: 수평에 가까운 곧은 선
  - 위쪽 가운데: 휘어진 곡선
  - 위쪽 오른쪽: 기울어진 곧은 선
  - 아래쪽 왼쪽: 기울어진 곧은 선
- 각 선의 양끝 근처에 작은 점 표시가 보이며, 선의 끝을 강조하는 듯한 시각 요소로 보입니다.
- “(정답)” 표시 옆에는 아래쪽 왼쪽의 기울어진 곧은 선이 따로 배치되어 있어, 정답 예시처럼 보입니다.

[수식 단계]
- 해당 사항 없음. 수식 변형이나 등호 연결이 보이지 않습니다.

[등호별 변환 의미]
- 해당 사항 없음. 등호가 포함된 식이 없습니다.

[세부 내용]
- 왼쪽 상단에 문제 번호 “11.”이 있고, 그 오른쪽에 굵은 제목처럼 “직선을 선택하세요.”가 배치되어 있습니다.
- 그 아래에는 선택 대상이 되는 선 그림들이 흩어져 배치되어 있습니다.
  - 첫 번째 선은 화면 상단 중앙-left 쪽에 가로로 놓여 있으며, 양끝에 점이 있습니다.
  - 두 번째 선은 상단 중앙에 있으며, 아래로 휘어진 곡선 형태입니다.
  - 세 번째 선은 상단 오른쪽에 있으며, 오른쪽 위 방향으로 기울어진 직선입니다.
  - 네 번째 선은 하단 왼쪽에 있으며, 왼쪽 아래에서 오른쪽 위로 올라가는 직선입니다.
- 하단 왼쪽에는 “(정답)” 텍스트가 있고, 그 바로 오른쪽 또는 아래쪽에 정답 예시로 보이는 선이 따로 표시되어 있습니다.
- 맨 아래에는 “(해설) 양쪽으로 끝없이 늘이게 곧은 선을 찾습니다.”라는 문장이 있어, 직선의 개념을 설명하고 있습니다.

[Python DSL 작성 힌트]
- 문제 상단의 텍스트와 그림 영역을 분리해서 배치하면 좋습니다.
- 선 도형은 각각 독립 객체로 다루고, 직선/곡선 여부가 한눈에 비교되도록 위치를 유지하세요.
- “(정답)”과 “(해설)” 텍스트는 하단에 별도 텍스트 블록으로 넣는 구성이 적절합니다.
- 각 선의 끝점에 작은 점이 있는 형태를 반영할 수 있으면 좋습니다.
- 선택지처럼 보이는 선들은 서로 충분히 간격을 두어 배치하고, 곡선 1개와 직선 여러 개의 대비가 드러나게 구성하세요.

[주의할 점]
- 정답을 텍스트로 단정하여 추가 설명하지 마세요. 화면에 이미 “(정답)” 표시가 있지만, 어떤 선택이 정답인지 분석 문장으로 확정할 때는 주의가 필요합니다.
- 선의 정확한 길이, 각도, 곡률은 이미지에 보이는 정도만 반영하고, 임의로 수치를 정하지 마세요.
- 해설 문장은 문제풀이 결과가 아니라 화면에 보이는 텍스트로만 취급하세요.
- 빈칸 문제는 아니므로 답을 추론하거나 채우는 형식으로 작성하지 마세요.

[불확실한 부분]
- 아래쪽 왼쪽 선이 “정답” 예시인지, 또는 별도 정답 표시용 그림인지 화면 구성상 그렇게 보이지만 100% 단정하기는 어렵습니다.
- 각 선 끝의 점 표시가 실제 점인지, 편집 과정에서 붙은 마커인지 이미지 해상도상 완전히 명확하지 않습니다.
- 상단의 선들이 정확히 동일한 두께인지, 약간의 시각적 차이가 있는지는 확대 없이 판단이 어렵습니다.

--- RAW VISION DRAFT END ---
