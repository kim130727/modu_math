# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008849.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008849

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 화면은 **초등 수학 객관식/선택형 문제** 형식으로 보입니다.
- 상단 왼쪽에 문제 번호와 지시문이 있고, 그 아래 중앙 영역에 **연산식이 들어 있는 하늘색 직사각형 박스 2개**가 좌우로 나란히 배치되어 있습니다.
- 하단 왼쪽에는 **정답 표기**와 **해설 영역**이 있으며, 해설 안에는 **무게 단위 변환을 이용한 세로셈 형태**와 그 결과를 비교하는 식이 함께 제시됩니다.
- 하단 오른쪽에는 **회색 화살표**가 있고, 그 뒤에 비교식이 이어집니다.

# [보이는 텍스트]
- `93. 무게가 더 무거운 쪽을 선택하세요.`
- 왼쪽 하늘색 박스: `9 kg - 2 kg 600 g`
- 오른쪽 하늘색 박스: `7 kg 600 g`
- `(정답) 7 kg 600 g`
- `(해설)`
- 해설 내 보이는 숫자/단위:
  - `8`
  - `1000`
  - `9 kg`
  - `- 2 kg 600 g`
  - `6 kg 400 g`
  - `6 kg 400 g < 7 kg 600 g`

# [반복되는 시각 패턴]
- 상단의 두 하늘색 박스는 **같은 크기와 비슷한 스타일**로 보이며, 둘 다 가운데 정렬된 수식을 담고 있습니다.
- 해설 부분에서는 **kg와 g 단위를 맞추기 위한 자리 배치**가 반복적으로 나타납니다.
- 숫자 `8`, `1000`은 **노란색 강조 표시**처럼 따로 띄워져 있고, 그 아래의 세로셈 구조와 연결되는 설명 요소처럼 보입니다.
- `kg`와 `g`가 **두 열처럼 분리된 형태**로 배치되어, 왼쪽은 kg, 오른쪽은 g를 다루는 구조가 반복됩니다.
- 마지막에는 **회색 오른쪽 화살표** 뒤에 결과식이 이어져, 계산 과정에서 비교식으로 넘어가는 흐름이 보입니다.

# [수식 단계]
- 해설의 세로셈은 **위에 있는 값 `9 kg`에서 아래의 `2 kg 600 g`를 빼는 형태**로 보입니다.
- 그 아래 결과로 **`6 kg 400 g`**이 제시되어 있습니다.
- 이어서 회색 화살표 `→` 뒤에 **`6 kg 400 g < 7 kg 600 g`** 형태의 비교가 나타납니다.

# [등호별 변환 의미]
- 이 문제 화면에서는 **등호 `=`가 여러 번 이어지는 변형식**은 뚜렷하게 보이지 않습니다.
- 대신 해설은 **뺄셈 결과를 구한 뒤, 그 결과와 다른 수를 비교하는 방식**으로 구성되어 있습니다.
- 따라서 등호 변환 설명보다는:
  - `9 kg - 2 kg 600 g` 형태의 식을
  - 아래의 세로셈으로 옮겨 계산하고
  - 그 결과 `6 kg 400 g`를
  - `7 kg 600 g`과 비교하는 흐름
  으로 이해할 수 있습니다.

# [세부 내용]
- 문제 문장은 화면 상단 왼쪽에 있으며, 번호 `93.` 다음에 지시문이 이어집니다.
- 중앙의 두 하늘색 박스는 선택지처럼 보이며:
  - 왼쪽 박스에는 `9 kg - 2 kg 600 g`
  - 오른쪽 박스에는 `7 kg 600 g`
  가 적혀 있습니다.
- 왼쪽 아래 해설에서는 `9 kg`와 `2 kg 600 g`를 세로로 맞춰 적고, 그 아래에 결과를 적는 구조입니다.
- 해설식 주변에는 **작은 숫자 `8`, `1000`이 각각 따로 강조**되어 있어, 무게 단위 변환에 필요한 보조 정보처럼 보입니다.
- 결과 아래에는 회색 화살표가 있고, 화살표 오른쪽에 `6 kg 400 g < 7 kg 600 g`가 한 줄로 제시됩니다.
- 전체적으로 **왼쪽이 계산 과정, 오른쪽이 비교 결과**처럼 구성되어 있습니다.

# [Python DSL 작성 힌트]
- 상단 문제 문장은 텍스트 객체로 배치합니다.
- 선택지는 **동일한 크기의 하늘색 직사각형 2개**로 만들고, 각 박스 내부 텍스트는 가운데 정렬합니다.
- 해설 영역은 왼쪽 하단에 배치하고, 아래 요소를 분리해서 그립니다:
  - `(해설)` 라벨
  - 노란색 강조 숫자 `8`, `1000`
  - `kg`와 `g`가 분리된 세로셋/정렬형 계산식
  - 결과 `6 kg 400 g`
  - 회색 화살표
  - 비교식 `6 kg 400 g < 7 kg 600 g`
- 세로셈은 kg 열과 g 열이 서로 정렬되도록 배치하면 좋습니다.
- 결과적으로 **연산식 박스, 풀이 과정, 비교식**을 각각 독립된 그룹으로 나누어 작성하는 것이 مناسب합니다.

# [주의할 점]
- `9 kg - 2 kg 600 g`의 실제 계산 결과를 **직접 추론해 채워 넣으면 안 됩니다.**
- 빈칸이 보이더라도, 화면에 명시되지 않은 값은 임의로 넣지 마세요.
- `8`과 `1000`의 정확한 역할은 화면만으로 단정하지 말고, **단위 변환용 보조 표기** 정도로만 서술하세요.
- 왼쪽 하늘색 박스의 식과 아래 해설의 세로셈은 **형식이 다르므로 동일한 객체로 합치지 않는 것**이 좋습니다.
- 정답 표기 `(정답) 7 kg 600 g`는 화면에 보이지만, 이것을 새로 계산해서 얻은 결과처럼 서술하지 마세요.

# [불확실한 부분]
- 하늘색 박스가 **선택지인지, 비교 대상 제시인지**는 문맥상 선택지처럼 보이지만, 화면만으로 100% 단정하기는 어렵습니다.
- 해설 속 `8`과 `1000`의 정확한 배치 의미는 일부 가려져 있거나 작아 보여서, **단위 변환 과정의 보조 수치** 정도로만 해석하는 것이 안전합니다.
- 아래 세로셈의 일부 줄맞춤은 이미지 해상도상 약간 흐릿하여, 각 기호의 정확한 칸 위치는 다소 불확실합니다.

--- RAW VISION DRAFT END ---
