# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008882.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008882

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 페이지짜리 초등 수학/자료해석 문제 화면이다.
- 상단에는 문제 번호와 지시문이 한 줄 또는 두 줄로 배치되어 있고, 그 아래 중앙에 제목 형태의 문구가 있다.
- 가운데에는 표가 있으며, 운동 종류와 학생 수를 비교하는 형태다.
- 표 아래에는 문장형 선택지/서술 문장이 중앙 정렬로 제시되어 있다.
- 화면 하단 왼쪽에는 `(정답)`과 `(해설)` 표기가 따로 있고, 해설 문장이 이어진다.
- 전체적으로 텍스트 + 표 중심의 정적 레이아웃이며, 도형이나 화살표는 보이지 않는다.

[보이는 텍스트]
- `□ 46. 연우네 학교 학생들이 방과 후 수업으로 배우고 싶어 하는 운동을 조사하여 나타낸 표입니다. 표를 보고 알맞은 말을 선택해 보세요.`
- 제목처럼 보이는 문구: `배우고 싶어 하는 운동`
- 표 머리글: `운동`
- 표 항목: `축구`, `골프`, `야구`, `스쿼시`, `합계`
- 표 아래 행 레이블: `학생 수(명)`
- 수치: `50`, `30`, `55`, `50`, `185`
- 문장: `가장 많은 학생이 배우고 싶어 하는 운동은 ( 축구 , 골프 , 야구 , 스쿼시 )입니다.`
- 하단 표기: `(정답)야구`
- 하단 해설: `(해설) 학생 수에서 55가 가장 큰 수이므로 가장 많은 학생이 배우고 싶어 하는 운동은 야구입니다.`

[반복되는 시각 패턴]
- 표는 연한 초록색 계열의 테두리와 셀 배경을 사용한 격자형 구조로 보인다.
- 첫 번째 행은 항목명, 두 번째 행은 학생 수를 담는 2행 구조이다.
- 왼쪽 첫 칸은 항목 구분용, 오른쪽으로 운동 종류가 반복적으로 나열된다.
- 각 운동명 아래에 숫자가 하나씩 대응되도록 정렬되어 있다.
- 선택지 문장은 괄호 안에 여러 운동 이름이 쉼표로 나열되어 있어, 보기 선택형 문항의 형태를 띤다.
- 하단의 `(정답)`, `(해설)` 표기는 소괄호 라벨 반복 패턴이다.

[수식 단계]
- 해당 없음. 이 문제 화면에는 등호를 사용한 연산식 변형 과정이 보이지 않는다.
- 표 안에는 숫자 비교에 필요한 값만 나열되어 있다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 문제 번호 `46` 앞에 작은 빈 사각형 기호가 있다.
- 문제 문장은 상단에서 왼쪽 정렬로 시작하지만, 중간에 줄바꿈이 일어나며 오른쪽으로도 여백이 넓다.
- 중앙의 표 상단에는 별도 제목 `배우고 싶어 하는 운동`이 표 바깥에 위치해 있다.
- 표는 가로로 6개 열처럼 보이며, 첫 열은 항목명, 나머지 5개 열은 운동 종류 및 합계이다.
- 첫 행의 배경은 옅은 연두색/연녹색 계열로 채워져 있고, 둘째 행의 숫자 칸은 흰색 바탕에 보인다.
- 열 간 경계선은 얇은 초록색 선으로 표시되어 있다.
- 표의 숫자 `50, 30, 55, 50, 185`는 각 셀 중앙에 배치되어 있다.
- 표 아래의 문장은 중앙 정렬로 배치되어 있으며, 괄호 안 선택지가 한 줄에 들어가도록 구성되어 있다.
- 하단 해설은 왼쪽 아래에 작게 배치되어 있고, 정답과 해설이 연속 문장으로 제시된다.
- 해설 문장에서는 `55`가 가장 큰 수라는 사실을 근거로 삼고 있지만, 이 문서는 시각 분석이므로 그 결론 자체를 풀이로 확장하지는 않는다.

[Python DSL 작성 힌트]
- 문제 영역을 상단 설명, 중앙 표, 하단 문장, 하단 정답/해설의 4개 블록으로 나누어 배치하면 좋다.
- 표는 `2행 x 6열` 형태로 구성하고, 첫 행은 항목명, 둘째 행은 `학생 수(명)`과 각 숫자를 넣는 구조로 작성한다.
- 표 바깥 제목 `배우고 싶어 하는 운동`은 표 상단 중앙에 별도로 텍스트 박스로 두는 편이 자연스럽다.
- 선택지 문장은 중앙 정렬 텍스트로 처리하고, 괄호와 쉼표를 그대로 유지한다.
- 하단의 `(정답)`, `(해설)`은 왼쪽 정렬로 별도 텍스트 블록을 만들고, 해설 문장은 줄바꿈을 포함해 배치할 수 있다.
- 셀 배경색, 테두리색, 정렬 방식(가운데 정렬), 숫자 크기 차이를 반영하면 원본과 유사해진다.

[주의할 점]
- 정답을 시각 초안에 반영하더라도, 빈칸이나 보기의 정답을 추론해서 다른 형태로 채우지 말아야 한다.
- 표의 숫자와 항목은 보이는 그대로만 옮기고, 계산 과정을 새로 만들지 않는다.
- 중앙 문장의 괄호 안 목록은 보기로서 제시된 것이므로 재배열하지 않는다.
- `정답`, `해설`은 화면 하단에 실제로 보이지만, Python DSL에서 별도 주석/레이블로 처리할지 여부는 구현 방식에 따라 다를 수 있다.
- 색상은 연한 초록 계열로 보이나, 정확한 색상값은 이미지 기반 구현에서 약간 달라질 수 있다.

[불확실한 부분]
- 표의 정확한 셀 크기와 각 열의 비율은 이미지 해상도에 따라 약간 다르게 해석될 수 있다.
- 문제 번호 왼쪽의 작은 사각형 기호는 체크박스인지 장식 기호인지 확정하기 어렵다.
- 상단 문제 문장이 실제로 한 줄인지 두 줄인지, 줄바꿈 지점은 이미지 확대 정도에 따라 다르게 보일 수 있다.
- 하단 해설 문구의 줄바꿈 위치는 화면 렌더링 환경에 따라 달라질 수 있다.

--- RAW VISION DRAFT END ---
