# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008994.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008994

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 지시문이 있고, 그 아래 중앙 쪽에 작은 도형 그림이 배치되어 있습니다.
- 중간에는 보라색 테두리의 큰 사각형 박스가 있으며, 그 안에 선택형 문장이 크게 들어 있습니다.
- 하단에는 `(정답)` 표기와 함께 답이 짧게 적혀 있고, 그 아래에 `(해설)` 문장이 한 줄로 제시되어 있습니다.
- 전체적으로 **문제 → 도형/보기 → 선택 문장 → 정답/해설**의 순서로 구성된 초등 수학 개념 확인형 지면입니다.

# [보이는 텍스트]
- `□ 32. 알맞은 말을 선택하세요.`
- 도형 위/주변에는 별도의 글자가 보이지 않음
- 큰 박스 안 문장:
  - `( 한, 두, 네 ) 각이 모두 직각이고 ( 한, 두, 네 ) 변의 길이가`
  - `모두 같은 사각형을 정사각형이라고 합니다.`
- 하단:
  - `(정답) 네, 네`
  - `(해설) 정사각형은 네 각이 모두 직각이고 네 변의 길이가 모두 같은 사각형을 말합니다.`

# [반복되는 시각 패턴]
- 큰 박스 안에서 **괄호로 묶인 선택지**가 두 번 반복됩니다.
  - 첫 번째는 `각이 모두 직각` 앞의 빈 선택 부분
  - 두 번째는 `변의 길이가 모두 같은` 앞의 빈 선택 부분
- 같은 문장 구조 안에 **선택지-조건-정의**가 반복되는 형태입니다.
- 하단 해설에서도 같은 내용이 다시 한 번 자연스러운 문장으로 반복됩니다.
- 도형 그림은 정사각형을 연상시키는 **네 변 도형 + 네 모서리 직각 표시**가 반복적 시각 요소로 보입니다.

# [수식 단계]
- 해당 없음
- 이 문제에는 등호나 식 변형 단계가 보이지 않습니다.

# [등호별 변환 의미]
- 해당 없음
- 등호를 포함한 변환 과정이 제시되지 않았습니다.

# [세부 내용]
- 상단 문제 문구 `알맞은 말을 선택하세요.`는 보기 중 올바른 말을 고르는 형식임을 나타냅니다.
- 중앙 도형은 정사각형처럼 보이는 사각형이며, 네 꼭짓점 근처에 분홍색 직각 표시가 각각 있습니다.
- 도형의 네 변은 모두 같은 길이처럼 표현되어 있으며, 대칭적인 모양으로 배치되어 있습니다.
- 큰 보라색 테두리 상자는 하나의 문장 완성형 문제를 담고 있습니다.
- 문장 구조는 다음과 같습니다.
  - `(   ) 각이 모두 직각이고`
  - `(   ) 변의 길이가 모두 같은`
  - `사각형을 정사각형이라고 합니다.`
- 각 괄호 안에는 선택해야 하는 말이 들어가도록 되어 있으며, 실제 화면에는 선택지로 `한, 두, 네`가 제시되어 있습니다.
- 아래의 정답과 해설은 문제의 의미를 설명하는 참고 정보로 보이며, 본문 문제와 직접적인 답안 작성 칸은 별도로 보이지 않습니다.

# [Python DSL 작성 힌트]
- 큰 박스는 **테두리 있는 사각형 컨테이너**로 표현하면 좋습니다.
- 박스 내부 문장은 한 줄이 아니라 **2줄로 줄바꿈**되어 보이므로, 텍스트 줄바꿈을 반영해야 합니다.
- 선택지는 괄호로 묶인 형태이므로, `(` `)`와 쉼표를 포함한 텍스트 배치를 유지하는 것이 중요합니다.
- 도형은 단순 사각형이지만, **네 모서리의 직각 표시**가 핵심이므로 작은 직각 마크를 각 꼭짓점 근처에 추가해야 합니다.
- 하단의 `(정답)`, `(해설)`은 본문과 다른 작은 글씨 크기로 분리 배치하는 것이 자연스럽습니다.
- 문제 번호 `32.`와 지시문은 화면 상단 좌측에 한 줄로 배치합니다.
- 정답과 해설은 시각적으로 본문과 구분되는 별도 텍스트 블록으로 처리하면 좋습니다.

# [주의할 점]
- 답을 직접 추론해서 채우는 방식으로 쓰지 말고, 화면에 실제로 보이는 선택지와 정답 표기만 구분해서 적어야 합니다.
- `한, 두, 네`는 보기로 보이며, 문제 해결 결과가 아니라 **선택 후보**임을 분명히 해야 합니다.
- 정사각형 그림은 정확한 크기나 선 굵기를 단정하기 어렵기 때문에, **대략적인 사각형 그림**으로 설명하는 것이 안전합니다.
- 하단 해설 문장은 정답 설명용 텍스트이므로, 본문 문제와 동일한 내용이 다시 제시된 것으로 봐야 합니다.
- `□ 32.` 앞의 작은 네모 표시는 문제 번호 영역의 일부처럼 보이지만, 의미는 확정하기 어렵습니다.

# [불확실한 부분]
- 상단의 `□` 기호가 체크박스인지, 인쇄 서식의 일부인지 확실하지 않습니다.
- 도형이 완전한 정사각형인지, 화면 해상도상 약간의 왜곡이 있는지는 단정하기 어렵습니다.
- 보라색 테두리 박스의 정확한 선 두께와 여백은 이미지상 대략적으로만 파악됩니다.
- `네`가 정답으로 표시되어 있지만, 본 요청에서는 답을 풀이하지 않고 시각적으로만 기록합니다.

--- RAW VISION DRAFT END ---
