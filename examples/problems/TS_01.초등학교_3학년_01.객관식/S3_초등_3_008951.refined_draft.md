[문제 개요]
- 문제 번호: `21.`
- 문제 문장: `학생 수를 학교별로 조사하여 표로 나타내었습니다. 학생 수가 나 학교의 2배인 학교는 어디일까요?`
- 표 제목: `학교별 학생 수`
- 표 아래 보조 문구: `( 가 , 나 , 다 , 라 ) 학교`
- 하단에 관찰되는 텍스트: `(정답)다`, `(해설)` 및 해설 문장
- 이 문항은 표의 값을 보고, `나 학교의 2배`에 해당하는 학교를 찾는 형식으로 보임

[원본에 보이는 요소]
- 상단 왼쪽에 문제 번호 `21.`이 보임
- 상단에는 문제 문장이 2줄로 배치됨
- 중앙 상단에 연한 살구색 배경의 제목 박스가 있음
  - 박스 안 텍스트: `학교별 학생 수`
- 제목 박스 아래에 표가 있음
- 표는 테두리가 있는 직사각형 격자 형태
- 표 오른쪽 아래쪽에 괄호가 포함된 문구 `( 가 , 나 , 다 , 라 ) 학교`가 보임
- 화면 하단 왼쪽에 `(정답)다`가 보임
- 그 아래에 `(해설)`로 시작하는 문장이 이어짐
- 배경은 흰색이며, 제목 박스와 표는 연한 살구색 계열로 통일되어 보임

[수식/관계 구조]
- 표의 구조:
  - 첫째 행(머리글): `학교 | 가 | 나 | 다 | 라 | 합계`
  - 둘째 행(값): `학생 수(명) | 250 | 160 | 320 | 340 | `
- 수량 대응:
  - `가` 학교 학생 수 = `250`
  - `나` 학교 학생 수 = `160`
  - `다` 학교 학생 수 = `320`
  - `라` 학교 학생 수 = `340`
- 비교 관계:
  - 문제는 `나 학교의 2배`와 같은 학생 수를 가진 학교를 찾는 관계를 묻고 있음
- 해설에 보이는 계산식:
  - `160 × 2 = 320(명)`

[등호별 변환 의미]
- 화면에 등호가 들어간 식은 해설의 `160 × 2 = 320(명)` 하나임
- 단계 의미:
  - `160 × 2` : 나 학교 학생 수 `160명`을 2배로 하는 계산
  - `= 320(명)` : 그 계산 결과가 `320명`임을 표시
- 해설 문장 흐름에서 이 계산은 `나 학교의 2배`를 수치로 확인하는 역할을 함
- 별도의 연속 등식 체인은 보이지 않음

[빈칸 정책]
- 표의 마지막 열 `합계`에 해당하는 값 칸은 비어 있음
- 이 빈칸은 추론으로 채우지 말고, **빈칸 상태 그대로 유지**해야 함
- 화면 하단의 `(정답)다`는 원본에 인쇄되어 보이는 사실로만 기록하고, 빈칸에 대한 임의 채움으로 해석하지 않음
- 문제 풀이 결과를 재생성하여 다른 빈칸을 채우지 않음
- DSL 작성 시에도 빈칸은 빈칸 컴포넌트/미기입 상태로 두는 것이 원칙

[반복 패턴]
- 학교 이름 `가, 나, 다, 라`가 표 머리글에서 순서대로 반복 제시됨
- 각 학교 아래에 학생 수가 1:1로 대응되어 반복적인 항목-값 구조를 이룸
- 문제 문장과 해설 문장 모두 `학생 수`, `학교`, `2배`라는 핵심어를 반복 사용함
- 표 제목 박스와 표 본문이 같은 계열 색상으로 묶여 있어 하나의 정보 블록처럼 보임
- 괄호 문구 `( 가 , 나 , 다 , 라 ) 학교`는 학교 이름들을 다시 묶어 보여주는 반복적 보조 표기임

[DSL 작성 힌트]
- 상단 문제 문장은 별도 텍스트 블록으로 두 줄 배치가 적절함
- 제목 `학교별 학생 수`는 표와 분리된 작은 배경 박스에 넣는 구성이 적합함
- 표는 `2행 × 6열` 구조로 보는 것이 자연스러움
  - 첫 열: 항목명
  - 나머지 열: `가, 나, 다, 라, 합계`
- 각 셀은 중앙 정렬에 가깝게 배치되어 있음
- 숫자 `250, 160, 320, 340`은 각 학교 열 아래에 정렬됨
- `합계` 열의 아래 셀은 비워 두어야 함
- 표 아래의 `( 가 , 나 , 다 , 라 ) 학교`는 표와 분리된 보조 텍스트로 배치
- 하단의 `(정답)다`와 `(해설)`은 별도 텍스트 블록으로 분리하는 것이 좋음
- 해설의 계산식 `160 × 2 = 320(명)`은 문장 내 수식으로 넣거나 독립 수식 텍스트로 둬도 무방함
- 표와 제목 박스는 연한 살구색 톤, 흰 배경과의 대비를 살려 표현하는 것이 적절함
- 셀 테두리는 비교적 얇고 균일하게 보임

[semantic/solvable 힌트]
- 표에서 `나` 학교의 학생 수는 `160명`으로 보임
- 문제는 `나 학교의 2배`와 같은 학생 수를 가진 학교를 찾는 질문임
- 해설에는 `160 × 2 = 320(명)`이 보이며, 이는 `나` 학교의 2배가 `320명`임을 나타내는 관찰된 설명임
- 표 안에서 `320`은 `다` 학교 아래에 위치함
- 따라서 원본 해설과 정답 표시는 `다`를 가리키는 것으로 보임
- 단, DSL 작성 관점에서는 이 값들을 관찰 사실로 기록하되, 별도로 계산을 재생성하여 빈칸을 채우지 않음

[주의 및 TODO]
- 정답이 화면에 `다`로 보이지만, 풀이를 새로 생성하는 방식으로 쓰지 말고 관찰된 텍스트로만 취급
- `합계` 칸의 공란은 유지
- 문제 문장과 해설 문장은 원본 그대로 보이는 텍스트를 우선
- 표 아래 괄호 문구의 오른쪽 끝이 약간 잘려 보일 수 있으므로, 위치와 줄바꿈은 이미지 기준으로 유동적으로 처리
- 해설의 줄바꿈은 원본에 보이는 대로 반영하되, DSL에서 자동 줄바꿈이 달라질 수 있음
- `160 × 2 = 320(명)`은 해설 문장 속 식으로 보이며, 별도 변형식은 없음
- 제목 박스의 정확한 픽셀 좌표는 불명확하므로 배치 관계만 참고
