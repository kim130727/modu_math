[문제 개요]
- 한 장짜리 초등 수학 문제 화면이다.
- 주제는 누름 못과 띠 종이를 이용해 원을 그리는 상황을 보고, 문장 선택지 중 알맞은 말을 고르는 문제이다.
- 화면에는 상단 문제 문장, 가운데 좌우 삽화 2개, 하단의 주황색 선택 문장 박스, 맨 아래 정답/해설 표시가 함께 보인다.

[원본에 보이는 요소]
- 상단 문제 번호: `□ 72.`
- 상단 문제 문장:
  - `누름 못과 띠 종이를 이용하여 원을 그렸습니다. 그림을 보고 알맞은 말을`
  - 다음 줄: `선택하세요.`
- 왼쪽 삽화 옆 초록색 설명:
  - `① 한쪽 구멍에`
  - `누름 못을 꽂아`
  - `띠 종이를 고정`
  - `해요.`
- 오른쪽 삽화 옆 초록색 설명:
  - `② 다른 구멍에`
  - `연필을 꽂고`
  - `한 바퀴 돌려`
  - `원을 그려요.`
- 주황색 테두리 박스 안 문장:
  - `누름 못이 꽂힌 점에서 원 위에 찍은 4개의 점까지의 길이는`
  - 다음 줄: `모두 ( 같습니다, 다릅니다 ).`
- 하단 표시:
  - `(정답) 같습니다`
  - `(해설) 누름 못이 꽂힌 점에서 원 위에 찍은 4개의 점까지의 길이는 모두 같습니다.`

[수식/관계 구조]
- 계산식은 보이지 않는다.
- 수학적 관계는 “누름 못이 꽂힌 점”과 “원 위에 찍은 4개의 점” 사이의 거리 비교 문장으로 제시된다.
- 선택 구조:
  - 문장 말미의 괄호 안에 `같습니다, 다릅니다` 두 선택어가 함께 제시된다.
- 그림의 구조적 관계:
  - 하나의 원과 그 원 위의 4개 점이 중심/기준점과 관계를 이루는 형태로 보인다.
  - 오른쪽 그림은 중심 기준으로 상하좌우 대칭처럼 배치되어 있다.

[등호별 변환 의미]
- 등호나 수식 변형 연쇄는 보이지 않는다.
- 문장형 선택 문제이므로 단계별 등호 변환 설명은 해당 없음.

[빈칸 정책]
- 이 문제는 계산 빈칸을 채우는 형태가 아니라, 괄호 안 선택지 중 하나를 고르는 형식이다.
- `(같습니다, 다릅니다)`는 빈칸으로 추론해서 채우지 말고, 원문 그대로 선택지 묶음으로 유지해야 한다.
- 정답이 화면에 인쇄되어 보이더라도, DSL 작성용 초안에서는 정답을 추론해 빈칸에 자동 대입하지 않는다.
- 다만 원본에 `(정답) 같습니다`가 보이므로, 이는 관찰 사실로만 별도 기록할 수 있다.

[반복 패턴]
- 좌우에 비슷한 구조의 원/정사각형 도형이 두 번 나타난다.
  - 왼쪽: 조작 과정 설명용 삽화
  - 오른쪽: 결과 도형 요약용 삽화
- 왼쪽 삽화의 초록색 곡선 화살표와 번호 `①`, `②`는 순서를 안내하는 반복 패턴이다.
- 오른쪽 삽화에는 원 둘레의 위/아래/왼쪽/오른쪽에 점이 배치되어 반복성과 대칭성을 보여 준다.
- 선택문장 박스에서는 `같습니다, 다릅니다`가 괄호 안에서 대조 선택 구조로 반복된다.

[DSL 작성 힌트]
- 전체 레이아웃은 다음 순서로 나누면 적절하다:
  1. 상단 문제 번호와 문제 문장
  2. 중앙 좌측 삽화
  3. 중앙 우측 삽화
  4. 주황색 테두리의 선택 문장 박스
  5. 하단 정답/해설 텍스트
- 왼쪽 삽화는 다음 요소를 분리해서 다루면 좋다:
  - 바깥 사각형
  - 원
  - 손 그림
  - 연필 그림
  - 띠 종이처럼 보이는 띠 모양
  - 초록색 설명 텍스트 2개
  - 초록색 곡선 화살표
- 오른쪽 삽화는 다음 요소를 분리하면 재현이 쉽다:
  - 바깥 사각형
  - 원
  - 중심 표시처럼 보이는 작은 점/원
  - 점선 십자선
  - 원 둘레의 4개 점
- 주황색 박스는 테두리만 있는 큰 직사각형으로 보고, 내부 텍스트는 가운데 정렬된 2줄로 배치하는 것이 자연스럽다.
- 하단의 `(정답)`, `(해설)`은 본문과 다른 별도 텍스트 블록으로 분리하는 것이 좋다.
- 문제 문장은 상단에서 줄바꿈이 있으므로 2줄 텍스트로 유지하는 것이 좋다.

[semantic/solvable 힌트]
- 이 문제는 “그림을 보고 알맞은 말을 선택”하는 문장형 문제이다.
- 보이는 텍스트만 보면, 누름 못이 꽂힌 점과 원 위의 4개 점 사이 길이에 대한 비교 문장이다.
- 원의 성질을 설명하는 학습 문제로 보이며, 도형의 대칭성과 같은 길이 관계를 묻는 맥락이다.
- 원의 중심, 둘레의 점, 점선 십자선은 의미 해석에 중요하지만, 본 문서에서는 정답 추론을 하지 않는다.
- 원본에 `(정답) 같습니다`가 인쇄되어 보이므로, 이는 화면 관찰 사실로만 별도 기록 가능하다.

[주의 및 TODO]
- 왼쪽 삽화의 손, 연필, 띠 종이, 누름 못 위치는 중요한 시각 정보이므로 생략하지 말아야 한다.
- 오른쪽 삽화의 점선은 상하좌우를 나타내는 십자형 보조선으로 보이지만, 정확한 기하적 의미는 과도하게 단정하지 말 것.
- 바깥 사각형과 원의 접함 여부는 그림 스타일상 그렇게 보이지만, 엄밀한 도형 조건으로 단정하지 말고 TODO로 남길 수 있다.
- 오른쪽 중앙의 작은 원/점은 중심 표시처럼 보이나 장식적 요소일 가능성도 있어 확정하지 말 것.
- 정답이 화면에 보이더라도, DSL용 refined draft에서는 답을 채워 넣지 않는 정책을 유지한다.
- `나누기 n은 분모의 ×n으로 반영된다.` 문구는 본 문제에는 해당 없음.
