# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008914.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008914

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 번호와 문장이 2줄로 배치되어 있습니다.
  - 첫 줄 왼쪽에 작은 네모 표시가 있고, 그 오른쪽에 `80.`이 있습니다.
  - 이어서 본문 문장이 길게 이어집니다.
- 가운데에는 제목이 한 줄로 가운데 정렬되어 있고, 그 아래에 표가 있습니다.
- 표 아래에는 괄호로 묶인 항목 목록이 한 줄로 제시되어 있습니다.
- 더 아래 왼쪽에는 `(정답)` 표시와 함께 정답이 한 단어로 적혀 있고, 그 아래에는 `(해설)` 문장이 이어집니다.
- 전체적으로 “문제 문장 → 표 → 선택지 같은 항목 → 정답/해설” 순서의 정보 구조입니다.

[보이는 텍스트]
- `□ 80. 운동회에서 청군과 백군이 경기별로 얻은 점수를 표로 나타내었습니다. 청군이 가장 많은 점수를 얻은 종목을 선택하시오.`
- 제목: `운동회에서 경기별 얻은 점수`
- 표 머리글:
  - `경기`
  - `달리기`
  - `줄다리기`
  - `공 굴리기`
  - `박 터뜨리기`
  - `합계`
- 표 내용:
  - `청군 점수(점)` / `300` / `100` / `50` / `100` / `550`
  - `백군 점수(점)` / `100` / `200` / `100` / `50` / `450`
- 표 아래 괄호 안 항목: `( 달리기 , 줄다리기 , 공 굴리기 , 박 터뜨리기 )`
- 하단:
  - `(정답)달리기`
  - `(해설)청군 점수에서 가장 큰 수를 찾으면 300이므로 청군이 가장 많은 점수를 얻은 종목은 달리기입니다.`
- 주의: 해설 문장에는 수치와 결론이 함께 보이지만, 이 초안에서는 문제 해결 과정 자체를 재진술만 합니다.

[반복되는 시각 패턴]
- 표는 행과 열이 있는 격자 형태이며, 상단 머리글 행이 연한 분홍색 배경으로 강조되어 있습니다.
- 첫 열은 항목명(경기, 청군 점수(점), 백군 점수(점))이 들어가고, 나머지 열은 종목별 점수와 합계가 들어갑니다.
- 각 경기명은 열 제목으로 반복 배치되어 있고, 두 팀(청군/백군)의 점수가 같은 열 구조에 정렬되어 비교되도록 되어 있습니다.
- 숫자들은 각 열 중앙에 배치되어 보이며, 합계 열은 오른쪽 끝에 따로 모여 있습니다.
- 표 아래 괄호 안의 항목들은 쉼표로 구분된 목록 형태로, 보기 선택지처럼 나열되어 있습니다.
- 하단의 정답/해설은 본문과 분리되어 별도 설명 블록처럼 배치되어 있습니다.

[수식 단계]
- 이 문제 화면에는 등호가 연속으로 이어지는 수식 변형 단계가 보이지 않습니다.
- 표의 수치는 비교 자료로만 제시되어 있고, 계산식을 전개하는 형태는 나타나지 않습니다.

[등호별 변환 의미]
- 해당 없음.
- 화면에 등호를 포함한 변형식이 표시되지 않습니다.

[세부 내용]
- 문제의 핵심은 “청군이 가장 많은 점수를 얻은 종목”을 표에서 찾는 유형으로 보입니다.
- 표는 두 팀의 종목별 점수를 한눈에 비교할 수 있게 구성되어 있습니다.
- `청군 점수(점)` 행에서 각 종목 점수가 `300, 100, 50, 100`으로 보이고, `합계`는 `550`으로 표시됩니다.
- `백군 점수(점)` 행에서는 `100, 200, 100, 50`과 `합계 450`이 보입니다.
- 표 아래에 제시된 괄호 안 목록은 선택 대상 종목들로 보이며, 정답 표시는 그중 하나를 지목하는 형식입니다.
- 해설 문장은 “가장 큰 수”를 찾는 방식으로 설명하고 있으나, 이 초안에서는 그 결과를 새로 계산해 적지 않습니다.

[Python DSL 작성 힌트]
- 표를 먼저 큰 직사각형 격자로 배치하고, 상단 머리글 행은 배경색이 있는 셀로 표현하는 것이 좋습니다.
- 첫 번째 열은 왼쪽 정렬 텍스트 셀, 나머지 열은 가운데 정렬 숫자 셀로 구성하면 화면과 유사합니다.
- 제목 `운동회에서 경기별 얻은 점수`는 표 상단 중앙에 단독 텍스트로 배치합니다.
- 표 아래의 `( 달리기 , 줄다리기 , 공 굴리기 , 박 터뜨리기 )`는 하나의 줄 텍스트로 두고, 쉼표와 괄호 간격을 유지해야 합니다.
- 하단의 `(정답)`, `(해설)`은 일반 본문보다 조금 작은 블록처럼 분리 배치하면 좋습니다.
- 문제 번호 `80.`과 왼쪽의 작은 네모 표시도 별도 텍스트 요소로 두면 원본에 가깝습니다.

[주의할 점]
- 표의 수치나 정답을 새로 계산해서 채워 넣지 마세요.
- 보기 목록의 순서를 임의로 바꾸지 마세요.
- `청군`, `백군`의 팀명과 각 종목명 표기를 그대로 유지해야 합니다.
- `공 굴리기`, `박 터뜨리기`처럼 띄어쓰기가 있는 항목은 그대로 반영해야 합니다.
- 표 배경색, 테두리, 가운데 정렬 같은 시각 요소를 빠뜨리지 않도록 주의하세요.
- 하단의 해설 문장은 문제 본문과 분리된 별도 영역으로 처리하는 것이 좋습니다.

[불확실한 부분]
- 왼쪽 상단의 작은 네모 기호는 선택표시인지 인쇄상 마크인지 화면만으로는 용도가 확정되지 않습니다.
- 표의 정확한 선 굵기나 셀 내부 여백은 이미지 해상도상 세밀하게 단정하기 어렵습니다.
- `(정답)달리기`의 붙여쓰기 방식은 원본 그대로 보이지만, DSL에서는 공백 처리 방식이 달라질 수 있습니다.

--- RAW VISION DRAFT END ---
