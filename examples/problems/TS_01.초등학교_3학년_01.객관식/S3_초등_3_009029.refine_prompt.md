# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009029.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009029

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# 시각 분석 초안

## [전체적인 구성]
- 화면 상단 왼쪽에 문제 번호 **“70.”** 이 있고, 그 오른쪽에 문제 제목/안내 문장 **“정사각형을 알아보려고 합니다. 물음에 답하세요.”** 가 한 줄로 배치되어 있습니다.
- 그 아래에는 **연한 파란색 격자 배경** 위에 여러 도형이 가로 방향으로 나란히 놓여 있습니다.
- 도형 아래쪽에는 **(1)번 문항**이 큰 글씨로 이어져 있고, 그 아래에 **선택지 괄호**가 별도로 표시되어 있습니다.
- 화면 맨 아래 왼쪽에는 **정답/해설처럼 보이는 작은 글씨의 문장**이 일부 보이지만, 전체 문장이 다 보이지는 않습니다.
- 전체적으로는 **도형 식별 문제**와 **선택형 답안 표시**가 함께 있는 구성입니다.

## [보이는 텍스트]
보이는 텍스트를 눈에 보이는 그대로 옮기면 다음과 같습니다.

- `70.`
- `정사각형을 알아보려고 합니다. 물음에 답하세요.`
- `(1) 네 각이 모두 직각인 사각형을 모두 찾아 기호를 선택하세요.`
- `( 가 , 나 , 다 , 라 , 마 )`
- 아래쪽 왼편에 보이는 작은 글씨:
  - `(`으로 시작하는 문장 일부
  - `네 각이 모두 직각인 사각형을 찾습니다.`
  - 그 아래 또 다른 줄에 `(`으로 시작하는 짧은 표기와 `가, 라`가 보임

※ 아래쪽 작은 글씨는 화면이 잘려 있어 정확한 전체 문장은 일부만 판독됩니다.

## [반복되는 시각 패턴]
- 파란 격자 위에 **다섯 개의 도형**이 **좌→우 순서**로 배치되어 있습니다.
- 각 도형 안에는 하나씩 **한글 기호**가 들어 있습니다.
- 도형의 외곽선은 모두 검은색이며, 도형 내부에는 글자가 중앙 근처에 배치되어 있습니다.
- 도형의 종류가 서로 달라서, **정사각형/사각형 여부를 비교하도록 만든 패턴**으로 보입니다.
- 문제 문장 아래의 선택지는 **괄호 안에 기호들을 나열**하는 방식입니다.
- 아래쪽 작은 글씨 역시 **기호를 나열한 형태**가 보이는데, 이는 선택 결과나 해설로 보일 수 있습니다. 다만 문제 풀이를 확정할 수는 없습니다.

## [수식 단계]
- 해당 없음.  
- 화면에는 등호 `=` 가 있는 수식이나 변형식이 보이지 않습니다.

## [등호별 변환 의미]
- 해당 없음.  
- 등호를 이용한 단계적 변형이 나타나지 않습니다.

## [세부 내용]
### 1) 상단 도형 영역
- 격자 배경은 옅은 파란색 점선/실선 형태로 보이며, 도형의 위치와 비율을 맞추는 기준처럼 사용됩니다.
- 왼쪽부터 순서대로 도형이 배치되어 있습니다.
  1. **가**가 들어 있는 도형: 세로로 긴 사각형처럼 보이며, 내부에 글자 `가`가 있습니다.
  2. **나**가 들어 있는 도형: 마름모 형태입니다.
  3. **다**가 들어 있는 도형: 윗변과 아랫변이 평행해 보이는 사다리꼴 형태입니다.
  4. **라**가 들어 있는 도형: 정사각형처럼 보이는 네모입니다.
  5. **마**가 들어 있는 도형: 오른쪽으로 기울어진 사각형/사다리꼴처럼 보이는 형태입니다.
- 각 도형은 일정한 간격을 두고 가로로 배열되어 있어, **기호별 도형 분류**를 하도록 구성된 것으로 보입니다.

### 2) 문항 영역
- `(1)` 다음에 긴 문장이 이어집니다.
- 문항의 핵심은 **“네 각이 모두 직각인 사각형”**을 찾는 것이라고 읽힙니다.
- 문장 끝에는 **기호를 선택**하라는 지시가 있습니다.
- 오른쪽 아래에 선택지처럼 **`( 가, 나, 다, 라, 마 )`**가 따로 큼직하게 제시되어 있습니다.

### 3) 하단 작은 글씨
- 화면 맨 아래 왼쪽에는 작은 글씨가 일부 보입니다.
- 첫 줄은 괄호로 시작하는 문장이고, 다음 줄에 **“네 각이 모두 직각인 사각형을 찾습니다.”**라는 문장이 보입니다.
- 또 다른 줄에 **`가, 라`**가 보이는데, 이것이 선택 결과처럼 보일 수는 있으나 화면 일부만 보여 정확히 확정할 수는 없습니다.

## [Python DSL 작성 힌트]
- 상단에 **문제 번호 + 제목 문장**을 한 줄로 배치합니다.
- 그 아래에는 **연한 파란색 격자 배경 영역**을 크게 깔고, 그 위에 도형 5개를 좌→우로 배열합니다.
- 각 도형은:
  - 검은 외곽선
  - 내부 중앙의 한글 기호 1개
  - 도형 종류가 서로 다름
  으로 구성합니다.
- 도형별 위치와 간격을 일정하게 유지하고, **가/나/다/라/마**가 각각 다른 도형 안에 들어가도록 표시합니다.
- 문항 텍스트는 도형 영역 아래에 왼쪽 정렬로 배치하고, 선택지는 그 아래 또는 오른쪽 아래에 괄호 형태로 배치합니다.
- 하단의 작은 글씨는 필요하면 **보조 설명 또는 해설 영역**처럼 별도 처리하되, 원문이 잘린 부분은 무리하게 복원하지 않는 것이 좋습니다.

## [주의할 점]
- 문제를 실제로 풀어서 답을 확정하지 마세요.
- 선택지 `( 가, 나, 다, 라, 마 )`를 답으로 고정하지 마세요.
- 아래쪽에 보이는 `가, 라`를 정답으로 단정하지 마세요.
- 도형의 정확한 길이비나 각도는 이미지 해상도상 완벽히 확정하기 어려우므로, **모양의 종류와 배치 중심**으로 기록하는 것이 안전합니다.
- 특히 오른쪽 끝의 `마` 도형은 일부가 작고 기울어져 보여, 정확한 분류는 시각 자료를 그대로 반영해 서술해야 합니다.

## [불확실한 부분]
- 하단의 작은 글씨는 화면이 잘려 있어 **전체 문장과 의미를 정확히 판독하기 어렵습니다.**
- `마`가 들어 있는 도형은 정확히 어떤 사각형인지 이미지 만으로 단정하기 어렵습니다.
- `가` 도형이 정사각형인지, 직사각형인지, 혹은 세로로 긴 직사각형인지 해상도에 따라 다르게 보일 수 있습니다.
- 따라서 도형의 이름을 확정하기보다는, **보이는 외형 중심으로 묘사**하는 것이 적절합니다.

--- RAW VISION DRAFT END ---
