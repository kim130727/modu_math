# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008866.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008866

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 문제 번호 **15.**가 왼쪽 상단에 있고, 그 오른쪽에 지문이 두 줄로 배치되어 있다.
- 중앙에는 **수조 모양의 그림**이 있고, 그 오른쪽에 **컵 4개 그림**이 가로로 나란히 배치되어 있다.
- 컵 그림 각각 위에는 **가, 나, 다, 라**라는 글자가 하나씩 있다.
- 아래쪽에는 **표**가 있으며, 윗줄은 컵 이름, 아랫줄은 **부은 횟수(회)**를 나타낸다.
- 표 아래 오른쪽에는 **(가, 나, 다, 라)**가 한 번 더 적혀 있다.
- 더 아래 왼쪽에는 **(정답) 나**가 보이고, 그 아래에 **(해설)** 문장이 이어진다.

[보이는 텍스트]
- `15. 수조에 물을 가득 채우려면 가, 나, 다, 라 컵으로 다음과 같이 부어야 합니다. 가, 나, 다, 라 컵 중에서 들이가 가장 적은 컵을 찾아 기호를 선택하세요.`
- 컵 그림 위 글자: `가`, `나`, `다`, `라`
- 표의 첫 칸: `컵`
- 표의 둘째 줄 왼쪽 칸: `부은 횟수(회)`
- 표의 수치: `14`, `17`, `10`, `9`
- 표 아래: `(가, 나, 다, 라)`
- `(정답) 나`
- `(해설) 무는 횟수가 많을수록 컵의 들이가 적고, 무는 횟수가 적을수록 컵의 들이가 많습니다.`
- `부은 횟수가 많은 것부터 차례대로 쓰면 17>14>10>9이므로 들이가 가장 적은 컵은 나 컵입니다.`

[반복되는 시각 패턴]
- 컵 4개가 **좌에서 우로** 일정한 간격으로 나열되어 있다.
- 각 컵은 서로 다른 색과 모양으로 구분되어 있고, 위의 글자도 4개가 같은 형식으로 배치되어 있다.
- 표 또한 **4개의 선택지(가, 나, 다, 라)**가 열 형태로 반복된다.
- 숫자 정보는 한 줄에 4개가 나열되어 있으며, 컵 이름과 대응되도록 정렬되어 있다.
- 아래 해설에는 비교 기호 `>`가 연속으로 사용되어 수치의 크기 순서를 보여준다.

[수식 단계]
- 해설 문장 안에 비교식이 있다.
- `17>14>10>9`
- 이 표기는 각 컵의 **부은 횟수**를 큰 것부터 작은 것 순서로 나열한 모습이다.
- 별도의 등호 변형식은 보이지 않는다.

[등호별 변환 의미]
- 해당 없음.  
- 화면에는 `=`로 이어지는 변형 과정이 보이지 않고, 비교 부등호 `>`만 보인다.

[세부 내용]
- 왼쪽의 그림은 **수조/통**처럼 생긴 빈 상자 형태로 그려져 있으며, 내부가 비어 있는 입체 도형 느낌이다.
- 오른쪽의 4개 컵은 색과 모양이 서로 다르다.
  - `가`: 주황색 컵
  - `나`: 연한 분홍색 작은 그릇/컵 형태
  - `다`: 흰색 바탕에 파란 장식이 있는 컵
  - `라`: 연한 초록색의 긴 컵
- 표는 연한 분홍색 테두리로 구획되어 있고, 윗줄은 머리글, 아랫줄은 수치 행이다.
- 각 컵 아래의 수치는 각각의 **부은 횟수**로 보이며, 문제의 핵심 비교 정보로 사용된다.
- 문제는 “**들이가 가장 적은 컵**”을 찾는 형태의 선택 문제로 보인다.
- 해설 문장에는 “무는 횟수”라고 적힌 부분이 보이는데, 문맥상 다른 단어의 일부가 잘려 보이거나 OCR상의 표기 불일치일 가능성이 있다.

[Python DSL 작성 힌트]
- 상단 지문은 2줄 텍스트 박스로 배치한다.
- 중앙에는 왼쪽에 수조 그림, 오른쪽에 컵 4개 그림을 가로 정렬로 놓는다.
- 컵 그림 위에 `가/나/다/라` 라벨을 각각 별도 텍스트로 붙인다.
- 그 아래에는 2행 5열 표를 만든다.
  - 1행 1열: `컵`
  - 1행 2~5열: `가, 나, 다, 라`
  - 2행 1열: `부은 횟수(회)`
  - 2행 2~5열: `14, 17, 10, 9`
- 표 아래 우측에 `(가, 나, 다, 라)`를 별도 텍스트로 배치한다.
- 하단에는 `(정답) 나`와 해설 문장을 줄바꿈 포함하여 배치한다.
- 답을 나타내는 표시가 따로 있지 않으므로, 정답 텍스트는 해설 영역에서만 처리한다.
- 그림은 설명용이므로 실제 SVG/도형으로 단순화해도 되고, 컵별 색상 차이만 구분되면 된다.

[주의할 점]
- **정답을 추론해서 다시 쓰지 말고**, 화면에 보이는 텍스트만 기록한다.
- 표의 수치와 컵 이름 대응을 임의로 바꾸지 말 것.
- `가, 나, 다, 라`의 배열 순서는 화면에서 보이는 좌→우 순서를 유지할 것.
- 해설의 문장 일부는 어색하거나 OCR 오독 가능성이 있으므로, 의미를 단정적으로 보정하지 말 것.
- 문제를 푸는 설명이 아니라 **시각 구조 분석**에 집중할 것.
- 수조 그림과 컵 그림은 정답에 직접 표시가 없으므로, 위치와 관계만 설명한다.

[불확실한 부분]
- 해설의 `무는 횟수`는 원문 일부가 잘려 보이거나 OCR 오류일 가능성이 있다.
- 컵 그림의 정확한 형태는 단순 아이콘 수준으로만 확인 가능하며, 실제 세부 윤곽은 약간 다를 수 있다.
- 왼쪽의 큰 도형이 정확히 어떤 수조 구조인지 그림만으로는 세부 명칭을 단정하기 어렵다.
- `(정답) 나` 표시는 문제 이미지에 포함되어 있으나, 이것이 편집용 표시인지 학습용 표시인지 구분은 불확실하다.

--- RAW VISION DRAFT END ---
