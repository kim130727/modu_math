# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008860.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008860

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에는 문제 번호 `7.`과 함께 긴 문장형 질문이 왼쪽에서 오른쪽으로 배치되어 있다.
- 중앙에는 그림 비교가 위아래 두 줄로 배치되어 있다.
  - 위쪽 줄: 주황색 세숫대야 그림 → 회색 오른쪽 화살표 → 파란 컵 여러 개
  - 아래쪽 줄: 분홍색 바가지 그림 → 회색 오른쪽 화살표 → 파란 컵 여러 개
- 오른쪽 아래쪽에는 괄호 안에 보기 형태의 선택어가 `（ 세숫대야 , 바가지 ）`로 표시되어 있다.
- 왼쪽 아래에는 `(정답)`과 `(해설)`이 붙은 텍스트가 인쇄되어 있다.  
  - 이 부분은 문제지 본문이라기보다 해설 영역처럼 보인다.

[보이는 텍스트]
- `7.`
- `세숫대야와 바가지에 물을 가득 채운 후 물양과 같은 컵에 옮겨 담았더니 다음과 같았습니다. 세숫대야와 바가지 중에서 어느 것이 들이가 더 많을까요?`
- `( 세숫대야 , 바가지 )`
- `(정답)세숫대야`
- `(해설)세숫대야는 9컵, 바가지는 3컵이므로 세숫대야의 들이가 더 많습니다.`

[반복되는 시각 패턴]
- 물건 1개가 왼쪽에 있고, 오른쪽으로 향하는 화살표 뒤에 컵들이 배열되는 비교 구조가 두 번 반복된다.
- 위아래 두 줄 모두 같은 형태의 시각 패턴을 사용한다.
  - 대상 물건 그림
  - 오른쪽 화살표
  - 그 물건의 양을 나타내는 파란 컵 여러 개
- 파란 컵은 모두 같은 모양과 색으로 반복되며, 개수를 다르게 배치하여 양의 차이를 보여준다.
- 위쪽과 아래쪽의 컵 배열은 각각 한 줄 또는 두 줄처럼 보이도록 배치되어 있어, 개수를 시각적으로 세기 쉽게 만든다.

[수식 단계]
- 이 문제 화면에는 등호(`=`)를 포함한 수식 변형 단계가 보이지 않는다.
- 따라서 단계별 계산식은 없고, 그림과 컵 개수 비교로만 구성되어 있다.

[등호별 변환 의미]
- 해당 없음. 화면에 등호가 등장하는 수식 전개가 보이지 않는다.

[세부 내용]
- 문제는 “세숫대야”와 “바가지”의 들이(용량)를 비교하는 내용이다.
- 위쪽 그림의 주황색 용기 옆에는 파란 컵이 더 많이 배치되어 있고, 아래쪽 그림의 분홍색 용기 옆에는 파란 컵이 더 적게 배치되어 있다.
- 보이는 해설 문장에는 세숫대야가 `9컵`, 바가지는 `3컵`이라고 적혀 있으나, 이는 화면에 인쇄된 해설 정보로 보이며 문제 해결 과정의 일부로 제시되어 있다.
- 선택지처럼 보이는 `( 세숫대야 , 바가지 )`는 둘 중 하나를 고르는 형식의 응답 위치로 보인다.
- 왼쪽 아래의 `(정답)` 및 `(해설)` 표시는 본문 문제와 분리된 설명 영역이다.

[Python DSL 작성 힌트]
- 전체 레이아웃을 상단 문제문장, 중앙 비교 그림 2개, 하단 선택/해설 텍스트로 나누어 배치하면 좋다.
- 중앙 비교 그림은 위아래 2행 구조로 구현한다.
  - 1행: `주황색 세숫대야` + `오른쪽 화살표` + `파란 컵 여러 개`
  - 2행: `분홍색 바가지` + `오른쪽 화살표` + `파란 컵 여러 개`
- 파란 컵은 반복 아이콘으로 처리하고, 각 행의 개수 차이가 드러나도록 간격과 줄바꿈을 맞춘다.
- 선택지는 텍스트 괄호 형태로 오른쪽 아래에 따로 배치한다.
- `(정답)`, `(해설)`은 문제 본문과 구분되는 작은 텍스트 블록으로 좌하단에 넣는 구성이 적절하다.
- 문제문장은 상단에 여러 줄로 자동 줄바꿈되도록 길게 배치해야 한다.

[주의할 점]
- 그림 속 컵의 개수를 임의로 바꾸지 말아야 한다.
- 정답을 빈칸에 미리 채워 넣지 말아야 한다.
- 해설 문장에 나온 수치를 이용해 문제 본문을 재구성할 때, 본문과 해설을 섞지 않도록 주의해야 한다.
- 보이는 선택지와 해설 영역을 문제 본문과 혼동하지 않도록 구분해야 한다.
- 세숫대야와 바가지의 위치는 위아래로 고정되어 있으므로, 좌우를 바꾸지 말아야 한다.

[불확실한 부분]
- 아래쪽 그림의 파란 컵 개수는 해상도상 일부 겹쳐 보여 정확한 개수 확인이 애매할 수 있다.
- 위쪽 컵 배열도 약간 겹쳐 보이므로, 실제 아이콘 개수를 DSL로 옮길 때는 원본 이미지 기준으로 다시 세어 확인하는 것이 좋다.
- `(정답)`과 `(해설)`이 원래 문제지의 일부인지, 편집용 표시인지 화면만으로는 완전히 단정하기 어렵다.

--- RAW VISION DRAFT END ---
