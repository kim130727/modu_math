# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009045.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009045

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 지시문이 있고, 그 아래 오른쪽에 연한 분홍색 테두리의 큰 선택 박스가 배치되어 있습니다.
- 선택 박스 안에는 6개의 보기 그림이 2행 3열 형태로 정돈되어 있습니다.
- 각 보기에는 한글 보기표시(가, 나, 다 / 라, 마, 바)와 함께 선 모양이 하나씩 그려져 있습니다.
- 화면 하단에는 별도의 텍스트로 `(정답)`과 `(해설)`이 표시되어 있습니다.
- 전체적으로 “보기 중 조건에 맞는 선을 고르는” 객관식형 시각 자료입니다.

[보이는 텍스트]
- `□ 83. 곧은 선을 모두 선택하세요.`
- 보기 박스 안:
  - `가`
  - `나`
  - `다`
  - `라`
  - `마`
  - `바`
- 하단:
  - `(정답) 가, 마, 바`
  - `(해설) 가, 마, 바와 같이 두 점을 굽게 이은 선은 선분이라고 합니다.`
- 주의: 하단의 정답/해설은 화면에 실제로 적혀 있는 텍스트이며, 이를 근거로 문제를 풀어 재진술하지는 않습니다.

[반복되는 시각 패턴]
- 보기 6개가 일정한 간격으로 배열되어 있어, 각 항목이 독립적인 선택 대상처럼 보입니다.
- 각 보기마다:
  - 왼쪽에 한글 라벨 1개
  - 오른쪽 또는 아래쪽에 하나의 선 그림
  - 라벨과 그림이 같은 칸 안에서 짝을 이룸
- 선의 형태가 서로 다르게 반복됩니다.
  - 직선처럼 보이는 선
  - 곡선처럼 휘어진 선
  - S자 형태의 곡선
  - 소용돌이 모양의 곡선
  - 완만한 아치형 곡선
- 전체 구조는 “라벨+그림”의 반복 패턴으로 정리할 수 있습니다.

[수식 단계]
- 해당 없음.
- 화면에는 등호가 포함된 수식 변형 과정이 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.
- 등호를 기준으로 한 수식 변화가 표시되어 있지 않습니다.

[세부 내용]
- 상단 문제 문구:
  - 왼쪽에 작은 네모 기호가 있고, 그 옆에 문제 번호 `83.`이 있습니다.
  - 문장 `곧은 선을 모두 선택하세요.`가 이어집니다.
- 선택 박스:
  - 연한 분홍색 사각형 테두리 안에 6개의 보기 배치가 들어 있습니다.
  - 왼쪽 위는 `가`, 가운데 위는 `나`, 오른쪽 위는 `다`.
  - 왼쪽 아래는 `라`, 가운데 아래는 `마`, 오른쪽 아래는 `바`.
- 각 보기의 그림 형태:
  - `가`: 왼쪽 아래에서 오른쪽 위로 올라가는 짧은 직선 형태로 보입니다.
  - `나`: 위쪽으로 볼록한 완만한 곡선입니다.
  - `다`: S자처럼 굽은 곡선입니다.
  - `라`: 아래쪽에서 안쪽으로 말리는 나선형 곡선입니다.
  - `마`: 가로로 긴 수평선입니다.
  - `바`: 왼쪽 위에서 오른쪽 아래로 내려가는 직선 형태로 보입니다.
- 하단 해설 문장:
  - `가, 마, 바와 같이 두 점을 굽게 이은 선은 선분이라고 합니다.`
  - 문장 중 `가, 마, 바`는 앞의 보기 이름을 다시 언급하는 형태입니다.

[Python DSL 작성 힌트]
- 큰 외곽 박스 1개를 먼저 그린 뒤, 그 안에 2행 3열의 보기 배치를 구성하면 좋습니다.
- 각 보기 칸에는:
  - 텍스트 라벨 1개
  - 선 도형 1개
  를 함께 배치하는 구조로 작성하면 됩니다.
- 선 도형은 유형만 표현하고, 정답을 표시하는 별도 표시는 넣지 않는 것이 안전합니다.
- 라벨 위치는 그림의 왼쪽 위쪽에 작게 두고, 선은 칸의 중앙~오른쪽 영역에 배치하면 원본과 비슷합니다.
- 하단의 `(정답)`, `(해설)` 텍스트는 문제 원문 재현용으로만 필요할 수 있으며, 학습용 도면 생성 시에는 제외할 수도 있습니다.
- 전체 문장을 이미지 상단 왼쪽에, 보기 박스는 그보다 아래쪽 오른편에 배치하는 구성이 자연스럽습니다.

[주의할 점]
- 답을 추론해 빈칸에 채우지 마세요. 이 자료는 시각 분석이 목적입니다.
- `곧은 선`과 `선분`의 개념을 설명하는 해설이 있지만, 여기서는 문제 해결이 아니라 화면 요소 분석만 해야 합니다.
- 선이 약간 기울어져 있어도, 본문에서 이를 곧은 선으로 분류하는 기준을 임의로 단정하지 마세요.
- `정답`과 `해설`은 화면에 적혀 있지만, DSL 작성 시 실제로 포함할지 여부는 별도 판단이 필요합니다.
- 각 보기의 선 모양은 손그림처럼 약간 비대칭이며, 완전한 기하학 도형처럼 딱딱하지 않습니다.

[불확실한 부분]
- 좌상단의 작은 네모 기호가 체크박스인지, 문제 번호 표식인지 화면만으로는 단정하기 어렵습니다.
- 선의 정확한 곡률과 기울기는 이미지 해상도상 미세하게 달라 보일 수 있습니다.
- `가`와 `바`의 직선이 실제로는 아주 약간 굽어 보일 가능성이 있으나, 화면상으로는 곧은 선으로 인식됩니다.
- 외곽 분홍색 박스의 정확한 색상명과 선 굵기는 화면 캡처 환경에 따라 다르게 보일 수 있습니다.

--- RAW VISION DRAFT END ---
