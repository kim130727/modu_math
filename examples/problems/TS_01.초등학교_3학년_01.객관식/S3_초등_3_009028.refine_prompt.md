# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009028.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009028

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 번호 `67.`과 함께 지시문 `직각삼각형을 모두 찾아 기호를 선택하세요.`가 왼쪽에서 오른쪽으로 배치되어 있습니다.
- 그 아래에는 연한 분홍색 테두리의 큰 박스가 있고, 그 안에 삼각형 도형 4개가 가로로 나열되어 있습니다.
- 각 도형 내부에는 각각 `가, 나, 다, 라`라는 글자가 하나씩 들어 있습니다.
- 큰 박스 아래 중앙에는 선택지를 적는 형태로 보이는 `( 가 , 나 , 다 , 라 )`가 있습니다.
- 중간 왼쪽에는 `(정답) 나, 라`라는 텍스트가 보입니다.
- 그 아래에는 `(해설)` 문장과 함께, 같은 4개의 삼각형이 다시 한 번 연한 분홍색 테두리 박스 안에 재배치되어 있습니다.
- 아래쪽 도형들 중 `나`, `라` 도형에는 직각 표시로 보이는 작은 파란색 직각 기호가 추가되어 있습니다.

[보이는 텍스트]
- `67.`
- `직각삼각형을 모두 찾아 기호를 선택하세요.`
- `가`
- `나`
- `다`
- `라`
- `( 가 , 나 , 다 , 라 )`
- `(정답) 나, 라`
- `(해설)가와 다에는 직각인 각이 하나도 없으므로 직각삼각형이 아닙니다.`

[반복되는 시각 패턴]
- 삼각형 4개가 같은 문제 안에서 두 번 반복되어 나타납니다.
- 첫 번째 줄의 도형 배치는 왼쪽부터 `가`, `나`, `다`, `라` 순서입니다.
- 두 번째(해설) 줄에서도 같은 순서의 도형이 다시 등장하지만, 직각이 있는 것으로 보이는 도형에는 파란색 직각 표시가 추가되어 있습니다.
- 두 도형(`나`, `라`)은 직각 표시가 있는 반면, `가`, `다`는 표시가 없습니다.
- 분홍색 테두리 박스가 상단 문제 도형 묶음과 하단 해설 도형 묶음을 각각 감싸는 형태로 사용됩니다.

[수식 단계]
- 이 문제 화면에는 등호를 포함한 수식 변형 단계가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호를 이용한 변형식이 표시되어 있지 않습니다.

[세부 내용]
- 첫 번째 박스 안의 `가` 도형은 왼쪽 위에서 시작해 아래쪽 오른쪽으로 이어지는 긴 변과, 아래쪽 좌측에서 우측으로 이어지는 변이 보이는 삼각형입니다.
- `나` 도형은 위쪽이 수평에 가깝고 오른쪽 변이 수직에 가깝게 내려오는 삼각형이며, 해설 그림에서는 오른쪽 위 꼭짓점에 파란색 직각 표시가 있습니다.
- `다` 도형은 비교적 대칭적인 삼각형처럼 보이며, 해설 그림에서도 직각 표시가 없습니다.
- `라` 도형은 바닥이 길게 수평으로 놓인 삼각형이고, 해설 그림에서는 위쪽 한 꼭짓점에 파란색 직각 표시가 있습니다.
- 문제 문장 `직각삼각형을 모두 찾아 기호를 선택하세요.`는 여러 개를 고르는 문제 형식입니다.
- `( 가 , 나 , 다 , 라 )`는 선택 가능한 기호를 괄호 안에 나열한 형태로 보이며, 실제 정답을 직접 채우는 입력칸처럼 보이기보다는 보기 목록에 가깝습니다.

[Python DSL 작성 힌트]
- 상단 문제 영역과 하단 해설 영역을 서로 분리해서 배치하는 구성이 적절합니다.
- 삼각형 4개를 가로 정렬로 배치하고, 각 도형 중심 부근에 `가/나/다/라` 텍스트를 넣는 방식이 자연스럽습니다.
- 해설 영역에서는 동일한 4개 도형을 다시 배치하되, 직각으로 표시된 도형에만 작은 파란색 직각 표시를 추가하는 구조를 반영하면 좋습니다.
- 분홍색 테두리 박스는 둥근 모서리 형태로 보이며, 각 도형 묶음을 감싸는 역할을 합니다.
- `정답` 표시는 별도 텍스트 블록으로 처리하고, 실제 선택 답안을 코드에서 추론해 채우지 않도록 주의해야 합니다.
- 도형의 각도나 변 길이는 정확한 수치보다 상대적 형태를 우선하여 표현하는 것이 좋습니다.

[주의할 점]
- 정답을 추론하여 빈칸처럼 보이는 부분에 직접 채우지 마세요.
- `가`, `나`, `다`, `라` 중 어떤 것이 정답인지 시각 자료에 보이는 그대로만 기록하세요.
- 해설의 문장을 문제 풀이로 확장하지 말고, 화면에 적힌 내용만 옮기세요.
- `나`, `라`에 있는 파란색 직각 표시는 해설 그림에서만 추가로 보입니다.
- 직각 표시가 없는 도형에 대해 직각이라고 단정하지 마세요.

[불확실한 부분]
- 첫 번째 박스의 도형 내부 글자 위치는 정확히 중심인지 약간 치우쳤는지 해상도상 다소 애매합니다.
- `가`, `나`, `다`, `라` 도형의 변 각도는 이미지 품질 때문에 완전히 정밀하게 판독하기 어렵습니다.
- 아래쪽 해설 문장의 문장 부호와 공백은 화면상 약간 붙어 보이므로, DSL 작성 시 자연스러운 띄어쓰기를 별도로 조정해야 할 수 있습니다.

--- RAW VISION DRAFT END ---
