# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008761.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008761

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호 `63.`이 있고, 그 오른쪽으로 문제 지시문이 두 줄로 배치되어 있습니다.
- 중앙에는 저울 그림이 크게 배치되어 있으며, 저울의 왼쪽 접시에는 `사과`, 오른쪽 접시에는 `배`가 놓여 있습니다.
- 하단에는 보기 형식의 문장 두 개가 `(1)`, `(2)`로 번호 매겨져 세로로 나열되어 있습니다.
- 그 아래에 `(정답)`이 있고, 이어서 선택 결과가 짧게 제시되어 있습니다.
- 맨 아래에는 `(해설)`이 있으며, 한 문장으로 이유를 설명하고 있습니다.
- 전체적으로 “그림 보고 알맞은 문장 선택” 문제 형식입니다.

[보이는 텍스트]
- `63. 저울을 사용하여 사과와 배의 무게를 비교하려고 합니다. 알맞은 말을 선택하세요.`
- 그림 속 라벨: `사과`, `배`
- `(1) ( 사과 , 배 )를 올려놓은 접시가 아래로 내려갔습니다.`
- `(2) ( 사과 , 배 )가 더 무겁습니다.`
- `(정답)(1) 배 (2) 배`
- `(해설)배를 올려놓은 접시가 아래로 내려갔으므로 배가 더 무겁습니다.`

[반복되는 시각 패턴]
- 괄호 형태의 선택지 패턴이 두 번 반복됩니다.
  - `( 사과 , 배 )`처럼 두 단어가 쉼표로 구분된 형태
  - 각각의 문장 속 빈칸 역할을 하는 선택 구간으로 보입니다.
- `(1)`, `(2)`처럼 번호가 붙은 문장 구조가 반복됩니다.
- 정답과 해설도 괄호 표기 `(정답)`, `(해설)`로 구분되어 있습니다.
- 그림에서는 좌우 두 접시가 대응되는 대칭 구조이고, 각 접시 위에 사물 1개씩 놓여 있습니다.

[수식 단계]
- 해당 없음. 수식이나 등호로 이어진 변형식은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호를 포함한 계산/변형 과정이 제시되지 않았습니다.

[세부 내용]
- 저울 그림은 파란색 받침대와 중앙 기둥, 좌우 접시로 구성되어 있습니다.
- 왼쪽 접시 위에는 빨간색 사과 그림이 있고, 위에 `사과`라는 라벨 상자가 있습니다.
- 오른쪽 접시 위에는 노란색/주황색 배 그림이 있고, 위에 `배`라는 라벨 상자가 있습니다.
- 저울의 오른쪽 접시가 왼쪽보다 아래쪽에 위치해 보이며, 이를 바탕으로 아래 문장에서 비교 결과를 묻고 있습니다.
- 문장 `(1)`은 “접시가 아래로 내려갔다”는 관찰을 바탕으로, 빈칸에 사과와 배 중 하나를 넣는 형태입니다.
- 문장 `(2)`는 “더 무겁습니다” 앞의 주어가 빈칸 처리된 형태입니다.
- 하단 `(정답)` 줄에는 두 문항에 대해 모두 `배`가 적혀 있습니다.
- 다만 이는 화면에 제시된 정답 정보일 뿐, 여기서는 계산으로 검증하거나 재도출하지 않습니다.

[Python DSL 작성 힌트]
- 좌측 상단에 문제 번호와 지시문 텍스트를 배치하세요.
- 중앙에는 저울 일러스트를 넣고, 왼쪽 접시 위에 사과, 오른쪽 접시 위에 배를 배치하세요.
- 각 과일 위에는 연한 배경색의 작은 라벨 박스로 `사과`, `배`를 각각 표시하세요.
- 저울은 좌우 접시의 높이 차이가 보이도록 표현해, 오른쪽이 더 내려간 시각 상태를 반영하면 좋습니다.
- 하단에는 `(1)`, `(2)`가 붙은 두 문장을 줄바꿈으로 배치하고, 괄호 안의 `사과 , 배` 부분을 선택형 텍스트처럼 처리하세요.
- 그 아래에 `(정답)`과 `(해설)`을 별도 줄로 구분해 넣으세요.
- 텍스트는 왼쪽 정렬 위주, 그림은 가운데 정렬 위주로 구성하면 원본과 유사합니다.

[주의할 점]
- 문제를 풀어 결과를 추론해 넣지 마세요.
- 빈칸/선택지 자리에 답을 자동 완성하지 마세요.
- 그림의 저울 기울기 방향은 시각적으로만 설명하고, 그 의미를 해설로 확장하지 마세요.
- `(정답)` 줄은 화면에 보이는 문자열만 기록하고, 별도 계산 과정은 추가하지 마세요.
- `사과`, `배` 라벨의 위치와 접시 좌우 배치를 혼동하지 않도록 주의하세요.

[불확실한 부분]
- 저울의 정확한 기울기 각도는 이미지 해상도상 아주 미세하게 보일 수 있어 단정하지 않는 것이 좋습니다.
- 저울 중앙 바늘/지시선의 세부 형태는 작게 보여 일부 디테일이 불명확할 수 있습니다.
- `(정답)` 줄의 글자 간 간격이나 줄바꿈 위치는 화면 크기에 따라 약간 다르게 보일 수 있습니다.

--- RAW VISION DRAFT END ---
