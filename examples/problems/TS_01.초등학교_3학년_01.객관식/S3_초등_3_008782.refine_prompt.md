# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008782.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008782

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
## [전체적인 구성]
- 화면 왼쪽 상단에 문제 번호와 지시문이 있고, 오른쪽에는 주황색 테두리의 큰 선택 상자가 배치되어 있습니다.
- 선택 상자 안에는 보기 3개가 세로로 나열되어 있으며, 각 보기는 동그라미 안의 기호와 등식 형태의 문장으로 구성되어 있습니다.
- 선택 상자 아래쪽 왼편에는 `(정답)` 표기가 있고, 그 옆에 동그라미 안 기호가 표시되어 있습니다.
- 그 아래에는 `(해설)` 표기와 함께 하나의 식이 가로로 길게 제시되어 있습니다.
- 전체적으로 “옳지 않은 것 찾기” 유형의 객관식 문제 구성입니다.

## [보이는 텍스트]
- `□ 2. 무게의 단위를 잘못 나타낸 것을 찾아 기호를 선택하세요.`
- 주황색 박스 안의 보기:
  - `㉠ 3 kg 40 g = 3040 g`
  - `㉡ 4000 kg = 4 t`
  - `㉢ 2 kg 700 g = 2070 g`
- 아래쪽:
  - `(정답) ㉢`
  - `(해설) ㉢ 2 kg 700 g = 2 kg + 700 g = 2000 g + 700 g = 2700 g`

## [반복되는 시각 패턴]
- 각 보기 앞에 원형 기호가 붙어 있습니다.
  - `㉠`, `㉡`, `㉢`처럼 보기 번호를 원 안에 넣은 형태입니다.
- 보기마다 `숫자 + 단위`가 좌변과 우변에 반복적으로 등장합니다.
- 세 번째 보기는 해설 문장에서도 같은 기호 `㉢`가 다시 사용되어 강조됩니다.
- 해설 식은 `=` 기호를 여러 번 사용하여 단계적으로 이어지는 형태입니다.

## [수식 단계]
- 해설에 등호가 연속으로 있는 변형식이 있습니다.
- 보이는 순서:
  - `2 kg 700 g`
  - `= 2 kg + 700 g`
  - `= 2000 g + 700 g`
  - `= 2700 g`
- 각 단계는 같은 양을 다른 표현으로 바꾼 형태로 보입니다.
- 마지막 결과는 오른쪽에 붙어 있으며, 중간 과정이 왼쪽에서 오른쪽으로 이어집니다.

## [등호별 변환 의미]
- 첫 번째 등호:
  - `2 kg 700 g`를 `2 kg + 700 g`로 쪼개어 쓴 것으로 보입니다.
- 두 번째 등호:
  - `2 kg`를 `2000 g`로 바꾼 것으로 보입니다.
- 세 번째 등호:
  - `2000 g + 700 g`을 하나의 무게값으로 정리한 형태입니다.
- 다만 이 섹션은 해설의 보이는 흐름을 설명하는 것이며, 정답 계산 자체를 새로 수행하는 것은 아닙니다.

## [세부 내용]
- 문제 지시문은 “무게의 단위를 잘못 나타낸 것”을 고르고, 그에 해당하는 기호를 선택하라고 합니다.
- 선택지는 모두 같은 형식으로 구성되어 있습니다.
  - 왼쪽에 원형 보기 기호
  - 가운데에 무게 표현
  - 등호
  - 오른쪽에 변환된 값 또는 단위
- 첫 번째 보기 `3 kg 40 g = 3040 g`는 좌우가 한 줄로 정렬되어 있습니다.
- 두 번째 보기 `4000 kg = 4 t`는 단위 `kg`와 `t`가 함께 보입니다.
- 세 번째 보기 `2 kg 700 g = 2070 g`도 같은 형식이지만, 아래 해설에서 다시 따로 설명됩니다.
- 정답 표기에서는 `㉢`가 표시되어 있고, 해설에서도 동일한 기호가 다시 등장합니다.
- 해설 식은 띄어쓰기가 비교적 넓게 들어가 있어 각 항이 분명히 구분됩니다.

## [Python DSL 작성 힌트]
- 상단 문제 문장은 텍스트 객체로 한 줄 배치합니다.
- 주황색 큰 테두리 사각형을 먼저 그리고, 그 안에 보기 3개를 세로 정렬합니다.
- 각 보기의 시작 부분에는 원형 기호 `㉠`, `㉡`, `㉢`를 따로 배치하고, 뒤에 식 텍스트를 붙입니다.
- 보기 텍스트는 등호를 기준으로 좌우가 같은 줄에 오도록 정렬합니다.
- 하단의 `(정답)`과 `(해설)`은 왼쪽 정렬로 배치하되, `(정답)` 옆에는 동그라미 기호를 따로 표시합니다.
- 해설 식은 한 줄 긴 수식으로 배치하고, `=`가 여러 번 반복되도록 텍스트를 분리하지 말고 연속 문자열처럼 표현하는 것이 좋습니다.
- 필요하다면 해설 식의 각 변환 단계에 대해 별도의 텍스트 박스를 사용해도 됩니다.

## [주의할 점]
- 이 문제는 단위 변환의 옳고 그름을 묻는 형식이므로, 보기의 참/거짓을 직접 판정해 답을 써 넣지 마세요.
- 빈칸 문제는 아니므로, 답 칸을 추론해 추가하지 마세요.
- 해설에 있는 등식 전개를 임의로 바꾸거나 생략하지 마세요.
- `kg`, `g`, `t`처럼 단위가 명확히 보이므로, 단위 기호를 오타 없이 그대로 옮겨야 합니다.
- 원형 보기 기호와 `(정답)` 옆의 기호는 서로 같은 도형 요소로 보이지만 역할이 다르므로 구분해서 설명하는 것이 좋습니다.

## [불확실한 부분]
- 화면 맨 앞의 작은 빈 사각형 기호 `□`는 문제 번호 표시와 함께 있는 장식 요소처럼 보이지만, 정확한 용도는 화면만으로 단정하기 어렵습니다.
- 주황색 박스의 정확한 색상값, 선 두께, 내부 여백은 이미지 해상도에 따라 달라질 수 있습니다.
- `(정답)` 옆의 원형 기호가 실제로 어떤 문자를 나타내는지, 이미지 품질상 약간 흐릿하게 보일 수 있습니다.
- 해설의 줄 간격과 수식 사이 공백 폭은 실제 DSL에서 시각적으로 약간 다르게 보일 수 있습니다.

--- RAW VISION DRAFT END ---
