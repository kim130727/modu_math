# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008929.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008929

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 페이지형 학습 문제 화면이다.
- 상단에는 문제 설명 문장이 있고, 그 아래에 연한 민트색 배경의 큰 모둠 상자가 있다.
- 모둠 상자 안에는 4개의 작은 카드형 박스가 가로로 나란히 배치되어 있으며, 각 카드 위쪽에 동물 이름 표지가 붙어 있다.
- 하단에는 95번 문항이 있고, 왼쪽에 소진의 캐릭터 그림, 오른쪽에 말풍선 형태의 설명 박스가 있다.
- 맨 아래에는 `(정답)`과 `(해설)`이 텍스트로 제시되어 있다.

[보이는 텍스트]
- 상단 안내문:
  - `[95~97] 소진이는 반 학생들이 기르고 싶어 하는 동물을 조사하여 표를 나타내려고 합니다. 물음에 답하세요.`
- 상단 제목:
  - `기르고 싶어 하는 동물`
- 카드 제목(왼쪽부터):
  - `강아지`
  - `고양이`
  - `햄스터`
  - `앵무새`
- 95번 문제:
  - `95. 소진이가 자료를 수집한 방법을 선택하세요.`
- 말풍선 안 문장:
  - `난 정확한 자료 수집 결과를 남기기 위해 시간과 노력이 들더라도`
  - `(직접 손 들기, 붙임딱지 붙이기) 방법을 선택했어.`
- 캐릭터 이름표:
  - `소진`
- 정답 표시:
  - `(정답) 붙임딱지 붙이기`
- 해설 표시:
  - `(해설) 직접 손 들기 방법은 짧은 시간에 자료를 수집할 수 있으나 모든 학생이 한 번에 참여해야 합니다.`

[반복되는 시각 패턴]
- 동물 카드 4개가 같은 크기와 비슷한 테두리 스타일로 반복 배치되어 있다.
- 각 카드에는 노란색 별 모양이 여러 개씩 흩어져 있어, 동물별 선호 인원을 시각적으로 나타내는 표식처럼 보인다.
- 카드 제목은 모두 회색 계열의 작은 라벨 박스에 들어가 있다.
- 상단 큰 영역 전체도 점선 느낌의 테두리로 한 번 감싸져 있고, 그 안에 개별 카드가 다시 반복된다.
- 하단 문제 영역에서는 왼쪽 인물 그림 + 오른쪽 말풍선 박스 구조가 반복적으로 사용된 설명형 레이아웃이다.

[수식 단계]
- 해당 없음. 화면에 등호가 이어지는 계산식이나 변형식은 보이지 않는다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 상단의 큰 민트색 영역은 자료 수집 결과를 시각적으로 나타내려는 표 형식 또는 분류표의 일부처럼 보인다.
- 각 동물 카드 안에는 별 모양 표시가 들어가 있다.
  - 강아지 카드: 별이 가장 많이 보인다.
  - 고양이 카드: 별이 여러 개 보인다.
  - 햄스터 카드: 별이 적은 편으로 보인다.
  - 앵무새 카드: 별이 더 적게 보인다.
- 별의 정확한 개수는 문제 풀이 대상이 아니므로 여기서는 세지 않는다.
- 소진 캐릭터 옆 말풍선은 선택지 문장 구조를 포함하고 있으며, 괄호 안에 `직접 손 들기, 붙임딱지 붙이기`가 함께 제시되어 있다.
- 말풍선의 본문은 “정확한 자료 수집 결과를 남기기 위해 시간과 노력이 들더라도”라는 이유 설명 뒤에, 두 방법 중 하나를 고르게 하는 형태다.
- 하단의 `(정답)`과 `(해설)`은 문제 풀이 후 설명용으로 보이며, 본 시각 분석에서는 내용만 확인한다.

[Python DSL 작성 힌트]
- 상단 안내문, 큰 제목, 카드 4개, 하단 문제 번호, 캐릭터, 말풍선, 정답/해설 텍스트를 각각 독립 객체로 분리해 배치하면 좋다.
- 큰 민트색 배경 박스 안에 4개의 소카드를 가로 정렬하는 구조를 먼저 잡고, 각 카드 안에는 별 아이콘들을 개별 요소로 배치하는 방식이 적합하다.
- 동물 이름 라벨은 카드 상단에 겹쳐 붙은 작은 둥근 사각형으로 표현하면 원본 느낌에 가깝다.
- 소진 캐릭터는 말풍선 왼쪽 아래쪽에 위치하고, 말풍선의 꼬리는 캐릭터 쪽을 향하게 두면 자연스럽다.
- `(직접 손 들기, 붙임딱지 붙이기)`는 괄호가 포함된 한 줄 텍스트로 처리하되, 선택지 강조가 필요한 경우 색이나 굵기를 분리할 수 있다.
- `(정답)`과 `(해설)`은 본문과 다른 스타일의 보조 텍스트로 하단에 배치하면 된다.

[주의할 점]
- 별 모양의 정확한 개수를 추론하여 표나 답으로 사용하지 말아야 한다.
- 카드 안의 별이 어떤 의미인지 문제 풀이로 확정하지 말고, “시각적으로 자료를 나타낸다” 정도로만 서술해야 한다.
- 말풍선 안의 선택지는 답을 채워 넣는 식으로 재작성하지 말고, 화면에 보이는 그대로 보존해야 한다.
- 정답 표시가 화면 하단에 보이더라도, 시각 분석 초안에서는 이를 근거로 문제를 풀거나 선택지를 확정하지 않는다.
- 상단 제목과 안내문은 서로 다른 텍스트 블록이므로 합쳐서 한 문장으로 만들지 않는 것이 좋다.

[불확실한 부분]
- 별 모양의 정확한 개수는 이미지 해상도상 세기 어렵고, 이 분석에서는 확정하지 않았다.
- 카드 안의 별이 실제로 학생 수를 의미하는지, 단순 장식인지 화면만으로는 100% 단정할 수 없다.
- 말풍선의 문장 줄바꿈은 이미지상 두 줄로 보이지만, 원문 입력 시 줄바꿈 위치는 달라질 수 있다.
- 상단 큰 영역의 정확한 색상 명칭과 테두리 굵기는 화면 해상도에 따라 다르게 인식될 수 있다.

--- RAW VISION DRAFT END ---
