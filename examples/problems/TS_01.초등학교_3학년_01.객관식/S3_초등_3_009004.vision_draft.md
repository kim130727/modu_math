[전체적인 구성]
- 상단에 문제 번호와 지시문이 있고, 그 아래에 도형 선택용 그림 상자가 크게 배치되어 있습니다.
- 첫 번째 도형 묶음은 주황색 테두리의 긴 사각형 안에 네 개의 도형이 가로로 나열되어 있습니다.
- 각 도형 안에는 한 글자씩 들어가 있으며, 아래쪽에 선택 결과를 적는 듯한 괄호 형태의 텍스트가 있습니다.
- 중간 왼쪽에는 `(정답)` 표기가 있고, 그 옆에 짧은 답안 텍스트가 보입니다.
- 하단에는 `(해설)` 표기와 함께, 다시 한 번 주황색 테두리 상자 안에 도형들이 가로로 배열되어 있으며 각 도형의 네 모서리에는 직각 표시가 파란색으로 들어가 있습니다.
- 맨 아래에는 해설 문장이 한 줄로 제시되어 있습니다.

[보이는 텍스트]
- `44. 직사각형을 모두 찾아 기호를 선택하세요.`
- 도형 내부 글자: `가`, `나`, `다`, `라`
- 괄호 표기: `(가, 나, 다, 라)`
- `정답) 가, 다`
- `해설)`
- `네 각이 모두 직각인 사각형은 가, 다입니다.`

[반복되는 시각 패턴]
- 같은 네 개의 도형이 위쪽과 아래쪽에 반복 배치되어 있습니다.
- 위쪽 도형 묶음과 아래쪽 도형 묶음은 모두 주황색 테두리 상자 안에 들어 있습니다.
- 각 도형은 서로 다른 모양이지만, 글자 표기는 동일하게 한 글자씩 들어가 있습니다.
- 아래쪽 해설 그림에서는 각 도형의 모서리에 작은 파란색 직각 표시가 반복적으로 나타납니다.
- 직각 표시가 있는 도형과 없는 도형이 대비되도록 배열되어 있습니다.

[수식 단계]
- 해당 없음. 수식 변형식이나 등호가 연속된 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 문제 지시문은 “직사각형을 모두 찾아 기호를 선택”하라고 되어 있어, 여러 도형 중 조건에 맞는 것을 고르는 유형으로 보입니다.
- 위쪽의 첫 번째 도형 묶음은 선택 대상 도형들로 보이며, 각각 왼쪽부터 `가`, `나`, `다`, `라`의 기호가 붙어 있습니다.
- 도형 모양은 각각 다르게 그려져 있습니다.
  - `가`: 가로로 긴 직사각형처럼 보이는 네모 도형
  - `나`: 마름모 형태의 도형
  - `다`: 세로로 긴 네모 도형
  - `라`: 오른쪽 윗변이 비스듬한 사각형처럼 보이는 도형
- 아래쪽 해설 그림에는 `가`, `다`, `라` 도형이 다시 제시되며, 각 꼭짓점에 파란색 직각 표시가 있는 도형과 없는 도형이 구분되어 보입니다.
- 해설 문장은 직사각형의 조건을 설명하는 형태로 보이지만, 여기서는 조건 자체를 푸는 것이 아니라 문장 구조만 확인할 수 있습니다.
- `(가, 나, 다, 라)`는 선택 가능한 전체 기호 목록처럼 배치되어 있고, 실제 정답 표기는 별도로 `정답) 가, 다`로 적혀 있습니다.

[Python DSL 작성 힌트]
- 상단 문제 문장은 텍스트 객체로 배치합니다.
- 첫 번째 주황색 테두리 상자는 긴 가로 직사각형 프레임으로 그립니다.
- 그 안에 도형 4개를 좌→우 순서로 정렬합니다.
- 각 도형 내부에 `가, 나, 다, 라` 텍스트를 중앙 정렬로 넣습니다.
- 두 번째 괄호 텍스트 `(가, 나, 다, 라)`는 도형 묶음 아래 중앙에 배치합니다.
- 중간의 `정답) 가, 다`는 본문 텍스트로 따로 배치합니다.
- 하단 `해설` 영역은 또 하나의 주황색 테두리 상자로 감싸고, 내부에 4개 도형을 같은 순서로 배치합니다.
- 해설 도형에는 파란색 직각 표시를 각 꼭짓점 근처에 작게 그려 넣습니다.
- 맨 아래 해설 문장은 별도 텍스트 라인으로 넣습니다.
- 각 도형의 기울기, 변의 각도, 직각표시 유무를 도형별로 분리해서 그리는 것이 중요합니다.

[주의할 점]
- 정답을 새로 추론해 입력하지 마세요.
- 보이는 텍스트 중 `정답) 가, 다`는 이미지에 실제로 적힌 내용이므로, 이를 다른 답으로 바꾸지 마세요.
- 도형 이름을 단정할 때는 보이는 형태 중심으로 설명하고, 성질 판정은 필요 이상으로 확장하지 마세요.
- `라` 도형은 직사각형처럼 보이지 않지만, 해설용 그림과 함께 제시되어 있어 혼동될 수 있으므로 형태만 객관적으로 적는 것이 좋습니다.
- 파란색 표시의 의미를 단정적으로 확장하지 말고, 그림에 보이는 표시로만 서술하세요.

[불확실한 부분]
- `라` 도형의 정확한 변 구조는 이미지 해상도상 다소 애매합니다. 오른쪽 변과 윗변의 관계를 단정적으로 말하기보다 “한쪽 변이 비스듬한 사각형” 정도로 표현하는 것이 안전합니다.
- 아래 해설 상자 안의 파란색 표시가 각 도형의 네 모서리 모두에 정확히 동일하게 그려졌는지, 또는 일부만 강조된 것인지 해상도상 완전히 확정하기 어렵습니다.
- `정답) 가, 다`의 줄 시작 괄호/기호 표기는 이미지에서 약간 흐려져 보여 정확한 문장부호 형태는 유동적으로 볼 수 있습니다.
