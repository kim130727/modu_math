[문제 개요]
- 문제 번호는 `29.`로 보입니다.
- 지시문은 `직사각형이면 ○, 직사각형이 아니면 ×를 선택하세요.`입니다.
- 화면은 도형 판별형 문제로 보이며, `○`와 `×` 중 하나를 고르는 형식입니다.
- 하단에는 해설 문장이 함께 제시되어 있습니다.

[원본에 보이는 요소]
- 상단 왼쪽에 `29.`가 있습니다.
- 상단 한 줄 지시문에 `직사각형이면 ○, 직사각형이 아니면 ×를 선택하세요.`가 보입니다.
- 오른쪽 위에 세로로 긴 사각형 1개가 있습니다.
- 그 사각형 아래에 파란색 `×` 표식이 있습니다.
- 왼쪽 중간에 작은 파란색 원형 표시가 있습니다.
- `(정답)`이라는 글자 옆에 빈 원이 있습니다.
- 왼쪽 아래에 `(해설)`이라는 글자와 함께 직각 표시가 있는 사각형이 있습니다.
- 맨 아래에는 `네 각이 모두 직각인 사각형을 직사각형이라고 합니다.`라는 문장이 있습니다.

[수식/관계 구조]
- 등식이나 계산식은 보이지 않습니다.
- 이 문제는 수식 구조보다 판별 관계가 핵심입니다.
- 보이는 관계:
  - `직사각형` 여부를 보고 `○/×`를 고르는 구조
  - 해설 그림은 네 꼭짓점에 직각 표시가 있어 `직사각형`의 정의를 시각적으로 설명함

[등호별 변환 의미]
- 해당 없음.
- 등호 연쇄나 식 변형이 보이지 않습니다.

[빈칸 정책]
- `(정답)` 옆의 빈 원은 비워 둡니다.
- 보이는 기호가 있더라도, 추론해서 `○` 또는 `×`를 채우지 않습니다.
- 빈칸/선택칸은 관찰 가능한 형태만 기록하고, 값은 확정하지 않습니다.

[반복 패턴]
- 원형 기호가 여러 곳에 반복됩니다.
  - 지시문에는 `○`가 등장합니다.
  - `(정답)` 옆에 빈 원이 있습니다.
  - 왼쪽 중간에 작은 파란색 원형 표시가 있습니다.
  - 오른쪽 위 도형 아래에 파란색 `×`가 있습니다.
- 사각형 도형이 2개 보입니다.
  - 오른쪽 위: 테두리만 있는 세로형 사각형
  - 왼쪽 아래: 네 꼭짓점에 직각 표시가 있는 사각형
- 해설용 도형은 직사각형의 성질을 보여주는 반복/대조 요소로 읽힙니다.

[DSL 작성 힌트]
- 상단에 문제 번호 `29.`와 지시문을 한 줄 텍스트로 배치하세요.
- 오른쪽 위에는 세로로 긴 사각형 1개를 그리세요.
- 그 사각형 아래에 작은 `×` 표식을 붙이세요.
- 왼쪽 중간에는 작은 파란 원형 표시를 따로 배치하세요.
- `(정답)` 텍스트 옆에는 빈 원을 하나 두세요.
- 왼쪽 아래에는 해설용 사각형을 그리되, 네 꼭짓점마다 직각 표시를 추가하세요.
- 하단에는 설명 문장을 한 줄로 넓게 배치하세요.
- 전체적으로 좌우 분리 배치와 상하 간격이 중요합니다.
- 도형 선은 얇고 회색/검정 계열로, 보조 표식은 파란색 계열로 구분하는 편이 좋습니다.

[semantic/solvable 힌트]
- 보이는 해설 문장은 `네 각이 모두 직각인 사각형을 직사각형이라고 합니다.`입니다.
- 이는 직사각형의 정의를 설명하는 문장입니다.
- 오른쪽 위 도형과 아래의 `×` 표식은 판별 결과를 암시하는 배치처럼 보이지만, 정답 칸은 여전히 빈 상태로 기록해야 합니다.
- 문제를 풀어 답을 확정하지 말고, 시각적으로 제시된 정의와 예시만 구조화하세요.

[주의 및 TODO]
- 오른쪽 위 도형이 문제의 대상인지, 오답 예시인지 배치만으로는 완전히 단정하지 마세요.
- 왼쪽 중간의 작은 파란색 원형 표시가 인쇄 요소인지 편집 마킹인지 불확실합니다.
- `(정답)` 옆 빈 원이 실제 선택 칸인지 답안 기입용인지 확정하지 마세요.
- 해설 문장은 정의 설명으로 유지하고, 답을 직접 써 넣는 형태로 바꾸지 마세요.
- 정답 추론 금지 원칙을 유지하고, 보이는 요소만 DSL에 반영할 것.
