[전체적인 구성]
- 화면 왼쪽 위에 문제 번호와 지시문이 있고, 그 아래 중앙에는 연한 하늘색 테두리의 큰 직사각형 박스가 있습니다.
- 큰 박스 안에는 선분 후보 그림이 3개 가로로 나란히 배치되어 있습니다.
- 큰 박스 아래 왼쪽에는 `(정답)` 표시와 함께 짧은 선분 그림이 하나 따로 있습니다.
- 화면 맨 아래에는 `(해설)`로 시작하는 설명 문장이 한 줄로 있습니다.
- 전체적으로 “보기 고르기” 형식의 도형 판별 문제처럼 보입니다.

[보이는 텍스트]
- `91. 선분 ㄴㄷ을 선택해 보세요.`
- `(정답)`
- `(해설) 선분 ㄴㄷ은 점 ㄴ과 점 ㄷ을 곧게 이은 선입니다.`
- 각 그림 위에는 점 이름처럼 보이는 문자 `ㄴ`, `ㄷ`가 표시되어 있습니다.
- 큰 박스 안의 세 그림 각각에도 `ㄴ`, `ㄷ` 표기가 있습니다.

[반복되는 시각 패턴]
- 큰 박스 안에서 비슷한 형태의 가는 선분 그림이 3개 반복됩니다.
- 각 그림은 선분 양 끝점 근처에 작은 검은 점이 있고, 그 위나 근처에 문자 라벨 `ㄴ`, `ㄷ`가 붙어 있습니다.
- 세 그림 모두 점과 선이 연결된 형태이지만, 방향과 기울기가 서로 다릅니다.
- 아래의 `(정답)` 영역에도 같은 스타일의 선분이 한 개 더 제시되어 있습니다.

[수식 단계]
- 해당 없음. 등호로 이어지는 수식 변형은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 문제 번호 `91`은 왼쪽 상단에 작게 표시되어 있습니다.
- 지시문 `선분 ㄴㄷ을 선택해 보세요.`는 숫자와 함께 한 줄로 인쇄되어 있습니다.
- 중앙의 연한 하늘색 테두리 박스는 선택지 영역처럼 보입니다.
- 박스 안의 첫 번째 그림:
  - 왼쪽 아래 쪽에 `ㄴ` 표기가 있고, 오른쪽 위 쪽에 `ㄷ` 표기가 있습니다.
  - 두 점을 잇는 선이 약간 오른쪽으로 올라가는 방향입니다.
- 박스 안의 두 번째 그림:
  - 아래쪽 왼편에 `ㄴ`, 위쪽 오른편에 `ㄷ` 표기가 있습니다.
  - 선이 더 가파르게 올라가는 방향입니다.
- 박스 안의 세 번째 그림:
  - 왼쪽 위 쪽에 `ㄴ`, 오른쪽 쪽에 `ㄷ` 표기가 있습니다.
  - 선이 거의 수평에 가깝게 약간 내려가는 방향입니다.
- `(정답)` 표시 옆에는 짧은 선분이 하나 있고, 왼쪽 끝점 근처에 `ㄴ`, 오른쪽 끝점 근처에 `ㄷ`가 표시되어 있습니다.
- `(해설)` 문장은 선분의 정의를 문자로 설명하는 문장입니다.

[Python DSL 작성 힌트]
- 전체 레이아웃은 상단 제목/지시문, 중앙 선택지 박스, 하단 정답 예시, 최하단 해설 문장으로 나누어 배치하면 좋습니다.
- 큰 선택지 박스는 연한 색 테두리의 직사각형으로 그리고, 그 안에 선분 3개를 가로 정렬로 배치하세요.
- 각 선분은 얇은 검은색 선과 끝점의 작은 검은 점 2개로 표현하면 됩니다.
- 각 끝점 위/근처에 `ㄴ`, `ㄷ` 라벨을 작게 배치하세요.
- `(정답)` 영역의 선분은 별도 위치에 작게 두고, 앞에 `(정답)` 텍스트를 붙이세요.
- 해설 문장은 하단에 왼쪽 정렬로 배치하는 형태가 자연스럽습니다.

[주의할 점]
- 문제를 해결하려고 어떤 선분이 정답인지 추론해 적지 마세요.
- 선택지 3개와 정답 예시 그림은 서로 다른 개체이므로 위치를 혼동하지 마세요.
- `ㄴ`, `ㄷ` 표기는 점의 이름으로 보이지만, 실제 글자 크기와 위치는 그림마다 조금씩 다릅니다.
- 큰 박스 안의 선분들은 모두 비슷한 길이지만 정확히 같은 길이는 아닐 수 있습니다.
- 해설 문장에 나온 정의를 근거로 정답을 단정적으로 써넣지 마세요.

[불확실한 부분]
- 큰 박스 안 3개의 그림이 각각 어떤 세부 각도인지, 픽셀 기준 정확한 기울기는 이미지상 확정하기 어렵습니다.
- `(정답)` 영역의 선분 길이와 각도는 화면 해상도 때문에 정확히 측정하기 어렵습니다.
- `ㄴ`, `ㄷ` 라벨이 선분의 정확히 어느 위치에 맞닿아 있는지는 약간의 오차가 있을 수 있습니다.
