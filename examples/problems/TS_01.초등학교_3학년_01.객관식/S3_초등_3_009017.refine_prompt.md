# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009017.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009017

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 왼쪽 위에 문제 번호와 지시문이 있고, 오른쪽에는 보라색 테두리의 큰 사각형 박스 안에 그림 선택지가 배치되어 있습니다.
- 전체적으로 **선분의 양 끝점 이름을 보고 반직선을 고르는 문제**로 보입니다.
- 하단에는 **정답 표기 영역**과 **해설 문장**이 별도로 있습니다.
- 보기 그림은 선분/반직선 형태의 가로선 2개가 위아래로 배치되어 있어, 둘 중 하나를 선택하는 구조입니다.

[보이는 텍스트]
- 상단 문제 문구: `55. 반직선 ㄷ ㄹ을 선택하세요.`
- 오른쪽 박스 안의 위쪽 그림: 점 위에 `ㄷ`, 오른쪽 점 위에 `ㄹ`
- 오른쪽 박스 안의 아래쪽 그림: 점 위에 `ㄷ`, 오른쪽 점 위에 `ㄹ`
- 하단 정답 표시: `(정답)ㄷ`
- 하단 해설: `(해설) 점 ㄷ에서 시작하여 점 ㄹ을 지나는 선을 그립니다.`

[반복되는 시각 패턴]
- 오른쪽 보라색 테두리 박스 안에 **가로선 2개가 위아래로 반복**됩니다.
- 두 그림 모두:
  - 왼쪽 쪽에 작은 점이 하나 있고 그 위에 `ㄷ` 표기가 있음
  - 오른쪽 쪽에 작은 점이 하나 있고 그 위에 `ㄹ` 표기가 있음
  - 점들을 연결하는 가로선이 있음
- 위 그림과 아래 그림은 매우 비슷해 보여서, 시각적으로는 **중복 제시된 선택지/예시**처럼 보입니다.
- 각 그림의 선 끝에 화살표가 보이지는 않고, 점 표시만 있습니다.

[수식 단계]
- 해당 없음.  
- 화면에 등호를 포함한 식 변형이나 계산 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.  
- 등호가 연결된 변형식이 제시되어 있지 않습니다.

[세부 내용]
- 문제 번호 `55`는 왼쪽 상단에 작게 표시되어 있습니다.
- 지시문 `반직선 ㄷ ㄹ을 선택하세요.`에서 `ㄷ`, `ㄹ`은 한글 자모 기호로 점 이름을 나타내는 것으로 보입니다.
- 오른쪽의 큰 보라색 사각형은 선택 영역처럼 보이며, 내부에 두 개의 선 그림이 배치되어 있습니다.
- 두 선 그림 모두 수평 방향이며, 점의 위치가 선 위에 정확히 찍혀 있습니다.
- 선의 길이는 위아래 그림 모두 비슷하고, 오른쪽 점이 더 오른쪽에 있습니다.
- 하단의 `(정답)ㄷ`는 정답을 글자로 제시한 영역처럼 보입니다.
- 해설 문장에서는 `점 ㄷ에서 시작하여 점 ㄹ을 지나는 선`이라고 설명하고 있어, **시작점과 지나가는 점의 관계**를 강조합니다.
- 다만 실제 그림에는 반직선의 방향을 나타내는 화살표가 뚜렷하게 보이지 않아, 시각적으로는 선분처럼도 보일 수 있습니다.

[Python DSL 작성 힌트]
- 상단 텍스트, 오른쪽 선택 박스, 하단 정답/해설을 **세로로 구획**해서 배치하면 좋습니다.
- 오른쪽 보라색 박스는 `rect` 또는 `box` 형태로 그리고, 그 안에 위아래로 두 개의 동일한 선 그림을 배치합니다.
- 각 선 그림은 다음 요소로 분리하면 좋습니다:
  - 왼쪽 점
  - 오른쪽 점
  - 점 위의 자모 표기 `ㄷ`, `ㄹ`
  - 그 사이를 잇는 가로선
- 점과 글자는 선 위쪽에 맞춰 정렬하고, 두 보기 사이 간격을 충분히 둡니다.
- 하단에는 `(정답)`, `(해설)` 라벨을 따로 두고, 해설 문장은 한 줄로 이어 쓰면 됩니다.
- 문제 문구의 `55.`는 번호 텍스트로 따로 처리하고, 본문 지시문과 간격을 두는 것이 좋습니다.

[주의할 점]
- 정답을 추론해 그림을 수정하거나, 빈칸을 채우면 안 됩니다.
- 보이는 것만 기준으로 작성하고, 실제 의도는 해설 문장에서 드러나는 범위까지만 반영하세요.
- 위아래 두 그림이 거의 같아 보여도, 임의로 하나를 삭제하거나 다른 도형으로 바꾸지 마세요.
- 선의 끝에 화살표가 없다는 점이 중요할 수 있으므로, 화살표를 임의로 추가하지 마세요.
- 하단의 `(정답)ㄷ`은 화면에 보이는 텍스트이므로, 별도 추론 없이 그대로 기록해야 합니다.

[불확실한 부분]
- 오른쪽 박스 안의 두 그림이 실제로 서로 다른 선택지인지, 화면 캡처에서 구분이 흐린 것인지 확실하지 않습니다.
- 선이 반직선을 나타내는지, 단순한 선분 예시인지 시각만으로는 애매합니다.
- 하단 정답 표기 `(정답)ㄷ`이 최종 정답 입력인지, 예시 출력인지 화면만으로는 추가 맥락이 필요합니다.

--- RAW VISION DRAFT END ---
