# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008948.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008948

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 화면 상단에 문제 번호와 문제 문장이 2줄로 제시되어 있습니다.
- 가운데에는 파란색 테두리의 **표**가 있고, 표 위쪽에 연한 파란색 배경의 제목 상자가 붙어 있습니다.
- 표 아래 오른쪽에는 보기처럼 보이는 괄호 속 항목 나열이 있습니다.
- 화면 하단 왼쪽에는 **(정답)** 표시와 **(해설)** 문장이 함께 보입니다.
- 전체적으로는 “운동별 학생 수”를 표로 보여주고, 그중 가장 작은 값을 찾는 형태의 자료 해석 문제입니다.  
  - 다만, 이 답은 화면에 이미 제시된 **해설/정답 표시**를 옮겨 적는 수준이며, 여기서는 문제를 새로 풀지 않습니다.

# [보이는 텍스트]
- `□ 18. 사진이나 반 학생들이 좋아하는 운동을 조사하여 표로 나타내었습니다. 가장 적은 학생들이 좋아하는 운동은 무엇일까요?`
- 표 제목: `좋아하는 운동별 학생 수`
- 표의 첫째 줄(항목명): `운동 | 축구 | 야구 | 농구 | 배구 | 합계`
- 표의 둘째 줄: `학생 수(명) | 5 | 6 | 1 | 4 | 16`
- 표 아래 괄호: `( 축구, 야구, 농구, 배구 )`
- 하단 텍스트:
  - `(정답)농구`
  - `(해설)좋아하는 운동별 학생 수를 비교하면 6>5>4>1이므로 가장 적은 학생들이 좋아하는 운동은 농구입니다.`

# [반복되는 시각 패턴]
- 표는 **2행 구성**으로 보이며, 첫 행은 항목명, 둘째 행은 수치입니다.
- 열은 왼쪽부터 오른쪽으로 `운동`, `축구`, `야구`, `농구`, `배구`, `합계` 순서로 배열되어 있습니다.
- 각 운동 항목 아래에 하나씩 숫자가 배치되어 있어, 운동별 학생 수를 비교하는 구조입니다.
- 표 제목 상자는 표의 중앙 상단에 넓게 배치되어 있고, 표와 같은 색조의 연한 파란색 배경을 사용합니다.
- 오른쪽 아래의 `( 축구, 야구, 농구, 배구 )`는 보기 또는 후보 항목처럼 나열되어 있습니다.
- 하단 해설에서는 숫자들을 `6>5>4>1`처럼 **부등호로 비교한 연속 표현**이 나타납니다.

# [수식 단계]
- 화면에 별도의 식 변형 과정은 길게 이어지지 않습니다.
- 다만 해설 부분에 비교식이 보입니다:
  - `6>5>4>1`
- 이 표현은 여러 값을 크기 순서로 나열한 형태로 보입니다.
- 등호(=)는 없고, 부등호(>)만 사용되어 있습니다.

# [등호별 변환 의미]
- 해당 없음.
- 화면에는 `=`를 포함한 변형식이 제시되지 않았습니다.
- 해설의 `6>5>4>1`은 변형 단계라기보다 비교 결과를 한 줄로 정리한 표현으로 보입니다.

# [세부 내용]
- 문제는 “가장 적은 학생들이 좋아하는 운동”을 묻고 있습니다.
- 표의 각 운동별 학생 수는 다음 칸 구조로 보입니다.
  - `축구` 아래에 `5`
  - `야구` 아래에 `6`
  - `농구` 아래에 `1`
  - `배구` 아래에 `4`
  - `합계` 아래에 `16`
- `합계` 칸은 다른 운동 칸과 같은 행에 있으나, 의미상 전체 인원 합을 나타내는 칸으로 보입니다.
- 표 선은 파란색이며, 칸 구분선이 모두 직선으로 명확하게 그려져 있습니다.
- 제목 상자는 표보다 위에 따로 분리되어 있고, 표의 전체 폭보다 약간 좁거나 비슷한 폭으로 보입니다.
- 해설 문장에서는 비교 대상인 운동별 수를 나열한 뒤, 가장 작은 값을 가진 운동을 결론으로 제시합니다.
- 다만 본 작업에서는 그 결론을 새로 계산하거나 재도출하지 않고, **보이는 텍스트를 그대로 설명**하는 데 집중합니다.

# [Python DSL 작성 힌트]
- 문제 문장을 상단 텍스트 박스로 배치합니다.
- 가운데에 2행 6열 표를 생성합니다.
  - 첫 행: `운동 | 축구 | 야구 | 농구 | 배구 | 합계`
  - 둘째 행: `학생 수(명) | 5 | 6 | 1 | 4 | 16`
- 표 위에 별도의 제목 박스 `좋아하는 운동별 학생 수`를 중앙 정렬로 둡니다.
- 표 아래 오른쪽에는 괄호로 묶인 항목 나열 `( 축구, 야구, 농구, 배구 )`를 넣습니다.
- 하단 왼쪽에는 `(정답)농구`, `(해설)...` 형태의 텍스트를 별도 문단으로 배치합니다.
- 표 색상은 연한 파란 배경과 파란 테두리로 표현하면 원본 느낌을 살리기 쉽습니다.
- 숫자와 글자 크기, 셀 정렬(가운데 정렬)을 구분해서 지정하면 좋습니다.

# [주의할 점]
- 답을 직접 계산해서 채워 넣지 마세요.
- 표의 `합계` 칸이 있더라도 별도로 재구성하거나 해석을 덧붙이지 마세요.
- 하단의 `(정답)농구`는 화면에 보이는 내용이지만, 이것을 새로 추론한 결과처럼 쓰지 않도록 주의하세요.
- `6>5>4>1`을 등호 변형식으로 오해하지 마세요. 이는 비교 표현입니다.
- 문제 본문과 해설은 서로 다른 영역이므로, DSL에서도 구역을 분리하는 것이 좋습니다.

# [불확실한 부분]
- 문제 문장 맨 앞의 작은 네모 `□`는 체크박스인지 문제 번호 장식인지 정확히 단정하기 어렵습니다.
- 표 제목 상자와 표 본체의 정확한 좌표, 행 높이, 열 너비는 이미지에서 대략적으로만 확인됩니다.
- 하단 해설의 줄바꿈 위치는 화면 해상도에 따라 달라질 수 있습니다.
- `(정답)농구`의 표기 위치가 본래 정답란인지, 해설 보조 표기인지는 화면만으로 완전히 확정하기 어렵습니다.

--- RAW VISION DRAFT END ---
