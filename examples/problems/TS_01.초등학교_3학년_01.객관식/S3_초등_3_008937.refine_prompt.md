# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008937.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008937

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단에는 문제 번호와 본문 문장이 한 줄~두 줄로 배치되어 있습니다.
- 중앙에는 **“목장별 우유 생산량”**이라는 제목의 표가 있고, 표 안에 목장 이름과 그림(원형 기호)로 우유 생산량이 표현되어 있습니다.
- 표 아래에는 **그림 기호의 뜻**을 나타내는 범례가 있습니다.
- 더 아래에는 보기 ①~④가 두 줄로 나뉘어 배치되어 있고, 정답 표시와 해설 문장이 맨 아래에 있습니다.

[보이는 텍스트]
- `5. 연서네 마을의 목장에서 일주일 동안 생산한 유유의 양을 조사하여 그림그래프로 나타내었습니다. 우유 생산량이 가장 적은 목장은 어디인가요?`
  - 주의: 화면에는 `유유`로 보이지만 문맥상 `우유`를 뜻하는 것으로 보일 수 있으나, **보이는 텍스트 자체는 그대로 `유유`처럼 보입니다.**
- 제목: `목장별 우유 생산량`
- 표의 열 이름: `목장`, `우유 생산량`
- 행 이름: `가`, `나`, `다`, `라`
- 범례: `10 L`, `1 L`
- 보기:
  - `① 가 목장`
  - `② 나 목장`
  - `③ 다 목장`
  - `④ 라 목장`
- 하단:
  - `(정답)③`
  - `(해설)큰 그림 수가 가장 적은 목장은 다 목장입니다.`

[반복되는 시각 패턴]
- 표의 각 행에는 **큰 원형 기호와 작은 원형 기호가 반복**되어 있습니다.
- 큰 원형 기호는 안쪽에 한 번 더 원이 그려진 **이중 원**처럼 보입니다.
- 작은 원형 기호는 **단일 원**으로 보입니다.
- 이 두 종류의 원이 가로로 여러 개 나열되어 각 목장의 생산량을 표시합니다.
- 범례에서도 같은 원형 기호 2종이 다시 등장하며, 각각 `10 L`, `1 L`과 연결되어 있습니다.
- 각 행마다 원의 개수는 다르며, 이를 비교하는 문제가 아니라 **그림 개수 자체를 보고 가장 적은 목장을 고르는 형식**으로 보입니다.

[수식 단계]
- 해당 없음. 화면에는 등호가 있는 계산식이나 변형 단계가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호를 포함한 식 변형이 나타나지 않습니다.

[세부 내용]
- 표는 2열 구조입니다.
  - 왼쪽 열: 목장 이름
  - 오른쪽 열: 해당 목장의 우유 생산량을 그림으로 표시
- 행은 4개이며 순서는 `가`, `나`, `다`, `라`입니다.
- 각 행의 그림 구성은 다음처럼 보입니다.
  - `가`: 큰 원 여러 개 뒤에 작은 원 2개가 붙어 있음
  - `나`: 큰 원이 더 많이 이어져 있고, 작은 원은 보이지 않음
  - `다`: 큰 원 여러 개 뒤에 작은 원 여러 개가 이어져 있음
  - `라`: 큰 원 여러 개 뒤에 작은 원이 가장 많이 이어져 있음
- 범례는 그림의 의미를 설명합니다.
  - 큰 이중 원 1개 = `10 L`
  - 작은 단일 원 1개 = `1 L`
- 선택지 번호는 파란색 동그라미 안 숫자로 표시되어 있습니다.
- 해설 문장은 정답 선택지를 직접 뒷받침하는 짧은 설명입니다.

[Python DSL 작성 힌트]
- 표 구조를 먼저 잡고, `2열 x 4행` 형태로 배치하는 것이 좋습니다.
- 표 상단에 제목을 가운데 정렬로 두고, 그 아래 표를 놓는 구성이 자연스럽습니다.
- 각 행에는 목장 이름 텍스트와, 오른쪽 칸에 원형 기호 묶음을 가로 방향으로 반복 배치합니다.
- 원형 기호는 두 종류로 분리해 표현해야 합니다.
  - 큰 원: 이중 원 모양
  - 작은 원: 단일 원
- 범례는 표 아래 오른쪽에 가깝게 배치된 것으로 보이며, 기호와 단위를 짝지어 표현하면 됩니다.
- 보기 ①~④는 2줄 2칸 형태로 배치된 것으로 볼 수 있습니다.
- 정답 표시 `(정답)③`은 보기 아래 왼쪽쪽 하단에 따로 두면 됩니다.
- 해설 문장은 가장 아래 한 줄 텍스트로 처리하면 됩니다.

[주의할 점]
- 우유/유유 표기는 화면에서 보이는 그대로 확인해야 합니다. 텍스트 인식 시 오타 가능성이 있습니다.
- 표 안의 원 개수를 임의로 계산해 값을 확정하지 마세요.
- 각 행의 생산량을 수치로 환산해 결론을 단정하지 말고, **그림이 가장 적어 보이는 행이 어떤지**를 시각적으로만 기록하세요.
- 범례의 `10 L`, `1 L`가 각각 어떤 원에 대응하는지는 보이는 기호와 함께 분리해서 적는 것이 좋습니다.
- 정답 표시가 이미 아래에 있지만, DSL 작성용 초안에서는 정답을 다시 추론하여 확장하지 않는 편이 안전합니다.

[불확실한 부분]
- 본문 첫 문장의 `유유`는 실제로는 `우유`일 가능성이 있으나, 이미지 해상도상 문자 판독이 완전히 확실하지 않습니다.
- 각 행의 원 개수는 멀리서 보면 구분이 어려운 부분이 있어, 정확한 개수는 별도 확대 확인이 필요할 수 있습니다.
- 범례의 원 모양이 표 안의 큰 원/작은 원과 정확히 1:1 대응하는지는 문맥상 자연스럽지만, OCR/판독 관점에서는 다시 확인하는 것이 좋습니다.

--- RAW VISION DRAFT END ---
