# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008920.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008920

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호가 있고, 그 뒤로 긴 문장형 문제 지시가 이어진다.
- 중앙에는 제목이 있고, 그 아래에 **표 형태의 그림그래프**가 배치되어 있다.
- 표 아래쪽에는 돼지 그림의 범례가 있으며, 큰 돼지 그림과 작은 돼지 그림이 각각 몇 마리를 뜻하는지 표시한다.
- 하단에는 선택지 3개가 가로로 배치되어 있고, 그 아래에 정답 표시와 해설 문장이 있다.
- 전체적으로 “문장 → 그림그래프 → 선택지 → 정답/해설”의 순서로 구성되어 있다.

[보이는 텍스트]
- `86.`
- `마을별 기르고 있는 돼지 수를 그림그래프로 나타내었습니다. 초록 마을보다 돼지를 더 많이 기르는 마을을 고르세요.`
- 제목: `마을별 돼지 수`
- 표 머리글: `마을`, `돼지 수`
- 행 이름: `초록`, `하늘`, `금빛`, `바람`
- 범례:
  - 큰 돼지 그림 옆에 `10마리`
  - 작은 돼지 그림 옆에 `1마리`
- 선택지:
  - `① 하늘 마을`
  - `② 금빛 마을`
  - `③ 바람 마을`
- ` (정답) ①`
- ` (해설) 초록 마을보다 큰 그림이 더 많은 마을을 찾으면 하늘 마을입니다.`

[반복되는 시각 패턴]
- 각 마을 이름 오른쪽에 돼지 그림들이 반복적으로 나열되어 있다.
- 돼지 그림은 크기가 두 종류로 보이며, 큰 그림과 작은 그림이 섞여 있다.
- 표의 각 행은 같은 구조로 반복된다:
  - 왼쪽에는 마을 이름
  - 오른쪽에는 해당 마을의 돼지 수를 나타내는 그림 묶음
- 범례에서도 같은 돼지 그림이 다시 등장하여, 그림 1개가 나타내는 수를 설명한다.
- 선택지는 숫자 원형 기호(①, ②, ③)와 마을 이름이 함께 반복되는 형태이다.

[수식 단계]
- 해당 없음.  
  - 화면에는 등호를 이용한 식 변형이나 계산 단계가 보이지 않는다.
  - 그림그래프만 제시되어 있으며, 수식 전개는 없다.

[등호별 변환 의미]
- 해당 없음.
  - 등호가 포함된 연속 변형식이 보이지 않는다.

[세부 내용]
- 표는 연한 갈색 테두리로 둘러싸여 있고, 내부는 행과 열로 나뉘어 있다.
- 표 상단에는 제목 `마을별 돼지 수`가 가운데 정렬로 놓여 있다.
- 표의 첫 번째 열은 마을 이름을 쓰는 좁은 열, 두 번째 열은 그림이 들어가는 넓은 열이다.
- 각 행의 돼지 그림은 왼쪽에서 오른쪽으로 한 줄에 정렬되어 있으며, 큰 돼지와 작은 돼지가 섞여 있다.
- `초록`, `하늘`, `금빛`, `바람` 네 마을이 세로로 순서대로 배치되어 있다.
- 범례는 표의 오른쪽 아래 바깥쪽에 놓여 있으며, 큰 돼지 그림이 `10마리`, 작은 돼지 그림이 `1마리`라는 뜻으로 보인다.
- 선택지는 표 아래 넓은 여백에 가로 방향으로 배치되어 있고, 각각 원형 번호가 붙어 있다.
- 하단의 `(정답) ①`과 `(해설)`은 인쇄된 정답 안내로 보이며, 문제 본문과는 구분되는 추가 정보이다.

[Python DSL 작성 힌트]
- 먼저 상단 문제 문장을 좌측 상단에 배치한다.
- 중앙에 제목 `마을별 돼지 수`를 놓고, 그 아래 표를 구성한다.
- 표는 2열 구조로 잡고, 왼쪽 열에는 `마을`, `초록`, `하늘`, `금빛`, `바람` 텍스트를 세로로 넣는다.
- 오른쪽 열에는 각 행마다 돼지 아이콘을 가로로 여러 개 반복 배치한다.
- 큰 돼지 아이콘과 작은 돼지 아이콘을 서로 다른 크기로 준비해 범례도 함께 배치한다.
- 표 아래에는 범례를 오른쪽 아래 쪽에 두고, 선택지 3개를 가로로 나열한다.
- 선택지 앞에는 ①, ②, ③ 원형 번호를 붙인다.
- 정답/해설 문구는 문제 제작용이라면 제외할 수도 있으나, 스크린샷 재현 시에는 하단에 별도 텍스트로 배치한다.
- 그림 수를 직접 계산해 넣는 식의 표현은 피하고, 보이는 개수만 그대로 배치하는 방식으로 작성한다.

[주의할 점]
- 돼지 그림의 개수를 임의로 해석해 답을 추론하지 마세요.
- 큰 돼지와 작은 돼지를 각각 몇 마리로 볼지는 범례 기준으로만 처리하고, 문제 풀이 결과는 쓰지 마세요.
- 표 내부 그림의 정확한 개수는 스크린샷 기준으로 보이는 만큼만 재현하세요.
- 하단의 정답 표시와 해설은 문제 화면의 일부이지만, 실제 문항 재구성 시 포함 여부를 구분해야 합니다.
- 선택지의 순서와 번호를 바꾸지 마세요.
- 표의 선 색, 제목 위치, 범례 위치처럼 레이아웃 정보를 유지하는 것이 중요합니다.

[불확실한 부분]
- 각 행의 돼지 아이콘 정확한 개수는 이미지 해상도 때문에 일부 겹쳐 보여 완전하게 판독되지 않을 수 있다.
- 일부 작은 돼지 아이콘은 크기와 간격이 비슷해 개별 개수를 세는 데 오차가 있을 수 있다.
- 큰 돼지 아이콘과 작은 돼지 아이콘의 실제 도형 비율은 화면상 약간 다르게 보일 수 있다.
- 표의 정확한 선 두께와 색상은 이미지 압축 영향으로 다소 달라질 수 있다.

--- RAW VISION DRAFT END ---
