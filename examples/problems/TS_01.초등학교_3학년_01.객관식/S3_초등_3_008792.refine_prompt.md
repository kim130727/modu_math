# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008792.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008792

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 장의 객관식/선택형 문제처럼 보이는 단일 문항 화면이다.
- 좌측 상단에 문제 번호가 있고, 그 오른쪽에 문제 지시문이 한 줄로 배치되어 있다.
- 중앙 영역에는 같은 형태의 “2L” 예시가 여러 번 반복되어 있으며, 각각 파란 점선 가이드 위에 놓여 있다.
- 좌측 하단에는 “(정답)” 표기가 붙은 위치에 또 하나의 “2L”이 제시되어 있어, 정답으로 고를 대상을 시각적으로 표시한 구성으로 보인다.
- 하단에는 “(해설)”로 시작하는 설명 문장이 이어진다.
- 전체적으로 글자체의 크기 비교와 모양 비교를 묻는 시각 문제 형태이다.

[보이는 텍스트]
- `□ 17. 2 L를 바르게 쓴 것을 선택해 보세요.`
- `2L`
- `2L`
- `2L`
- `(정답)`
- `2L`
- `(해설)리터는 숫자와 같은 크기의 알파벳 대문자로 씁니다.`

[반복되는 시각 패턴]
- `2L` 형태가 총 여러 번 반복되어 보인다.
- 각 `2L`은 모두 파란 점선의 수평 기준선 위에 배치되어 있어, 글자의 높이와 정렬을 비교하도록 만든 패턴처럼 보인다.
- 반복되는 `2L` 사이에는 내용 차이가 거의 없고, 위치만 다르게 배치되어 있다.
- 일부는 화면 중앙 상단, 일부는 오른쪽 상단, 일부는 왼쪽 중하단에 있어 선택지처럼 흩어져 있다.
- `(정답)` 표기 옆의 `2L`은 다른 것들보다 더 강조된 위치에 있다.

[수식 단계]
- 해당 없음.  
- 화면에 등호가 이어진 식 변형이나 계산 단계는 보이지 않는다.

[등호별 변환 의미]
- 해당 없음.  
- 등호가 등장하는 수식 구조가 보이지 않는다.

[세부 내용]
- 문제 번호 `17`은 왼쪽 상단에 있고, 그 앞에 작은 네모 모양 기호가 있다.
- 지시문 `2 L를 바르게 쓴 것을 선택해 보세요.`는 숫자와 단위 표기 방법을 묻는 문장이다.
- 중앙의 `2L`들은 모두 같은 문자 조합으로 보이지만, 실제로는 글자 크기나 간격, 정렬을 비교하게 하는 시각 자료로 쓰였을 가능성이 있다.
- 파란 점선 가이드는 각 `2L`의 기준선 또는 글자 위치를 맞추는 용도로 보인다.
- 하단 해설 문장에는 “리터는 숫자와 같은 크기의 알파벳 대문자로 씁니다.”라고 적혀 있어, 단위 표기에서 대문자 `L`을 쓰는 규칙을 설명하는 것으로 읽힌다.
- 다만 여기서도 정답 자체를 새로 계산하거나 추론해서 채우는 것은 적절하지 않다.
- 전체 구성상, 학습자가 올바른 단위 표기를 시각적으로 고르게 하는 문제로 보인다.

[Python DSL 작성 힌트]
- 큰 틀은 다음 요소로 분리하면 좋다:
  - 문제 번호 영역
  - 상단 지시문 텍스트
  - 중앙의 `2L` 반복 배치 3개
  - 좌측 하단 `(정답)` 라벨과 그 옆/아래의 `2L`
  - 하단 `(해설)` 문장
- 각 `2L`은 동일 문자열이므로, 좌표만 달리 주어 여러 번 배치하는 방식이 적절하다.
- 파란 점선 가이드는 각 `2L` 뒤에 길게 펼쳐진 수평 점선 여러 줄로 표현하면 된다.
- `(정답)`은 괄호 포함 텍스트로 따로 배치하고, 그 아래 또는 오른쪽에 `2L`을 두는 레이아웃이 적합해 보인다.
- 해설 문장은 한 줄 텍스트로 처리하되, 문장 시작의 `(해설)`을 별도 텍스트로 분리해도 된다.
- 글자 크기 차이가 실제로 중요한 경우, `L`과 `2`의 시각적 균형을 맞춰야 하므로 폰트 크기와 baseline 정렬을 신경 써야 한다.

[주의할 점]
- `2L`을 다른 문자 조합으로 바꾸지 말아야 한다.
- 정답을 임의로 텍스트로 추가하거나 강조 표시를 추론해서 넣지 말아야 한다.
- 해설 문장을 문제 본문으로 오해하지 않도록 위치를 구분해야 한다.
- 파란 점선은 실제 글자 요소가 아니라 가이드/배경 요소로 보이므로, 텍스트와 분리해서 다루는 것이 좋다.
- 화면 좌우의 `2L` 배치는 약간 비대칭이므로, 완전히 대칭 구조로 단정하지 않는 것이 안전하다.
- `(정답)` 주변의 위치 관계는 다소 흐릿하게 보일 수 있으므로, 정확한 상대좌표는 추정이 필요하다.

[불확실한 부분]
- 중앙과 오른쪽의 `2L`들이 모두 완전히 동일한 크기인지, 혹은 일부만 약간 다른 크기인지 화면만으로는 확정하기 어렵다.
- 파란 점선이 정확히 몇 줄인지, 각 줄의 길이가 동일한지 이미지 해상도상 정확히 세기 어렵다.
- `(정답)` 표기의 정확한 수평 정렬 기준은 약간 흐려 보여, `2L`과의 상대 위치를 세밀하게 단정하기 어렵다.
- 문제 지시문에서 `2 L` 사이의 공백이 원문 그대로인지, 편집 과정에서 생긴 간격인지 확실하지 않다.

--- RAW VISION DRAFT END ---
