# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008869.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008869

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 장의 초등 수학 문제 이미지로 보이며, 왼쪽 위에 문제 번호와 본문 질문이 있고, 그 아래에 **그림 비교형 도식**이 배치되어 있습니다.
- 그림은 **왼쪽의 ‘어항’과 ‘수조’**, 그리고 가운데의 **오른쪽 방향 화살표**, 오른쪽의 **변화된 상태의 어항과 수조**로 구성됩니다.
- 하단에는 보기처럼 보이는 괄호 표기와, 정답/해설 텍스트가 별도로 있습니다.
- 전체적으로 “이동/옮김 전과 후의 상태 비교”를 그림으로 설명하는 형식입니다.

[보이는 텍스트]
- 문제 번호: `22.`
- 본문 질문:
  - `어항에 물을 가득 채운 후 수조에 옮겨 담았습니다.`
  - `그림과 같이 수조를 가득 채우고 어항에 물이 남았다면 어항과 수조 중 들이가 더 많은 것은 어느 것인가요?`
- 그림 아래 라벨:
  - 왼쪽 그릇 아래: `어항`
  - 가운데 왼쪽 상자 아래: `수조`
- 그림 아래 중앙 괄호 표기:
  - `( 어항, 수조 )`
- 하단 정답 표기:
  - `(정답)어항`
- 하단 해설 표기:
  - `(해설)어항의 물이 수조에 모두 들어가지 않으므로 어항의 들이가 더 많습니다.`

[반복되는 시각 패턴]
- 좌우 비교 구조가 반복됩니다.
  - 왼쪽에는 원형 어항과 직육면체 수조가 함께 있고,
  - 오른쪽에는 같은 종류의 두 용기가 다시 나타나지만 물의 양이 달라진 상태로 표현됩니다.
- 물 표시는 파란색 음영/채움으로 통일되어 있습니다.
- 두 장면 사이에는 **회색 오른쪽 화살표**가 있어 “왼쪽 상태 → 오른쪽 상태”의 변화 흐름을 나타냅니다.
- 어항과 수조 모두 외곽선은 하늘색 계열의 선으로 그려져 있어 동일한 도식 스타일을 유지합니다.

[수식 단계]
- 해당 없음
- 이미지에는 등호를 포함한 수식 변형 과정이 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음
- 등호가 이어지는 단계적 계산식은 관찰되지 않습니다.

[세부 내용]
- 왼쪽 첫 그림:
  - 원형 어항 안에 물이 거의 가득 차 있는 모습이 보입니다.
  - 어항 입구 쪽에서 물이 넘치거나 옮겨지는 듯한 곡선형 물결/흐름 표현이 있습니다.
  - 어항 아래에 `어항`이라는 글자가 붙어 있습니다.
- 왼쪽 두 번째 그림:
  - 직육면체 모양의 수조가 보이며, 아래쪽에만 물이 일부 차 있는 상태로 표현됩니다.
  - 수조 내부는 투명한 상자 느낌이며, 바닥선과 옆면 점선/보조선이 보입니다.
  - 아래에 `수조`라고 적혀 있습니다.
- 가운데:
  - 왼쪽에서 오른쪽으로 향하는 굵은 회색 화살표가 있습니다.
  - 이 화살표는 “물을 옮긴 뒤의 결과”를 나타내는 시각적 연결 요소로 보입니다.
- 오른쪽 첫 그림:
  - 같은 형태의 어항이 다시 나오지만, 물 높이가 왼쪽보다 낮게 표현되어 있습니다.
  - 어항 위쪽에는 물결 모양의 테두리/장식선이 있습니다.
- 오른쪽 두 번째 그림:
  - 수조는 거의 가득 찬 상태로 채워져 보이며, 상단 부분까지 파란 음영이 들어가 있습니다.
  - 내부에 점선과 투명한 면이 보여 입체 상자 형태를 강조합니다.
- 하단 괄호 표기:
  - `( 어항, 수조 )`처럼 두 대상 이름이 쉼표로 구분되어 있으며, 중간에 공백이 있는 형태입니다.
  - 이 표기는 선택지 또는 답안 대상 목록처럼 보입니다.
- 정답과 해설:
  - 정답은 텍스트로 따로 표시되어 있고,
  - 해설 문장은 “모두 들어가지 않음”과 “들이가 더 많음”의 관계를 말로 설명합니다.

[Python DSL 작성 힌트]
- 문제 문장은 상단 텍스트 박스로 분리해서 배치하면 좋습니다.
- 중앙 그림은 좌우 두 그룹으로 나누어 구성하는 것이 적절합니다.
  - 왼쪽 그룹: `어항` 그림 + `수조` 그림
  - 오른쪽 그룹: `어항` 그림 + `수조` 그림
- 가운데에는 오른쪽 방향 화살표를 단독 요소로 넣습니다.
- 각 용기 아래 라벨 텍스트를 별도 배치합니다.
- 물 채움 정도는 색 채움 영역으로 표현하고, 용기 외곽선은 투명한 상자/원형 테두리로 표시하면 좋습니다.
- 하단의 `( 어항, 수조 )`는 선택지/참조 텍스트처럼 별도 줄에 중앙 정렬로 두는 방식이 자연스럽습니다.
- 정답/해설은 문제 본문과 시각적으로 분리된 하단 텍스트 블록으로 처리하면 됩니다.

[주의할 점]
- 물의 양이 정확히 몇 %인지 추정하지 마세요. 그림은 상대적 양만 보여줍니다.
- “가득 채움”이라는 표현이 있어도, 실제 그림에서 완전히 만수 상태인지 단정하지 말고 보이는 범위만 적으세요.
- 어항과 수조의 형태를 수치화하지 말고, 원형/직육면체라는 시각적 특징만 기술하세요.
- 하단의 `(정답)어항`은 문제 풀이 결과이므로, DSL 작성 시 정답값으로 자동 주입하지 않도록 주의하세요.
- 그림 속 물결선, 점선, 테두리선은 장식/입체감 표현일 수 있으므로 기능을 단정하지 마세요.

[불확실한 부분]
- 어항과 수조의 정확한 크기 비율은 이미지 해상도상 완전히 명확하지 않습니다.
- 오른쪽 어항의 물 높이가 왼쪽 대비 얼마나 줄었는지는 정밀하게 판독하기 어렵습니다.
- 수조 내부의 점선은 바닥선인지, 깊이 표현인지, 혹은 구조선인지 단정하기 어렵습니다.
- 하단의 `( 어항, 수조 )`가 실제 선택지인지, 단순 표시인지 이미지만으로는 확정하기 어렵습니다.

--- RAW VISION DRAFT END ---
