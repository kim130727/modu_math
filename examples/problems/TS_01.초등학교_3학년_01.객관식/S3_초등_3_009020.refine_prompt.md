# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009020.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009020

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 문장이 있고, 그 아래 중앙 영역에 두 명의 인물 그림이 좌우로 배치되어 있습니다.
- 각 인물의 위쪽에는 말풍선이 하나씩 있으며, 말풍선 안에 각각 설명 문장이 들어 있습니다.
- 두 인물 아래에는 이름표처럼 보이는 작은 네모 박스가 각각 붙어 있습니다.
- 인물들 아래쪽 중앙에는 선택지처럼 괄호 안에 이름이 나열되어 있습니다.
- 화면 하단 왼쪽에는 `정답` 표기와 해설 문장이 이어집니다.

[보이는 텍스트]
- `□ 58. 반직선과 직선의 다른 점을 바르게 말한 사람의 이름을 선택해 보세요.`
- 왼쪽 말풍선: `반직선은 한쪽으로 늘어나고 직선은 양쪽으로 늘어난다.`
- 오른쪽 말풍선: `반직선은 시작점이 없지만, 직선은 시작점이 있어.`
- 이름표 왼쪽: `성진`
- 이름표 오른쪽: `희경`
- 중앙 하단: `( 성진 , 희경 )`
- 하단 해설 부분: `(정답)성진`
- 해설 문장:
  - `반직선은 한 점에서 시작하므로 시작점이 있고 직선은 두 점을 지나는 선이므로 시작점이 없습니다.`
  - `따라서 바르게 말한 사람은 성진입니다.`

[반복되는 시각 패턴]
- 두 인물 그림이 좌우 대칭에 가깝게 배치되어 있고, 각각 대응되는 말풍선이 위쪽에 연결되어 있습니다.
- 이름표는 두 그림 아래에 같은 형태의 작은 사각형 박스로 반복됩니다.
- 말풍선은 흰색 바탕, 회색 테두리의 둥근 형태로 비슷한 스타일이 반복됩니다.
- 아래쪽의 괄호 표기는 선택지처럼 보이며, 이름을 쉼표로 구분해 나열하는 형태입니다.

[수식 단계]
- 해당 없음. 수식 변형이나 등호가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 문제는 반직선과 직선의 차이를 설명한 두 사람 중에서, 올바르게 말한 사람의 이름을 고르는 형식입니다.
- 왼쪽 인물과 오른쪽 인물은 각각 한 개의 말풍선을 통해 자신의 설명을 제시합니다.
- 말풍선은 인물의 머리 위쪽에 있으며, 말풍선의 꼬리가 인물 방향으로 향합니다.
- 두 이름은 인물 바로 아래의 작은 박스 안에 표시되어 있어, 각 설명이 누구의 말인지 시각적으로 연결됩니다.
- 중앙 하단의 `( 성진 , 희경 )`은 답을 적는 공간이라기보다 선택지 또는 후보 이름을 보여주는 구성으로 보입니다.
- 하단의 정답과 해설은 문제를 푼 뒤 확인용 정보로 배치되어 있습니다.

[Python DSL 작성 힌트]
- 상단 문제 문장은 텍스트 한 줄로 배치하세요.
- 중앙에는 인물 2개를 좌우로 배치하고, 각 인물 위에 둥근 말풍선을 연결하세요.
- 각 인물 아래에 이름이 들어간 작은 사각형 라벨을 넣으세요.
- 말풍선 텍스트는 줄바꿈이 자연스럽게 들어가도록 2줄 정도로 구성해도 좋습니다.
- 하단에는 `( 성진 , 희경 )` 형태의 괄호 텍스트를 별도로 배치하세요.
- 맨 아래에는 `정답`과 해설 문장을 왼쪽 정렬로 넣는 구성을 고려하세요.
- 인물 그림은 일러스트 요소로 처리하고, 얼굴과 손 모양이 보이는 정도의 아이콘/캐릭터로 재현하면 됩니다.

[주의할 점]
- 정답을 시각 분석 초안에서 확정적으로 다시 쓰지 않아도 되지만, 보이는 텍스트로는 하단에 이미 `정답` 표기가 있습니다.
- 문제를 푸는 과정이나 논리 판단은 작성하지 마세요.
- 말풍선 내용과 하단 해설은 별도 텍스트 블록으로 구분해서 다루는 것이 좋습니다.
- 이름표와 괄호 안 이름 목록을 혼동하지 않도록 주의하세요.
- 화면 좌상단의 작은 네모는 체크박스처럼 보이나 실제 기능은 확실하지 않으므로 단정하지 마세요.

[불확실한 부분]
- 좌상단의 `□`는 선택 표시용 체크박스처럼 보이지만, 실제 입력용인지 단순 인쇄 요소인지는 확실하지 않습니다.
- 인물 그림의 세부 표정과 손 모양은 작은 크기라 일부 디테일이 정확히 구분되지 않을 수 있습니다.
- 하단의 `( 성진 , 희경 )`이 정답 기입란인지 선택지인지 화면만으로는 100% 단정하기 어렵습니다.

--- RAW VISION DRAFT END ---
