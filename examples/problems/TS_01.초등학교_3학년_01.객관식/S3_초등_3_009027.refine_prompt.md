# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009027.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009027

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 번호와 지시문이 있고, 그 아래에 삼각형 4개가 가로로 나열되어 있습니다.
- 각 삼각형 위쪽 근처에 보기 기호가 하나씩 붙어 있습니다: `가`, `나`, `다`, `라`.
- 중간에는 문장형 보기/풀이 문장이 줄 단위로 배치되어 있습니다.
- 하단에는 `(3)`이 붙은 문장과, 그 아래에 선택 결과처럼 보이는 짧은 텍스트가 추가로 보입니다.
- 전체적으로 “도형을 보고 해당하는 기호를 고르는” 객관식/선택형 문제 형태입니다.

[보이는 텍스트]
- `64. 삼각형을 보고 알맞은 기호를 모두 선택하세요.`
- `가`
- `나`
- `다`
- `라`
- `(3) 직각삼각형은 ( 가, 나, 다, 라 )입니다.`
- `(3) 직각삼각형은 나, 라입니다.`
- `(3) 나, 라`

[반복되는 시각 패턴]
- 삼각형 도형이 4개 반복되어 가로 방향으로 배치되어 있습니다.
- 각 도형 위에 대응하는 한글 기호가 반복적으로 1개씩 표시되어 있습니다.
- 아래쪽에는 `(3)`으로 시작하는 문장이 반복되는 형태로 보이며, 같은 번호가 여러 줄에 걸쳐 나타납니다.
- 괄호 `( )` 안에 여러 기호를 나열하는 형식이 보입니다.
- 마지막 줄은 쉼표로 구분된 짧은 답안 후보처럼 보이나, 이것이 최종 정답인지 단정할 수는 없습니다.

[수식 단계]
- 해당 없음.  
- 등호(`=`)가 있는 수식 변형 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.  
- 등호를 이용한 식 변환이 없어서 단계별 의미 분석 대상이 없습니다.

[세부 내용]
- 상단 지시문은 `삼각형을 보고 알맞은 기호를 모두 선택하세요.`로 읽힙니다.
- 도형 4개는 모두 선분 3개로 이루어진 삼각형입니다.
- 왼쪽부터 순서대로:
  - 첫 번째 도형: 위쪽 꼭짓점이 뾰족하게 올라간 삼각형
  - 두 번째 도형: 왼쪽 변이 세로에 가깝고, 아래 변이 가로에 가까운 직각 형태로 보이는 삼각형
  - 세 번째 도형: 오른쪽 위 꼭짓점이 뾰족하고 아래쪽 변이 비스듬한 삼각형
  - 네 번째 도형: 오른쪽 위가 직각처럼 보이는 형태로, 위쪽 가로선과 오른쪽 세로선이 만나는 삼각형
- 보기 기호 `가`, `나`, `다`, `라`는 각각 도형의 위쪽 근처에 따로 배치되어 있으며, 도형과 1:1 대응처럼 보입니다.
- 아래 문장 `(3) 직각삼각형은 ( 가, 나, 다, 라 )입니다.`는 선택지를 제시하는 문장 형태입니다.
- 그 아래 줄 `(3) 직각삼각형은 나, 라입니다.`는 선택 결과 또는 풀이 예시처럼 보입니다.
- 가장 아래의 `(3) 나, 라`는 앞 문장의 축약 표시처럼 보이지만, 정확한 용도는 화면만으로 확정하기 어렵습니다.

[Python DSL 작성 힌트]
- 도형 배치는 가로 4열 구조로 잡는 것이 좋습니다.
- 각 삼각형은 별도의 선분 3개로 구성된 폴리곤으로 처리하면 됩니다.
- 각 도형 위에 텍스트 라벨 `가`, `나`, `다`, `라`를 각각 배치합니다.
- 아래 문장은 일반 텍스트로 줄바꿈하여 배치하면 됩니다.
- 괄호가 있는 문장에서는 `(`, `)` 및 쉼표 위치를 실제 보이는 대로 유지하는 것이 중요합니다.
- 선택지처럼 보이는 문장은 원문 그대로 표시하고, 답은 채우지 말고 빈 상태로 둡니다.
- 문제 번호 `64.`와 지시문은 상단 좌측에 두는 구성이 자연스럽습니다.
- 삼각형의 정확한 좌표는 중요하지 않지만, 서로 간 간격과 기호 위치 관계는 유지하는 것이 좋습니다.

[주의할 점]
- 정답을 추론해서 채우지 마세요.
- 빈칸이 보이더라도 답을 임의로 넣지 마세요.
- `직각삼각형은 나, 라입니다.` 같은 문장은 화면에 보이는 텍스트일 뿐, 풀이 결과로 단정해 해석하지 마세요.
- 도형의 종류를 단정할 때도, 실제 화면상 형태를 기준으로만 서술하세요.
- 마지막 줄 `(3) 나, 라`는 일부가 잘린 표시일 가능성도 있으므로, 최종 정답 형식으로 단정하지 않는 것이 좋습니다.

[불확실한 부분]
- 두 번째와 네 번째 삼각형의 직각 여부는 시각적으로 그렇게 보이지만, 이미지 해상도 때문에 완전히 단정하기는 어렵습니다.
- 아래쪽의 `(3) 직각삼각형은 나, 라입니다.`와 `(3) 나, 라`가 문제 본문인지 풀이 예시인지, 또는 OCR 결과인지 확실하지 않습니다.
- 각 기호 `가/나/다/라`가 정확히 어떤 도형과 대응되는지, 레이아웃상은 위에 붙어 있으나 텍스트 배치가 약간 어긋나 보여 완전한 확정은 어렵습니다.

--- RAW VISION DRAFT END ---
