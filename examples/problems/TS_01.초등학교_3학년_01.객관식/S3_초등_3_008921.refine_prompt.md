# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008921.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008921

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 문제 번호로 보이는 `87.`이 왼쪽 상단에 있고, 그 오른쪽에 문제 지시문이 한 줄 이상으로 배치되어 있습니다.
- 중앙에는 제목이 `마을별 병원 수`인 그림그래프/표 형태의 시각 자료가 있습니다.
- 표 오른쪽에는 기호 범례가 따로 있으며, 큰 빨간 십자와 작은 빨간 십자가 각각이 의미하는 값이 텍스트로 함께 제시되어 있습니다.
- 표 아래쪽에는 보기처럼 보이는 괄호 안 텍스트 `( 마을별 가게 수 , 마을별 병원 수 )`가 있습니다.
- 더 아래에는 `(정답)`과 `(해설)`이 각각 붙은 문장이 별도로 인쇄되어 있습니다.
- 전체적으로 “조사하여 그림그래프로 나타내었습니다”라는 설명과 함께, 어떤 그림그래프가 맞는지 고르는 형식으로 보입니다.

[보이는 텍스트]
- `87.`
- `마을별 병원 수를 조사하여 그림그래프로 나타내었습니다.`
- `무엇을 나타내는 그림그래프인지 알맞은 것을 선택하세요.`
- 제목: `마을별 병원 수`
- 표의 열 제목: `마을`, `병원 수`
- 행 이름: `가`, `나`, `다`, `라`
- 범례 텍스트: `10개`, `1개`
- 아래 괄호 텍스트: `( 마을별 가게 수 , 마을별 병원 수 )`
- 하단 문장: `(정답)마을별 병원 수`
- 하단 문장: `(해설)마을별 병원 수를 조사하여 그림그래프로 나타내었습니다.`

[반복되는 시각 패턴]
- 표 안에 빨간색 십자 모양이 여러 개 반복되어 병원 수를 나타내는 그림 기호로 사용됩니다.
- 같은 크기의 큰 십자와 작은 십자가가 반복되며, 오른쪽 범례에서 각각의 값이 따로 설명됩니다.
- 각 마을 행마다 십자 기호의 개수와 크기 조합이 다르게 배치되어 있습니다.
- 표는 행별로 수평 구획이 나뉘어 있고, 왼쪽 열에는 마을 이름이 세로로 정렬되어 반복됩니다.

[수식 단계]
- 해당 없음. 수식 변형이나 등호가 이어지는 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 표는 연한 초록색 테두리와 배경을 가진 격자 형태입니다.
- 왼쪽 좁은 열은 마을 이름을 넣는 칸이고, 오른쪽 넓은 열은 병원 수를 그림기호로 표시하는 칸입니다.
- 첫 행 머리글에서 왼쪽 칸에는 `마을`, 오른쪽 칸에는 `병원 수`가 적혀 있습니다.
- 행은 위에서부터 `가`, `나`, `다`, `라` 순서로 보입니다.
- 각 행의 오른쪽 칸에는 빨간 십자 기호가 가로 방향으로 나열되어 있습니다.
- 범례는 표의 오른쪽 바깥에 세로로 배치되어 있으며,
  - 위쪽에는 큰 빨간 십자 1개와 `10개`
  - 아래쪽에는 작은 빨간 십자 1개와 `1개`
  가 짝지어져 있습니다.
- 이 범례는 그림기호의 크기별 값이 다름을 알려주는 구조입니다.
- 표 아래의 괄호 안 텍스트는 선택지 또는 분류 항목처럼 보이며, 두 항목이 쉼표로 구분되어 있습니다.
- 하단의 `(정답)` 및 `(해설)` 문장은 인쇄된 해설 정보로 보이며, 문제 본문과는 구분된 위치에 있습니다.

[Python DSL 작성 힌트]
- 문제 전체를 상단 지시문 + 중앙 그림그래프 + 우측 범례 + 하단 해설 텍스트의 4개 영역으로 나눠서 배치하면 좋습니다.
- 표는 2열 5행 정도의 구조로 보이며, 첫 행은 헤더, 나머지 4행은 마을별 데이터 행으로 표현하면 적절합니다.
- 각 데이터 행의 우측 칸에는 빨간 십자 아이콘을 여러 개 반복 배치해야 합니다.
- 범례는 표 오른쪽에 별도 세로 블록으로 두고, 큰 십자와 작은 십자의 크기 차이를 분명히 표현해야 합니다.
- 표 제목 `마을별 병원 수`는 표 상단 중앙에 별도로 배치하는 방식이 자연스럽습니다.
- 보기로 보이는 `( 마을별 가게 수 , 마을별 병원 수 )`는 표 하단 중앙에 텍스트 박스로 넣을 수 있습니다.
- 하단의 `(정답)`과 `(해설)`은 원문 그대로 텍스트로 넣되, 문제 풀이 내용으로 확장하지 않는 것이 좋습니다.

[주의할 점]
- 병원 수를 실제로 계산하거나 각 마을의 값을 추정해 넣지 마세요.
- 십자 기호의 개수/크기를 임의로 해석하지 마세요. 보이는 그대로만 기록하세요.
- 범례의 큰 십자와 작은 십자의 의미를 숫자로 연결해 더 자세히 해석하지 마세요.
- 아래 괄호의 두 항목이 정답 선택지인지, 설명용 텍스트인지 단정하지 마세요.
- 하단 `(정답)`, `(해설)`은 인쇄된 보조 정보로 보이지만, 본문 문제와 동일한 중요도로 취급하지 않도록 주의하세요.

[불확실한 부분]
- 표 안의 각 행에 있는 십자 개수는 이미지 해상도상 일부 경계가 겹쳐 보여 정확한 개수를 단정하기 어려운 부분이 있습니다.
- 범례의 십자 크기 차이가 실제로는 선 굵기 차이인지, 도형 크기 차이인지 이미지상 완전히 분리해 보이지 않습니다.
- `( 마을별 가게 수 , 마을별 병원 수 )`가 실제 선택지인지, 예시 문구인지 화면 구성만으로는 확정하기 어렵습니다.
- 표 제목과 표의 실제 경계선이 약간 떨어져 있어, 제목이 표 내부인지 외부 캡션인지 다소 애매합니다.

--- RAW VISION DRAFT END ---
