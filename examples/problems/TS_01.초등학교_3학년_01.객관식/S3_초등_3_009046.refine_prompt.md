# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009046.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009046

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단에 문제 번호와 지시문이 있고, 그 아래에 반직선 그림 2개가 좌우로 나란히 배치되어 있습니다.
- 왼쪽에는 길이가 더 길어 보이는 반직선 1개, 오른쪽에는 별도의 반직선 1개가 있습니다.
- 아래쪽에는 `(정답)` 표시와 함께 짧은 반직선 1개가 있고, 그 아래에 `(해설)` 문장이 한 줄로 제시되어 있습니다.
- 전체적으로 “그림에서 알맞은 반직선을 고르는” 유형의 시각 자료입니다.

[보이는 텍스트]
- `84. 반직선 ㄱㄴ으로 알맞은 것을 고르세요.`
- 왼쪽 그림 위에는 `ㄱ`, `ㄴ` 표기가 보입니다.
- 오른쪽 그림 위에도 `ㄱ`, `ㄴ` 표기가 보입니다.
- 아래쪽에는 `(정답)` 표시가 있습니다.
- 해설 문장:
  - `(해설)반직선 ㄱㄴ은 점 ㄱ에서 시작하여 점 ㄴ을 지나는 반직선입니다.`

[반복되는 시각 패턴]
- 반직선 그림이 2개 제시되어 있고, 각각 시작점처럼 보이는 점과 방향을 나타내는 선분이 함께 있습니다.
- 각 그림 위에는 한글 자모 `ㄱ`, `ㄴ`이 각각 2곳에 표시되어 있습니다.
- 아래의 정답 예시도 같은 형태의 반직선 1개를 사용하고 있어, 위의 두 그림과 비교하도록 구성된 패턴입니다.
- 점은 검은색 실점(●)처럼 보이고, 선은 얇은 수평선으로 표현되어 있습니다.

[수식 단계]
- 해당 사항 없음. 수식 전개나 등호를 포함한 변형식은 보이지 않습니다.

[등호별 변환 의미]
- 해당 사항 없음.

[세부 내용]
- 왼쪽 그림:
  - 수평 방향의 반직선처럼 보이는 선이 왼쪽에서 오른쪽으로 뻗어 있습니다.
  - 왼쪽 끝 부분에 검은 점이 1개 있고, 그 위에 `ㄱ`이 표시되어 있습니다.
  - 선의 중간 오른쪽 부근에 또 다른 점이 1개 있고, 그 위에 `ㄴ`이 표시되어 있습니다.
  - 점 `ㄱ`에서 시작해 오른쪽으로 이어지는 형태로 읽히는 배치입니다.
- 오른쪽 그림:
  - 또 다른 수평 반직선이 제시되어 있습니다.
  - 왼쪽쪽에 검은 점 1개, 오른쪽쪽에 검은 점 1개가 있고 각각 위에 `ㄱ`, `ㄴ`이 표시되어 있습니다.
  - 선의 길이와 점 위치가 왼쪽 그림과 다르게 보입니다.
- `(정답)` 아래의 예시:
  - `(정답)` 텍스트 오른쪽에 짧은 수평 반직선이 있습니다.
  - 왼쪽 끝에 점이 있고 그 위에 `ㄱ`이 있으며, 선의 오른쪽 중간쯤에 점이 있고 그 위에 `ㄴ`이 있습니다.
  - 해설의 설명과 맞물려, 점 `ㄱ`에서 시작하여 점 `ㄴ`을 지나가는 구조로 보이도록 배치되어 있습니다.
- 해설 문장:
  - 반직선의 정의를 글로 설명하는 문장입니다.
  - `점 ㄱ에서 시작`과 `점 ㄴ을 지나는` 표현이 포함되어 있어, 그림 판별의 기준을 안내합니다.

[Python DSL 작성 힌트]
- 문제 상단 텍스트는 한 줄 문장으로 배치하고, 번호 `84.`를 포함해야 합니다.
- 중앙에는 반직선 그림 2개를 좌우로 나란히 배치합니다.
- 각 그림은:
  - 수평선 1개
  - 점 2개
  - 점 위의 자모 라벨 2개
  - 이런 요소로 구성합니다.
- 아래쪽에는 `(정답)` 라벨과 함께 짧은 반직선 예시를 추가하고, 그 아래에 해설 문장을 텍스트로 넣습니다.
- 점과 라벨의 상대 위치를 정확히 유지하는 것이 중요합니다.
- 왼쪽/오른쪽 그림의 선 길이, 점 간 간격, 라벨의 위쪽 배치를 구분해서 표현하면 좋습니다.
- 텍스트 요소는 한국어 문장과 자모를 그대로 렌더링할 수 있게 처리합니다.

[주의할 점]
- 그림만 보고 정답을 추론하여 특정 그림을 선택한 것처럼 쓰지 마세요.
- 빈칸은 없지만, 정답 예시가 있다고 해서 문제의 선택지를 확정적으로 서술하지 않는 것이 좋습니다.
- `ㄱ`, `ㄴ` 표기는 점의 이름이므로 다른 기호로 바꾸지 마세요.
- 해설 문장의 의미를 그대로 옮기되, 문제 해결 과정으로 확대 해석하지 마세요.
- 반직선과 일반 선분을 혼동하지 않도록, 보이는 선의 시작점과 끝점 표현을 분명히 구분하세요.

[불확실한 부분]
- 왼쪽과 오른쪽 그림이 실제로 어떤 선택지 번호에 해당하는지, 이미지 일부만으로는 명확하지 않습니다.
- 각 반직선의 정확한 길이와 점의 좌표적 위치는 시각적으로만 대략 확인 가능하며, 픽셀 단위로는 확정할 수 없습니다.
- `정답` 아래 그림이 실제로 정답 표시인지, 예시인지의 편집 의도는 해설 문구를 통해 추정한 것입니다.

--- RAW VISION DRAFT END ---
