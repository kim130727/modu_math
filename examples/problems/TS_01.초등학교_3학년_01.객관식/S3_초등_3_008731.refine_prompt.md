# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008731.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008731

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]

- 화면 상단에 문제 문장이 있고, 그 아래에 **수의 크기를 비교하는 나무형 도식**이 크게 배치되어 있습니다.
- 도식은 위에서 아래로 내려가며 **1개의 상자 → 2개의 상자 → 각 상자 아래에 2개씩 총 4개의 수 상자**로 분기되는 구조입니다.
- 도식 아래쪽에는 **(정답)**, **(해설)**이 따로 인쇄되어 있고, 해설 아래에 **한 번 더 같은 형태의 나무형 도식**이 작게 다시 등장합니다.
- 맨 아래에는 두 수의 크기 비교와 결론 문장이 문장형으로 제시되어 있습니다.
- 전체적으로 **빈 상자에 들어갈 수를 고르는 문제**이며, 상자 사이의 연결선은 “상위 기준을 이용해 하위 후보를 나누는 구조”처럼 보이지만, 실제로는 문제 해결을 위해 쓰이는 **시각적 비교 트리**입니다.

# [보이는 텍스트]

## 상단 문제 문장
- `50. 두 분수의 크기를 비교하여 더 큰 분수를 빈칸에 써넣으려고 합니다. ㉠에 들어갈 분수로 알맞은 것을 선택해 보세요.`

## 도식 속 문자/수
- 최상단 상자: `㉠`
- 왼쪽 중간 상자: 빈칸
- 오른쪽 중간 상자: 빈칸
- 아래 후보 분수들:
  - `15/7`
  - `22/7`
  - `2 3/7`
  - `2 6/7`

## 해설 영역 텍스트
- `(정답) 22/7`
- `(해설) 15/7 < 22/7, 2 3/7 < 2 6/7`
- 아래 문장:
  - `22/7 와 2 6/7의 크기를 비교하면 22/7 = 3 1/7 이므로 22/7 > 2 6/7입니다.`
  - `따라서 ㉠에 알맞은 수는 22/7입니다.`

> 위 해설 문장은 **보이는 그대로 적은 것**이며, 계산 결과의 타당성은 여기서 검토하지 않습니다.

# [반복되는 시각 패턴]

- 같은 형태의 **연두색 테두리 상자**가 여러 번 반복됩니다.
- 상자 내부는 대부분 흰 배경이고, 일부 상자에는 수가 들어가며 일부는 비어 있습니다.
- 상자 간 연결은 **회색 선**으로 구성되어 있고, 위에서 아래로 분기되는 트리 구조입니다.
- 수가 들어 있는 하위 상자들은 모두 같은 크기와 스타일로 반복되며,
  - 분수 형태 `15/7`, `22/7`
  - 대분수 형태 `2 3/7`, `2 6/7`
  처럼 서로 다른 표기 방식이 섞여 있습니다.
- 해설 아래의 두 번째 도식도 같은 구조를 반복하지만,
  - 일부 수가 **파란색 글자**로 표시되어 있습니다.
  - 상단의 빈 상자 `㉠`도 다시 한 번 나타납니다.

# [수식 단계]

- 해설 부분에 **직접적인 등식/부등식 전개**가 보입니다.
- 보이는 수식 흐름은 다음과 같은 형태입니다.
  1. `15/7 < 22/7, 2 3/7 < 2 6/7`
  2. `22/7` 와 `2 6/7`를 비교하는 문장
  3. `22/7 = 3 1/7`
  4. `22/7 > 2 6/7`
- 첫 줄은 두 쌍의 수를 각각 비교하는 **부등식 나열**처럼 보입니다.
- 그 다음 줄은 `22/7`와 `2 6/7`를 직접 비교하는 **설명 문장**입니다.
- 이어서 `22/7 = 3 1/7`이라는 **표현 변환**이 제시되어 있습니다.
- 마지막으로 `22/7 > 2 6/7`라는 **비교 결과**가 문장 안에 포함되어 있습니다.

# [등호별 변환 의미]

- `22/7 = 3 1/7`
  - 보이는 의미상, 분수 `22/7`를 대분수 형태로 바꿔 쓴 것으로 보입니다.
  - 이때 **표현 방식이 바뀐 것**이지, 도식 자체가 바뀐 것은 아닙니다.
- 등호가 이 한 곳에만 명시적으로 보이며, 연속된 여러 등호 전개는 보이지 않습니다.
- 따라서 이 자료에서는 **등호 변환 단계가 길게 이어지지는 않음**이 관찰됩니다.

# [세부 내용]

- 최상단의 `㉠` 상자는 도식의 최상위 목표 위치에 있으며, 아래 두 개의 중간 상자로 분기됩니다.
- 왼쪽 중간 상자 아래에는 `15/7`, `22/7` 두 수가 있고,
  오른쪽 중간 상자 아래에는 `2 3/7`, `2 6/7` 두 수가 있습니다.
- 즉, 한 중간 상자 아래에 **2개의 후보 수**가 대응되는 구조입니다.
- 상자 테두리는 연한 연두색, 내부는 흰색 또는 연한 배경으로 보이며, 수가 있는 하위 상자는 안쪽에 여백이 있는 편입니다.
- 해설 아래의 작은 도식에서는
  - `22/7`와 `2 6/7`가 파란색으로 강조되어 있어,
  - 최종 선택 또는 비교 대상임을 시각적으로 나타내는 듯합니다.
- 맨 아래 문장은 수식과 문장을 섞어 배치한 형태로,
  - `22/7`와 `2 6/7`를 비교하는 서술
  - `22/7 = 3 1/7`
  - `22/7 > 2 6/7`
  의 순서로 이어집니다.
- 마지막 결론 문장은 `㉠에 알맞은 수는 22/7입니다.`라고 끝납니다.

# [Python DSL 작성 힌트]

- 이 문제는 **트리형 도식 + 해설 텍스트**가 핵심이므로, DSL에서는 다음 요소를 분리해 표현하면 좋습니다.
  1. 상단 문제문장
  2. 큰 도식(상자 5개 이상, 연결선 포함)
  3. 해설 영역의 텍스트 블록
  4. 반복 도식의 강조색 변화
  5. 맨 아래 결론 문장
- 도식 배치는 대체로 **중앙 정렬**입니다.
- 상자 좌표를 잡을 때는
  - 최상단 1개
  - 그 아래 좌우 2개
  - 그 아래 각자 2개씩
  의 계층 구조를 유지하면 됩니다.
- 빈 상자는 **텍스트 없는 사각형**으로 처리하고, `㉠`은 글자만 중앙에 배치합니다.
- 수가 들어간 상자들은 **분수형 텍스트**와 **대분수형 텍스트**를 구분해 렌더링하는 것이 좋습니다.
- 해설 아래의 작은 도식은 상단 도식과 같은 구조를 **축소 복제**한 것으로 보면 됩니다.
- 강조된 파란색 수는 일반 검정 텍스트와 스타일을 구분해서 표현하세요.

# [주의할 점]

- **정답을 추론해서 새로 쓰지 마세요.** 화면에 이미 `(정답) 22/7`가 보이더라도, 초안에서는 그 사실을 “보이는 텍스트”로만 다루면 됩니다.
- 빈 상자 안에 어떤 수가 들어가는지 **직접 계산해 채우지 마세요.**
- `22/7 = 3 1/7` 같은 표기는 **보이는 그대로** 옮기고, 그 옳고 그름을 평가하지 마세요.
- `2 3/7`, `2 6/7`는 대분수 표기이므로 `2+3/7`처럼 임의로 바꾸지 마세요.
- 도식의 연결선은 단순한 장식이 아니라 **상하 관계를 나타내는 구조 요소**로 보이므로, Python DSL에서 선과 상자를 함께 배치해야 합니다.
- 해설 영역의 파란색 글자는 강조 표시일 가능성이 높으므로, 일반 본문과 스타일을 분리하는 것이 좋습니다.

# [불확실한 부분]

- 최상단 `㉠`이 정확히 어떤 빈칸을 의미하는지, 문제 문장만으로는 **상자 내부의 목표값인지 선택지인지** 완전히 단정하기 어렵습니다.
- 도식의 의미가 “비교하여 더 큰 수를 고르는 것”으로 읽히지만, 각 중간 상자가 어떤 비교 기준인지까지는 그림만으로 완전히 확정하기 어렵습니다.
- 해설 아래의 작은 도식에서 파란색으로 표시된 수들이 **정답 강조인지, 풀이 과정 중 선택 대상인지**는 시각적으로는 보이지만 기능적으로는 단정하지 않는 편이 안전합니다.
- 화면 하단 문장의 줄바꿈과 수식 배치가 다소 촘촘하여, 실제 OCR/DSL 작성 시에는 **문장 단위와 수식 단위 분리**가 필요할 수 있습니다.

--- RAW VISION DRAFT END ---
