[전체적인 구성]
- 화면은 초등 수학 문제 1개로 구성되어 있습니다.
- 왼쪽 위에 문제 번호 `26.`이 있고, 그 뒤로 긴 문장형 질문이 이어집니다.
- 문제의 핵심 시각 자료는 가운데에 있는 **사각형 표(친구들이 좋아하는 과일)** 와 그 아래의 **조사 결과 표**입니다.
- 전체적으로 위쪽에는 개별 학생들이 좋아하는 과일을 그림으로 나타낸 표가 있고, 아래쪽에는 과일 종류별 학생 수를 정리한 표가 배치되어 있습니다.
- 맨 아래에는 `(정답)`, `(해설)` 표기가 보이며, 해설 문장이 작게 적혀 있습니다.  
  - 이 부분은 문제 본문과는 별도의 안내/정답 영역으로 보입니다.

[보이는 텍스트]
- 문제 번호: `26.`
- 문제 문장:
  - `은호네 모둠 학생들이 좋아하는 과일을 조사하여 나타낸 자료와 표 중에서 어느 것이 더 편리할까요?`
- 위쪽 그림 표 제목:
  - `친구들이 좋아하는 과일`
- 그림 표 안 이름들:
  - 1행: `은호, 민지, 정민, 준수, 호진`
  - 2행: `선아, 효성, 영준, 규진, 현영`
  - 3행: `승희, 선미, 은수, 재민, 은채`
- 과일 그림 아래 범례:
  - `사과`, `포도`, `딸기`, `바나나`
- 아래쪽 표 제목:
  - `친구들이 좋아하는 과일`
- 아래쪽 표의 열 제목:
  - `과일, 사과, 포도, 딸기, 바나나, 합계`
- 아래쪽 표의 행 제목:
  - `학생 수(명)`
- 아래쪽 표에 보이는 수:
  - `5, 3, 6, 1, 15`
- 오른쪽 아래 쪽 표시:
  - `(자료, 표)`
- 하단 표시:
  - `(정답)표`
  - `(해설)조사한 학생 수는 표의 합계를 보면 쉽게 알 수 있습니다.`

[반복되는 시각 패턴]
- 위쪽 자료는 **3행 × 5열** 형태의 격자 표로 보입니다.
- 각 칸에는 학생 이름 1개와 과일 그림 1개가 함께 배치되어 있습니다.
- 과일 그림은 범례에 따라 반복적으로 사용됩니다.
  - 사과 그림
  - 포도 그림
  - 딸기 그림
  - 바나나 그림
- 아래쪽 자료는 **2행짜리 요약 표**로 보입니다.
  - 첫 행: 과일 종류와 합계 제목
  - 둘째 행: 각 과일별 학생 수
- 아래쪽 표에서는 열이 왼쪽에서 오른쪽으로 정렬되어 있고, 숫자는 각 과일 아래에 1개씩 배치되어 있습니다.
- 두 자료 모두 제목이 표 위쪽 중앙에 따로 표시되어 있어, “같은 주제의 다른 표현”이라는 느낌을 줍니다.

[수식 단계]
- 해당 없음.
- 그림 비교 문제이며, 등호로 연결된 수식 변형은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.
- 등호가 포함된 연산식이나 변형 과정은 보이지 않습니다.

[세부 내용]
- 위쪽의 큰 표는 학생 이름과 좋아하는 과일을 **개별적으로 대응**시킨 자료로 보입니다.
- 각 행은 같은 높이에서 좌우로 5칸씩 나뉘어 있으며, 각 칸 위쪽에 이름, 아래쪽에 과일 그림이 배치된 형태입니다.
- 위쪽 표는 분홍색 계열의 둥근 테두리로 둘러져 있고, 내부는 점선 격자로 나뉘어 있습니다.
- 표 제목 `친구들이 좋아하는 과일`은 분홍색 배경의 둥근 직사각형 안에 있습니다.
- 범례는 위쪽 표 아래에 가로 방향으로 나열되어 있으며, 작은 과일 아이콘과 텍스트가 짝지어져 있습니다.
- 아래쪽 표는 파란색 테두리와 옅은 파란색 배경의 머리글 셀로 구성되어 있습니다.
- 첫 번째 열은 항목 이름을 적는 용도이고, 나머지 열은 과일 종류별 수를 적는 용도로 보입니다.
- 숫자 `15`는 맨 오른쪽 `합계` 칸에 들어가 있습니다.
- 문제의 질문은 “자료와 표 중에서 어느 것이 더 편리할까요?”이므로, 시각적으로는 **개별 자료(그림 표)** 와 **정리된 표**를 비교하도록 유도하는 구성입니다.  
  - 다만 이것은 문제 의도에 대한 해석일 뿐, 답을 확정하는 내용은 아닙니다.

[Python DSL 작성 힌트]
- 위쪽 영역은 다음 요소로 나눠 작성하면 좋습니다.
  - 제목 박스
  - 큰 격자 표
  - 각 칸의 이름 텍스트
  - 각 칸의 과일 아이콘
  - 하단 범례
- 아래쪽 영역은 다음 요소로 나눠 작성하면 좋습니다.
  - 제목 박스
  - 2행 표
  - 열 머리글 셀
  - 숫자 셀
- 표 구조를 구현할 때는:
  - 행/열 경계선
  - 셀 내부 가운데 정렬
  - 제목의 별도 박스 처리
  - 범례 아이콘과 텍스트의 수평 정렬
  를 따로 표현하는 것이 좋습니다.
- 과일 아이콘은 동일한 그림이 여러 번 반복되므로, 아이콘 타입별로 재사용 가능한 객체처럼 다루면 편리합니다.
- 아래쪽 표의 `합계` 칸은 다른 숫자 셀보다 의미가 다르므로 별도 스타일을 둘 수 있습니다.
- 전체 레이아웃은 위쪽 큰 표와 아래쪽 작은 표의 **세로 배치** 관계를 먼저 잡고, 각 영역 내부를 다시 분할하는 방식이 적합해 보입니다.

[주의할 점]
- 문제를 풀거나 비교 결과를 단정하지 마세요.
- `15`를 이용해 학생 수를 역산하거나, 과일별 개수를 다시 계산하지 마세요.
- 위쪽 표의 과일 개수와 아래쪽 표의 수가 서로 어떻게 대응되는지는 시각적으로만 설명하고, 계산 결과로 정리하지 마세요.
- `자료, 표`라는 표기는 괄호 안에 함께 보이지만, 이것이 정답인지 선택지인지는 확정하지 마세요.
- 해설 문장은 문제 본문이 아니라 별도 영역이므로, 본문 분석과 혼동하지 않는 것이 좋습니다.
- 그림이 작게 보여 일부 과일 아이콘은 세부 모양이 흐릴 수 있으니, 아이콘 종류는 범례와 반복 패턴에 근거해 설명하는 정도로만 적는 것이 안전합니다.

[불확실한 부분]
- 위쪽 표 내부의 일부 학생 이름은 이미지 해상도 때문에 글자가 아주 선명하지 않을 수 있습니다.
- 과일 아이콘의 정확한 세부 디자인은 작아서 일부는 생략된 형태로 보일 수 있습니다.
- 오른쪽 아래의 `(자료, 표)` 표기가 문제의 선택지인지, 편집용 메모인지 이미지 만으로는 단정하기 어렵습니다.
- 하단의 `(정답)표`, `(해설)` 표시는 인쇄된 교재의 정답/해설 영역처럼 보이지만, 실제 편집 의도는 이미지 만으로 완전히 확정할 수 없습니다.
