# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008835.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008835

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 장의 객관식 문제 화면이다.
- 상단 왼쪽에 문제 번호와 문장형 지시문이 있고, 가운데에는 그림이 크게 배치되어 있다.
- 하단 왼쪽과 오른쪽에는 보기 4개가 2열 형태로 배치되어 있다.
- 보기 아래쪽 왼편에 정답 표기와 해설 문장이 추가로 인쇄되어 있다.
- 이 문제는 그림 속 물체의 크기 관계를 바탕으로 순서를 고르는 형태로 보인다.  
  - **보이는 사실:** 그림과 보기들이 제시되어 있다.
  - **추론 의미:** 크기의 비교 결과를 순서대로 배열하는 문제로 해석된다.

[보이는 텍스트]
- 상단 문제 문장:
  - `77. 무게가 무거운 것부터 순서대로 바르게 나타낸 것을 고르세요.`
- 그림 속 표기:
  - `식탁`
  - `배`
  - `파인애플`
- 보기:
  - `① 식탁, 배, 파인애플`
  - `② 식탁, 파인애플, 배`
  - `③ 배, 파인애플, 식탁`
  - `④ 파인애플, 배, 식탁`
- 하단 표기:
  - `(정답)②`
  - `(해설)손으로 들었을 때 힘이 많이 드는 것부터 순서대로 쓰면 식탁, 파인애플, 배입니다.`

[반복되는 시각 패턴]
- 보기 번호가 `①, ②, ③, ④`처럼 원 안의 숫자로 반복된다.
- 각 보기는 쉼표로 구분된 3개 항목의 나열 구조를 가진다.
- 그림에서는 물체 이름이 화살표로 가리켜져 있다.
- 같은 종류의 물체를 나타내는 텍스트가 그림 아래 또는 옆에 반복 배치되어 있다.
- 화살표는 각각 특정 물체를 향해 위로/옆으로 연결되어 있어 이름과 대상이 대응되도록 만든다.

[수식 단계]
- 해당 없음.  
- 이 문제 화면에는 등호 `=`를 이용한 수식 변형 단계가 보이지 않는다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 화면 중앙의 그림은 식탁 위에 과일 2개가 놓여 있는 장면으로 보인다.
- 식탁은 파란색 계열의 천이 덮인 직사각형 탁자로 그려져 있다.
- 탁자 다리는 아래로 보이며, 천 가장자리는 물결 모양 장식처럼 표현되어 있다.
- 그림 속 물체 위치:
  - 왼쪽 쪽에는 `식탁`이라는 글자에서 분홍색 화살표가 탁자 상판 쪽을 향한다.
  - 가운데 왼쪽에는 `배`라는 글자가 아래에 있고, 위쪽의 배 그림을 향하는 세로 화살표가 있다.
  - 가운데 오른쪽에는 `파인애플`이라는 글자가 아래에 있고, 위쪽의 파인애플 그림을 향하는 세로 화살표가 있다.
- 과일 그림은 크기 차이를 강조한 삽화 스타일이다.
  - 배는 상대적으로 작게, 파인애플은 더 크게 그려져 있다.
- 문제의 핵심은 그림 속 대상들을 어떤 기준으로 순서를 정하는지 파악하는 형식이다.
  - **보이는 사실:** 물체 3개가 이름과 함께 제시되어 있다.
  - **추론 의미:** 보기 중 하나를 고르는 선택형 문제다.

[Python DSL 작성 힌트]
- 문제 제목/지시문/그림/보기/정답 및 해설을 영역별로 분리해서 배치하면 좋다.
- 그림 영역에서는:
  - 식탁을 직사각형 테이블과 천 덮개, 다리까지 포함한 도형 묘사로 표현
  - `식탁`, `배`, `파인애플` 라벨과 각각의 화살표를 연결
- 보기 영역에서는:
  - 2열 배치로 ①~④를 정렬
  - 각 보기의 텍스트를 한 줄씩 나열
- 하단 보충 텍스트 영역에는:
  - `(정답)②`
  - `(해설)...`
  를 별도 줄로 표시
- 빈칸이나 계산식은 없으므로 입력형 슬롯은 필요 없어 보인다.

[주의할 점]
- 이 문제는 수학 계산식이 아니라 그림과 보기로 구성된 선택형 문제이므로, 수식처럼 다루지 않도록 주의한다.
- 정답 번호와 해설 문장은 화면에 보이지만, 이를 문제 내용과 혼동하지 말아야 한다.
- 보기의 항목 순서는 실제로 제시된 그대로 기록해야 하며, 추정으로 바꾸지 않는다.
- 그림 속 물체의 크기나 위치를 보고 의미를 단정하더라도, 여기서는 답을 직접 확정해서 서술하지 않는다.
- 화살표 방향과 라벨 연결을 정확히 구분해야 한다.

[불확실한 부분]
- `식탁` 화살표가 정확히 어느 선분/면을 가리키는지는 작은 이미지 해상도상 약간 अस्पষ্ট할 수 있다.
- 파인애플과 배의 상대적 크기·원근감은 그림 스타일 때문에 실제 비율과 다를 수 있다.
- 하단의 `(해설)` 문장은 인쇄된 해설로 보이지만, DSL에서 포함 범위는 작성 목적에 따라 조정될 수 있다.

--- RAW VISION DRAFT END ---
