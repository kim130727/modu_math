[전체적인 구성]
- 문제 번호 `61.`이 왼쪽 위에 있고, 그 오른쪽에 지시문이 한 줄로 배치되어 있습니다.
- 중앙에는 분홍색 테두리의 큰 사각형 영역이 있으며, 세로선으로 좌우 2칸으로 나뉘어 있습니다.
- 왼쪽 칸 제목은 `반직선 ㄴㄱ`, 오른쪽 칸 제목은 `반직선 ㄴㄷ`로 보입니다.
- 각 칸 안에는 주황색 둥근 사각형 테두리의 선택지 박스가 2개씩 세로로 배치되어, 전체 4개의 그림 선택지가 있습니다.
- 아래쪽 왼편에는 `(정답)` 표시와 함께 주황색 테두리의 작은 그림 2개가 가로로 놓여 있습니다.
- 최하단에는 `(해설)` 문구와 설명 문장이 한 줄로 있습니다.

[보이는 텍스트]
- `61.`
- `점 ㄴ에서 반직선 ㄴㄱ, 반직선 ㄴㄷ을 바르게 그은 것을 선택하세요.`
- 왼쪽 상단 칸 제목: `반직선 ㄴㄱ`
- 오른쪽 상단 칸 제목: `반직선 ㄴㄷ`
- `(정답)`
- `(해설)점 ㄴ에서 시작하여 점 ㄱ과 ㄷ을 지나는 반직선을 찾습니다.`
- 도형 안에 보이는 글자: `ㄴ`, `ㄱ`, `ㄷ`

[반복되는 시각 패턴]
- 각 선택지는 모두 흰 배경의 주황색 둥근 사각형 안에 들어 있습니다.
- 각 그림은 점 `ㄴ`, 점 `ㄱ`, 점 `ㄷ`의 배치와 선의 방향이 달라지는 구조입니다.
- 왼쪽 열의 두 그림은 대각선 형태의 선이 보이고, 오른쪽 열의 두 그림은 수평선 형태의 선이 보입니다.
- 각 그림마다 검은 점(끝점 또는 기준점으로 보이는 점)이 있으며, 일부 점 옆에 `ㄴ`, `ㄱ`, `ㄷ` 표기가 붙어 있습니다.
- 아래 정답 그림 2개는 위 선택지의 작은 축약 형태처럼 보이며, 같은 유형의 그림을 다시 제시합니다.

[수식 단계]
- 해당 없음. 수식이나 등호를 이용한 변형식은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 큰 분홍색 박스는 좌우 2열 구조입니다.
- 왼쪽 제목 `반직선 ㄴㄱ` 아래:
  - 첫 번째 그림: 점 `ㄴ`에서 오른쪽 위 방향으로 이어지는 대각선 선분이 있고, 선 위쪽 끝 부근에 `ㄱ` 표기가 있습니다. 오른쪽 아래 쪽에는 `ㄷ` 점이 따로 떨어져 있습니다.
  - 두 번째 그림: 비슷한 대각선 구조인데, 점 `ㄴ`이 왼쪽 아래 쪽에 있고 `ㄱ` 표기는 오른쪽 위 끝 부근에 있습니다. 역시 `ㄷ` 점은 오른쪽 아래에 따로 있습니다.
- 오른쪽 제목 `반직선 ㄴㄷ` 아래:
  - 첫 번째 그림: 아래쪽에 수평선이 있고 왼쪽 쪽에 `ㄴ` 점, 오른쪽 쪽에 `ㄷ` 점이 있으며, 위쪽에는 `ㄱ` 점이 따로 있습니다.
  - 두 번째 그림: 비슷한 수평선 구조인데, `ㄴ` 점이 선의 왼쪽 쪽에, `ㄷ` 점이 선의 오른쪽 쪽에 배치되어 있고, 위쪽에는 `ㄱ` 점이 따로 있습니다.
- 아래 `(정답)` 영역:
  - 왼쪽 작은 그림은 대각선 형태로, `ㄴ`에서 `ㄱ` 방향으로 향하는 선이 보이며 `ㄷ`는 떨어져 있습니다.
  - 오른쪽 작은 그림은 수평선 형태로, `ㄴ`에서 `ㄷ` 방향으로 향하는 선이 보이며 `ㄱ`는 떨어져 있습니다.
- 해설 문장은 점 `ㄴ`을 시작점으로 삼아 `ㄱ`, `ㄷ`을 지나는 반직선을 찾는다고 설명합니다. 다만 이 문장은 해설 텍스트의 내용만 옮긴 것이며, 실제 정답을 계산하거나 판단한 것은 아닙니다.

[Python DSL 작성 힌트]
- 큰 박스 1개를 만들고, 내부를 세로 기준선으로 좌우 2열 분할하는 구조로 잡으면 좋습니다.
- 각 열 상단에는 제목 텍스트를 중앙 정렬로 넣고, 그 아래에 선택지 박스 2개를 세로로 배치합니다.
- 선택지 박스는 모두 동일한 크기의 둥근 사각형으로 통일하고, 내부에는 점과 선을 상대 위치로 배치합니다.
- 점 표시는 작은 검은 원, 글자 `ㄴ`, `ㄱ`, `ㄷ`는 점 근처에 별도 텍스트 객체로 두는 방식이 적절합니다.
- 아래쪽 `(정답)` 영역은 작은 그림 2개를 가로로 놓고, 왼쪽에 `(정답)` 라벨을 붙이는 구성이 보입니다.
- 최하단 해설 문장은 일반 텍스트 한 줄로 처리하면 됩니다.
- 선의 방향은 대각선/수평선 두 유형으로 구분해 표현하면 시각 구조를 재현하기 쉽습니다.

[주의할 점]
- 이 문제는 그림 선택 문제이므로, 선이 실제로 어느 점에서 시작하는지와 어느 점을 지난다고 표시되는지에 주의해야 합니다.
- 텍스트 `반직선 ㄴㄱ`, `반직선 ㄴㄷ`는 각 열의 기준 설명이므로, 그림 내부의 점 배치와 혼동하지 않도록 해야 합니다.
- 정답 영역의 작은 그림은 해설용 재표시처럼 보이므로, 본문 선택지와 동일하게 다루되 위치만 따로 분리해야 합니다.
- 빈칸, 분수, 등호 변형은 없습니다.
- 보이는 도형은 모두 2차원 평면 위의 점과 직선 조각으로만 구성됩니다.

[불확실한 부분]
- 각 그림 안의 선이 정확히 어디까지 연장되는지, 또는 점이 선 위에 정확히 겹치는지 여부는 이미지 해상도상 약간 흐릿할 수 있습니다.
- 주황색 테두리 박스와 분홍색 큰 박스의 정확한 색상값은 단정하지 않고, 대략적인 시각 인상만 기록했습니다.
- 일부 글자 `ㄴ`, `ㄱ`, `ㄷ`의 위치가 점과 아주 가깝게 붙어 있어, 세밀한 좌표 복원 시 약간의 해석 차이가 있을 수 있습니다.
