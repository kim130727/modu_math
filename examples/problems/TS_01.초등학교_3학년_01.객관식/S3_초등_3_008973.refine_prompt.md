# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008973.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008973

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단에 문제 번호와 지시문이 한 줄로 배치되어 있습니다.
- 그 아래에는 연두색 테두리의 긴 직사각형 박스가 있고, 그 안에 3개의 계산식이 가로로 나란히 제시되어 있습니다.
- 박스 아래에는 괄호로 묶인 보기 번호 형태의 표기가 따로 있고, 그 아래쪽에 정답 표기와 해설 문장이 이어집니다.
- 전체적으로 “식 3개를 비교해서 가장 큰 값을 고르는” 유형의 객관식/선택형 구성입니다.
- 실제 선택 결과를 직접 적는 빈칸은 보이지 않고, 대신 정답 표시와 해설이 텍스트로 제시되어 있습니다.

[보이는 텍스트]
- `7. 계산 결과가 가장 큰 것을 찾아 기호를 선택해 보세요.`
- 박스 안:
  - `① 201 + 339`
  - `② 622 - 115`
  - `③ 746 - 228`
- 박스 아래:
  - `( ① , ② , ③ )`
- 아래쪽 텍스트:
  - `(정답) ①`
  - `(해설) ① 201 + 339 = 540   ② 622 - 115 = 507   ③ 746 - 228 = 518`
  - `540>518>507 이므로 계산 결과가 가장 큰 것은 ①입니다.`

[반복되는 시각 패턴]
- 세 개의 계산식이 동일한 형식으로 반복됩니다.
  - 각 항목은 번호 기호(①, ②, ③) + 숫자식의 구조입니다.
- 박스 안에서 3개의 식이 가로 정렬되어 있어, 서로를 비교하는 대상임이 시각적으로 드러납니다.
- 해설에서도 ①, ②, ③이 다시 한 번 같은 순서로 반복되어, 각각의 계산 결과를 대응시키는 패턴이 보입니다.
- 비교 결과는 부등호 `>`를 연속해서 사용해 큰 값부터 작은 값 순으로 정리되어 있습니다.

[수식 단계] (해당될 때)
- 해설에 등호를 포함한 계산 단계가 각각 한 번씩 보입니다.
  - `① 201 + 339 = 540`
  - `② 622 - 115 = 507`
  - `③ 746 - 228 = 518`
- 각 식은 원래의 계산식에서 바로 결과값으로 이어지는 형태이며, 중간 전개 과정은 보이지 않습니다.
- 마지막 줄에서는 세 결과값을 `540>518>507`의 형태로 비교하고 있습니다.

[등호별 변환 의미] (해당될 때)
- `201 + 339 = 540`
  - 왼쪽의 덧셈식에 대한 계산 결과가 오른쪽에 제시되어 있습니다.
- `622 - 115 = 507`
  - 왼쪽의 뺄셈식에 대한 계산 결과가 오른쪽에 제시되어 있습니다.
- `746 - 228 = 518`
  - 왼쪽의 뺄셈식에 대한 계산 결과가 오른쪽에 제시되어 있습니다.
- `540>518>507`
  - 세 계산 결과를 크기 비교로 나열한 표현입니다.
  - 단, 이 줄은 풀이 결과를 요약한 문장이지, 연산 과정 자체는 아닙니다.

[세부 내용]
- 문제 번호 `7.`이 맨 앞에 있으며, 그 뒤에 지시문이 이어집니다.
- 박스는 연두색 선으로 그어진 직사각형이며, 내부에 3개의 항목이 일정 간격으로 배치되어 있습니다.
- 각 항목은 단순한 텍스트식으로, 별도의 칸이나 계산칸은 없습니다.
- 박스 아래의 `( ① , ② , ③ )` 표기는 보기 번호를 다시 한 번 묶어 보여주는 역할로 보입니다.
- 하단의 `(정답) ①`은 선택된 기호를 직접 표시한 부분입니다.
- 해설은 한 줄에 3개의 계산식을 나열한 뒤, 다음 줄에서 크기 비교 결론을 말하는 구조입니다.
- 수식 주변에 추가 도형, 선, 화살표, 분수, 표는 보이지 않습니다.

[Python DSL 작성 힌트]
- 상단 문제 문장을 텍스트로 배치하고, 그 아래에 연두색 테두리의 긴 직사각형 박스를 생성하는 구성이 적절합니다.
- 박스 내부는 3열 가로 배치로 처리하면 좋습니다.
- 각 칸의 텍스트는 다음처럼 번호+식 형태로 넣을 수 있습니다.
  - `① 201 + 339`
  - `② 622 - 115`
  - `③ 746 - 228`
- 박스 아래의 `( ① , ② , ③ )`는 별도 텍스트로 중앙 정렬에 가깝게 배치하는 방식이 자연스럽습니다.
- 하단 정답 및 해설 영역은 일반 텍스트 블록으로 두 줄 이상 나누어 배치하면 됩니다.
- 해설 줄에서는 각 식과 결과를 `=`로 연결한 형태를 그대로 표시하면 됩니다.
- 마지막 결론 문장은 별도 텍스트 라인으로 처리하세요.

[주의할 점]
- 어떤 식의 값이 가장 큰지 실제로 계산해 확정하지 말고, 화면에 보이는 해설 텍스트만 사실대로 옮겨 적어야 합니다.
- 박스 안의 ①, ②, ③은 선택지 번호이며, 빈칸이 아닙니다.
- `( ① , ② , ③ )`는 답을 쓰는 칸처럼 보일 수 있으나, 화면상으로는 단순 표기일 수 있으니 빈칸으로 해석하지 마세요.
- 해설에 이미 정답이 표시되어 있지만, 초안 단계에서는 그 의미를 분석만 하고 문제 해결 과정으로 확장하지 마세요.
- 연두색 박스의 정확한 선 굵기나 세부 여백은 이미지 해상도에 따라 약간 달라 보일 수 있습니다.

[불확실한 부분]
- 박스의 정확한 색상 이름은 연두색/연녹색 계열로 보이지만, 실제 채색값은 이미지에 따라 다르게 인식될 수 있습니다.
- `(정답) ①`과 `(해설)`의 정확한 줄바꿈 위치는 이미지 축소 상태에서는 다소 अस्पष्ट하게 보일 수 있습니다.
- 해설의 부등호 사이 간격은 인쇄본과 OCR 해석에 따라 약간 달라질 수 있습니다.

--- RAW VISION DRAFT END ---
