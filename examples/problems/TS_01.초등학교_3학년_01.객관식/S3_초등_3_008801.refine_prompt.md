# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008801.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008801

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호 표시가 있고, 그 오른쪽으로 문제 문장이 한 줄 이상에 걸쳐 배치되어 있습니다.
- 가운데 영역에는 비교 대상처럼 보이는 그림 3개가 가로로 넓게 떨어져 배치되어 있습니다.
  - 왼쪽: 클립 그림
  - 가운데: 수박 그림
  - 오른쪽: 100원 동전 그림
- 하단 왼쪽에는 `(정답)` 표시와 함께 선택된 예시처럼 보이는 그림 2개가 나란히 배치되어 있습니다.
- 하단에는 `(해설)` 표시 뒤에 설명 문장이 한 줄로 이어집니다.

[보이는 텍스트]
- `□ 30.`
- `바둑돌과 같이 무게를 비교하는 데 사용할 수 있는 단위로 알맞은 것을`
- `모두 선택해 보세요.`
- `(정답)`
- `(해설) 무게가 일정한 작은 단위여야 무게를 비교하기 쉽습니다.`

[반복되는 시각 패턴]
- 가운데 그림 3개는 모두 하나의 비교 대상 예시로 보이며, 서로 크기와 종류가 다르게 배치되어 있습니다.
- 하단의 `(정답)` 영역에는 가운데에 있던 그림 중 일부와 같은 종류의 그림이 다시 등장하는 형태가 보입니다.
- 문제 화면 전체는 “문제 문장 → 그림 선택 대상 → 정답 예시 → 해설” 흐름으로 구성되어 있습니다.

[수식 단계]
- 해당 없음.  
  - 화면에 등호, 계산식, 변형식, 분수식 같은 수식 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.  
  - 등호를 포함한 식 전개가 표시되어 있지 않습니다.

[세부 내용]
- 문제 번호 앞에 작은 네모 체크박스처럼 보이는 기호가 있습니다.
- 문제 문장은 교재형 인쇄체로 표시되어 있고, 끝의 `선택해 보세요.`가 줄바꿈되어 보입니다.
- 가운데의 각 그림은 독립적으로 떨어져 있으며, 텍스트 라벨은 붙어 있지 않습니다.
- 왼쪽의 클립은 대각선 방향으로 기울어진 작은 물체 그림입니다.
- 가운데 수박은 가장 큰 그림으로, 초록색 바탕에 줄무늬가 있는 원형 과일 그림입니다.
- 오른쪽 100원 동전은 회색 원형 도안으로, 숫자 `100`이 중앙에 크게 보입니다.
- 하단 `(정답)` 옆에는 클립과 100원 동전 그림이 나란히 배치되어 있고, 둘 사이에 쉼표가 있어 두 개가 함께 제시된 형태입니다.
- 하단 `(해설)` 문장은 선택 근거를 설명하는 문장으로 보이며, 문제 풀이 과정이나 계산식은 없습니다.

[Python DSL 작성 힌트]
- 상단 문제 문장은 `텍스트 박스` 형태로 배치하고, 줄바꿈 위치를 원문과 비슷하게 반영하면 좋습니다.
- 가운데 영역은 `그림 3개를 가로 배치`하는 구조로 작성하면 됩니다.
  - 왼쪽: 클립 아이콘
  - 중앙: 수박 그림
  - 오른쪽: 100원 동전 그림
- 하단은 `(정답)` 라벨 + 그림 2개 + 쉼표 표시의 순서로 따로 배치합니다.
- `(해설)` 문장은 별도 텍스트 블록으로 아래쪽에 놓는 방식이 적절합니다.
- 각 그림은 서로 다른 객체이므로, 크기 차이와 위치 관계를 함께 지정하는 것이 좋습니다.
- 체크박스 기호 `□`도 문제 번호 앞에 포함하여 텍스트로 처리할 수 있습니다.

[주의할 점]
- 정답을 추론하여 새로 판단하지 마세요. 화면에 이미 보이는 정답 예시만 시각적으로 기술해야 합니다.
- 바둑돌과 같은 “비교 단위”의 실제 답을 계산하거나 해석하지 마세요.
- 하단의 `(정답)`은 풀이 결과를 표시한 영역처럼 보이지만, 그 의미를 확정적으로 설명하지 말고 시각적 배치만 묘사하세요.
- 그림의 정확한 실제 크기나 세부 색감은 인쇄물 특성상 약간 다르게 보일 수 있으므로, 보이는 범위에서만 서술하세요.

[불확실한 부분]
- 가운데 그림들이 단순 예시인지, 선택 대상 목록인지 화면만으로는 완전히 단정하기 어렵습니다.
- 하단 `(정답)` 영역의 클립과 100원 동전이 “선택된 답”인지 “예시 답안”인지 시각적으로는 보이지만, 인터랙티브한 선택 상태까지는 확인할 수 없습니다.
- 문제 번호 앞의 작은 네모가 실제 체크박스인지 인쇄용 기호인지는 확정할 수 없습니다.

--- RAW VISION DRAFT END ---
