# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009052.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009052

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 문제 번호가 왼쪽 상단에 작게 표시되어 있고, 그 오른쪽에 문제 지시문이 한 줄로 배치되어 있습니다.
- 본문 중앙에는 선의 종류를 비교하는 그림들이 여러 개 가로 방향으로 흩어져 배치되어 있습니다.
- 왼쪽 아래에는 `(정답)` 표시가 따로 있고, 그 아래쪽에 또 다른 선 그림이 있습니다.
- 화면 맨 아래에는 `(해설)` 문구와 함께 설명 문장이 한 줄로 이어집니다.
- 전체적으로 “직선/곡선/선분”을 시각적으로 구분하게 만드는 선택형 구성입니다.

[보이는 텍스트]
- `90.`
- `직선을 찾아 선택하세요.`
- `(정답)`
- `(해설)`
- `첫째는 선분이고 둘째는 굽은 선이므로 직선이 아닙니다.`

[반복되는 시각 패턴]
- 선을 나타내는 도형이 여러 개 반복되어 제시됩니다.
- 각 도형은 검은색 가는 선으로 그려져 있고, 끝점이나 중간점에 작은 점 표시가 붙어 있습니다.
- 가로로 거의 수평인 선, 곡선으로 휘어진 선, 대각선으로 기울어진 선이 서로 비교되도록 배치되어 있습니다.
- 점이 붙은 끝을 통해 선분처럼 보이는 요소와, 휘어진 형태를 통해 곡선처럼 보이는 요소가 대비됩니다.

[수식 단계]
- 해당 없음. 수식 변형이나 등호가 이어지는 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 상단 문제문은 `직선을 찾아 선택하세요.`로, 여러 선 그림 중 직선을 고르는 유형으로 보입니다.
- 화면 상단 중앙 쪽에는:
  - 왼쪽에 거의 수평인 짧은 선분 1개가 보입니다.
  - 가운데에는 부드럽게 휘어진 곡선 1개가 보입니다.
  - 오른쪽에는 우상향하는 대각선 모양의 선이 보입니다.
- 왼쪽 아래 `(정답)` 옆에는 대각선 선 그림 1개가 따로 제시되어 있습니다.
- 대각선 선 그림은 양 끝에 작은 점이 찍혀 있어 선분처럼 보입니다.
- 아래 해설 문장은 “첫째는 선분이고 둘째는 굽은 선이므로 직선이 아닙니다.”라고 되어 있어, 위쪽에 놓인 선들 중 일부는 직선이 아니라고 설명하는 형식입니다.
- 해설 문장 속 `첫째`, `둘째`는 위의 그림들을 순서대로 지칭하는 것으로 보입니다. 다만 정확히 어떤 그림을 가리키는지는 화면 배치상 추정이 포함될 수 있습니다.

[Python DSL 작성 힌트]
- 문제 제목/지시문은 상단 좌측에 텍스트 박스로 배치하면 자연스럽습니다.
- 중앙에는 선 도형 3개를 가로 방향으로 띄워 배치하는 구성이 적절합니다.
- 선 도형은 다음처럼 구분해서 표현하면 좋습니다:
  - 거의 수평인 선분 1개
  - 곡선 1개
  - 우상향 대각선 선분 1개
- `(정답)` 표시는 왼쪽 아래에 별도의 텍스트로 두고, 그 옆 또는 아래에 추가 선 도형을 배치합니다.
- `(해설)`은 하단 왼쪽에 두고, 그 오른쪽으로 설명 문장을 한 줄 텍스트로 이어 붙이는 구성이 보입니다.
- 선의 양 끝 점 표시가 필요한 경우, 선 자체와 끝점 점을 별도 요소로 나눠 그리는 방식이 안전합니다.
- 선이 직선인지 곡선인지, 선분인지 등을 판별하는 기능적 의미는 DSL에서 주석 또는 라벨로만 다루고, 답 자체는 넣지 않는 것이 좋습니다.

[주의할 점]
- 정답을 직접 확정해서 쓰지 마세요.
- `직선`과 `선분`은 서로 다른 개념이므로, 그림 설명에서 두 개념을 혼동하지 않도록 주의해야 합니다.
- 해설 문장은 문제 풀이가 아니라 보이는 텍스트의 일부로만 옮겨 적어야 합니다.
- 가운데의 각 선 그림이 정확히 어떤 순서로 번호화되어 있는지는 화면상 번호가 보이지 않으므로 임의로 단정하지 마세요.
- 점의 위치가 끝점인지, 장식용 표시인지 약간 애매할 수 있으므로 “작은 점 표시가 있다” 정도로 서술하는 것이 안전합니다.

[불확실한 부분]
- 상단 중앙의 각 선 그림이 정확히 문제에서 몇 번째 선택지인지 화면에 번호가 드러나지 않아 확정하기 어렵습니다.
- 왼쪽 아래 `(정답)` 옆의 대각선 선 그림이 실제 정답 표시인지, 해설용 예시인지 화면만으로는 완전히 단정하기 어렵습니다.
- 해설 문장의 `첫째`, `둘째`가 정확히 어떤 그림을 지칭하는지 배치상 추정은 가능하지만, 화면에 연결선이나 번호가 없어서 명확히 확인되지는 않습니다.

--- RAW VISION DRAFT END ---
