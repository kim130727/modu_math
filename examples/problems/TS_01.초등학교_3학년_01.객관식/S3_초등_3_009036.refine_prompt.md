# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009036.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009036

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 문제 번호와 지시문이 맨 위 왼쪽에 있고, 그 아래에 연한 파란색 격자 배경 위로 도형 5개가 가로로 나열되어 있습니다.
- 각 도형 안에는 각각 한 글자씩 `가, 나, 다, 라, 마`가 들어 있습니다.
- 도형 아래쪽 중앙에는 선택지처럼 보이는 괄호 묶음 `( 가, 나, 다, 라, 마 )`가 있습니다.
- 그 아래 왼쪽에는 정답 표시 `(정답)다, 마`가 보입니다.
- 이어서 `(해설)` 문장과 함께, 아래쪽에 다시 5개의 도형이 격자 위에 가로로 배열되어 있으며, 이 해설용 도형들에는 직각 표시가 추가되어 있습니다.

[보이는 텍스트]
- `74. 직사각형을 모두 찾아 선택하세요.`
- 도형 아래의 묶음: `( 가 , 나 , 다 , 라 , 마 )`
- `(정답)다, 마`
- `(해설)네 각이 모두 직각인 사각형은 다, 마입니다.`
- 도형 내부 글자: `가`, `나`, `다`, `라`, `마`

[반복되는 시각 패턴]
- 위쪽과 아래쪽 모두, 연한 파란색 사각 격자 위에 도형이 배치되어 있습니다.
- 같은 글자 표기 `가, 나, 다, 라, 마`가 도형 내부에 각각 한 번씩 등장합니다.
- 아래 해설 그림에서는 직각을 나타내는 작은 네모 표시가 사각형의 꼭짓점에 반복적으로 붙어 있습니다.
- 도형들은 모두 사각형 계열이지만, 모양이 서로 다르게 기울어져 있거나 한 변이 수직/수평인 형태로 보입니다.

[수식 단계]
- 해당 사항 없음.
- 수식, 등호, 계산식의 변형 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 사항 없음.
- 등호를 포함한 식 변형이 제시되지 않았습니다.

[세부 내용]
- 문제 상단의 지시문은 “직사각형을 모두 찾아 선택하세요.”로, 여러 도형 중 조건에 맞는 것을 고르는 형태입니다.
- 첫 번째 도형 `가`:
  - 왼쪽 변이 거의 수직이고, 아래 변이 거의 수평이며, 오른쪽 아래로 기울어진 대각선 변이 보입니다.
  - 내부 왼쪽 아래 쪽에 `가`가 적혀 있습니다.
- 두 번째 도형 `나`:
  - 위아래 변이 서로 평행하게 기울어진 사각형처럼 보이고, 좌우 변도 완전히 수직이 아닌 기울어진 형태입니다.
  - 내부 중앙에 `나`가 적혀 있습니다.
- 세 번째 도형 `다`:
  - 위, 아래가 수평이고 좌우가 수직인 직사각형처럼 보입니다.
  - 내부 중앙에 `다`가 적혀 있습니다.
- 네 번째 도형 `라`:
  - 왼쪽 아래에서 오른쪽 위로 올라가는 대각선 변과 수평/수직 변이 함께 보이는 형태입니다.
  - 내부 오른쪽 아래 쪽에 `라`가 적혀 있습니다.
- 다섯 번째 도형 `마`:
  - 네 변이 수직/수평으로 이루어진 큰 직사각형처럼 보입니다.
  - 내부 중앙에 `마`가 적혀 있습니다.
- 아래 해설 그림에서는 `다`와 `마` 도형의 네 꼭짓점 근처에 직각 표시가 들어 있어, 이 둘이 직사각형임을 강조합니다.
- 해설 문장도 “네 각이 모두 직각인 사각형”이라고 적혀 있어, 조건이 직각 4개임을 설명하는 구조입니다.

[Python DSL 작성 힌트]
- 문제 상단 텍스트 `74. 직사각형을 모두 찾아 선택하세요.`를 먼저 배치합니다.
- 그 아래에 격자 배경을 깔고, 5개의 도형을 좌→우 순서로 배치합니다.
- 각 도형 내부에 `가, 나, 다, 라, 마` 라벨을 하나씩 넣습니다.
- 도형 아래에는 선택지 묶음 `( 가 , 나 , 다 , 라 , 마 )`를 중앙 정렬로 배치합니다.
- 정답 표시는 별도 텍스트로 `(정답)다, 마`를 한 줄에 배치합니다.
- 해설 영역은 아래쪽에 다시 격자 배경을 깔고, 동일한 5개 도형을 다시 배치하되 직각 표시 사각형을 꼭짓점에 추가합니다.
- 직각 표시가 있는 꼭짓점은 작은 빈 사각형/ㄱ자 표시처럼 보이도록 도형 객체에 별도 마크를 추가하는 방식이 적절합니다.
- 도형의 외곽선은 선분 조합으로 표현하고, 내부 글자는 중앙 또는 약간 치우친 위치로 배치합니다.

[주의할 점]
- 정답을 다시 계산하거나 추론해서 새로 적지 마세요. 화면에 보이는 정답 표기를 그대로 옮기는 수준으로만 다루는 것이 안전합니다.
- `가, 나, 라`는 기울어진 사각형처럼 보여도 직사각형은 아닌 것으로 해설 그림에서 구분됩니다.
- `다, 마`는 아래 해설에서 직각 표시가 모두 붙어 있어 직사각형으로 강조되지만, 이 초안에서는 그 사실을 “보인다/표시되어 있다” 수준으로만 서술해야 합니다.
- 글자 위치는 도형의 정확한 중심이 아니라 약간 왼쪽 또는 오른쪽으로 치우쳐 보일 수 있으니, 배치 시 너무 엄격하게 고정하지 않는 것이 좋습니다.
- 배경 격자는 연한 파란색이며, 도형 경계선은 검은색에 가깝습니다.

[불확실한 부분]
- 위쪽 도형의 정확한 꼭짓점 좌표와 각 변의 길이는 이미지 해상도 때문에 단정하기 어렵습니다.
- `나` 도형과 `라` 도형의 기울기 정도는 다소 애매하게 보일 수 있습니다.
- `가`와 `라`의 내부 글자 위치는 정확한 중앙이 아니라 도형 내부의 한쪽에 치우쳐 배치된 것으로 보입니다.
- 아래 해설 그림에서 직각 표시가 일부 꼭짓점에만 분명하게 보이며, 다른 꼭짓점의 세부 표시는 해상도상 흐릿합니다.

--- RAW VISION DRAFT END ---
