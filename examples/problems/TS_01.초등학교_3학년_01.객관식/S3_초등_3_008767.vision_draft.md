[전체적인 구성]
- 상단에 문제 번호가 포함된 문장형 질문이 있고, 그 아래에 그림 2개가 좌우로 배치되어 있습니다.
- 왼쪽에는 채리 상자 그림, 오른쪽에는 소고기 팩 그림이 있으며, 각 그림 옆에 말풍선 형태로 단위가 표시되어 있습니다.
- 중앙 하단에는 “채리 상자 → ( kg, g ), 소고기 팩 → ( kg, g )” 형태의 답안 작성용 빈칸 안내가 있습니다.
- 더 아래에는 `(정답)`과 `(해설)`이 작은 글씨로 인쇄되어 있어, 정답과 설명이 함께 제시된 형태입니다.

[보이는 텍스트]
- `□ 69. 체리 상자와 소고기 팩에는 각각 어떤 단위가 표시되어 있는지 선택해보세요.`
- 왼쪽 말풍선: `1 kg`
- 상자 그림 안쪽 라벨: `체리`, `1 kg`
- 오른쪽 말풍선: `600 g`
- 소고기 팩 라벨: `600 g`
- 말풍선 안 문장: `무게의 단위에는 킬로그램과 그램 등이 있어.`
- 하단 선택형 문장:
  - `체리 상자 → ( kg, g ), 소고기 팩 → ( kg, g )`
- `(정답)kg, g`
- `(해설)체리 상자에 1 kg이 쓰여 있으므로 표시된 단위는 kg, 소고기 팩에 600 g이 쓰여 있으므로 표시된 단위는 g입니다.`

[반복되는 시각 패턴]
- 단위 정보가 각각 말풍선으로 한 번씩 제시되어 있습니다.
- 두 물건 모두 “무게 단위”를 묻는 구조이므로, 그림 + 단위 표기 + 선택지의 대응 패턴이 반복됩니다.
- 왼쪽/오른쪽 각각에 대상 물건이 있고, 대상 옆에 수치와 단위가 붙어 있는 형태입니다.
- 아래쪽 답안 형식도 두 대상에 대해 동일한 괄호 구조 `( kg, g )`가 반복됩니다.

[수식 단계]
- 해당 없음: 등호를 포함한 수식 변형 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음: 등호 연결식이나 계산 과정이 제시되지 않았습니다.

[세부 내용]
- 왼쪽 그림:
  - 노란색 상자 형태의 체리 상자가 있습니다.
  - 상자 앞면에 `체리`와 작은 `1 kg` 표기가 함께 보입니다.
  - 상자 왼쪽에는 원형 말풍선이 있고 그 안에 `1 kg`이 적혀 있으며, 말풍선 꼬리가 상자를 가리킵니다.
- 오른쪽 그림:
  - 트레이에 담긴 소고기 팩 그림이 있습니다.
  - 팩 오른쪽 아래쪽에 작은 `600 g` 표기가 보입니다.
  - 팩 오른쪽에는 원형 말풍선이 있고 그 안에 `600 g`이 적혀 있으며, 말풍선 꼬리가 팩을 가리킵니다.
- 가운데 아래 답안 문장:
  - `체리 상자 → ( kg, g ), 소고기 팩 → ( kg, g )`
  - 괄호 안은 선택지처럼 보이며, 실제로 어떤 것을 고르는지 판단하는 자리로 보입니다.
- 하단 해설 문장:
  - 각각의 물건에 적힌 숫자와 단위를 보고 단위를 판별하는 설명이 있습니다.
  - 다만 이 부분은 해설 텍스트이므로, 문제 본문과는 분리해서 다루는 것이 좋습니다.

[Python DSL 작성 힌트]
- 전체를 하나의 상단 제목 + 그림 2개 + 하단 답안/해설 영역으로 나누어 배치하는 구성이 적절합니다.
- 왼쪽 체리 상자와 오른쪽 소고기 팩을 각각 별도 그룹으로 묶어 위치를 지정하세요.
- 각 그룹에는:
  - 대상 그림
  - 그림을 가리키는 원형 말풍선
  - 말풍선 안의 단위 텍스트
  - 그림 내부 또는 근처의 작은 단위 표기
  를 함께 배치하면 좋습니다.
- 하단의 `체리 상자 → ( kg, g ), 소고기 팩 → ( kg, g )`는 텍스트와 괄호를 포함한 한 줄 문장으로 처리하면 됩니다.
- `(정답)`과 `(해설)`은 별도 작은 텍스트 블록으로 분리하는 편이 좋습니다.

[주의할 점]
- 답을 직접 채우는 형태로 재작성하지 마세요.
- `kg`와 `g`의 선택 결과를 추론해서 빈칸에 넣지 마세요.
- 말풍선의 내용과 물건에 붙은 표기를 혼동하지 않도록 주의하세요.
- 상자와 소고기 팩의 그림은 서로 다른 대상이므로, 각각의 단위 표기를 독립적으로 인식해야 합니다.
- 하단의 `(정답)`과 `(해설)`은 문제 풀이가 아니라 원문에 포함된 추가 텍스트이므로, 이미지 요소와 구분해서 기록하세요.

[불확실한 부분]
- 상단 제목의 줄바꿈 위치는 이미지 해상도상 정확히 한 줄인지 두 줄인지 약간 애매할 수 있습니다.
- 말풍선 꼬리의 정확한 연결점은 그림 스타일상 세부 위치가 다소 흐릿할 수 있습니다.
- 하단 `(정답)` 표기의 띄어쓰기와 이어지는 텍스트 형식은 이미지에서 약간 압축되어 보여 정확한 줄바꿈은 단정하기 어렵습니다.
