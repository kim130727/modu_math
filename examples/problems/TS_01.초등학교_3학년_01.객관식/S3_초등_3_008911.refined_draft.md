[문제 개요]
- 문제 ID: `S3_초등_3_008911`
- 유형: 표를 읽고 가장 많은 값을 가진 항목을 고르는 문제
- 화면 구성 흐름: 문제 문장 → 표 제목 → 2행 다열 표 → 보기 괄호 묶음 → 정답/해설
- **보이는 사실**
  - 상단에 문제 번호 `77.`과 지시문이 한 줄 이상으로 표시됨
  - 가운데에 표가 크게 배치됨
  - 표 아래 오른쪽에 보기 형태의 괄호 묶음이 있음
  - 하단에 `(정답)`과 `(해설)` 텍스트가 인쇄됨
- **추론되는 수학적 의미**
  - 표의 수를 비교하여 가장 큰 값을 찾는 읽기 문제로 보임
- **렌더링/레이아웃 힌트**
  - 상단 지시문은 왼쪽 정렬, 2줄 줄바꿈 가능
  - 표 제목은 표의 위 중앙에 별도 텍스트
  - 표는 연한 색 배경과 얇은 테두리의 격자 구조
  - 보기 텍스트는 표 아래 오른쪽에 작게 배치
  - 정답/해설은 문제 본문과 분리된 설명 영역으로 배치
- **TODO**
  - 상단 첫 글자 앞의 `□` 기호의 정확한 의미는 확정하지 않음

[원본에 보이는 요소]
- 상단 문제 문장:
  - `□ 77. 정국이는 반 학생들이 생일에 받고 싶은 선물을 사주하여 표로 나타냈습니다. 가장 많은 학생들이 받고 싶은 선물을 선택하세요.`
  - 줄바꿈이 포함되어 있으며, 첫 글자 앞에 작은 네모 `□`가 보임
- 표 위 제목:
  - `생일에 받고 싶은 선물별 학생 수`
- 표 내부 텍스트:
  - 첫 행: `선물 | 책 | 휴대전화 | 게임기 | 옷 | 합계`
  - 둘째 행: `학생 수(명) | 6 | 12 | 8 | 4 | 30`
- 표 아래 보기:
  - `( 책 , 휴대전화 , 게임기 , 옷 )`
- 하단 인쇄 텍스트:
  - `(정답)휴대전화`
  - `(해설)표에서 가장 큰 수를 찾으면 12이므로 가장 많은 학생들이 받고 싶은 선물은 휴대전화입니다.`
- **보이는 사실**
  - 정답과 해설이 이미지에 실제로 인쇄되어 있음
- **추론되는 수학적 의미**
  - 표의 항목 중 학생 수가 가장 큰 선물은 `휴대전화`로 표시되어 있음
- **주의**
  - 정답은 이미지에 보이는 관찰 사실로만 기록하고, 새로 추론해 채우지 않음

[수식/관계 구조]
- 이 문항에는 등호를 사용한 수식 변형식이 없음
- 표 형식의 대응 관계만 존재함
- 관계 구조:
  - `선물 종류` ↔ `학생 수(명)`
  - `책 ↔ 6`
  - `휴대전화 ↔ 12`
  - `게임기 ↔ 8`
  - `옷 ↔ 4`
  - `합계 ↔ 30`
- **보이는 사실**
  - 숫자들은 표의 같은 행에 나열되어 있음
  - `합계`는 오른쪽 끝 열에 위치함
- **추론되는 수학적 의미**
  - 각 선물별 선호 인원 수를 비교하는 구조
- **렌더링/레이아웃 힌트**
  - 2행 6열 표로 처리
  - 열 간 간격과 세로 정렬이 균일
  - 헤더 행과 데이터 행의 배경색이 다소 연하게 구분됨
- **TODO**
  - 표의 정확한 셀 배경색/테두리색 값은 이미지 기준으로만 참고

[등호별 변환 의미]
- 해당 없음
- 등호 연쇄나 계산 변형이 제시되지 않음
- **보이는 사실**
  - 해설 문장에 `12`가 등장하지만, 이는 계산식 변형이 아니라 서술 설명임
- **추론되는 수학적 의미**
  - 최대값 비교 결과를 말로 설명한 것뿐임

[빈칸 정책]
- 이 문항에는 별도의 빈칸 입력칸이 보이지 않음
- 따라서 빈칸을 추론값으로 채우는 작업은 하지 않음
- **정책**
  - 보이지 않는 답이나 숨은 값은 절대 보완하지 않음
  - 이미지에 실제로 인쇄된 텍스트만 기록함
- **주의**
  - `(정답)휴대전화`는 빈칸 채우기가 아니라 인쇄된 결과로만 다룸

[반복 패턴]
- 2행 다열 표 구조
- 각 열에 항목명과 수치가 세로로 대응됨
- 보기 문자열이 괄호 안에 쉼표로 구분된 나열 형태
- 정답/해설이 하단에 별도 블록으로 배치됨
- **보이는 사실**
  - 표는 좌우로 같은 폭의 열이 반복되는 격자 패턴
- **추론되는 수학적 의미**
  - 데이터 테이블에서 최댓값을 찾는 반복적 읽기 패턴
- **렌더링/레이아웃 힌트**
  - 셀 경계는 얇고 균일하게 반복
  - 보기 문자열은 한 줄 문자열로 처리 가능
  - 정답/해설은 본문과 시각적으로 분리

[DSL 작성 힌트]
- 표 제목 `생일에 받고 싶은 선물별 학생 수`는 표 상단 중앙 텍스트로 분리
- 표는 2행 6열 구조로 잡는 것이 적절함
- 첫 행 셀 내용:
  - `선물`, `책`, `휴대전화`, `게임기`, `옷`, `합계`
- 둘째 행 셀 내용:
  - `학생 수(명)`, `6`, `12`, `8`, `4`, `30`
- 표 아래의 `( 책 , 휴대전화 , 게임기 , 옷 )`는 보기 텍스트로 별도 처리
- 하단의 `(정답)`, `(해설)`은 설명 영역으로 분리
- **보이는 사실**
  - 표와 설명 영역이 서로 떨어져 배치됨
- **렌더링/레이아웃 힌트**
  - 표 제목과 표 사이에 약간의 여백
  - 표 아래 보기와 정답/해설 사이에도 여백 확보
  - 표의 외곽선과 내부선은 연한 초록색 계열로 보임
- **TODO**
  - 정확한 글꼴 크기와 색상은 원본 이미지 기준으로만 조정

[semantic/solvable 힌트]
- 이 문제는 표에서 가장 큰 수를 찾는 유형으로 보임
- 비교 대상 수는 `6, 12, 8, 4, 30`이지만, 문제에서 요구하는 것은 선물 종류 선택임
- **보이는 사실**
  - 해설에 `표에서 가장 큰 수를 찾으면 12`라고 적혀 있음
- **추론되는 수학적 의미**
  - 가장 큰 학생 수에 대응하는 선물 항목을 고르는 구조
- **주의**
  - 답을 새로 계산해서 제시하지 말고, 이미지에 인쇄된 정답만 사실로 기록

[주의 및 TODO]
- 문제를 직접 풀이하는 문장은 쓰지 않음
- 추론으로 정답을 채우지 않음
- `□` 기호의 의미는 불확실하므로 단정하지 않음
- 상단 문장의 정확한 띄어쓰기/조사는 해상도상 일부 흐릴 수 있음
- 표 배경과 테두리 색은 연녹색 계열로 보이나 정확한 색상값은 TODO
- 보기 괄호의 공백 배치와 쉼표 간격은 원본과 최대한 동일하게 유지
- 정답/해설은 이미지에 보이는 인쇄 텍스트이므로, 별도 추측 문구로 바꾸지 않음
