# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009032.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009032

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단 왼쪽에 문제 번호와 제목 문장이 있고, 그 오른쪽/아래에 도형들이 가로 방향으로 나열된 그림이 있습니다.
- 그림 아래에는 (4)번 문항이 여러 줄로 이어져 있으며, 첫 줄은 “정사각형을 찾아 기호를 선택하세요.”라는 지시문, 그 아래 줄에는 선택지 형식의 괄호가 있습니다.
- 하단에는 정사각형의 정의를 설명하는 문장과, 마지막에 짧은 답안 형식처럼 보이는 “(4) 라”가 따로 적혀 있습니다.
- 전체적으로 “도형 그림 + 보기 + 설명 문장” 구조입니다.

[보이는 텍스트]
- `70. 정사각형을 알아보고 합시다. 물음에 답하시오.`
- `(4) 정사각형을 찾아 기호를 선택하세요.`
- `( 가 , 나 , 다 , 라 , 마 )`
- `(4) 정사각형은 네 각이 모두 직각이고 네 변의 길이가 모두 같은 사각형입니다.`
- `(4) 라`
- 그림 속 도형 안의 글자:
  - `가`
  - `나`
  - `다`
  - `라`
  - `마`

[반복되는 시각 패턴]
- 여러 도형이 같은 기준선 위에 가로로 배치되어 있고, 각 도형 내부에 한 글자씩 들어가 있습니다.
- 도형들은 모두 사각형 계열이지만 모양이 서로 다릅니다.
- 보기 `( 가 , 나 , 다 , 라 , 마 )`는 쉼표로 구분된 선택지 나열 형태입니다.
- 아래 설명문은 정의를 먼저 제시하고, 그 다음에 답처럼 보이는 단어를 한 번 더 제시하는 구조입니다.
- 같은 번호 `(4)`가 연속해서 여러 줄에 반복되어 보입니다.

[수식 단계]
- 해당 없음. 수식 변형이나 등호가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호가 보이지 않습니다.

[세부 내용]
- 그림은 연한 파란색 점선 격자 위에 놓여 있으며, 도형들의 위치와 간격을 비교할 수 있게 되어 있습니다.
- 왼쪽 첫 도형은 세로로 긴 직사각형처럼 보이며 내부에 `가`가 있습니다.
- 그 오른쪽에는 마름모 형태의 도형이 있고 내부에 `나`가 있습니다.
- 가운데에는 위아래 변이 서로 평행한 사다리꼴처럼 보이는 도형이 있고 내부에 `다`가 있습니다.
- 오른쪽 쪽에는 여러 도형이 겹치지 않게 이어져 보이며, 하나는 `라`, 다른 하나는 `마`가 들어 있습니다.
- `라`가 들어간 도형은 전체 윤곽이 정사각형인지 직사각형인지 판단이 필요해 보이는 형태로 보입니다.
- `마`가 들어간 도형은 오른쪽 아래 변이 기울어진 사각형처럼 보입니다.
- 보기는 괄호 안에 다섯 기호를 순서대로 제시하여, 그림 속 도형의 문자와 대응시키는 방식입니다.
- 아래의 정의 문장은 정사각형의 성질을 텍스트로 설명하는 참고 정보입니다.
- 마지막 줄 `(4) 라`는 사용자가 선택한 답 또는 정답 예시처럼 보이지만, 실제로는 이미지에 보이는 텍스트만 기록하는 것이므로 의미를 확정하지 않습니다.

[Python DSL 작성 힌트]
- 먼저 상단 제목 영역과 도형 영역, 하단 문장 영역을 세 구역으로 나누어 배치하면 좋습니다.
- 도형 영역에는 연한 파란색 격자 배경을 깔고, 그 위에 5개의 도형을 좌우로 배열하는 구성이 적절합니다.
- 각 도형은 별도의 다각형/사각형 요소로 그리고, 내부 중앙에 한 글자씩 텍스트를 넣습니다.
- 선택지 문장은 큰 글자 크기로 한 줄에 배치하고, 괄호와 쉼표 간격을 자연스럽게 유지합니다.
- 아래 정의 문장은 두 줄 텍스트로 분리해 배치하면 원본과 유사합니다.
- 마지막의 짧은 답안 형태 텍스트는 별도 줄에 왼쪽 정렬로 두는 것이 좋습니다.

[주의할 점]
- 정답을 추론해서 확정적으로 쓰지 마세요.
- `라`가 정사각형인지 여부는 문제 내용상 중요해 보이지만, 시각 분석 초안에서는 보이는 모양만 적어야 합니다.
- 도형의 정확한 변 길이 비율이나 각도는 그림만으로 단정하지 말고, 보이는 윤곽 수준에서 서술하세요.
- 일부 도형은 겹쳐 보이거나 선이 격자와 섞여 보여 경계가 애매할 수 있습니다.
- 아래의 `라` 표기는 정답 표기인지, 예시인지, 또는 인쇄상의 반복인지 단정하지 마세요.

[불확실한 부분]
- 오른쪽 도형들 가운데 `라`와 `마`가 각각 어떤 정확한 사각형 유형인지, 그림만으로는 단정하기 어렵습니다.
- 하단의 `(4) 라`는 답안 표시인지, 편집 과정에서 남은 텍스트인지 확실하지 않습니다.
- 도형 내부 글자의 정확한 위치는 이미지 해상도상 약간 흐릿할 수 있습니다.

--- RAW VISION DRAFT END ---
