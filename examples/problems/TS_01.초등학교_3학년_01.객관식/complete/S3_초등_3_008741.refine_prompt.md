# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008741.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008741

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 문제 번호 **27**이 왼쪽 상단에 있고, 그 오른쪽에 큰 글씨의 문제 지시문이 한 줄 이상으로 배치되어 있습니다.
- 화면의 중심에는 **비교 관계를 나타내는 나무 모양 도식**이 2개 보입니다.
  - 위쪽 도식: 맨 위에 원형 표시 안에 **⑦**이 있고, 그 아래가 좌우로 갈라지는 구조입니다.
  - 아래쪽 도식: 같은 구조를 다시 보여 주며, 오른쪽 아래 칸에 **4 1/9**가 파란색으로 채워져 있습니다.
- 도식 아래에는 **정답 표기**, **해설 문장**, 그리고 마지막 결론 문장이 이어집니다.
- 전체적으로 “분수/대분수의 크기를 비교하여 가장 알맞은 수를 고르는” 유형의 문제입니다.
- **보이는 사실**: 정답 칸은 비어 있지 않고, 이미지 하단에 별도로 정답이 표시되어 있습니다.
- **추론 의미**: 위의 빈칸(⑦)은 도식에서 비교 결과에 따라 채워야 하는 대상입니다.

# [보이는 텍스트]
- 문제 지시문:
  - `□ 27. 두 분수의 크기를 비교하여 더 큰 분수만 비켜나 써보려고 합니다. ⑦에 들어갈 분수로 알맞은 것을 가장 아래 칸의 분수들 중에 선택해 보세요.`
- 도식 안 텍스트:
  - 위쪽 도식의 맨 위 원 안: `⑦`
  - 왼쪽 중간 칸: `34/9`
  - 왼쪽 아래 칸: `31/9`
  - 왼쪽 아래 칸: `34/9`
  - 오른쪽 아래 칸: `3 5/9`
  - 오른쪽 아래 칸: `4 1/9`
- 정답 표기:
  - `(정답) 4 1/9`
- 해설에 보이는 식:
  - `(해설) 31/9 < 34/9, 3 5/9 < 4 1/9`
- 아래 설명 문장:
  - `34/9 와 4 1/9의 크기를 비교하면 34/9 = 3 7/9 이므로 34/9 < 4 1/9 입니다.`
  - `따라서 ⑦에 알맞은 수는 4 1/9입니다.`

# [반복되는 시각 패턴]
- 같은 형태의 **상자-분기-하위 상자** 구조가 두 번 반복됩니다.
- 상자 색과 계층이 비슷하게 구성되어 있습니다.
  - 맨 위 상자는 흰 바탕에 연한 노란 테두리 느낌.
  - 중간 상자는 흰 바탕.
  - 아래 상자들은 연한 노란 배경으로 표시되어 선택지처럼 보입니다.
- 각 중간 상자 아래에는 좌우 2개의 하위 상자가 같은 간격으로 배치되어 있습니다.
- 위 도식과 아래 도식은 구조가 동일하며, 아래 도식에서는 오른쪽 중간 칸에 파란색으로 `4 1/9`가 들어가 있어 선택 결과를 강조하는 형태입니다.
- 선택지처럼 보이는 아래쪽 네 칸은 각각 독립된 박스이며, 위의 중간 박스로 선이 연결됩니다.

# [수식 단계]
- 해설 문장 속 비교식이 보입니다.
  - `31/9 < 34/9`
  - `3 5/9 < 4 1/9`
- 추가로 분수와 대분수의 비교를 위해 같은 값을 다른 형태로 바꾼 식이 있습니다.
  - `34/9 = 3 7/9`
  - 그 결과 `34/9 < 4 1/9`
- 등호와 부등호가 섞여 있으나, **연산 과정 자체를 여러 단계로 전개한 모습은 아니고** 비교 결과를 문장으로 정리한 형태입니다.

# [등호별 변환 의미]
- `34/9 = 3 7/9`
  - **보이는 의미**: 분수 `34/9`를 대분수 형태 `3 7/9`로 바꿔 쓴 것입니다.
  - **추론 의미**: 비교를 쉽게 하려고 같은 값을 대분수로 표현한 것으로 보입니다.
- `34/9 < 4 1/9`
  - **보이는 의미**: 두 수의 크기 비교 결과를 나타냅니다.
  - **추론 의미**: 위 비교를 통해 `34/9`가 더 작다고 판단한 뒤, ⑦에는 다른 분수가 들어가야 함을 암시합니다.

# [세부 내용]
- 맨 위의 `⑦`은 최종적으로 고르는 분수를 적는 자리처럼 보입니다.
- 그 아래의 도식은 좌우로 분기되어 있는데, 왼쪽 가지에는 `34/9`가, 오른쪽 가지에는 빈칸이 있습니다.
- 왼쪽 `34/9` 아래에는 다시 두 개의 후보가 있고:
  - `31/9`
  - `34/9`
- 오른쪽 빈칸 아래에는 다시 두 개의 후보가 있고:
  - `3 5/9`
  - `4 1/9`
- 즉, 위에서 아래로 내려오며 **각 비교 그룹에서 더 큰 수를 남기는 방식**을 시각적으로 나타낸 구조로 보입니다.
- 아래쪽 반복 도식에서는 오른쪽 중간 칸이 `4 1/9`로 채워져 있어, 위 도식의 빈칸과 대응되는 예시처럼 보입니다.
- 하단 문장에서는 `34/9`와 `4 1/9`를 비교하여, `34/9`를 `3 7/9`로 바꾸어 비교하는 설명이 적혀 있습니다.
- 마지막 결론 문장은 “따라서 ⑦에 알맞은 수는 4 1/9”로 끝납니다.

# [Python DSL 작성 힌트]
- 상단 문제 문장을 먼저 텍스트로 배치한 뒤, 가운데에 **트리형 비교 도식**을 그리는 구조가 적합합니다.
- 도식은 다음 요소를 분리해서 표현하면 좋습니다.
  - 최상단 원형/작은 사각형 안의 `⑦`
  - 그 아래 수평선과 좌우 가지선
  - 왼쪽 중간 상자 `34/9`
  - 오른쪽 중간 상자 빈칸
  - 각 중간 상자 아래의 두 개 하위 상자
- 분수는 `분자/분모` 형태의 일반 텍스트가 아니라, 가능하면 **위아래 분리된 분수 슬롯**처럼 처리하면 원본과 더 유사합니다.
- 아래쪽 반복 도식은 위 도식과 동일한 좌표 체계로 복제하되, 오른쪽 중간 상자에만 파란색 강조 텍스트를 넣는 방식이 자연스럽습니다.
- 하단 해설 텍스트는 식과 문장을 줄바꿈하여 배치하고, `34/9 = 3 7/9` 부분은 수식 스타일로 따로 처리하는 것이 좋습니다.

# [주의할 점]
- **정답을 새로 추론해서 채우지 마세요.**
- 이 문제는 이미 이미지에 정답과 해설이 함께 보이므로, DSL 작성 시에도 **보이는 텍스트를 그대로 옮기는 것**에 집중해야 합니다.
- `⑦`은 도식의 최상단에 있는 기호이지, 계산식의 변수로 단정하면 안 됩니다.
- 아래쪽 도식의 파란색 `4 1/9`는 강조 표시이므로, 일반 텍스트와 색상을 구분하는 것이 좋습니다.
- `34/9`는 해설에서 `3 7/9`로 바뀌어 쓰이지만, 이것을 별도의 새로운 값으로 오해하지 않도록 주의해야 합니다.
- 도식의 선 연결 관계가 핵심이므로, 상자 간의 상대적 위치와 수평 정렬을 정확히 반영해야 합니다.

# [불확실한 부분]
- 최상단의 `⑦`이 **원형 안의 기호인지, 작은 상자 안의 기호인지**는 이미지 해상도상 경계가 다소 모호합니다. 다만 중심에 둥근 테두리처럼 보입니다.
- 상자 테두리 색과 배경색은 연한 노란색 계열로 보이지만, 정확한 색상 코드는 이미지에서 단정하기 어렵습니다.
- 위쪽 도식의 오른쪽 중간 칸은 비어 있으나, 박스 테두리가 희미하게 보여 정확한 크기와 선 굵기는 약간 불명확합니다.
- 하단 해설에서 사용된 `3 7/9`의 표기는 이미지에서 작은 글씨로 보이므로, 분수 슬롯의 형태를 완전히 판독하기보다는 텍스트로 처리하는 편이 안전합니다.

--- RAW VISION DRAFT END ---
