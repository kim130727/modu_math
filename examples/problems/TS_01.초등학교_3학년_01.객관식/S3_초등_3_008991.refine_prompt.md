# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008991.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008991

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 흰 배경의 한 문제 화면입니다.
- 좌상단에 문제 번호와 지시문이 있고, 우상단에는 도형 1개가 제시되어 있습니다.
- 왼쪽 중간에는 선택 기호처럼 보이는 작은 원형 표시가 있고, 그 아래에 `(정답)` 표시와 빈 원이 있습니다.
- 왼쪽 하단에는 `(해설)` 표시와 함께 직각 표시가 들어간 사각형 그림이 있습니다.
- 화면 맨 아래에는 설명 문장이 한 줄로 배치되어 있습니다.
- 전체적으로 “도형을 보고 ○ 또는 ×를 고르는” 판별형 문제 구성입니다.

[보이는 텍스트]
- `29.`
- `직사각형이면 ○, 직사각형이 아니면 ×를 선택하세요.`
- `(정답)`
- `(해설)`
- `네 각이 모두 직각인 사각형을 직사각형이라고 합니다.`
- 도형 안/근처에 원형 기호로 보이는 표시가 있으며, 우상단 도형 아래에는 파란색 `×` 표식이 보입니다.
- 왼쪽 중간에도 파란색 원형 표시가 하나 보입니다.

[반복되는 시각 패턴]
- 원형 선택 기호가 반복적으로 사용됩니다.
  - 지시문에는 `○`가 등장합니다.
  - 정답 표시 영역에는 빈 원이 있습니다.
  - 우상단 도형 아래에는 `×` 표식이 있습니다.
  - 왼쪽 중간에는 작은 파란색 원형 표시가 있습니다.
- 사각형 도형이 2개 제시됩니다.
  - 우상단: 단순한 사각형 윤곽만 있는 도형
  - 좌하단: 네 꼭짓점에 직각 표시가 있는 사각형
- 두 도형 모두 세로로 긴 직사각형에 가까운 비율로 보이며, 선은 얇은 회색/검정 계열입니다.

[수식 단계]
- 해당 없음. 이 문제는 등식 변형이나 계산식이 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호가 포함된 수식 전개가 없습니다.

[세부 내용]
- 우상단의 큰 도형:
  - 테두리만 있는 네 변의 도형입니다.
  - 정확한 판정용 예시인지, 문제의 대상 도형인지 화면 배치상 구분이 필요합니다.
  - 도형 아래에 파란색 `×`가 있어 “직사각형이 아님” 쪽 선택과 연결되는 듯 보입니다.
- 좌하단의 해설용 도형:
  - 네 꼭짓점마다 작은 직각 표시가 들어 있습니다.
  - 이 표시가 직사각형의 조건을 설명하는 시각 자료로 보입니다.
  - 도형 아래/옆의 텍스트는 “네 각이 모두 직각인 사각형을 직사각형이라고 합니다.”입니다.
- `(정답)` 옆의 빈 원은 학생이 선택해야 할 답 표시칸처럼 보입니다.
- 화면 내 파란색 원형 표시는 인터랙티브 표시 또는 편집 과정의 마킹일 가능성이 있습니다.
- 텍스트와 도형은 좌우로 분리되어 배치되어 있으며, 설명 문장은 하단 전체 폭에 걸쳐 있습니다.

[Python DSL 작성 힌트]
- 문제 번호 `29`와 지시문을 상단 텍스트로 배치하세요.
- 우상단에 사각형 1개를 단독 도형으로 그리세요.
- 그 도형 아래에 `×` 표식을 작게 넣으세요.
- 왼쪽 중간에는 작은 원형 기호 영역을 따로 두고, `(정답)` 옆에는 빈 원을 배치하세요.
- 좌하단에는 해설용 사각형을 그리고, 네 꼭짓점에 직각 표시를 각각 넣으세요.
- 하단에는 설명 문장을 한 줄로 넣으세요.
- 텍스트와 도형 간의 좌우/상하 간격이 중요하므로, 영역을 분리해서 배치하는 것이 좋습니다.

[주의할 점]
- 도형의 정확한 판정을 답으로 단정해 쓰지 마세요.
- 빈 원 `(정답)` 칸에 들어갈 기호를 추론해서 채우지 마세요.
- 우상단 도형이 단순 직사각형인지, 아니면 의도적으로 직사각형이 아닌 예시인지 시각 정보만으로 단정하지 말고, 보이는 표식과 함께 기록하세요.
- 파란색 작은 원형 표시는 인쇄 요소인지 작업용 표시인지 불확실하므로 과도하게 해석하지 마세요.
- 해설 문장은 정의 설명이므로, 답을 직접 제시하는 문장으로 바꾸지 마세요.

[불확실한 부분]
- 우상단 도형의 정확한 의미: 문제의 대상인지, 오답 예시인지가 배치상 완전히 명확하지 않습니다.
- 왼쪽 중간의 작은 파란 원형 표시가 정답 선택 UI인지 단순 편집 표시인지 불확실합니다.
- `(정답)` 옆 빈 원이 실제 선택 칸인지, 인쇄된 답안 표시용인지가 확실하지 않습니다.

--- RAW VISION DRAFT END ---
