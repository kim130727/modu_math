# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008843.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008843

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
## [전체적인 구성]
- 한 장짜리 초등 수학/과학 융합형 그림 문제처럼 보이는 지면이다.
- 상단에는 문제 번호와 지시문이 있고, 그 아래에 좌우로 두 개의 그림이 배치되어 있다.
- 왼쪽 그림은 **가**, 오른쪽 그림은 **나**로 구분되어 있으며, 각각 저울 위에 두 물건이 놓인 상태를 보여 준다.
- 각 그림 위에는 물건 이름이 작은 베이지색 라벨 박스로 표시되어 있다.
- 중간 아래에는 보기/문항 문장이 있고, 그 아래에 **해설** 문장이 이어진다.
- 맨 아래에는 답만 따로 적는 형태의 짧은 문장 영역이 보인다.

## [보이는 텍스트]
- 문제 번호와 문장:
  - `84. 저울로 각도기, 컴퍼스, 지우개의 무게를 비교하고 있습니다. 가장 가벼운 것은 무엇인지 알맞은 말을 선택해 보세요.`
- 왼쪽 그림 위의 표기:
  - `가`
  - `각도기`
  - `컴퍼스`
- 오른쪽 그림 위의 표기:
  - `나`
  - `지우개`
  - `컴퍼스`
- 문항 본문:
  - `(1) 가에서 각도기와 컴퍼스 중 더 가벼운 것은 ( 각도기, 컴퍼스 )이고,`
  - `나에서 지우개와 컴퍼스 중 더 가벼운 것은 ( 지우개, 컴퍼스 )입니다.`
- 해설 문장:
  - `(해설) (1) 가에서 각도기와 컴퍼스 중 컴퍼스를 올려놓은 접시가 내려갔으므로 더 가벼운 것은 각도기이고, 나에서 지우개와 컴퍼스 중 지우개를 올려놓은 접시가 내려갔으므로 더 가벼운 것은 컴퍼스입니다.`
- 하단 답안처럼 보이는 부분:
  - `(1) 각도기, 컴퍼스`

## [반복되는 시각 패턴]
- 두 개의 저울 그림이 좌우 대칭에 가깝게 반복된다.
- 각 저울은 가운데 지지대와 양쪽 접시로 이루어진 동일한 형태이다.
- 각 그림에서 물건 이름이 접시 위쪽에 라벨 박스로 붙어 있다.
- 두 그림 모두 한쪽 접시가 내려간 상태를 통해 비교를 나타내는 구조이다.
- 베이지색 배경의 이름 라벨이 반복적으로 사용되어 대상 물건을 식별하게 한다.

## [수식 단계]
- 해당 없음.
- 등호를 포함한 수식 변형 과정은 보이지 않는다.

## [등호별 변환 의미]
- 해당 없음.
- 등호 연쇄식이나 계산 단계가 제시되지 않았다.

## [세부 내용]
- **왼쪽 그림(가)**:
  - 왼쪽 접시에 `각도기`가 놓여 있다.
  - 오른쪽 접시에 `컴퍼스`가 놓여 있다.
  - 저울의 기울기상 오른쪽 접시가 더 내려가 보인다.
  - 시각적으로는 오른쪽 물건 쪽이 더 무거운 듯 표현되어 있다.
- **오른쪽 그림(나)**:
  - 왼쪽 접시에 `지우개`가 놓여 있다.
  - 오른쪽 접시에 `컴퍼스`가 놓여 있다.
  - 저울의 기울기상 왼쪽 접시가 더 내려가 보인다.
  - 시각적으로는 왼쪽 물건 쪽이 더 무거운 듯 표현되어 있다.
- 문항은 두 비교 결과를 바탕으로, 각 쌍에서 **더 가벼운 것**을 고르는 형식이다.
- 하단에는 선택 결과를 요약한 문장이 있으며, 괄호 안에 선택지 형태의 단어들이 쉼표로 구분되어 있다.
- **보이는 사실**과 **문맥적 의미**를 구분하면:
  - 보이는 사실: 저울의 접시 높이가 다르고, 각 물건 이름이 붙어 있다.
  - 문맥적 의미: 저울의 기울기로 무게의 상대적 비교를 하도록 유도하고 있다.

## [Python DSL 작성 힌트]
- 상단 문제 문장은 한 줄 또는 두 줄 텍스트 블록으로 배치한다.
- 좌우에 동일한 크기의 저울 일러스트 2개를 배치하고, 각 저울 위에 라벨 박스를 얹는다.
- 라벨 박스는 연한 베이지색 배경의 둥근 직사각형처럼 표현하면 유사하다.
- 각도기/컴퍼스/지우개 텍스트는 그림 위에 작은 글씨로 위치시킨다.
- 왼쪽 그림은 `가`, 오른쪽 그림은 `나`를 그림 상단 좌측 근처에 따로 배치한다.
- 문항 본문과 해설은 그림 아래에 좌측 정렬로 길게 배치한다.
- 마지막 답안 줄은 본문보다 아래쪽에 짧게 배치하며, 괄호와 쉼표를 포함한 텍스트로 표시한다.

## [주의할 점]
- 문제를 실제로 풀어서 답을 확정적으로 서술하면 안 된다.
- 빈칸이 없더라도, 괄호 안 선택지를 답으로 단정해 적지 않도록 주의한다.
- 저울의 정확한 기울기 방향은 그림 해상도상 약간 애매할 수 있으므로, 너무 단정적으로 묘사하지 않는 것이 좋다.
- 해설 문장에 이미 답이 들어 있어도, 시각 분석 초안에서는 그 내용을 사실로 그대로 옮기되 문제 풀이 과정처럼 재구성하지는 말아야 한다.
- 그림의 세부 장식(저울의 색, 반사광, 바탕 그림자 등)은 핵심 정보가 아니므로 DSL 작성 시 생략 가능하다.

## [불확실한 부분]
- 저울의 접시가 실제로 어느 정도로 내려갔는지, 혹은 완전히 수평에서 얼마나 기울었는지는 이미지 해상도상 정확히 판별하기 어렵다.
- 라벨 박스의 정확한 글꼴 크기와 세부 색조는 이미지마다 차이가 있을 수 있다.
- 하단의 `(1) 각도기, 컴퍼스` 문장은 답안 예시인지 최종 정답 표기인지 화면 구성만으로는 완전히 단정하기 어렵다.

--- RAW VISION DRAFT END ---
