[문제 개요]
- 문제 번호는 `□ 52.`로 보이며, 앞의 작은 네모는 체크박스처럼 보이지만 용도는 확정할 수 없습니다.
- 지시문은 `선분을 선택하세요.`입니다.
- 화면에는 선 모양의 보기 그림들이 가로로 배치되어 있습니다.
- 하단에는 `(정답)` 표시와 함께 긴 빈칸 선이 있고, 그 아래에 `(해설) 선분은 두 점을 곧게 이은 선입니다.`가 보입니다.

[원본에 보이는 요소]
- 상단 왼쪽: `□ 52.`
- 상단 문장: `선분을 선택하세요.`
- 보기 그림 3개가 가로로 배치됨
  - 왼쪽: 양 끝 점이 있는 곡선 모양
  - 가운데: 양 끝 점이 있는 곧은 선
  - 오른쪽: 양 끝 점이 있는 꺾은선 모양
- 하단 왼쪽: `(정답)`과 그 옆의 긴 수평 빈칸
- 하단: `(해설) 선분은 두 점을 곧게 이은 선입니다.`

[수식/관계 구조]
- 수식이나 등호 연쇄는 보이지 않습니다.
- 이 문제의 핵심 관계는 “선분”의 정의와 보기 그림의 형태 비교입니다.
- 보이는 사실:
  - 각 보기에는 양 끝에 점이 표시되어 있습니다.
  - 보기들의 선 형태가 서로 다릅니다.
- 추론되는 수학적 의미:
  - 선분은 두 점을 곧게 이은 선이라는 정의를 만족하는 그림을 고르는 형식입니다.
  - 곡선이나 꺾은선은 선분이 아닐 가능성이 높지만, 정답 자체를 여기서 확정하면 안 됩니다.

[등호별 변환 의미]
- 해당 없음.
- 등호가 포함된 계산식 또는 변형 과정이 보이지 않습니다.

[빈칸 정책]
- `(정답)` 옆의 빈칸은 실제로 채워진 내용이 보이지 않으므로 비워 두어야 합니다.
- 정답을 추론해서 넣지 않습니다.
- 빈칸은 “정답 입력/표시용 자리”로만 취급합니다.
- 체크박스처럼 보이는 `□`도 의미를 단정하지 않고, 보이는 기호로만 기록합니다.

[반복 패턴]
- 선 그림이 여러 개 반복적으로 제시되는 선택형 구조입니다.
- 각 보기마다 점-선-점 구조가 반복됩니다.
- 선의 종류만 달라지고, 양 끝 점은 공통적으로 표시됩니다.
- 하단의 `(정답)` 빈칸도 길게 뻗은 수평선 형태로 반복적 서식 요소처럼 보입니다.

[DSL 작성 힌트]
- 전체 레이아웃은 상단의 문제 문장 + 중간의 보기 그림들 + 하단의 정답/해설 영역으로 나누어 표현하는 것이 적합합니다.
- 문제 번호와 지시문은 한 줄 또는 좌상단 영역에 배치합니다.
- 보기는 가로 방향으로 3개를 나란히 배치하고, 각 보기를 독립된 그래픽 요소로 처리합니다.
- 각 보기에는 양 끝 점을 정확히 포함해 그리도록 합니다.
- 왼쪽 보기는 곡선, 가운데는 직선, 오른쪽은 꺾은선 형태가 드러나게 구분합니다.
- `(정답)` 텍스트와 빈칸 선은 분리된 요소로 두는 것이 좋습니다.
- `(해설)` 문장은 하단 설명 텍스트로 별도 배치합니다.
- 정렬은 전반적으로 좌측 정렬에 가깝고, 보기 영역은 상단 중앙 쪽에 넓게 퍼져 있습니다.

[semantic/solvable 힌트]
- 문제는 “선분”의 정의를 알고 있는지 확인하는 선택형 문제입니다.
- 보기 중에서 두 점을 곧게 이은 형태를 가진 그림을 찾는 구조입니다.
- 보이는 해설 문장은 선분의 정의를 직접 제시합니다.
- 정답은 원본 이미지에서 빈칸에 인쇄되어 보이지 않습니다.

[주의 및 TODO]
- 정답을 추론하지 마세요.
- 왼쪽의 작은 네모 `□`는 기능이 불명확하므로 체크박스/표식 가능성만 남기고 단정하지 마세요.
- 보기 그림의 개수는 이미지상 3개로 보이나, 레이아웃 분석 시 각 그림을 개별 객체로 분리하는 것이 안전합니다.
- 하단 `(정답)`의 빈칸은 입력란인지 서식선인지 확정하지 못하므로 TODO로 남겨 둘 수 있습니다.
- 원본 PNG 기준으로 추가 식별이 필요한 부분:
  - 보기 그림의 정확한 좌표 배치
  - 각 점의 위치와 크기
  - 빈칸 선의 길이와 시작/끝 위치
