# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008934.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008934

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 지시문이 있고, 그 아래에 **두 개의 표**와 **책 그림으로 된 도식**이 세로로 배치되어 있습니다.
- 위쪽 표는 **숫자 표** 형태로, 종류별 책의 수를 직접 숫자로 나타냅니다.
- 아래쪽 표는 **책 그림(아이콘)** 으로 종류별 수를 나타내며, 각 그림이 몇 권을 의미하는지 아래 설명과 함께 제시됩니다.
- 하단에는 **선택지 형태의 답**과 **해설 문장**이 보입니다.
- 전체적으로 “표를 보고 그림으로 나타낸 것이 맞는지”를 판단하는 구성입니다.

# [보이는 텍스트]
- `2. 표를 보고 그림그래프로 나타낸 것입니다. 잘못된 부분을 찾아 선택해 보세 요.`  
  - 줄바꿈이 섞여 보이며, 마지막 `요.`가 다음 줄로 내려가 있습니다.
- 위쪽 표 제목: `종류별 책의 수`
- 위쪽 표 머리글:
  - `종류`
  - `동화책`
  - `위인전`
  - `과학책`
  - `합계`
- 위쪽 표 내용:
  - `책의 수(권)`
  - `40`
  - `25`
  - `14`
  - `79`
- 아래쪽 표 제목: `종류별 책의 수`
- 아래쪽 표 머리글:
  - `종류`
  - `책의 수`
- 아래쪽 표 행 이름:
  - `동화책`
  - `위인전`
  - `과학책`
- 그림 범례:
  - 책 그림 옆에 `10권`
  - 작은 책 그림 옆에 `1권`
- 말풍선/문장 상자:
  - `10권과 1권을 나타내는 그림을 ( 둘러서 , 바꿔서 ) 그렸어.`
- 하단 선택/정답 영역:
  - `(정답)바꿔서`
- 해설 문장:
  - `(해설) [큰 책 그림]은 10권, [작은 책 그림]은 1권을 나타내는데 10권과 1권의 그림이 바뀌었습니다.`

# [반복되는 시각 패턴]
- **책 그림이 반복**되어 각 종류의 수를 나타냅니다.
- 같은 종류 안에서도 책 아이콘이 **가로로 연속 배치**되어 있고, 종류별 행마다 개수가 다릅니다.
- 범례에도 책 아이콘이 두 개 반복되어 있으며, 하나는 **큰 책 모양**, 다른 하나는 **작은 책 모양**입니다.
- 위쪽 숫자 표와 아래쪽 그림 표가 **같은 제목**을 공유하여 서로 대응되는 구조처럼 보입니다.
- 아래 표에서는 행마다 아이콘의 크기/형태가 반복적으로 사용되어 그림그래프임을 드러냅니다.

# [수식 단계] (해당될 때)
- 이 자료에는 등호가 연결된 수식 변형 단계는 보이지 않습니다.
- 숫자 표와 그림 표의 대응 관계를 비교하는 형태이며, 계산 과정을 단계별로 쓰는 구성은 아닙니다.

# [등호별 변환 의미] (해당될 때)
- 해당 없음.  
- 등호를 이용한 변형식이 나타나지 않습니다.

# [세부 내용]
- 위쪽 표는 **실제 수량**을 숫자로 제시합니다.
  - 동화책 40권, 위인전 25권, 과학책 14권, 합계 79권으로 보입니다.
- 아래쪽 표는 같은 종류를 **책 아이콘 개수**로 나타낸 그림표입니다.
  - 동화책 행에는 작은 책 그림이 여러 개 보입니다.
  - 위인전 행에는 작은 책 그림과 큰 책 그림이 섞여 보입니다.
  - 과학책 행에도 작은 책 그림과 큰 책 그림이 섞여 보입니다.
- 범례는 책 그림 하나가 의미하는 권수를 설명합니다.
  - 큰 책 그림이 `10권`
  - 작은 책 그림이 `1권`
  - 그런데 말풍선 문장에서는 이 둘이 **서로 바뀌었다**고 안내합니다.
- 하단의 `(정답)바꿔서`는 잘못된 부분의 핵심이 **10권과 1권 그림의 역할이 뒤바뀐 것**임을 가리키는 것으로 보입니다.
- 인물 그림은 오른쪽 아래에 배치되어 있으며, 설명을 받는 학생처럼 보이지만 문제 풀이 요소는 아닙니다.

# [Python DSL 작성 힌트]
- 상단 지시문은 한 줄 또는 두 줄로 배치하고, 번호 `2.`를 포함해 텍스트 블록으로 작성합니다.
- 위쪽 숫자 표는:
  - 제목 `종류별 책의 수`
  - 2행 5열 표 구조
  - 첫 행에 `종류 / 동화책 / 위인전 / 과학책 / 합계`
  - 둘째 행에 `책의 수(권) / 40 / 25 / 14 / 79`
- 아래쪽 그림 표는:
  - 제목 `종류별 책의 수`
  - 4행 2열 표 구조처럼 보이게 구성
  - 왼쪽 열은 종류명, 오른쪽 열은 책 아이콘들을 가로로 반복 배치
- 범례는 표 아래쪽에 책 아이콘 2개와 각각의 텍스트 `10권`, `1권`을 나란히 둡니다.
- 말풍선/문장 상자는 연한 분홍색 배경의 직사각형으로 처리하면 비슷합니다.
- 오른쪽 아래 인물 삽화는 별도 이미지 요소로 배치합니다.
- 하단의 `(정답)바꿔서`와 해설 문장은 문제 아래 여백에 텍스트로 추가합니다.

# [주의할 점]
- 문제를 풀어서 어느 그림이 정확히 몇 개인지 계산해 확정하지 마세요.
- `10권`과 `1권`의 대응이 바뀌었다는 **문제의 지적 대상**만 드러내고, 수량 해석을 새로 계산해 적지 마세요.
- 아래쪽 그림 표의 책 아이콘 개수는 보이는 그대로 그리되, 의미를 추론해 숫자로 환산하지 마세요.
- 범례의 두 책 그림은 크기 차이가 있으므로, Python DSL에서는 **서로 다른 아이콘 객체**로 다루는 것이 좋습니다.
- 표의 테두리는 연한 보라색 계열, 셀 배경은 연보라/흰색 계열로 보입니다.
- 상단 문장의 줄바꿈 위치는 원본에서 자연스럽지 않게 끊겨 있으므로, 편집 시 한 문장으로 정리해도 됩니다.

# [불확실한 부분]
- 아래 그림 표의 각 행에 있는 책 아이콘의 정확한 개수는 일부 가림/해상도 영향으로 세밀하게 다르게 보일 수 있습니다.
- 범례의 큰 책 그림과 작은 책 그림이 실제로 정확히 어느 크기 차이인지, 또 아이콘 내부 음영이 어떻게 처리되었는지는 해상도상 완전히 단정하기 어렵습니다.
- 오른쪽 인물 그림의 세부 표정과 의상 무늬는 참고 수준이며, 정확한 일러스트 재현은 어려울 수 있습니다.

--- RAW VISION DRAFT END ---
