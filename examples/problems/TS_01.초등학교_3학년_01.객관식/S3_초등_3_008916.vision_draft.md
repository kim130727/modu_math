[전체적인 구성]
- 문제 번호 `82.`가 왼쪽 위에 있고, 그 오른쪽에 긴 문장형 지시문이 2줄로 배치되어 있습니다.
- 중앙 상단에는 연한 민트색 배경의 큰 묶음 영역이 있고, 그 안에 `좋아하는 음료수`라는 제목이 둥근 글씨체로 들어 있습니다.
- 제목 아래에는 4개의 세로형 카드가 가로로 나란히 배치되어 있으며, 각 카드에는 음료 그림 1개와 별 모양 개수 표시가 있습니다.
- 그 아래에는 `좋아하는 음료수별 학생 수`라는 표 제목이 가운데 정렬로 있고, 바로 아래에 2행짜리 표가 있습니다.
- 화면 하단 왼쪽에는 `(정답)`과 `(해설)` 표기가 보이고, 해설 문장이 이어집니다.

[보이는 텍스트]
- `82.`
- `서윤이는 반 학생들이 좋아하는 음료수를 조사하여 자료와 표로 나타냈습니다.`
- `서윤이는 반 학생 수를 알기 쉬운 것은 자료와 표 중 어느 것인지 알맞은 것을 선택하세요.`
- 제목: `좋아하는 음료수`
- 카드 이름: `우유`, `주스`, `콜라`, `사이다`
- 표 제목: `좋아하는 음료수별 학생 수`
- 표 머리글: `음료수`, `우유`, `주스`, `콜라`, `사이다`, `합계`
- 표 둘째 줄: `학생 수(명)`
- 수치: `5`, `9`, `7`, `3`, `24`
- 오른쪽 아래: `( 자료 , 표 )`
- 하단: `(정답)표`
- 하단 해설: `(해설)표의 합계를 보면 쉽게 알 수 있습니다.`

[반복되는 시각 패턴]
- 음료 카드 4개가 같은 크기의 둥근 사각형 틀로 반복됩니다.
- 각 카드마다 왼쪽 위에 음료 그림 아이콘이 있고, 오른쪽 또는 상단에 음료 이름이 적혀 있습니다.
- 카드 내부에는 파란색 별 아이콘들이 여러 개 배치되어 있으며, 별 개수로 선호 정도/학생 수를 나타내는 형식입니다.
- 표는 열이 여러 개로 나뉘는 일반적인 격자 형태이고, 첫 열은 항목명, 나머지 열은 각 음료와 합계로 반복 구조를 이룹니다.
- 표의 첫 칸과 첫 행 일부는 회색/연한 파란색 계열로 강조되어 있습니다.

[수식 단계]
- 해당 없음: 화면에는 등호를 포함한 계산식 변형 단계가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음: 식의 좌변/우변 변형이나 등호 연결이 제시되지 않았습니다.

[세부 내용]
- 상단 자료 영역:
  - 큰 점선 테두리의 민트색 배경 박스 안에 4개의 음료 카드가 배치되어 있습니다.
  - 카드들은 좌우 간격이 거의 समान하고, 모두 흰색 바탕에 연보라색 테두리를 가진 형태입니다.
  - 각 카드 상단에는 실제 음료를 연상시키는 작은 그림이 있습니다.
  - 카드 안 별표는 진한 파란색이며, 카드별로 개수가 다르게 보입니다.
- 카드별 구성:
  - `우유` 카드: 별 5개가 보입니다.
  - `주스` 카드: 별이 여러 개 모여 있으며, 총 개수는 보이는 형태상 9개로 해석됩니다.
  - `콜라` 카드: 별이 7개로 보입니다.
  - `사이다` 카드: 별이 3개로 보입니다.
  - 위 개수들은 표의 수치와 대응되는 것으로 보입니다.
- 표 구성:
  - 표는 2행 6열 구조로 보입니다.
  - 첫 행에는 항목명과 음료명, `합계`가 가로로 배치되어 있습니다.
  - 둘째 행의 첫 칸에는 `학생 수(명)`이 들어 있고, 나머지 칸에는 숫자 `5, 9, 7, 3, 24`가 각각 들어 있습니다.
  - `합계` 칸의 `24`는 오른쪽 끝 열에 위치합니다.
- 문제 의도:
  - 자료와 표 중에서 `반 학생 수를 알기 쉬운 것`을 고르는 형식으로 보입니다.
  - 하단 해설 문장에 따르면 표의 합계를 확인하는 방식이 강조됩니다.

[Python DSL 작성 힌트]
- 상단 안내문은 2줄 텍스트로 배치하면 좋습니다.
- 민트색 점선 큰 박스 안에 제목 `좋아하는 음료수`를 상단 중앙에 올리고, 그 아래 4개의 동일한 카드형 박스를 가로 정렬하세요.
- 각 카드에는:
  - 작은 음료 그림 1개
  - 음료 이름 텍스트 1개
  - 파란 별 아이콘 여러 개
  를 포함시키면 됩니다.
- 표는 제목을 가운데에 두고, 아래에 2행 6열 표를 배치하세요.
- 첫 행은 `음료수 | 우유 | 주스 | 콜라 | 사이다 | 합계` 형태의 열 머리글,
- 둘째 행은 `학생 수(명) | 5 | 9 | 7 | 3 | 24` 형태로 보이게 구성하세요.
- 오른쪽 아래에는 보기 문자열 `( 자료 , 표 )`를 따로 두고, 아래쪽에 `(정답)`과 `(해설)` 텍스트를 배치하세요.
- 카드 내부의 별 개수는 자료 그림을 재현하는 용도로만 쓰고, 의미를 임의로 바꾸지 마세요.

[주의할 점]
- 별의 개수를 세더라도 답을 추론해 문제를 해결하는 식으로 쓰지 마세요.
- `24`를 보고 추가 계산을 적지 마세요.
- 표의 수치와 별 개수가 일치하는지 여부는 시각적으로 보이는 범위에서만 서술하세요.
- `( 자료 , 표 )`는 선택지처럼 보이므로, 정답을 미리 확정하지 말고 시각 정보만 기록하세요.
- 카드 테두리, 점선 박스, 표 셀 색상 등 레이아웃 요소를 빠뜨리지 않는 것이 중요합니다.

[불확실한 부분]
- 각 카드 안의 별 개수는 일부가 겹쳐 보이거나 작아서, 정확한 세기가 어려울 수 있습니다. 다만 표의 수치와 대응하는 자료로 배치된 것은 명확해 보입니다.
- 음료 그림 아이콘의 세부 모양은 작아 구체적 명칭까지 단정하기 어렵지만, 각각 우유/주스/콜라/사이다를 나타내는 것으로 보입니다.
- 하단의 `(정답)표`는 이미 정답 표시처럼 보이지만, 이것이 원본 문제의 정답 표기인지 편집본의 표시인지는 화면만으로는 완전히 단정하기 어렵습니다.
