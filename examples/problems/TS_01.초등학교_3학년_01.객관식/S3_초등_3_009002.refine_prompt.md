# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009002.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009002

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단 왼쪽에 문제 번호가 있고, 그 오른쪽으로 지시문이 한 줄로 배치되어 있습니다.
- 중앙에는 파란 격자 배경 위에 네 개의 도형이 가로 방향으로 나열되어 있습니다.
- 각 도형 안에는 한 글자씩 들어가 있으며, 아래쪽 중앙에는 괄호로 묶인 글자 목록이 따로 표시되어 있습니다.
- 하단 왼쪽에는 `(정답)` 표시와 함께 답이 텍스트로 제시되어 있고, 그 아래에는 `(해설)` 문장이 이어집니다.
- 전체적으로 “도형의 종류를 골라내는” 유형의 시각 자료로 보입니다.

[보이는 텍스트]
- `40. 정사각형을 모두 찾아 기호를 선택하세요.`
- 도형 안 글자: `가`, `나`, `다`, `라`
- 아래 괄호 표기: `( 가, 나, 다, 라 )`
- `정답) 가, 라`
- `해설) 네 각이 모두 직각이고 네 변의 길이가 모두 같은 사각형은 가, 라입니다.`

[반복되는 시각 패턴]
- 네 개의 도형이 모두 동일한 격자 배경 위에 놓여 있습니다.
- 각 도형은 하나의 기호(한글 자음/글자)로 구분되어 있고, 사용자가 보기 쉽게 왼쪽에서 오른쪽으로 일렬 배치되어 있습니다.
- 사각형 계열 도형이 반복되며, 서로 모양이 다르게 제시되어 있어 비교 대상처럼 보입니다.
- 아래의 괄호 목록 `( 가, 나, 다, 라 )`는 위의 도형들에 대응하는 선택지 묶음처럼 보입니다.

[수식 단계]
- 해당 없음.  
  이 문제는 수식 변형이나 등호를 이용한 계산 단계가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.  
  등호가 포함된 연산식이 제시되어 있지 않습니다.

[세부 내용]
- 문제 번호 `40`은 문장 맨 앞에 작게 표시되어 있습니다.
- 지시문은 “정사각형을 모두 찾아 기호를 선택하세요.”로, 정사각형 판별을 요구합니다.
- 중앙의 파란 격자 영역 안에는 4개의 도형이 있으며,
  - 첫 번째는 좌측에 있는 정사각형처럼 보이는 네모 도형 안에 `가`가 적혀 있습니다.
  - 두 번째는 좌우로 뾰족한 마름모/사각형 변형처럼 보이는 도형 안에 `나`가 적혀 있습니다.
  - 세 번째는 세로로 긴 직사각형 안에 `다`가 적혀 있습니다.
  - 네 번째는 마름모 형태의 도형 안에 `라`가 적혀 있습니다.
- 아래 괄호 ` ( 가, 나, 다, 라 ) `는 네 도형의 기호를 한 번 더 보여주는 형태입니다.
- 하단의 `정답) 가, 라`는 실제 정답 표시로 보이지만, 시각 분석 초안에서는 정답을 채택하지 않고 “하단에 정답 표기가 있다”는 사실만 확인하는 것이 안전합니다.
- 해설 문장은 정사각형의 성질을 설명하는 문장으로 읽힙니다.

[Python DSL 작성 힌트]
- 상단 텍스트와 중앙 도형 배열, 하단 정답/해설 영역을 분리해서 배치하면 좋습니다.
- 중앙 도형들은 같은 기준선 위에 가로로 정렬된 4개 객체로 모델링하면 됩니다.
- 각 도형 안의 한글 글자는 별도 텍스트 객체로 다루는 것이 좋습니다.
- 파란 격자 배경은 선택 사항일 수 있으나, 필요하다면 일정 간격의 점선/가는 선 격자로 표현할 수 있습니다.
- 괄호로 묶인 `( 가, 나, 다, 라 )`는 별도의 텍스트 줄로 처리하고, 도형군과 시각적으로 연결되도록 중앙 아래에 배치합니다.
- 하단의 `(정답)`, `(해설)` 라벨은 텍스트 블록으로 구분해서 배치합니다.

[주의할 점]
- 정답을 추론해서 새로 쓰지 마세요. 화면에 보이는 답 표기는 “있는 그대로 존재한다”는 사실만 다루는 것이 안전합니다.
- 도형의 정확한 종류를 단정할 때는 조심해야 합니다. 겉보기에는 정사각형/직사각형/마름모처럼 보이지만, 제작 의도상 약간의 왜곡이 있을 수 있습니다.
- 격자선은 배경 보조 요소이므로, 도형의 테두리와 혼동하지 않도록 구분해야 합니다.
- 글자 `가`, `나`, `다`, `라`는 선택지를 나타내는 기호처럼 보이므로, 도형 내부 텍스트와 아래 괄호의 목록을 서로 대응시켜 기록하면 좋습니다.

[불확실한 부분]
- 중앙의 각 도형이 실제로 어떤 도형 이름으로 분류되는지는 이 초안에서 확정하지 않는 것이 좋습니다.
- 특히 두 번째, 네 번째 도형은 정사각형 여부를 판단하는 기준을 시각적으로 해석해야 하므로, 단정적으로 서술하지 않는 것이 안전합니다.
- 하단의 `정답) 가, 라`와 해설 문구는 문제지 자체에 인쇄된 참고 정보로 보이지만, 작성 목적에 따라 포함/생략 기준을 따로 정해야 할 수 있습니다.

--- RAW VISION DRAFT END ---
