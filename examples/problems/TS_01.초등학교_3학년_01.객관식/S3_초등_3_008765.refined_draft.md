[문제 개요]
- 문제 번호는 `67.`로 보인다.
- 주제는 **저울 그림 2개를 보고, 포크 / 국자 / 숟가락 중 가장 가벼운 물건을 찾는 비교 문제**이다.
- 본문은 문장 완성형이며, 괄호 안에서 알맞은 말을 고르는 형식이다.
- 하단에는 `(정답)`과 `(해설)`이 인쇄되어 있다. 이는 **원본에 실제로 보이는 사실**로만 기록한다.

[원본에 보이는 요소]
- 상단 본문:
  - `□ 67. 저울을 사용하여 포크, 국자, 숟가락의 무게를 비교하고 있습니다. 포크, 국자, 숟가락 중에서 가장 가벼운 물건은 무엇인지 알아보려고 할 때 알맞은 말을 선택하세요.`
- 그림 2개가 가로로 배치되어 있다.
  - 왼쪽 저울 위 라벨: `포크`, `국자`
  - 오른쪽 저울 위 라벨: `국자`, `숟가락`
- 그림 아래 문장:
  - `포크와 국자 중 ( 포크 , 국자 )가 더 가볍고,`
  - `국자와 숟가락 중 ( 국자 , 숟가락 )이/가 더 가벼우므로`
  - `포크와 ( 국자 , 숟가락 )의 무게를 비교해야 합니다.`
- 하단:
  - `(정답)포크, 숟가락, 숟가락`
  - `(해설)포크와 국자 중 포크가 더 가볍고, 국자와 숟가락 중 숟가락이 더 가벼우므로 포크와 숟가락의 무게를 비교해야 합니다.`

[수식/관계 구조]
- 등호로 이어지는 계산식은 없다.
- 대신 **비교 관계**가 2단계로 제시된다.
  1. `포크`와 `국자`를 비교하는 저울 그림 1개
  2. `국자`와 `숟가락`을 비교하는 저울 그림 1개
- 이어서 위 비교 결과를 바탕으로 마지막 문장에 또 다른 비교 대상으로 연결하는 구조이다.
- 선택 슬롯은 괄호 `(...)`로 표시된 텍스트 선택형이다.
  - `( 포크 , 국자 )`
  - `( 국자 , 숟가락 )`
  - `( 국자 , 숟가락 )`

[등호별 변환 의미]
- 해당 없음.
- 등호 연쇄, 식 변형, 좌변/우변 이동은 보이지 않는다.
- 따라서 변환 단계 설명 대신 **문장 내 비교 대상의 연결 관계**만 해석하면 된다.

[빈칸 정책]
- 이 문제의 괄호는 **빈칸 답 입력칸**이라기보다 **보기에서 고르는 선택 슬롯**에 가깝다.
- 하지만 DSL 작성 시에는 괄호 안의 선택지를 임의로 채워 넣지 말고, 원문에 보이는 그대로 유지해야 한다.
- 정답으로 보이는 값이 이미지 하단에 인쇄되어 있더라도, **추론값을 빈칸에 대입하지 않는다.**
- 빈칸/선택지는 그대로 보존하고, 정답은 별도 메타 정보로 분리하는 것이 안전하다.

[반복 패턴]
- 저울 그림이 2회 반복된다.
- 반복 요소:
  - 저울 본체
  - 왼쪽 접시
  - 오른쪽 접시
  - 접시 위의 물건 그림
  - 각 물건 위의 작은 연한 배경 라벨
- 왼쪽 저울:
  - 왼쪽 접시 쪽에 `포크`
  - 오른쪽 접시 쪽에 `국자`
- 오른쪽 저울:
  - 왼쪽 접시 쪽에 `국자`
  - 오른쪽 접시 쪽에 `숟가락`
- 문장 패턴도 반복된다.
  - `A와 B 중 ( A , B )가 더 가볍고,`
  - `B와 C 중 ( B , C )이/가 더 가벼우므로`
  - `A와 ( B , C )의 무게를 비교해야 합니다.`
- 이 반복은 “비교 결과를 연결해 다음 비교 대상을 찾는” 서술형 구조로 볼 수 있다.

[DSL 작성 힌트]
- 상단 문제 번호 + 본문 지시문은 하나의 텍스트 블록으로 처리하면 좋다.
- 저울 그림 2개는 가로 배치가 핵심이다.
  - 두 저울 사이 간격이 있고
  - 각 저울은 독립된 그림 그룹으로 묶는 것이 적절하다.
- 각 저울 내부는 다음 요소로 분해하면 좋다.
  - 저울 받침대
  - 중앙 지지대
  - 좌우 접시
  - 접시 위 물건
  - 물건 이름 라벨 상자
- 라벨은 물건 위쪽의 작은 연한 배경 박스로 보인다.
- 설명문은 중앙 정렬에 가깝게 3줄로 배치되어 있다.
- 하단의 `(정답)`과 `(해설)`은 별도 보조 텍스트 블록으로 둘 수 있다.
- 선택 괄호 `(...)`는 실제 빈칸 슬롯처럼 보이므로, 텍스트상의 선택 구조를 유지하는 편이 좋다.
- 그림과 텍스트 사이 여백이 비교적 넓으므로, 레이아웃에서 수직 간격을 충분히 두는 것이 좋다.

[semantic/solvable 힌트]
- 이 문항의 핵심은 **두 번의 비교 결과를 통해 마지막 비교 대상을 정하는 것**이다.
- 그러나 이 답변에서는 비교 결과를 새로 계산하거나 추론하지 않는다.
- 원본 하단에 정답이 인쇄되어 보이지만, 문제 해결값을 본문에 직접 채워 넣지 않는다.
- 문장 의미상 `더 가벼운 것`을 고르는 구조이며, 괄호 안 후보 중 하나를 선택하는 형식이다.
- 저울 그림은 비교 관계를 시각적으로 보조하는 역할을 한다.

[주의 및 TODO]
- 이미지 기준으로 보면 본문 첫 줄의 표현이 raw draft와 약간 다르게 보일 수 있으므로, 최종 DSL용 텍스트는 **원본 PNG를 우선**해 교정해야 한다.
- 저울의 기울기 방향은 시각적으로 해석 여지가 있으므로, 본 답변에서는 어느 쪽이 더 가벼운지 단정하지 않는다.
- `(정답)`으로 보이는 문자열은 원본에 인쇄되어 있지만, DSL에서 정답 표시를 포함할지 여부는 별도 판단이 필요하다.
- `이/가` 조사 선택이 포함된 문구는 인쇄 상태에 따라 미세한 띄어쓰기 차이가 있을 수 있으므로, TODO로 점검해야 한다.
- 괄호 안 선택지는 변경하지 말고 원문 그대로 보존해야 한다.
