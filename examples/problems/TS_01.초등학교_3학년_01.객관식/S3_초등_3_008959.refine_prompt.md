# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008959.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008959

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 문제 번호 `29.`가 왼쪽 위에 있고, 그 오른쪽에 문제 문장이 2줄로 배치되어 있습니다.
- 상단에는 연한 하늘색 제목 상자 `아파트별 나무 수`가 있고, 그 아래에 표 1개가 있습니다.
- 가운데에는 같은 제목 `아파트별 나무 수`가 한 번 더 나오며, 그 아래에 그림 표 형태의 표가 있습니다.
- 하단 왼쪽과 가운데에는 보기로 보이는 작은 그림 선택지들이 동그라미 번호 `①~⑤`와 함께 흩어져 배치되어 있습니다.
- 맨 아래에는 `(정답) ⑤` 표기가 있고, 그 아래에 `(해설)` 문장이 2줄로 적혀 있습니다.

[보이는 텍스트]
- `29.`
- `아파트별 나무 수를 조사하여 표와 그림그래프로 나타내었습니다. 다 아파트의 나무 수를 바르게 나타낸 것은 어느 것일까요?`
- 제목: `아파트별 나무 수`
- 표의 열 제목: `아파트`, `가`, `나`, `다`, `라`, `합계`
- 표의 행 제목: `나무 수(그루)`
- 표 안 숫자: `46`, `19`, `23`, `115`
- 두 번째 표의 열 제목: `아파트`, `나무 수`
- 두 번째 표의 행: `가`, `나`, `다`, `라`
- 보기 번호: `①`, `②`, `③`, `④`, `⑤`
- 하단: `(정답) ⑤`
- 해설 문장:
  - `(해설)가, 나, 라 마을의 아파트의 나무 수는 46 + 19 + 23 = 88(그루)이므로`
  - `다 마을의 아파트의 나무 수는 115 - 88 = 27(그루)입니다.`

[반복되는 시각 패턴]
- 같은 제목 `아파트별 나무 수`가 두 번 반복되어, 위쪽은 숫자 표, 아래쪽은 그림그래프임을 보여 줍니다.
- 아래 표에서는 각 아파트 이름이 행으로 반복되고, 나무 그림 아이콘이 개수만큼 나열되어 있습니다.
- 보기 그림들도 같은 나무 아이콘을 사용하되, 개수와 크기/배열만 다르게 구성되어 있습니다.
- 선택지 번호 `①~⑤`는 각각 그림 묶음 옆에 작게 표시되어 있습니다.

[수식 단계]
- 해설에 등식이 2단계 나타납니다.
- `46 + 19 + 23 = 88`
- `115 - 88 = 27`
- 계산 과정을 직접 풀어 쓰기보다, 표의 합계와 나머지 항목을 연결하는 설명이 포함되어 있습니다.

[등호별 변환 의미]
- 첫 번째 `=`:
  - `46 + 19 + 23`이라는 세 아파트의 나무 수를 더한 값이 `88`이라고 나타냅니다.
  - 의미상 `가, 나, 라`에 해당하는 값을 합친 결과를 적은 형태입니다.
- 두 번째 `=`:
  - `115 - 88`의 계산 결과가 `27`이라고 적혀 있습니다.
  - 의미상 전체 합계 `115`에서 앞에서 합친 값 `88`을 뺀 나머지를 구한 형태입니다.

[세부 내용]
- 위쪽 표는 일반 표 형식입니다.
  - 첫 행은 열 제목이고, 마지막 열은 `합계`입니다.
  - 첫 번째 데이터 행은 `나무 수(그루)`이며, `가=46`, `나=19`, `다=빈칸`, `라=23`, `합계=115`로 보입니다.
  - `다` 칸에는 숫자가 직접 보이지 않고 빈칸처럼 비어 있습니다.
- 아래쪽 그림그래프 표는 2열 구조입니다.
  - 왼쪽 열은 아파트 이름 `가/나/다/라`.
  - 오른쪽 열은 각 아파트의 나무 수를 나무 그림으로 나타낸 영역입니다.
- `가` 행에는 나무 그림이 6개 정도 보이며, 큰 나무 5개와 작은 나무 1개가 이어져 있습니다.
- `나` 행에는 나무 그림이 6개 정도 보이며, 큰 나무 1개와 작은 나무 여러 개가 이어져 있습니다.
- `다` 행은 그림이 비어 있습니다.
- `라` 행에는 나무 그림이 4개 정도 보이며, 큰 나무 2개와 작은 나무 2개가 보입니다.
- 아래의 보기들은 각각 작은 그림 묶음으로, 나무 아이콘의 개수 조합이 서로 다릅니다.
- 보기 `⑤`는 큰 나무 2개와 작은 나무 3개처럼 보이는 묶음으로 배치되어 있습니다.
- 정답 표시는 `(정답) ⑤`로, 선택지 번호만 따로 강조되어 있습니다.

[Python DSL 작성 힌트]
- 상단 문제 문장은 텍스트 박스로 두 줄 줄바꿈을 반영해 배치하면 좋습니다.
- 첫 번째 표는 격자표 형태로, 연한 하늘색 헤더 셀과 흰색 데이터 셀을 구분해서 그리면 좋습니다.
- 표 내부의 `다` 칸은 숫자를 넣지 말고 빈칸으로 남겨야 합니다.
- 두 번째 표는 행/열이 있는 그림그래프 표로, 왼쪽 열의 항목명과 오른쪽 열의 아이콘 배열을 각각 셀 단위로 배치하면 좋습니다.
- 나무 아이콘은 크기 차이가 있으므로, 큰 나무와 작은 나무를 서로 다른 객체로 반복 배치하는 방식이 적절합니다.
- 보기 `①~⑤`는 각각 독립된 그림 묶음으로 좌표를 나눠 배치하세요.
- 하단 해설 문장은 별도 텍스트 영역으로 처리하고, 등식 `46 + 19 + 23 = 88`, `115 - 88 = 27`은 텍스트 그대로 표시하면 됩니다.

[주의할 점]
- `다`의 실제 수를 추론해서 표 안에 입력하지 마세요.
- 선택지의 그림 개수를 답으로 단정하지 말고, 보이는 그대로만 묘사하세요.
- 나무 아이콘의 정확한 개수는 이미지 해상도상 약간 헷갈릴 수 있으므로, 확정이 어려우면 “여러 개”, “몇 개”처럼 표현할 수 있습니다.
- 표와 그림그래프가 서로 연결된 문제이지만, 여기서는 풀이가 아니라 시각 구조 분석이 목적입니다.
- 정답 표시 `(정답) ⑤`는 화면에 보이는 사실로만 적고, 그 이유는 설명하지 마세요.

[불확실한 부분]
- 아래 그림그래프의 각 행에 있는 나무 아이콘 개수는 일부 작고 겹쳐 보여 정확한 개수를 단정하기 어렵습니다.
- 보기 ①~⑤의 나무 아이콘 구성도 해상도 때문에 세부 개수가 완전히 명확하지 않을 수 있습니다.
- 표의 빈칸은 실제로 입력 칸인지, 원래부터 비어 있는 영역인지 화면만으로는 확정하기 어렵습니다.

--- RAW VISION DRAFT END ---
