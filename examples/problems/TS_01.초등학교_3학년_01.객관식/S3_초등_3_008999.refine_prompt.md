# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008999.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008999

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호처럼 보이는 `37.`이 있고, 그 오른쪽에 문제 지시문이 한 줄로 배치되어 있습니다.
- 가운데에는 연한 파란색 격자 배경 위에 도형들이 가로로 나열되어 있는 그림이 있습니다.
- 그림 아래에는 선택지처럼 보이는 괄호 표기가 `（ 가, 나, 다, 라 ）` 형태로 가운데 정렬에 가깝게 놓여 있습니다.
- 그 아래 왼쪽에는 `정답)` 표시와 함께 정답 결과가 텍스트로 적혀 있고, 더 아래에는 `해설)`로 시작하는 설명 문장이 이어집니다.

[보이는 텍스트]
- `37. 네 각이 모두 직각인 사각형을 모두 찾아 기호를 선택하세요.`
- 그림 속 도형 안의 글자:
  - `가`
  - `나`
  - `다`
  - `라`
- 그림 아래:
  - `（ 가, 나, 다, 라 ）`
- 하단 텍스트:
  - `(정답)가, 나`
  - `(해설)네 각이 모두 직각인 사각형은 직사각형과 정사각형입니다. 따라서 가와 나입니다.`

[반복되는 시각 패턴]
- 네 개의 도형이 모두 사각형 계열로 보이며, 각각 안에 한 글자씩 `가, 나, 다, 라`가 들어 있습니다.
- 도형들은 모두 같은 격자 위에 놓여 있고, 서로 다른 모양이지만 한 문제 안에서 비교 대상이라는 점이 반복적으로 드러납니다.
- 아래의 괄호 표기 `（ 가, 나, 다, 라 ）`는 네 도형의 라벨을 다시 한 번 집합처럼 나열한 형식입니다.

[수식 단계]
- 해당 사항 없음. 수식 변형이나 등식 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 사항 없음. 등호를 포함한 식 변형이 보이지 않습니다.

[세부 내용]
- 첫 번째 도형 `가`:
  - 왼쪽에 위치한 가로로 긴 사각형입니다.
  - 내부에 `가` 글자가 중앙 부근에 놓여 있습니다.
  - 네 변이 직선이며, 격자에 맞춰 배치되어 있습니다.
- 두 번째 도형 `나`:
  - 가운데 왼쪽에 위치한 더 큰 세로형 사각형처럼 보입니다.
  - 내부에 `나` 글자가 중앙에 있습니다.
  - 바깥 윤곽은 직사각형 형태로 보입니다.
- 세 번째 도형 `다`:
  - 가운데 오른쪽에 위치한 좁고 세로로 긴 도형입니다.
  - 위쪽 변이 기울어진 선으로 되어 있어 일반 사각형처럼 보이지만, 직각 여부 판단 대상인 형태입니다.
  - 내부에 `다` 글자가 들어 있습니다.
- 네 번째 도형 `라`:
  - 오른쪽 끝에 위치한 사다리꼴처럼 보이는 도형입니다.
  - 윗변이 아랫변보다 짧아 보이고, 좌우 변이 대칭적이지 않습니다.
  - 내부에 `라` 글자가 있습니다.
- 전체 그림은 연한 파란색 격자선 위에 배치되어 있어 변의 직각 여부나 평행 여부를 시각적으로 비교하도록 만든 구성이 보입니다.

[Python DSL 작성 힌트]
- 그림 영역을 먼저 격자 배경 사각 영역으로 잡고, 그 안에 도형 4개를 좌→우 순서로 배치하는 구조가 적절합니다.
- 각 도형은 윤곽선 사각형/사다리꼴/기울어진 사각형 형태로 그리되, 내부 중앙에 각각 `가`, `나`, `다`, `라` 라벨을 넣으면 됩니다.
- 아래의 선택지 표기는 별도 텍스트 객체로 `（ 가, 나, 다, 라 ）`를 중앙 하단에 배치합니다.
- 하단의 `정답`, `해설` 텍스트는 일반 문단 텍스트로 분리해 배치합니다.
- 격자선은 옅은 파란색의 반복 선분으로 표현하면 원본과 유사합니다.
- 도형의 정확한 기울기와 비율은 사진상 원근에 따라 약간 왜곡되어 보이므로, 좌표는 대략적인 위치 관계를 우선 반영하는 것이 좋습니다.

[주의할 점]
- 정답인 기호를 시각 분석 단계에서 임의로 확정해 먼저 채워 넣지 마세요.
- `가, 나, 다, 라` 각각의 도형이 실제로 어떤 성질인지 문제 풀이로 판단하지 말고, 보이는 윤곽만 묘사하세요.
- 아래의 `(정답)`, `(해설)` 문구는 원본 이미지에 이미 포함된 텍스트로 보이며, 별도 생성 요소인지 구분이 필요합니다.
- 격자선과 도형 테두리의 색이 사진 해상도 때문에 약간 흐려져 보일 수 있습니다.
- `다`, `라` 도형은 직사각형처럼 보이지 않을 수 있으므로, 이름만 보고 형태를 단정하지 않는 것이 좋습니다.

[불확실한 부분]
- `다` 도형의 정확한 윗변 기울기와 좌우 변의 평행 관계는 이미지 해상도상 약간 불명확합니다.
- `라` 도형의 좌우 변이 완전히 대칭인지, 또는 약간 다른 각도인지도 확대 없이는 확정하기 어렵습니다.
- 격자 칸의 정확한 개수는 일부 흐림 때문에 셀 단위로 엄밀히 세기 어려울 수 있습니다.
- 하단의 `（ 가, 나, 다, 라 ）` 표기가 실제로 선택지인지, 단순 열거인지 문맥상 선택지처럼 보이지만 100% 단정할 수는 없습니다.

--- RAW VISION DRAFT END ---
