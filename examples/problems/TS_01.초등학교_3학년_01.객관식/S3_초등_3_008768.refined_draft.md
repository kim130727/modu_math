[문제 개요]
- 문제 ID: `S3_초등_3_008768`
- 화면 상단 왼쪽에 문항 번호 `70.`과 지시문 `무게를 알맞게 어림한 것을 찾아 선택하세요.`가 한 줄로 보임.
- 중앙에는 초록색 테두리의 큰 직사각형 박스가 있고, 그 안에 500원짜리 동전 그림과 선택지가 함께 배치되어 있음.
- 하단 왼쪽에는 `(정답)`과 `(해설)`이 인쇄되어 있으며, 정답과 간단한 해설 문장이 보임.
- 전체 구성은 `문제 문장 → 그림/보기 박스 → 정답/해설` 순서로 읽힘.

[원본에 보이는 요소]
- 상단 텍스트: `70. 무게를 알맞게 어림한 것을 찾아 선택하세요.`
- 중앙 박스 내부:
  - 동전 그림
  - 선택지 텍스트 `약 ( 7 g , 7kg , 7 t )`
- 동전 그림 안에 `500`이 보임.
- 동전 상단의 작은 연도 표기는 `2018`처럼 보이지만 선명하지 않음.
- 하단 표기:
  - `(정답)7 g`
  - `(해설)500원짜리 동전의 무게는 7kg보다 더 가벼우므로 약 7g이 알맞습니다.`

[수식/관계 구조]
- 수식 전개나 계산식, 등호 연쇄는 보이지 않음.
- 구조는 계산 문제가 아니라, 무게의 어림값을 고르는 선택형 문항임.
- 보기 구조:
  - `약 ( 7 g , 7kg , 7 t )`
- 동전 그림과 보기의 관계:
  - 500원짜리 동전의 무게를 대략 얼마로 볼지 묻는 형식으로 해석됨.
- `7 g`, `7kg`, `7 t`는 같은 수 `7`에 서로 다른 단위가 붙은 비교 보기로 보임.

[등호별 변환 의미]
- 해당 없음.
- 등호 기호가 있는 변형식이나 단계적 계산은 제시되지 않음.

[빈칸 정책]
- 빈칸형 입력이나 미완성 수식은 보이지 않음.
- 화면에 인쇄된 텍스트는 그대로 유지해야 하며, 정답을 공란으로 바꾸거나 추론값으로 채우지 않음.
- 하단의 `(정답)7 g`는 원본에 인쇄되어 보이는 사실로만 기록하고, 빈칸을 채우는 방식으로 재구성하지 않음.

[반복 패턴]
- 괄호 안에 보기 후보를 쉼표로 나열하는 형식이 보임: `( 7 g , 7kg , 7 t )`
- 숫자 `7`이 각 단위와 반복 결합됨.
- 중앙 박스에서 `그림(위) + 텍스트(아래)`의 상하 배치가 반복적으로 보이는 구성임.
- `(정답)`, `(해설)`은 문제집에서 자주 쓰이는 고정 레이블 패턴으로 보임.

[DSL 작성 힌트]
- 상단 좌측에 `70.`과 지시문을 한 줄로 배치해야 함.
- 중앙에는 초록색 외곽선의 직사각형 박스를 그려야 함.
- 박스 내부 상단 중앙에 동전 이미지를 배치하고, 그 아래 중앙 정렬로 보기 텍스트 `약 ( 7 g , 7kg , 7 t )`를 넣어야 함.
- 박스 내부는 위아래 여백이 충분히 있어야 하며, 이미지와 텍스트가 세로로 정렬된 느낌을 유지해야 함.
- 하단 왼쪽에는 `(정답)7 g`, `(해설)500원짜리 동전의 무게는 7kg보다 더 가벼우므로 약 7g이 알맞습니다.`를 줄바꿈 또는 문단 분리하여 배치해야 함.
- 보기의 공백, 괄호, 쉼표 순서를 원문과 가깝게 유지하는 것이 중요함.
- 동전 이미지 안의 `500` 표기가 식별되도록 처리해야 함.

[semantic/solvable 힌트]
- 문제는 무게의 어림값을 고르는 선택형 문항으로 보임.
- 보이는 해설 문장은 `500원짜리 동전의 무게는 7kg보다 더 가벼우므로 약 7g이 알맞습니다.`라고 적혀 있음.
- 다만 정답은 해설에 인쇄된 내용으로 보이는 사실만 다루어야 하며, 별도로 추론 과정은 확장하지 않음.
- `7 g`, `7kg`, `7 t`는 단위 비교를 통해 적절한 어림값을 고르게 하는 보기로 해석됨.

[주의 및 TODO]
- 동전 상단의 연도는 `2018`로 보이지만, 선명도가 낮아 확정 단정은 피해야 함.
- 동전 이미지가 사진인지 일러스트인지 화면만으로는 확실하지 않으므로 TODO로 남김.
- `7g`와 `7 g`의 띄어쓰기 차이는 원본에서 구분되어 보이므로, DSL 구성 시 표기 형태를 주의해서 반영해야 함.
- 초록색 테두리 박스의 위치와 크기는 대략적이므로, 실제 DSL에서는 원본 비율을 참고해 조정이 필요함.
- 정답을 새로 계산하거나 빈칸을 채우는 방식으로 재작성하지 말 것.
