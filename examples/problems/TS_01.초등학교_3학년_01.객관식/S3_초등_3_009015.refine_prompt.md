# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009015.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009015

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단 왼쪽에 문제 번호와 지시문이 있고, 중앙 영역에는 도형 예시가 3개 배치되어 있습니다.
- 아래쪽 왼편에는 `(정답)` 표시가 붙은 도형이 따로 있으며, 그 아래/옆에 해설 문장이 한 줄로 제시되어 있습니다.
- 전체적으로 “여러 도형 중에서 반직선”을 고르는 선택형 문제 형태입니다.
- 화면 왼쪽 아래에 정답 표시가 있어, 시각적으로 어떤 도형이 선택 대상인지 안내하는 구조입니다.

[보이는 텍스트]
- `53. 반직선을 선택하세요.`
- `(정답)`
- `(해설) 반직선은 한 점에서 시작하여 한쪽으로 끝없이 늘인 곧은 선입니다.`
- 텍스트는 상단의 문제 문장, 좌하단의 정답 표시, 하단의 해설 문장으로 나뉘어 보입니다.

[반복되는 시각 패턴]
- 도형들이 각각 독립된 위치에 떨어져 배치되어 있습니다.
- 선분/선 형태를 나타내는 검은 선과 끝점(작은 점) 표시가 반복적으로 사용됩니다.
- 가운데와 오른쪽 도형은 각각 시작점처럼 보이는 점과 선의 방향성이 드러나는 형태로 그려져 있습니다.
- 왼쪽 아래 도형은 `(정답)` 라벨과 함께 따로 분리되어 있어, 다른 도형들과 구분되는 반복 패턴 외 예외 배치로 보입니다.

[수식 단계]
- 해당 없음: 수식 변형이나 등호가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음: 등호가 포함된 식 전개가 없습니다.

[세부 내용]
- 상단 문제 문장 왼쪽에는 작은 사각형 모양의 번호 표시가 있고, 그 옆에 `53.`이 있습니다.
- 문제 문장 `반직선을 선택하세요.`는 상단 왼쪽에 비교적 크게 인쇄되어 있습니다.
- 중앙 상단에는 가로로 놓인 짧은 검은 선이 있으며, 양 끝에 점이 찍혀 있어 선분처럼 보입니다.
- 중앙에는 왼쪽 위에서 오른쪽 아래 방향으로 기울어진 검은 선이 있고, 한쪽 끝에 점이 있으며 다른 쪽 끝은 점 없이 이어진 형태로 보입니다.
- 오른쪽 상단에는 굽은 곡선 형태의 선이 있으며, 양쪽 끝 근처에 점처럼 보이는 표시가 있어 호 또는 곡선 도형처럼 보입니다.
- 왼쪽 하단에는 왼쪽 아래에서 오른쪽 위 방향으로 기울어진 선이 있고, 한쪽 끝에 점이 표시되어 있으며, 그 옆에 `(정답)` 이 적혀 있습니다.
- 해설 문장은 화면 맨 아래에 길게 배치되어 있으며, `반직선`의 정의를 설명하는 문장입니다.

[Python DSL 작성 힌트]
- 상단 문제 문장과 번호는 텍스트 객체로 분리해서 배치하는 것이 좋아 보입니다.
- 도형들은 서로 간격을 두고 3개를 위쪽에 배치하고, 정답 도형은 왼쪽 아래에 따로 배치하는 구성이 적절합니다.
- 각 선 도형은 다음 요소를 분리해 표현하면 좋습니다.
  - 선의 방향
  - 끝점의 점 표시 유무
  - 곡선 여부
- `(정답)` 라벨은 왼쪽 아래 도형 바로 옆 또는 아래에 붙여 배치하면 원본과 유사합니다.
- 해설 문장은 하단에 한 줄 텍스트로 넣되, 앞의 `(해설)` 라벨과 본문을 함께 표현하면 됩니다.

[주의할 점]
- 이 문제는 풀이형이 아니라 선택형 시각 자료이므로, 도형의 정확한 종류를 단정해서 설명할 때는 보이는 형태만 기준으로 써야 합니다.
- 빈칸이나 답안 칸은 보이지 않습니다.
- 도형의 끝점 점 표시는 작게 보여서, 선분/반직선/곡선의 해석이 헷갈릴 수 있으니 “보이는 형태”와 “의미 추정”을 구분해서 기록해야 합니다.
- 해설 문장은 정의를 설명하지만, 이를 문제 해결 과정으로 확장해서 쓰지 않도록 주의해야 합니다.

[불확실한 부분]
- 중앙의 기울어진 도형은 한쪽 끝만 점이 보이고 다른 쪽은 점이 뚜렷하지 않아, 실제 의도상 반직선인지 단정하기 전에 시각적 특징을 신중히 처리해야 합니다.
- 오른쪽의 굽은 도형은 곡선/호처럼 보이지만, 정확히 어떤 이름의 도형인지 그림만으로는 단정하기 어렵습니다.
- 왼쪽 아래 정답 도형의 끝점 표시가 작아서, 점의 위치와 선의 시작점 관계를 세밀하게 확인할 필요가 있습니다.

--- RAW VISION DRAFT END ---
