# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008980.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008980

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호 `15.`가 있고, 그 뒤로 문제 지시문이 한 줄로 배치되어 있다.
- 지시문 아래에는 보기처럼 보이는 도형들이 상단 중앙과 중단 왼쪽/중앙에 배치되어 있다.
- 화면 하단에는 `(해설)`로 시작하는 설명 문장이 길게 적혀 있다.
- 전체적으로 **객관식 선택형 문제**처럼 보이며, `○`와 `×` 기호를 사용해 정답을 고르도록 안내한다.
- 도형은 **직각의 예시/비예시**를 보여 주는 형태로 보인다.

[보이는 텍스트]
- `15. 직각이면 ○, 직각이 아니면 ×를 선택하세요.`
- `(정답)`
- `(해설)직각 삼각자의 직각 부분을 대어 보았을 때 꼭 맞게 겹쳐지는 각이 직각입니다.`
- 화면에 기호로 보이는 요소:
  - `○`
  - `×`
- 도형 내부/주변에 별도의 문자 라벨은 보이지 않는다.

[반복되는 시각 패턴]
- `○`와 `×`가 각각 별도의 표시로 반복 사용된다.
- 직각을 나타내는 **ㄱ자 형태의 선분 도형**이 두 번 보인다.
  - 하나는 상단 중앙 쪽에 배치
  - 다른 하나는 하단 왼쪽에 배치
- 두 도형 모두 **가로선과 세로선이 직각으로 만나는 모양**을 기본으로 하지만, 화면상에서는 길이와 위치가 조금 다르게 보인다.
- 선택 표시처럼 보이는 작은 원형 요소가 `(정답)` 옆에 있다.

[수식 단계]
- 해당 없음.  
- 등호, 계산식, 변형식은 보이지 않는다.

[등호별 변환 의미]
- 해당 없음.  
- 수식의 변형 단계가 나타나지 않는다.

[세부 내용]
- 상단의 지시문은 “직각이면 ○, 직각이 아니면 ×를 선택”하라는 의미로 읽힌다.
- 상단 중앙의 도형은:
  - 세로선이 아래로 내려오고,
  - 위쪽에 가로선이 오른쪽으로 뻗는 형태로 보인다.
  - 꼭짓점은 왼쪽 위 쪽에 있는 ㄱ자형 구조다.
- 중단 왼쪽에는 작은 파란색 `○` 기호가 따로 보인다.
- 중단 중앙에는 작은 파란색 `×` 기호가 따로 보인다.
- `(정답)` 텍스트 오른쪽에는 비어 있는 원형 표시가 있어, 답 표시를 입력하거나 선택하는 위치로 보인다.
- 하단 해설 문장은 직각의 정의를 설명하는 문장으로, **직각삼각자**를 대어 보았을 때 **꼭 맞게 겹쳐지는 각**이 직각이라고 설명한다.
- 하단 왼쪽에도 또 하나의 ㄱ자 형태 도형이 있으며, 위쪽에 짧은 가로선과 왼쪽으로 내려오는 세로선이 보인다.
- 이 하단 도형은 상단 도형과 같은 종류의 직각 예시로 보이지만, 화면상 일부가 잘려 보이거나 다른 방향으로 배치된 상태일 수 있다.

[Python DSL 작성 힌트]
- 문제 본문 텍스트와 해설 텍스트를 각각 분리해 배치하는 구조가 필요하다.
- `15.`와 지시문은 상단 한 줄에 연속 배치하면 된다.
- `○`, `×`는 단순 문자 요소로 처리하되, 위치가 각각 따로 보이므로 개별 좌표를 줄 수 있으면 좋다.
- `(정답)` 옆의 빈 원형은 선택용 입력 요소처럼 표현할 수 있다.
- 상단 중앙과 하단 왼쪽의 ㄱ자 도형은 각각 **두 개의 직선 요소가 직각으로 만나는 도형**으로 그리면 된다.
- 해설 문장은 줄바꿈을 포함해 하단에 배치하는 것이 자연스럽다.
- 도형의 선은 얇은 회색/검은색 계열로, 텍스트는 일반 검은색으로 분리하면 좋다.
- 상단 도형과 하단 도형의 상대적 크기와 위치 차이를 반영하면 원본 느낌을 살릴 수 있다.

[주의할 점]
- 정답이 무엇인지 추론해서 표시하면 안 된다.
- `○`와 `×`는 문제 지시의 선택 기호일 뿐, 실제 답을 채우는 값으로 해석하지 말아야 한다.
- 하단 해설의 의미를 바탕으로 상단 도형을 정답/오답으로 단정하지 말아야 한다.
- 도형이 정확히 어느 방향으로 놓였는지 단정하기 어려운 부분은 유연하게 처리해야 한다.
- 화면에 보이는 원형 표시와 파란색 기호를 혼동하지 않도록 주의해야 한다.

[불확실한 부분]
- 상단 중앙 도형과 하단 왼쪽 도형이 정확히 동일한 도형인지, 또는 일부가 잘린 별도 도형인지 확실하지 않다.
- 중단의 파란 `○`, `×` 기호가 실제 선택지인지, 화면 캡처 과정에서 우연히 포함된 표시인지 단정하기 어렵다.
- `(정답)` 옆의 빈 원형이 실제 클릭/기입 위치인지, 인쇄물의 장식 요소인지 화면만으로는 확실하지 않다.
- 하단 해설 문장의 줄바꿈 위치는 화면 기준으로 자연스럽게 끊어 보이지만, 원본 편집 의도와 완전히 일치한다고 단정할 수는 없다.

--- RAW VISION DRAFT END ---
