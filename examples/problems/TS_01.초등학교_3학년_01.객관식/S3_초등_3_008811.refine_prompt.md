# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008811.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008811

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 문제 번호 `44.`가 왼쪽 상단에 있고, 그 오른쪽에 문제 지시문이 한 줄로 배치되어 있습니다.
- 본문은 크게 **왼쪽 인물+말풍선**, **오른쪽 주스병/물병 그림**, **아래 설명 문장과 정답/해설 영역**으로 나뉩니다.
- 전체적으로 비교 문장 완성을 요구하는 초등 수학/언어 혼합형 시각 자료입니다.
- 수식이나 계산식은 보이지 않고, 그림과 문장으로 관계를 설명하는 형태입니다.

[보이는 텍스트]
- 상단 문제 지시문:
  - `44. 상아가 주스병과 물병의 들이를 비교하려고 합니다. 알맞은 말을 선택하세요.`
- 왼쪽 인물 이름표:
  - `상아`
- 말풍선 안 문장:
  - `주스병에 물을 가득 채운 후`
  - `물병에 옮겨 담아 비교해야지.`
- 주스병 아래 라벨:
  - `주스병`
- 물병 아래 라벨:
  - `물병`
- 초록색 손글씨 형태 설명:
  - `물이 가득 차지 않은 것의`
  - `들이가 더 많아요.`
- 하단 본문 문장:
  - `주스병의 물이 물병에 모두 들어가므로 ( 주스병 , 물병 )의 들이가 더 많습`
  - 다음 줄에 이어서 `니다.`
- 정답 표시:
  - `(정답)물병`
- 해설:
  - `(해설)주스병의 물이 물병에 모두 들어가므로 물병의 들이가 더 많습니다.`

[반복되는 시각 패턴]
- 왼쪽에는 사람 캐릭터와 이름표가 있고, 오른쪽에는 비교 대상인 두 개의 용기 그림이 배치되어 있습니다.
- 이름표는 연한 초록색 테두리의 둥근 사각형으로 보이며, 사람 캐릭터 아래에 붙어 있습니다.
- 말풍선은 연보라색 큰 직사각형 말풍선이며, 왼쪽 인물에게서 나오는 형태로 연결되어 있습니다.
- 오른쪽에는 위쪽에 기울어진 주스병, 아래쪽에 세워진 물병이 있으며, 각각 글자 라벨이 붙어 있습니다.
- 물병 쪽에는 초록색 곡선 화살표와 손글씨가 있어, 시각적으로 중요한 비교 포인트를 강조합니다.
- 하단의 문장 영역은 본문 문장, 정답, 해설이 순서대로 아래로 정렬되어 있습니다.

[수식 단계]
- 해당 없음: 이 그림에는 등호가 포함된 수식 단계가 보이지 않습니다.
- 계산 과정이나 변형식이 제시되지 않았습니다.

[등호별 변환 의미]
- 해당 없음: 등호가 나타나는 수식 변형이 없습니다.

[세부 내용]
- 문제는 두 용기 `주스병`과 `물병`의 **들이(용량)** 를 비교하는 상황입니다.
- 그림에서는 주스병에서 물을 물병으로 옮겨 담는 상황을 통해, 어느 쪽의 들이가 더 큰지 말하도록 유도합니다.
- 말풍선의 내용은 상아가 “주스병에 물을 가득 채운 뒤 물병에 옮겨 담아 비교해야 한다”는 생각을 하고 있음을 보여 줍니다.
- 오른쪽 그림의 주스병은 기울어진 상태로 액체가 부어지는 장면이고, 물병은 세워진 상태로 물이 담겨 있는 모습입니다.
- 초록색 손글씨는 “물이 가득 차지 않은 것”의 들이가 더 많다는 취지로 보이지만, 문장 구조상 문제의 핵심 결론과 연결되는 설명으로 읽힙니다.
- 하단 문장은 빈칸 선택형/괄호 선택형처럼 보이며, `( 주스병 , 물병 )` 중 알맞은 말을 넣는 구조입니다.
- 아래에는 `(정답)`과 `(해설)`이 함께 제시되어 있어, 학습용 예시 화면처럼 보입니다.

[Python DSL 작성 힌트]
- 상단 문제 번호와 지시문을 한 줄 텍스트로 배치합니다.
- 왼쪽에 인물 캐릭터와 이름표를 세로로 정렬하고, 그 오른쪽에 연보라색 말풍선을 둡니다.
- 오른쪽 상단에는 기울어진 주스병 그림, 그 아래쪽에는 세워진 물병 그림을 배치하고 각각 아래에 라벨을 둡니다.
- 물병 주변에 초록색 곡선 화살표와 손글씨 주석을 넣습니다.
- 하단에는 본문 문장을 큰 글씨로 가운데 정렬에 가깝게 배치하고, 그 아래에 정답/해설 텍스트를 작은 글씨로 나열합니다.
- 괄호 안 선택 영역 `( 주스병 , 물병 )`은 실제 빈칸이 아니라 선택지처럼 보이는 텍스트이므로, 텍스트 박스 형태를 유지하는 것이 좋습니다.

[주의할 점]
- 문제를 풀어 답을 새로 추론하지 마세요.
- `주스병`, `물병` 중 어느 것이 정답인지 시각적으로 보이는 표기만 그대로 옮겨야 합니다.
- 초록색 손글씨는 필기체처럼 보여 정확한 띄어쓰기와 줄바꿈을 그대로 옮기기 어려울 수 있습니다.
- 하단 문장의 일부는 줄바꿈 때문에 잘려 보이므로, 원문 전체를 임의로 보완하지 않도록 주의하세요.
- 그림 속 액체의 양이나 병의 실제 용량 관계를 수치로 해석하지 말고, 화면에 보이는 비교 상황만 기술하세요.

[불확실한 부분]
- 초록색 손글씨의 정확한 문구는 일부 흐릿하여, `물이 가득 차지 않은 것의 들이가 더 많아요.`로 보이지만 완전 확정은 어렵습니다.
- 하단 본문에서 `( 주스병 , 물병 )`처럼 보이는 선택 영역의 앞뒤 공백과 괄호 배치는 화면상 약간 모호합니다.
- `정답`과 `해설`의 줄 시작 위치는 보이지만, 편집용으로는 약간 잘려 보일 수 있어 정확한 좌표 기반 복원 시 확인이 필요합니다.

--- RAW VISION DRAFT END ---
