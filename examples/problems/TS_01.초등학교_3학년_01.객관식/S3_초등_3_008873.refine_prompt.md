# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008873.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008873

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
## [전체적인 구성]
- 상단 왼쪽에 문제 번호가 **“26.”**으로 표시되어 있습니다.
- 그 오른쪽에 문제 문장이 한 줄(또는 두 줄로 이어져) 배치되어 있습니다.
- 문장 아래쪽 중앙에는 **연녹색 계열의 표**가 있습니다.
- 표 아래에는 괄호로 묶인 선택지 형태의 문구가 한 줄로 배치되어 있습니다.
- 그 아래 왼쪽에는 **“(정답)”** 표시와 함께 정답이 제시되어 있습니다.
- 가장 아래에는 **해설 문장**이 두 줄 정도로 이어져 있습니다.

## [보이는 텍스트]
- `26. 수조에 물을 가득 채우려면 다음과 같이 각각 부어야 합니다. ㉠ 그릇, ㉡ 그릇, ㉢ 그릇 중 들이가 가장 많은 것을 선택해 보세요.`
- 표의 첫째 행:
  - `㉠ 그릇`
  - `㉡ 그릇`
  - `㉢ 그릇`
- 표의 둘째 행:
  - `그릇의 수`
  - `3개`
  - `6개`
  - `4개`
- 표 아래:
  - `( ㉠ 그릇 , ㉡ 그릇 , ㉢ 그릇 )`
- 정답 표시:
  - `(정답) ㉠ 그릇`
- 해설:
  - `(해설) 물을 부은 그릇의 수가 적을수록 그릇의 들이가 더 많습니다.`
  - `물을 부은 그릇의 수를 비교하면 3<4<6이므로 들이가 가장 많은 것은 ㉠ 그릇입니다.`

## [반복되는 시각 패턴]
- 선택지가 **㉠, ㉡, ㉢**처럼 원 안에 들어간 기호로 반복됩니다.
- 표의 열 제목도 같은 선택지 기호를 사용해 **3개 항목을 나란히 비교**하는 구조입니다.
- 수치 정보는 각 선택지 아래에 **한 칸씩 대응**되어 배치되어 있습니다.
- 표 전체는 **가로로 3개의 비교 칸**이 반복되는 형태입니다.
- 표 아래의 괄호 표기는 보기 항목을 다시 한 번 나열하는 반복 구조입니다.

## [수식 단계]
- 해당 없음: 화면에는 계산식, 등식 변형, 단계별 수식 전개가 보이지 않습니다.
- 다만 숫자 비교 표현으로 `3<4<6`이 텍스트로 제시되어 있습니다.

## [등호별 변환 의미]
- 해당 없음: 등호(`=`)를 이용한 변형 과정이 나타나지 않습니다.
- 비교 기호 `<`만 보이며, 이는 크기 비교를 뜻하는 형태로 보입니다.

## [세부 내용]
- 문제는 **수조에 물을 가득 채우는 데 필요한 그릇의 종류를 비교**하는 내용입니다.
- 핵심 시각 정보는 표이며, 각 그릇 종류마다 필요한 **그릇의 수**가 대응되어 있습니다.
- 표 구조를 보면:
  - 왼쪽 첫 칸은 항목명처럼 보이는 라벨 영역입니다.
  - 그 오른쪽으로 3개의 선택지 열이 있고, 각 열 위에는 그릇 종류가 적혀 있습니다.
  - 둘째 행에는 각 종류에 대응하는 숫자와 `개` 단위가 있습니다.
- 표 아래의 괄호 안에는 선택지 3개가 다시 한 번 순서대로 정리되어 있어, 답을 고르도록 유도합니다.
- 하단 해설은 **작은 수의 그릇의 수와 더 많이 채워지는 관계**를 말로 설명합니다.
- 해설 마지막 문장에는 숫자 비교 `3<4<6`이 포함되어 있습니다.

## [Python DSL 작성 힌트]
- 표를 중심 요소로 잡아, **2행 4열 구조**로 표현하면 적절합니다.
- 첫 번째 열은 라벨 칸으로 두고, 두 번째~네 번째 열에 각각 `㉠ 그릇`, `㉡ 그릇`, `㉢ 그릇`을 배치합니다.
- 둘째 행의 첫 칸에는 `그릇의 수`, 나머지 칸에는 각각 `3개`, `6개`, `4개`를 넣는 형태가 자연스럽습니다.
- 표 아래에는 괄호로 묶인 보기 문자열을 별도 텍스트로 둡니다.
- 정답 표시와 해설 문장은 표 아래에 세로로 이어 붙이는 방식이 좋습니다.
- 비교식 `3<4<6`은 수식 블록이 아니라 **텍스트 또는 인라인 비교표현**으로 처리해도 됩니다.

## [주의할 점]
- 문제를 실제로 풀어 정답을 새로 계산해 넣지 마세요.
- 화면에 보이는 정답 표시는 그대로 인식하되, 다른 값으로 추론하지 마세요.
- `㉠, ㉡, ㉢` 기호는 숫자 번호가 아니라 **선택지 식별자**로 다뤄야 합니다.
- 표의 첫 행과 둘째 행을 혼동하지 않도록 주의하세요.
- 해설의 비교 `3<4<6`은 **순서 비교 문구**로 보이며, 이를 수식 계산으로 바꾸지 마세요.

## [불확실한 부분]
- 표의 테두리 색이 연녹색 계열로 보이지만, 인쇄/해상도에 따라 정확한 색상은 달라질 수 있습니다.
- 문제 문장이 줄바꿈된 정확한 위치는 이미지 해상도상 약간 다르게 해석될 수 있습니다.
- 원형 기호 `㉠, ㉡, ㉢`의 실제 폰트 모양은 확대 시 약간 달라 보일 수 있습니다.

--- RAW VISION DRAFT END ---
