[문제 개요]
- 문제 번호는 `53.`으로 보입니다.
- 지문은 호정이네 마을의 목장에서 생산한 우유의 양을 표로 나타낸 뒤, 알맞은 것을 선택하라는 형식입니다.
- 중앙에 제목 `목장별 우유 생산량`이 있고, 그 아래 2행 표가 배치되어 있습니다.
- 표 아래에는 설명 문장 1개가 있고, 더 아래에 `(정답)2`, `(해설)`이 보입니다.
- 이 화면은 문제 본문, 표, 설명 문장, 정답/해설이 세로로 이어지는 구성입니다.

[원본에 보이는 요소]
- 문제 번호: `53.`
- 지문: `호정이네 마을의 목장에서 생산한 우유의 양을 조사하여 표로 나타냈습니다. 알맞은 것을 선택하세요.`
- 제목: `목장별 우유 생산량`
- 표의 첫째 줄: `목장 / 가 / 나 / 다 / 라 / 합계`
- 표의 둘째 줄: `생산량(kg) / 36 / 31 / 25 / 43 / 135`
- 표 아래 문장: `우유 생산량이 두 자리 수이므로 그림을 ( 1, 2 )가지로 나타냅니다.`
- 하단 표기: `(정답)2`
- 하단 해설: `(해설)우유 생산량이 두 자리 수이므로 그림을 2가지로 나타냅니다.`

[수식/관계 구조]
- 이 문항에는 등호를 포함한 수식 전개나 계산식 연쇄는 보이지 않습니다.
- 표는 정보 제시용 구조이며, 직접적인 등식 변형 단계는 없습니다.
- 표 안의 숫자들은 자료값으로 보이며, 계산 과정을 화면에서 따로 보여주지는 않습니다.

[등호별 변환 의미]
- 등호가 포함된 변형식은 보이지 않습니다.
- 따라서 등호별 단계 설명은 해당 사항이 없습니다.

[빈칸 정책]
- `우유 생산량이 두 자리 수이므로 그림을 ( 1, 2 )가지로 나타냅니다.`의 괄호는 원문에 보이는 그대로 유지합니다.
- 빈칸이나 선택 표기는 추론값으로 채우지 않습니다.
- 화면에 인쇄된 `2`는 보이는 사실로만 기록할 수 있지만, 빈칸 내부를 계산으로 확정해 쓰지는 않습니다.
- 정답이 별도로 인쇄되어 보이는 경우에도, 문제 풀이 결과로 확장하지 않고 관찰 사실로만 다룹니다.

[반복 패턴]
- 표는 2행 6열 구조로 보입니다.
- 첫 행은 분류/항목명, 둘째 행은 대응되는 수치 배열입니다.
- 열 구성은 `목장, 가, 나, 다, 라, 합계`의 반복적 분류 형태입니다.
- 숫자 셀 `36, 31, 25, 43, 135`는 각 열 중앙에 놓인 형태로 보입니다.
- 표는 주황색 계열의 테두리와 셀 배경을 사용한 반복 패턴을 가집니다.
- 제목은 표 위 가운데 정렬입니다.
- 하단의 `(정답)2`, `(해설)`은 본문과 분리된 작은 주석형 텍스트로 보입니다.

[DSL 작성 힌트]
- 제목 `목장별 우유 생산량`은 표 상단 중앙 배치가 적절합니다.
- 표는 2행 6열로 잡으면 됩니다.
- 1행 셀: `목장 / 가 / 나 / 다 / 라 / 합계`
- 2행 셀: `생산량(kg) / 36 / 31 / 25 / 43 / 135`
- 표 전체는 연한 주황색 계열의 테두리와 셀 배경으로 표현하는 것이 원본 느낌에 가깝습니다.
- 표의 각 셀은 중앙 정렬 텍스트로 두는 것이 자연스럽습니다.
- 지문은 표 위쪽 좌측 정렬, 설명 문장은 표 아래쪽 중앙 정렬에 가깝게 배치합니다.
- `(정답)2`, `(해설)`은 본문과 분리된 하단 주석 영역에 작은 글씨로 둡니다.
- `우유 생산량이 두 자리 수이므로 그림을 ( 1, 2 )가지로 나타냅니다.`는 문장 텍스트로 유지하고, 괄호 안은 비워진 인쇄 형태를 그대로 반영합니다.
- 표의 선 굵기, 셀 간격, 정확한 색상값은 과도하게 단정하지 말고 유사한 분위기로만 구현합니다.

[semantic/solvable 힌트]
- 이 화면은 표 정보와 문장 선택형 문제를 함께 제시합니다.
- 표의 수치들은 목장별 우유 생산량 자료로 보입니다.
- 표 아래 문장은 두 자리 수와 그림의 가지 수를 연결하는 서술형 문장입니다.
- 다만 여기서는 문제 해결을 진행하지 않고, 보이는 내용만 구조화합니다.
- `(정답)2`는 화면에 보이는 사실이지만, 빈칸이나 선택지의 값을 추론해 채우는 용도로 사용하지 않습니다.

[주의 및 TODO]
- 정답을 계산해서 서술하지 마세요.
- 표의 `135`가 무엇을 의미하는지 풀이식으로 확장하지 마세요.
- `36, 31, 25, 43, 135`의 합산 관계를 직접 설명하지 마세요.
- 괄호 `( 1, 2 )`는 빈칸 정책에 따라 그대로 유지해야 합니다.
- 표의 정확한 위치, 선 두께, 글꼴은 해상도 한계로 단정하지 않습니다.
- 표 아래 문장의 실제 의도는 문맥상 보이지만, 인쇄된 형태 중심으로만 다룹니다.
- `나누기 n은 분모의 ×n으로 반영된다.`에 해당하는 변형은 이 화면에 보이지 않습니다.
