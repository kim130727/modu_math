# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008970.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008970

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면은 왼쪽 위에 문제 번호와 지시문이 있고, 그 아래에 답안 선택용으로 보이는 수식 2개가 나란히 배치되어 있습니다.
- 오른쪽 상단 쪽에는 분홍색 테두리의 큰 직사각형 박스가 있으며, 그 안에 두 개의 뺄셈식이 좌우로 배치되어 있습니다.
- 왼쪽 아래에는 정답과 해설이 텍스트로 이어져 있으며, “(정답)”, “(해설)” 표시가 각각 포함되어 있습니다.
- 전체적으로 객관식 형태처럼 보이지만, 별도의 선택지 표나 동그라미 표시는 보이지 않습니다.

[보이는 텍스트]
- `4. 계산 결과가 더 큰 것을 선택하세요.`
- 박스 안: `724-359` , `483-105`
- 아래 텍스트:
  - `(정답)483-105`
  - `(해설)724-359 = 365, 483-105 = 378`
  - `365<378이므로 계산 결과가 더 큰 것은 483-105입니다.`
- 줄바꿈 위치상 해설 문장은 두 줄로 이어져 보입니다.

[반복되는 시각 패턴]
- 박스 안에 같은 형식의 뺄셈식이 2개 반복되어 있습니다.
- 두 식 모두 `세 자리 수 - 세 자리 수` 형태입니다.
- 각 식은 기호와 숫자가 한 줄에 간단히 배열되어 있고, 별도의 세로셈 칸이나 계산 칸은 보이지 않습니다.
- 아래 해설 부분에서는 `식 = 결과, 식 = 결과`처럼 등호가 반복되는 패턴이 보입니다.
- 그 뒤에 비교 기호 `<`가 사용되어 두 결과를 비교하는 문장이 이어집니다.

[수식 단계]
- 해설에 나타난 수식 흐름은 다음과 같이 보입니다.
  - `724-359 = 365`
  - `483-105 = 378`
  - `365<378`
- 마지막 문장에서는 비교 결과를 바탕으로 더 큰 계산 결과가 무엇인지 문장으로 정리하고 있습니다.
- 주의: 위 결과값 자체는 화면에 인쇄된 내용이며, 여기서는 계산하거나 검증하지 않습니다.

[등호별 변환 의미]
- 첫 번째 등호: `724-359 = 365`
  - 왼쪽의 뺄셈식을 계산한 결과를 오른쪽에 적어 놓은 형태로 보입니다.
- 두 번째 등호: `483-105 = 378`
  - 다른 뺄셈식의 계산 결과를 오른쪽에 적어 놓은 형태로 보입니다.
- 이후의 비교식 `365<378`
  - 두 계산 결과를 크기 비교한 표시로 보입니다.
- 마지막 문장
  - 비교 결과를 근거로 어느 식의 계산 결과가 더 큰지 설명하는 결론 문장입니다.

[세부 내용]
- 문제 번호 `4.`가 지문 앞에 붙어 있습니다.
- 지시문은 “계산 결과가 더 큰 것을 선택하세요.”로, 두 식 중 결과가 더 큰 것을 고르는 유형입니다.
- 박스는 연한 분홍색 테두리이며 내부는 흰색 배경입니다.
- 박스 내부의 두 수식은 서로 간격을 두고 수평으로 배치되어 있습니다.
- 왼쪽 아래의 `(정답)`과 `(해설)`은 본문보다 약간 작은 글자처럼 보이며, 해설은 일반 문장 형태로 이어집니다.
- 수식과 문장은 모두 검은색 계열 글자로 보입니다.
- 별도의 그림, 도형, 화살표, 표, 빈칸 입력칸은 보이지 않습니다.

[Python DSL 작성 힌트]
- 상단 문제 문구와 박스 안의 두 수식을 각각 독립 텍스트 요소로 배치하는 방식이 적절해 보입니다.
- 분홍색 테두리 박스는 큰 사각형 컨테이너로 처리하고, 내부의 두 뺄셈식은 좌우 정렬된 텍스트로 넣는 구성이 좋습니다.
- 하단의 정답/해설 영역은 일반 텍스트 블록으로 두되, `(정답)`과 `(해설)` 라벨을 유지해 주는 것이 좋습니다.
- 비교식 `365<378`과 결론 문장은 한 줄 또는 두 줄 텍스트로 처리하면 됩니다.
- 수식을 실제 계산하는 요소는 만들지 말고, 화면에 보이는 문자열 그대로 배치하는 방향이 맞습니다.

[주의할 점]
- 문제를 푸는 답을 새로 추론해서 채우지 마세요.
- 화면에 보이는 정답/해설 텍스트를 임의로 수정하지 마세요.
- 박스 안의 두 식은 “선택지”처럼 보이지만, 실제로는 단순 텍스트 배치로 처리해야 할 수 있습니다.
- `724-359`, `483-105` 사이에 등호나 다른 기호는 보이지 않습니다.
- 해설의 결과값은 검증하지 말고, 보이는 그대로 옮기는 데 집중하세요.

[불확실한 부분]
- 분홍색 테두리 박스의 정확한 크기와 좌우 여백은 이미지 해상도에 따라 약간 다르게 인식될 수 있습니다.
- `(정답)`과 `(해설)`의 글자 크기 차이는 명확하지 않아서, 실제 DSL에서는 같은 스타일로 처리해도 큰 차이가 없을 수 있습니다.
- 박스 내부 두 수식의 정확한 세로 위치는 이미지 기준으로 약간 오차가 있을 수 있습니다.
- 해설 문장의 줄바꿈은 편집 환경에 따라 한 줄로 합쳐질 가능성이 있습니다.

--- RAW VISION DRAFT END ---
