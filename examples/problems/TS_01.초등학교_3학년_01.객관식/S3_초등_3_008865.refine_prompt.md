# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008865.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008865

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 한 문제짜리 초등 수학/문장형 판단 문제 화면이다.
- 상단 왼쪽에 문제 번호 `14.`가 있고, 그 뒤로 문장이 가로로 이어진다.
- 문장 아래 중앙 부근에 그림이 배치되어 있으며, 왼쪽의 리필용 샴푸와 오른쪽의 샴푸통이 **화살표(→)** 로 연결되어 있다.
- 그림 아래에는 두 물건의 이름을 나타내는 짧은 글자 표기가 각각 `가`, `나`로 따로 놓여 있다.
- 그 아래에는 선택형 문장 완성 형태의 답안 문장이 한 줄로 제시되어 있고, 괄호 안에 선택지처럼 `많습니다, 적습니다`가 함께 보인다.
- 하단에는 `(정답)`과 `(해설)`이 별도로 인쇄되어 있다.

# [보이는 텍스트]
- `14.`
- `어머니께서 리필용 샴푸를 사서 빈 샴푸통에 모두 부었더니 그림과 같이`
- `샴푸통이 가득 차지 않았습니다. 알맞은 말을 선택하세요.`
- 그림 아래 표기: `가`, `나`
- 문장형 빈칸/선택 문장:
  - `가 통은 나 통보다 들이가 더 ( 많습니다, 적습니다 ).`
- 하단:
  - `(정답)적습니다`
  - `(해설)샴푸통이 가득 차지 않았으므로 리필용 샴푸 통은 샴푸통보다 들이가 더 적습니다.`
- 그림 내부 글자:
  - 왼쪽 용기: `리필용 샴푸`
  - 오른쪽 용기: `샴푸통`

# [반복되는 시각 패턴]
- 그림 중심 문제에서 흔히 보이는 **좌→우 비교 구조**가 있다.
  - 왼쪽: 작은 포장 형태의 용기
  - 가운데: 오른쪽을 가리키는 화살표
  - 오른쪽: 큰 통/용기
- 두 대상 아래에 각각 `가`, `나`를 붙여 대응 관계를 표시한다.
- 답 문장은 `가 통은 나 통보다 ...`처럼 두 대상을 비교하는 동일한 문장 틀을 사용한다.
- 비교 표현은 `많습니다 / 적습니다` 중 하나를 고르는 형태로 제시된다.

# [수식 단계]
- 해당 없음.
- 이 문제는 수식 변형 단계가 아니라 그림을 보고 비교 표현을 고르는 문장형 문제이다.

# [등호별 변환 의미]
- 해당 없음.
- 등호 `=` 나 여러 단계의 식 변형이 보이지 않는다.

# [세부 내용]
- 문제 상단의 서술은 “리필용 샴푸를 사서 빈 샴푸통에 모두 부었더니”라는 상황을 설명한다.
- 그림에서는 왼쪽의 리필용 샴푸 용기에서 오른쪽의 샴푸통으로 내용물이 이동한 것을 화살표로 나타낸다.
- 오른쪽 샴푸통은 가득 차 보이지 않으며, 내용물이 위쪽까지 완전히 차오르지 않은 상태로 그려져 있다.
- 따라서 이 문제는 **두 용기의 들이(용량)를 비교**하는 내용으로 보인다.
- 아래 문장 `가 통은 나 통보다 들이가 더 ( 많습니다, 적습니다 ).`에서
  - `가`는 왼쪽 용기,
  - `나`는 오른쪽 용기에 대응한다.
- `가 통`, `나 통`이라는 표현이 인쇄되어 있으나, 실제 그림의 물건 이름은 각각 `리필용 샴푸`, `샴푸통`이다.
- 하단의 `(정답)적습니다`와 `(해설)...적습니다.`가 함께 보이지만, DSL 작성 시에는 정답을 직접 채우기보다는 **인쇄된 텍스트 존재 여부**만 반영하는 것이 안전하다.

# [Python DSL 작성 힌트]
- 상단 문장 영역을 먼저 배치하고, 그 아래 중앙에 그림을 놓는 구성이 적절하다.
- 그림은 다음 요소로 나누어 표현하면 좋다.
  1. 왼쪽 리필용 샴푸 용기 그림
  2. 가운데 오른쪽 방향 화살표
  3. 오른쪽 샴푸통 그림
  4. 각 그림 아래의 `가`, `나` 라벨
- 오른쪽 샴푸통 내부의 내용물 색/곡선은 실제 그림처럼 간단한 채움선과 곡면으로 표현하면 된다.
- 비교 문장은 하단에 한 줄로 배치하고, 괄호 안의 선택지 `많습니다, 적습니다`는 동일한 서체로 표시한다.
- 해설/정답 영역은 문제 본문과 분리된 아래쪽 텍스트 블록으로 처리하면 된다.

# [주의할 점]
- 정답인 `적습니다`를 문제 본문 답칸처럼 미리 채우지 말고, **보이는 글자**로만 다뤄야 한다.
- `가`, `나`가 각각 어느 그림에 대응하는지는 그림 위치를 기준으로만 설명하고, 문제를 풀어 결론을 확정하지 않도록 한다.
- `가 통`, `나 통`은 인쇄된 문구이므로, 실제 그림의 용기 이름과 혼동하지 않도록 한다.
- 그림 속 샴푸통이 “가득 차지 않았음”을 시각적으로 보여주지만, 이것을 넘어 용량 차이를 수치적으로 추정하지 않는다.
- `(정답)`과 `(해설)`은 본문과 다른 메타 정보처럼 보이므로 별도 영역으로 다루는 것이 좋다.

# [불확실한 부분]
- 왼쪽 용기와 오른쪽 용기의 정확한 도형 비율, 색상 그라데이션, 테두리 두께는 이미지 해상도상 일부만 확인 가능하다.
- `가`, `나`의 글자 크기와 정확한 정렬 위치는 약간의 오차가 있을 수 있다.
- 하단의 `(해설)` 문장은 줄바꿈 때문에 일부가 두 줄로 보이며, 정확한 줄 분할은 이미지 편집 시 달라질 수 있다.
- 문제 본문의 마지막 문장부호와 줄바꿈 위치는 인쇄 상태에 따라 약간 달라질 수 있다.

--- RAW VISION DRAFT END ---
