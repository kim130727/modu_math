# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008798.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008798

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 왼쪽 위에 문제 번호와 지시문이 있고, 그 아래 왼쪽에는 항아리 그림이 배치되어 있습니다.
- 화면 오른쪽에는 문장형 문제 본문이 크게 제시되어 있습니다.
- 하단 왼쪽에는 `(정답)` 표시와 함께 선택된 단위가 보이고, 그 아래에 `(해설)` 문장이 따로 표시되어 있습니다.
- 전체적으로 “그림 + 문장 + 정답/해설”로 이루어진 초등 수학 단원형 보기 문제 형태입니다.

[보이는 텍스트]
- `□ 27. 알맞은 단위를 선택하세요.`
- `항아리의 둘이는 약 24 ( mL , L )입니다.`
- `(정답) L`
- `(해설) 항아리의 둘이는 약 24L입니다.`
- 그림 속 항아리 외에는 숫자나 수식은 보이지 않습니다.

[반복되는 시각 패턴]
- 문장 안에 단위 선택지가 괄호 안에 함께 제시되어 있습니다: `( mL , L )`
- 하단에는 정답과 해설이 분리되어 있으며, 정답은 한 글자 단위 기호로 표시됩니다.
- 본문과 해설 모두 같은 수치 `24`를 포함하지만, 해설은 단위가 붙은 완성 문장으로 제시됩니다.
- 그림은 단위 선택을 돕는 시각 자료 역할을 하는 단독 일러스트로 보입니다.

[수식 단계]
- 해당 없음.  
- 등호를 포함한 변형식이나 계산 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.  
- 등호가 없는 문장형 문제입니다.

[세부 내용]
- 왼쪽 상단의 작은 사각형 기호 `□`는 체크 표시처럼 보이며, 문제 번호 앞에 붙어 있습니다.
- 문제 번호는 `27.`로 표시됩니다.
- 본문 문장 `항아리의 둘이는 약 24 ( mL , L )입니다.`에서 빈칸 형태의 입력칸은 보이지 않고, 대신 괄호 안에서 단위를 고르는 형식입니다.
- `mL`와 `L`은 쉼표로 구분되어 있으며, 괄호 내부에 병렬로 배치되어 있습니다.
- 항아리 그림은 화면 왼쪽 중간에 크게 배치되어 있고, 갈색 계열의 단지 모양으로 묘사되어 있습니다.
- 아래쪽 `(정답) L`은 정답을 직접 제시하는 영역으로 보이며, 같은 줄에서 `L`이 강조된 형태입니다.
- `(해설)` 뒤 문장은 본문을 답에 맞게 다시 쓴 형태로 보입니다.

[Python DSL 작성 힌트]
- 문제 번호와 지시문은 상단 좌측 텍스트 박스로 배치합니다.
- 항아리 그림은 본문보다 왼쪽 아래에 독립 이미지로 배치합니다.
- 본문 문장은 오른쪽 중앙에 큰 글씨로 배치하고, 괄호 안의 단위 선택지를 텍스트로 그대로 표현합니다.
- 하단에는 `(정답)`과 `(해설)`을 별도의 텍스트 블록으로 분리해 배치합니다.
- 선택지 표시는 드롭다운이 아니라 문장 내부 괄호 텍스트로 처리하는 방식이 적합합니다.

[주의할 점]
- `둘이`처럼 보이는 부분은 인쇄 상태에 따라 약간 흐릿할 수 있으니, 실제 문구는 이미지에서 확인한 그대로 다루는 것이 좋습니다.
- 정답을 문제 풀이로부터 추론해 설명하지 말고, 화면에 보이는 정답 표기를 그대로 옮겨야 합니다.
- `mL`와 `L`의 대소문자 구분을 유지해야 합니다.
- 본문에 빈칸 선이나 입력 상자는 보이지 않으므로, 이를 임의로 추가하지 않도록 주의해야 합니다.

[불확실한 부분]
- 본문 첫 단어가 `둘이는`으로 보이지만, 해상도상 `들이는`처럼 읽힐 가능성도 완전히 배제할 수는 없습니다.
- 다만 화면상 보이는 형태를 기준으로 하면 `항아리의 둘이는 약 24 ( mL , L )입니다.`로 읽히는 상태입니다.
- 그림의 정확한 도형 의미나 재질 설명은 시각적으로만 추정 가능하며, 문제 해결에는 사용하지 않는 것이 안전합니다.

--- RAW VISION DRAFT END ---
