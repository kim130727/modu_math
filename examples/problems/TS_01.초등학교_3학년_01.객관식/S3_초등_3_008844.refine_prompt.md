# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008844.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008844

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 상단 왼쪽에 문제 번호가 있고, 그 옆으로 문장형 지시가 2줄 정도로 배치되어 있습니다.
- 중앙에는 저울 그림이 2개 나란히 배치되어 있으며, 각각 위에 물건 이름이 붙어 있습니다.
- 각 저울 위에는 비교 대상이 서로 다른 두 물건이 놓여 있고, 저울의 기울기 상태로 무게 비교를 시각적으로 표현합니다.
- 하단에는 선택형/서술형에 가까운 문장이 이어지며, 괄호와 빈칸 형태가 포함된 문장이 보입니다.
- 가장 아래쪽에는 답을 적는 듯한 짧은 텍스트 “각도기”가 별도로 보입니다.  
  - 이것이 인쇄된 정답 예시인지, 사용자가 적은 필기인지 화면만으로는 확정하기 어렵습니다.

# [보이는 텍스트]
- `84.`
- `저울로 각도기, 컴퍼스, 지우개의 무게를 비교하고 있습니다.`
- `가장 가벼운 것은 무엇인지 알맞은 말을 선택해 보세요.`
- `가`
- `각도기`
- `컴퍼스`
- `나`
- `지우개`
- `컴퍼스`
- `가장 가벼운 것은 ( 각도기 , 컴퍼스 , 지우개 )입니다.`
- `(2) 컴퍼스보다 각도기가 가볍고, 지우개보다 컴퍼스가 가벼우므로 가장 가벼운 것은 각도기입니다.`
- `각도기`

# [반복되는 시각 패턴]
- 두 개의 저울 그림이 좌우로 반복 배치되어 있습니다.
- 각 저울은 같은 형태의 받침대와 중앙 기둥, 양쪽 접시 구조를 공유합니다.
- 물건 이름은 저울 위쪽에 작은 말풍선/라벨 박스처럼 표시되어 있습니다.
- 첫 번째 저울과 두 번째 저울 모두 “왼쪽 접시 vs 오른쪽 접시” 비교 구조를 보여 줍니다.
- 저울의 기울기 방향이 서로 다르게 보이는데, 이는 상대적 무게 비교를 반복적으로 나타내는 장치로 보입니다.
- 괄호 안의 선택지 `( 각도기 , 컴퍼스 , 지우개 )`는 같은 형식의 항목을 쉼표로 나열한 패턴입니다.

# [수식 단계]
- 해당 없음.
- 화면에는 등호가 연결된 수식 변형 단계가 보이지 않습니다.
- 대신 문장으로 비교 관계를 설명하는 형식입니다.

# [등호별 변환 의미]
- 해당 없음.
- 등호가 있는 수식 전개가 나타나지 않습니다.
- 비교 문장 안의 “보다”, “가볍고”, “가장 가벼운”은 논리적 비교 표현이지 수식 변형 단계는 아닙니다.

# [세부 내용]
- 첫 번째 저울:
  - 왼쪽 접시에 `각도기`가 놓여 있습니다.
  - 오른쪽 접시에 `컴퍼스`가 놓여 있습니다.
  - 저울의 모양상 왼쪽이 더 내려가 보이며, 시각적으로 두 물건의 무게 차이를 암시합니다.
- 두 번째 저울:
  - 왼쪽 접시에 `지우개`가 놓여 있습니다.
  - 오른쪽 접시에 `컴퍼스`가 놓여 있습니다.
  - 저울의 모양상 오른쪽이 더 내려가 보이며, 역시 비교 결과를 시각적으로 나타냅니다.
- 문제 문장:
  - “저울로 각도기, 컴퍼스, 지우개의 무게를 비교하고 있습니다.”라는 설명 뒤에,
  - “가장 가벼운 것은 무엇인지 알맞은 말을 선택해 보세요.”라는 질문이 이어집니다.
- 아래 문장:
  - `(2) 컴퍼스보다 각도기가 가볍고, 지우개보다 컴퍼스가 가벼우므로 가장 가벼운 것은 각도기입니다.`
  - 이 문장은 비교 관계를 설명하는 완성 문장처럼 보입니다.
  - 다만 화면 하단의 `각도기`가 정답 입력 결과인지, 별도 표기인지 구분이 어렵습니다.

# [Python DSL 작성 힌트]
- 저울 그림 2개를 각각 독립된 객체로 배치하는 구성이 필요합니다.
- 각 저울에 대해:
  - 받침대,
  - 중앙 축,
  - 좌우 접시,
  - 접시에 놓인 물건 그림,
  - 물건 이름 라벨
  을 따로 표현하면 좋습니다.
- 상단 문제 문장은 2줄로 줄바꿈을 유지해서 배치하는 것이 자연스럽습니다.
- 선택지 문장은 괄호와 쉼표를 그대로 살려 텍스트 박스로 넣는 방식이 적절합니다.
- 마지막의 `각도기` 텍스트는 인쇄된 답안인지 사용 흔적인지 불확실하므로, 필요 시 별도 주석 처리하는 것이 안전합니다.
- 저울의 기울어진 방향은 이미지 기반으로 재현해야 하며, 비교 결과를 글로 단정하지 말고 시각적 방향만 먼저 묘사하는 것이 좋습니다.

# [주의할 점]
- 문제를 풀어 정답을 추론해 적지 마세요.
- “가장 가벼운 것”의 결론을 시각 분석 문서에서 단정적으로 다시 쓰지 마세요.
- 저울의 어느 쪽이 더 무거운지에 대한 해석은, 그림의 기울기를 근거로 한 시각 정보와 분리해서 설명하는 것이 안전합니다.
- 하단의 `각도기`는 화면에 보이지만, 그것이 문제 본문인지, 정답 표시인지, 캡처 후 남은 흔적인지 확정하지 마세요.
- 숫자 `(2)`는 문항 번호의 일부로 보이나, 보기/해설 번호일 가능성도 있으므로 맥락을 분리해서 기록하세요.

# [불확실한 부분]
- 하단의 `각도기` 텍스트가 인쇄된 정답인지, 화면에 우연히 포함된 별도 텍스트인지 불확실합니다.
- 두 저울의 정확한 기울기 정도는 이미지 해상도 때문에 세밀하게 단정하기 어렵습니다.
- `가`, `나` 표기가 각 저울의 이름인지, 그림 구분용 라벨인지 완전히 확정되지는 않습니다.
- 첫 번째 저울과 두 번째 저울에서 접시의 높낮이 차이를 정확한 수치로 읽을 수는 없습니다.

--- RAW VISION DRAFT END ---
