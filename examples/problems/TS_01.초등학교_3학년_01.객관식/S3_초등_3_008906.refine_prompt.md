# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008906.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008906

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 번호 `72.`가 있고, 그 오른쪽에 문장형 문제 지시가 이어집니다.
- 중앙에는 `팔린 빵의 수`라는 제목이 있는 그림그래프 표가 있습니다.
- 표 아래쪽에는 그림 1개가 얼마를 뜻하는지 나타내는 범례가 있습니다.
- 오른쪽 아래에는 선택지처럼 보이는 괄호 묶음 `( 소보로빵 , 크림빵 )`이 보입니다.
- 화면 하단에는 `(정답)`과 `(해설)`이 인쇄되어 있어, 원문 문제와 함께 정답 설명이 포함된 자료처럼 보입니다.

[보이는 텍스트]
- `72.`
- `어느 가게에서 하루에 팔린 빵의 수를 조사하여 그림그래프로 나타내었습니다.`
- `소보로빵과 크림빵 중 더 많이 팔린 빵을 선택하세요.`
- `팔린 빵의 수`
- 표 머리글:
  - `종류`
  - `빵의 수`
- 행 이름:
  - `단팥빵`
  - `소보로빵`
  - `크림빵`
  - `식빵`
- 범례:
  - 그림 1개 옆에 `10개`
  - 더 작은 그림 1개 옆에 `1개`
- 오른쪽 아래:
  - `( 소보로빵 , 크림빵 )`
- 하단:
  - `(정답)소보로빵`
  - `(해설)소보로빵은 큰 그림 3개, 작은 그림 1개이고, 크림빵은 큰 그림 2개, 작은 그림 6개 이므로 더 많이 팔린 빵은 소보로빵입니다.`

[반복되는 시각 패턴]
- 각 빵 종류의 행마다 같은 형태의 빵 그림들이 가로로 나열되어 있습니다.
- 큰 빵 그림과 작은 빵 그림이 반복해서 사용됩니다.
- 범례에서도 본문과 같은 큰 그림/작은 그림이 다시 한 번 표시되어, 각각의 값이 무엇을 의미하는지 알려 줍니다.
- 표 안의 네 행은 모두 동일한 구조로, 왼쪽에는 종류 이름, 오른쪽에는 그림들이 배치되어 있습니다.

[수식 단계]
- 이 자료에는 등호가 여러 번 이어지는 계산식은 보이지 않습니다.
- 대신 그림의 개수를 세어 비교하는 그림그래프 형태입니다.
- 범례에 따르면:
  - 큰 그림 1개 = `10개`
  - 작은 그림 1개 = `1개`
- 표의 해당 행들에서 큰 그림과 작은 그림의 개수를 읽어 비교하는 구조입니다.

[등호별 변환 의미]
- 해당 없음.
- 수식 변형 단계나 등호를 따라가는 식은 보이지 않습니다.

[세부 내용]
- 표는 테두리가 있는 직사각형 형태이며, 세로로 2칸, 가로로 여러 행으로 나뉘어 있습니다.
- 맨 위 머리줄은 연한 노란색 계열 배경으로 보이고, 왼쪽 칸에 `종류`, 오른쪽 칸에 `빵의 수`가 들어 있습니다.
- 각 행의 왼쪽 칸에는 빵 종류 이름이 중앙 정렬에 가깝게 들어가 있습니다.
- 오른쪽 칸에는 빵 모양 그림이 가로로 배열되어 있습니다.
- `단팥빵` 행에는 큰 그림 1개와 작은 그림 여러 개가 보입니다.
- `소보로빵` 행에는 큰 그림 여러 개와 작은 그림 1개가 보입니다.
- `크림빵` 행에는 큰 그림 여러 개와 작은 그림 여러 개가 보입니다.
- `식빵` 행에도 큰 그림 여러 개와 작은 그림 여러 개가 보입니다.
- 표 아래 범례는 오른쪽 아래쪽에 따로 배치되어 있으며, 큰 그림 옆에 `10개`, 작은 그림 옆에 `1개`가 적혀 있습니다.
- 오른쪽 아래의 `( 소보로빵 , 크림빵 )`은 보기 항목처럼 보이며, 문제에서 둘 중 하나를 고르는 구조로 읽힙니다.
- 하단의 `(정답)`과 `(해설)`은 학습지/문제 해설용 추가 텍스트로 보입니다.

[Python DSL 작성 힌트]
- 먼저 바깥 제목과 문제 문장을 텍스트 박스로 배치합니다.
- 그 아래 중앙에 그림그래프 표를 그리되, 표의 큰 외곽선과 내부 가로줄, 세로줄을 분명히 나눕니다.
- 표의 첫 행은 머리글 배경색을 다르게 처리하면 좋습니다.
- 각 행 왼쪽에는 텍스트 라벨(`단팥빵`, `소보로빵`, `크림빵`, `식빵`)을 넣고, 오른쪽에는 빵 아이콘을 가로로 반복 배치합니다.
- 큰 빵 아이콘과 작은 빵 아이콘은 서로 다른 크기로 정의해 범례와 본문에 동일하게 재사용합니다.
- 범례는 표 아래 오른쪽에 작은 아이콘 2개와 설명 텍스트 `10개`, `1개`를 함께 놓는 방식이 적합합니다.
- 보기 `( 소보로빵 , 크림빵 )`는 독립된 텍스트 박스 또는 괄호가 있는 선택지처럼 별도 배치합니다.
- 하단의 `(정답)`, `(해설)` 문장은 별도 텍스트 블록으로 처리하면 됩니다.

[주의할 점]
- 그림의 개수를 읽더라도 정답을 이 문서에서 확정해서 쓰지 마세요.
- `소보로빵`과 `크림빵` 중 어느 것이 더 많은지 판단 결과를 여기서 단정적으로 재서술하지 않는 것이 안전합니다.
- 표 안의 빵 그림 개수는 반드시 실제 보이는 개수만 기준으로 적어야 하며, 범례의 의미를 임의로 확장하지 마세요.
- 해설 문구에 정답이 적혀 있더라도, 시각 분석 초안에서는 정답을 그대로 결론처럼 쓰지 않는 편이 좋습니다.
- 작은 그림과 큰 그림의 모양이 비슷해 보여도 크기 차이가 있다는 점을 반영해 배치해야 합니다.

[불확실한 부분]
- 각 행의 빵 그림 개수는 이미지 해상도상 일부가 겹쳐 보여 정확한 셈이 헷갈릴 수 있습니다.
- 특히 `단팥빵`, `크림빵`, `식빵` 행의 작은 그림 개수는 시각적으로 밀집되어 있어 판독 시 주의가 필요합니다.
- 오른쪽 아래의 `( 소보로빵 , 크림빵 )`가 실제 선택지인지, 편집 과정에서 들어간 부가 표기인지 완전히 단정하기는 어렵습니다.
- 하단 `(정답)`, `(해설)` 영역은 원본 시험지의 일부인지 해설 캡처의 일부인지 문맥상 추정만 가능합니다.

--- RAW VISION DRAFT END ---
