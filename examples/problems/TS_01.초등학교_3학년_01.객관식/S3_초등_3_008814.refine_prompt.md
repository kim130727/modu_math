# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008814.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008814

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 페이지형 객관식/선택형 문제 화면이다.
- 상단 왼쪽에 문제 번호가 있고, 그 오른쪽으로 문제 문장이 한 줄 이상 이어진다.
- 중앙에는 컵 모양의 그릇 그림 2개가 좌우로 배치되어 있다.
  - 왼쪽 그림은 `가`
  - 오른쪽 그림은 `나`
- 그림 아래 오른쪽 쪽에 선택지 표기처럼 보이는 `( 가 , 나 ) 그릇` 문구가 있다.
- 하단 왼쪽에는 `(정답)` 표시와 함께 답이 제시되어 있고, 그 아래에 `해설` 문장이 길게 적혀 있다.

[보이는 텍스트]
- `47.`
- `가 그릇과 나 그릇에 물을 가득 채운 후 모양과 크기가 같은 그릇에 옮겨 담았더니 그림과 같이 물이 채워졌습니다. 들어가 더 많은 그릇을 선택하세요.`
  - 화면에 보이는 문장은 줄바꿈되어 있으며, 문장 일부가 다음 줄로 이어진다.
- 그림 옆 표기: `가`, `나`
- 선택지 형태의 문구: `( 가 , 나 ) 그릇`
- `(정답) 나`
- `(해설) 모양과 크기가 같은 그릇에 옮겨 담았을 때 물의 높이가 더 높은 그릇의 들이가 더 많습니다.`

[반복되는 시각 패턴]
- 같은 형태의 그릇 그림이 2개 반복된다.
  - 둘 다 위가 넓고 아래가 좁지 않은, 거의 원통형에 가까운 투명한 컵/비커 모양이다.
  - 둘 다 물이 파란색 음영으로 채워져 있으며, 물 표면이 곡선처럼 표현되어 있다.
- 두 그림 모두 그림 위쪽 또는 옆에 한글 자모 표기 `가`, `나`가 붙어 있어 대상 구분을 돕는다.
- 문제는 “같은 모양과 크기의 그릇” 비교를 위해 시각적으로 두 대상의 물 높이를 비교하도록 반복 구조를 사용한다.

[수식 단계]
- 해당 없음. 수식, 등호, 계산식, 변형 단계는 보이지 않는다.

[등호별 변환 의미]
- 해당 없음. 등호로 이어진 식 변환이 없다.

[세부 내용]
- 왼쪽 `가` 그릇:
  - 투명한 용기 그림 안에 물이 아래쪽 절반보다 약간 낮게 차 있는 모습으로 보인다.
  - 물 표면은 수평에 가깝고 약간 곡선 처리되어 있다.
- 오른쪽 `나` 그릇:
  - 같은 크기와 형태의 용기이며, 왼쪽보다 물 높이가 더 높게 채워져 있다.
  - 물 표면 역시 곡선 처리되어 있고 왼쪽보다 윗부분에 위치한다.
- 두 그림은 같은 기준선 위에 놓인 것으로 보이며, 높이 비교가 가능하도록 나란히 배치되어 있다.
- 문제의 의도는 그림에서 보이는 물 높이 차이를 바탕으로 “들이가 더 많은 그릇”을 고르게 하는 형식으로 해석된다.
  - 다만 이 해석은 문제 문맥에 따른 의미이며, 답 자체를 계산하거나 확정하는 방식으로 쓰지 않는다.
- 하단의 `(정답) 나`는 화면에 인쇄된 정답 표시로 보인다.
- `해설` 문장은 “같은 모양과 크기의 그릇에 물을 옮겼을 때 물 높이가 더 높은 그릇이 들이가 더 많다”는 일반 원리를 설명한다.

[Python DSL 작성 힌트]
- 상단 텍스트 영역과 그림 영역, 하단 정답/해설 영역을 분리해서 배치하는 구성이 적절하다.
- 두 개의 그릇 도형은 좌우 대칭에 가깝게 배치하고, 각 도형 위쪽 근처에 작은 원형/문자 라벨 `가`, `나`를 둔다.
- 각 그릇은 외곽선, 내부 물채움, 물 표면 곡선이 구분되도록 레이어를 나누면 좋다.
- 선택지 문구 `( 가 , 나 ) 그릇`은 오른쪽 중간 영역에 별도 텍스트로 배치한다.
- 정답 표시는 본문과 다른 작은 하단 텍스트 블록으로 처리한다.
- 해설 문장은 하단에 한 줄 또는 두 줄로 배치한다.
- 그림의 물 높이는 정밀한 수치가 아니라 상대적 높이 차이를 표현하는 것이 핵심이다.

[주의할 점]
- 답을 추론해서 적지 말고, 화면에 실제로 보이는 것만 기록해야 한다.
- 문제 문장 중 일부는 줄바꿈 때문에 읽는 방식이 달라질 수 있으니, 의미를 임의로 다듬지 않는 것이 좋다.
- 용기 내부의 정확한 용적 비율을 숫자로 환산하지 말아야 한다.
- 물의 높이 차이는 보이지만, 이를 수치화하거나 계산식으로 바꾸지 않는다.
- `(정답) 나`는 시각 자료에 포함된 텍스트이지만, 분석 초안에서는 정답 자체를 근거로 문제를 푸는 서술을 피한다.

[불확실한 부분]
- 문제 본문 마지막 문장은 화면상 약간 작게 보여 일부 글자가 붙어 읽힐 수 있다. 문장 연결은 보이는 범위 내에서만 해석했다.
- 그릇의 외곽선은 실제로는 약간의 곡선과 그림자 효과가 있으나, 세부 곡률은 이미지 해상도 때문에 완전히 확정하기 어렵다.
- `가`, `나` 표시는 작은 원 안의 문자처럼 보이는데, 정확히 원형 테두리인지 단순 문자 강조인지 다소 अस्पष्ट하다.

--- RAW VISION DRAFT END ---
