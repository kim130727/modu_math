# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008838.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008838

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 화면 상단에 문제 번호 **“80.”**이 보이고, 그 오른쪽으로 긴 문장형 질문이 이어집니다.
- 중앙에는 **양팔저울 그림 2개**가 좌우로 나란히 배치되어 있습니다.
  - 왼쪽 저울: 왼쪽 접시에 **양파**, 오른쪽 접시에 **가지**가 놓여 있습니다.
  - 오른쪽 저울: 왼쪽 접시에 **오이**, 오른쪽 접시에 **양파**가 놓여 있습니다.
- 저울 그림 아래에는 선택 문장이 2줄로 제시되어 있고, 각 줄 안에 **괄호와 쉼표로 된 선택형 빈칸**처럼 보이는 부분이 있습니다.
- 그 아래에는 **(해설)**로 시작하는 설명 문단이 있으며, 마지막에는 **(정답)** 표시와 함께 답이 제시되어 있습니다.

# [보이는 텍스트]
다음 텍스트가 화면에 보입니다.

- `80. 양파, 가지, 오이 중에서 가장 가벼운 채소가 무엇인지 알아보는 방법을 설명하고 있습니다. 알맞은 말을 선택하세요.`
- `양파와 가지 중 ( 양파 , 가지 )가 더 가볍고,`
- `오이와 양파 중 ( 오이 , 양파 )가 더 가볍습니다.`
- `(해설)양파와 가지 중 양파를 올려놓은 접시가 내려갔으므로 가지가 더 가볍고, 오이와 양파 중 양파를 올려놓은 접시가 내려갔으므로 오이가 더 가볍습니다.`
- `따라서 가지와 오이의 무게를 저울을 사용하여 비교합니다.`
- `(정답)가지, 오이`

# [반복되는 시각 패턴]
- **저울 비교 그림**이 두 번 반복됩니다.
  - 각 그림은 같은 형태의 양팔저울 구조를 공유합니다.
  - 가운데 지지대, 양쪽 접시, 받침대가 동일한 스타일로 표현되어 있습니다.
- 각 저울 위에는 채소 이름이 적힌 **라벨 박스**가 반복됩니다.
  - 라벨은 연한 색 배경의 작은 직사각형 박스처럼 보입니다.
- 두 그림 모두 저울의 한쪽이 내려간 상태를 나타내며, 이를 통해 **무게 비교 상황**을 시각적으로 반복 제시합니다.
- 아래 문장에서는 **“(   ,   )가 더 가볍다”** 형태가 두 번 반복되어, 보기 중 하나를 고르는 구조입니다.

# [수식 단계]
- 이 문제에는 등호(`=`)로 이어지는 수식 단계는 보이지 않습니다.
- 따라서 수식 변형 과정이나 계산식 전개는 해당되지 않습니다.

# [등호별 변환 의미]
- 등호가 포함된 단계는 보이지 않습니다.
- 변환 의미를 따로 설명할 수 있는 수식 구조는 없습니다.

# [세부 내용]
- 첫 번째 저울:
  - 왼쪽 접시에 **양파**가 놓여 있습니다.
  - 오른쪽 접시에 **가지**가 놓여 있습니다.
  - 저울의 기울어진 상태를 통해 두 물체의 무게 차이를 표현합니다.
- 두 번째 저울:
  - 왼쪽 접시에 **오이**가 놓여 있습니다.
  - 오른쪽 접시에 **양파**가 놓여 있습니다.
  - 역시 한쪽 접시가 내려간 모습으로 비교 결과를 보여줍니다.
- 문장 속 괄호 부분은 선택지처럼 보이며, 실제로는 **둘 중 더 가벼운 채소 이름을 고르는 빈칸형 선택 구조**입니다.
- 해설 문단은 왜 그런 선택을 하는지 설명하는 문장으로 구성되어 있으며, 마지막에 **정답**이 따로 표기되어 있습니다.

# [Python DSL 작성 힌트]
- 전체 레이아웃은 다음처럼 나눠서 구성하면 좋습니다.
  1. 상단 문제 번호와 문제 문장 텍스트
  2. 중앙 좌우 배치의 저울 그림 2개
  3. 각 저울 위의 채소 라벨 박스
  4. 아래쪽의 선택 문장 2줄
  5. 해설 문단과 정답 표기
- 저울 그림은 **좌우 대칭 구조**로 그리되, 실제로는 한쪽이 내려간 기울어진 형태를 반영해야 합니다.
- 각 접시 위의 채소 이미지는 **접시 내부에 배치**하고, 이름표는 채소 위쪽 또는 근처에 작은 박스로 배치하면 좋습니다.
- 선택 문장에서는 괄호 안의 보기들을 **텍스트 박스 또는 선택형 괄호 요소**로 표현하는 것이 적절합니다.
- 해설과 정답은 본문과 분리된 텍스트 블록으로 배치하면 원본 구조와 유사합니다.

# [주의할 점]
- **문제를 풀지 말아야 합니다.** 시각 분석만 작성해야 합니다.
- 괄호 안의 보기 순서나 정답을 새로 추론해 넣지 마세요.
- 저울의 기울어진 방향을 설명할 때는 **보이는 상태만** 근거로 말해야 하며, 원인-결과 해석은 해설에 있는 내용과 분리해서 다뤄야 합니다.
- 텍스트는 줄바꿈이 여러 번 들어가 있으므로, 실제 DSL 작성 시 **문장 단위 줄 배치**를 맞추는 것이 중요합니다.
- 해설에 있는 문장은 문제 본문과 다른 영역이므로, 본문과 구분해서 배치해야 합니다.

# [불확실한 부분]
- 저울 그림의 정확한 기울기 각도는 이미지 해상도상 정밀하게 판독하기 어렵습니다.
- 채소 그림의 세부 색감과 윤곽선은 약간의 차이가 있을 수 있습니다.
- 각 라벨 박스의 정확한 여백과 정렬은 이미지 크기와 렌더링 방식에 따라 달라질 수 있습니다.
- 해설 문단의 줄바꿈 위치는 화면 확대/축소에 따라 약간 다르게 보일 수 있습니다.

--- RAW VISION DRAFT END ---
