# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008931.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008931

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 번호와 지시문이 한 줄~두 줄로 배치되어 있다.
- 그 아래 중앙에 제목처럼 보이는 **“종류별 책의 수”**가 있고, 바로 아래에 연한 분홍색 배경의 **표**가 있다.
- 표 아래에는 말풍선이 달린 캐릭터 3명이 가로로 나란히 배치되어 있다.
- 화면 하단 왼쪽에는 **“(정답) 연재”**가 보이고, 그 아래에는 **“(해설)”**로 시작하는 설명 문장이 있다.
- 전체적으로는 **표 읽기 + 말풍선에 적힌 말의 진위 판단**을 요구하는 구성으로 보인다.

[보이는 텍스트]
- 문제 번호: **99.**
- 지시문: **상희네 교실에 책이 종류별로 몇 권 있는지 조사하였습니다. 상희, 정국, 연재 중 표를 보고 알 수 있는 내용을 잘못 말한 사람을 선택하세요.**
- 표 제목: **종류별 책의 수**
- 표 머리글:
  - **종류**
  - **동화책**
  - **위인전**
  - **과학책**
  - **동시집**
  - **합계**
- 표의 수치:
  - **책의 수(권)**
  - **35**
  - **22**
  - **15**
  - **10**
  - **82**
- 말풍선 1: **동화책이 가장 많아.**
- 말풍선 2: **우리 반에 있는 책은 모두 82권이야.**
- 말풍선 3: **위인전은 과학책보다 7권 더 적어.**
- 이름 표지:
  - **상희**
  - **정국**
  - **연재**
- 하단 표기:
  - **(정답) 연재**
  - **(해설) 위인전은 22권, 과학책은 15권이므로 위인전은 과학책보다 22-15=7(권) 더 많습니다.**
    - 주의: 이 문장은 화면에 실제로 보이는 해설 텍스트이며, 계산 결과를 보여 주는 형태다.

[반복되는 시각 패턴]
- 말풍선 3개가 거의 동일한 크기와 형태로 반복된다.
- 각 말풍선 아래에 어린이 캐릭터와 이름표가 1개씩 배치되어 있어, 발화자와 이름을 연결하는 구조가 반복된다.
- 표는 열 머리글과 수치 행이 1개씩 있는 **2행 구조**로 보이며, 각 항목이 가로 방향으로 나열된다.
- 이름표는 모두 둥근 사각형 형태의 테두리 안에 들어 있다.
- 표의 수치는 각 열 아래에 가운데 정렬된 형태로 반복 배치되어 있다.

[수식 단계] (해당될 때)
- 하단 해설에 수식 형태가 1회 보인다:
  - **22 - 15 = 7(권)**
- 이 식은 위인전과 과학책의 차이를 나타내는 형태로 적혀 있다.
- 등호 뒤에 단위 **(권)** 이 붙어 있다.
- 화면에는 이외의 연속된 변형식이나 단계별 등호 사슬은 보이지 않는다.

[등호별 변환 의미] (해당될 때)
- **22 - 15 = 7(권)**  
  - 보이는 의미: 22권과 15권의 차이가 7권임을 적은 식이다.
  - 화면의 해설 문장에서는 이 결과를 바탕으로 “더 많습니다”라는 표현을 설명하고 있다.
- 등호가 여러 번 이어진 형태는 아니어서, 단계별 변환은 하나만 확인된다.

[세부 내용]
- 표는 연한 분홍색 배경과 옅은 선으로 구획되어 있다.
- 표의 가장 왼쪽 열은 항목 이름을 적는 칸이고, 그 오른쪽에 책 종류별 수량이 순서대로 배치되어 있다.
- 책 종류는 **동화책, 위인전, 과학책, 동시집, 합계** 순서로 보인다.
- 수치 행은 각각 **35, 22, 15, 10, 82**로 읽힌다.
- 말풍선 3개는 모두 위쪽을 향한 꼬리 모양이 있으며, 각 캐릭터의 머리 위에 연결되어 있다.
- 왼쪽 캐릭터의 이름표는 **상희**, 가운데는 **정국**, 오른쪽은 **연재**로 보인다.
- 말풍선 내용의 위치는 캐릭터마다 동일한 구조를 따르며, 글씨는 말풍선 안에서 여러 줄로 줄바꿈되어 있다.
- 오른쪽 말풍선은 위인전과 과학책의 비교를 말하고, 가운데 말풍선은 전체 합계 수를 말하고, 왼쪽 말풍선은 가장 많은 종류를 말하는 형태로 보인다.
- 하단의 **(정답)** 표기는 문제의 정답을 따로 표시한 것으로 보이며, 그 아래 **(해설)** 은 풀이 설명 영역이다.

[Python DSL 작성 힌트]
- 표 영역은 **표 제목 1개 + 2행 6열 구조**로 잡는 것이 좋아 보인다.
- 셀 배치가 중요하므로:
  - 첫 행에 열 제목,
  - 둘째 행에 수치
  형태로 분리해서 표현할 수 있다.
- 말풍선은 3개를 각각 독립 객체로 두고, 각 객체에:
  - 위치,
  - 말풍선 텍스트,
  - 아래 인물,
  - 이름표
  를 묶어서 표현하면 좋다.
- 캐릭터는 얼굴/머리카락/옷/이름표가 보이지만, 아주 정밀한 그림 묘사보다 **말풍선과 이름 연결**이 핵심이다.
- 문제문과 표, 말풍선, 하단 해설을 세로 순서대로 배치하는 구조가 필요하다.
- 해설 문장에 수식이 포함되어 있으므로, 텍스트 객체와 수식 객체를 분리해도 좋다.
- 정답 표기는 별도 텍스트로 둬서 문제 본문과 구분하는 것이 좋다.

[주의할 점]
- 이 문제의 핵심은 **누가 잘못 말했는지 찾는 것**이지만, 여기서는 실제로 누구인지 풀이하지 말아야 한다.
- 표의 숫자와 말풍선 문장은 **그대로 옮겨 적는 수준**으로만 다뤄야 한다.
- 하단 해설은 화면에 보이는 텍스트이지만, 이를 근거로 문제의 답을 다시 산출하거나 재진술하지 말아야 한다.
- “가장 많아”, “모두 82권이야”, “7권 더 적어” 같은 표현은 **표의 수치와 비교 관계**를 나타내는 문장으로 보이지만, 그 참/거짓 판정은 수행하지 않는다.
- 표의 제목과 본문 문제 문장은 줄바꿈이 되어 있어 한 줄 텍스트로만 처리하지 않도록 주의한다.
- 말풍선 내부 줄바꿈 위치는 실제 이미지와 완전히 같지 않아도 되지만, 문장 단위 분리는 보존하는 것이 좋다.

[불확실한 부분]
- 캐릭터의 표정, 머리 모양, 옷의 정확한 색감과 장식은 일부만 명확하고 세부 디테일은 다소 흐릴 수 있다.
- 표의 선 두께와 셀 경계는 연한 분홍색 계열로 보이지만, 정확한 색상 코드는 이미지 만으로 확정하기 어렵다.
- 하단 해설의 줄바꿈 위치는 이미지에서 한 줄로 이어지지 않고 끊겨 보이므로, DSL에서는 임의로 줄을 나눠야 할 수 있다.
- 제목 “종류별 책의 수”의 정확한 크기나 글꼴은 단정하기 어렵다.

--- RAW VISION DRAFT END ---
