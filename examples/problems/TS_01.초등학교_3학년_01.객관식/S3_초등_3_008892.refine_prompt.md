# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008892.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008892

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단에는 문제 번호 `56.`과 함께 긴 설명 문장이 왼쪽부터 한 줄 이상으로 배치되어 있습니다.
- 중앙에는 제목 `일주일 동안 팔린 음식의 수`가 있고, 그 아래에 큰 표/그림 표가 있습니다.
- 표는 세로로 4칸으로 나뉘어 있으며, 각 칸의 위쪽에는 음식 그림(그릇 모양 아이콘)이 쌓여 있고 아래쪽에는 음식 이름이 적혀 있습니다.
- 표 아래에는 그림의 크기 범례가 따로 있으며, 큰 그릇 아이콘 옆에 `100그릇`, 작은 그릇 아이콘 옆에 `10그릇`이 적혀 있습니다.
- 그 아래에는 비교 문장이 있으며, 괄호로 된 선택형 빈칸 ` ( 많이 , 적게 ) `가 포함되어 있습니다.
- 하단에는 `(정답)`과 `(해설)` 표시가 따로 있고, 해설 문장 안에도 큰 그릇 아이콘이 한 번 더 들어가 있습니다.

[보이는 텍스트]
- `56. 어느 음식점에서 일주일 동안 팔린 음식의 수를 그림그래프로 나타내었습니다. 음식점 주인은 다음 주에 음식의 재료를 어떻게 준비하면 좋을지 알맞은 말을 선택해 보세요.`
- 제목: `일주일 동안 팔린 음식의 수`
- 음식 이름:
  - `김치찌개`
  - `비빔밥`
  - `만둣국`
  - `된장찌` / 다음 줄 `개`  
    - 즉, 줄바꿈되어 `된장찌개`로 읽히는 형태입니다.
- 범례:
  - 큰 그릇 그림 옆 `100그릇`
  - 작은 그릇 그림 옆 `10그릇`
- 문장:
  - `된장찌개의 재료보다 김치찌개의 재료를 더 ( 많이 , 적게 ) 준비합니다.`
- 아래쪽:
  - `(정답)많이`
  - `(해설)` 뒤에 큰 그릇 아이콘이 있고,
    - `(큰 그림)의 수가 많을수록 더 많이 팔린 음식입니다. 된장찌개보다 김치찌개가 더 많이 팔렸으므로 된장찌개의 재료보다 김치찌개의 재료를 더 많이 준비합니다.`

[반복되는 시각 패턴]
- 음식별로 같은 형태의 그릇 그림이 세로로 반복되어 있습니다.
- 각 칸 안에서 그릇 그림은 위에서 아래로 쌓이는 방식으로 배열되어 있으며, 칸마다 개수가 다릅니다.
- 표의 아래쪽에는 음식 이름이 모두 같은 높이의 띠 영역에 들어가 있어, 그림 영역과 이름 영역이 분리되어 보입니다.
- 범례에서도 실제 표의 그림과 같은 모양의 아이콘이 크기만 다르게 반복됩니다.
- 선택형 표현 `( 많이 , 적게 )`는 괄호 안에 두 단어가 쉼표로 나란히 배치된 형태입니다.

[수식 단계]
- 해당 없음.  
- 화면에는 등호 `=`로 이어지는 수식 변형이 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.  
- 등호를 이용한 단계적 변환식이 제시되지 않았습니다.

[세부 내용]
- 표는 연한 테두리로 둘러싸인 직사각형이고, 내부가 4개의 세로 구역으로 나뉘어 있습니다.
- 각 구역의 상단에는 음식 판매량을 나타내는 그릇 아이콘들이 세로로 정렬되어 있습니다.
- 첫 번째 칸은 `김치찌개`, 두 번째는 `비빔밥`, 세 번째는 `만둣국`, 네 번째는 `된장찌개`입니다.
- 네 번째 음식 이름은 칸 폭이 좁아서 `된장찌`와 `개`로 두 줄에 걸쳐 보입니다.
- 범례의 큰 그릇과 작은 그릇은 표 아래 오른쪽 쪽에 배치되어 있으며, 각각 `100그릇`, `10그릇`의 의미를 설명합니다.
- 해설 문장 앞의 큰 그릇 아이콘은 설명 대상인 “큰 그림”을 시각적으로 다시 보여주는 역할을 합니다.
- `(정답)`은 아래쪽 왼편에 따로 표시되어 있고, 정답 텍스트는 `많이`로 보입니다.  
  - 다만 이 값은 보이는 사실로만 적고, 문제 풀이 과정으로 확장하지는 않습니다.

[Python DSL 작성 힌트]
- 상단 문제 문장은 줄바꿈을 포함한 텍스트 블록으로 처리하면 좋습니다.
- 중앙 표는 `4열 × 2행` 구조로 볼 수 있습니다.
  - 첫 행: 그림 영역
  - 둘째 행: 음식 이름 영역
- 각 열의 그림은 동일한 스타일의 그릇 아이콘 여러 개를 세로로 쌓아 배치합니다.
- 음식 이름 셀은 중앙 정렬에 가깝게 배치되어 있으며, 마지막 항목은 줄바꿈 텍스트로 처리해야 합니다.
- 범례는 아이콘 2개와 텍스트 2개를 가로로 배치하는 구조입니다.
- 선택 문장 `(..., ...)`은 괄호와 쉼표를 포함한 텍스트로 그대로 넣되, 빈칸 선택형으로 보이도록 구성할 수 있습니다.
- 해설 영역은 별도 텍스트 블록으로 분리하고, 앞에 아이콘을 인라인으로 배치하는 방식이 적합합니다.
- 표와 범례 사이, 범례와 문장 사이에는 충분한 세로 간격이 있습니다.

[주의할 점]
- 그림의 정확한 개수는 화면에서 작게 보여 판독이 애매할 수 있으므로, 임의로 수치를 단정하지 않는 것이 좋습니다.
- `된장찌개`는 줄바꿈 때문에 두 줄로 보이지만 하나의 음식명입니다.
- `(많이, 적게)` 중 정답을 추론해서 한쪽만 쓰지 말아야 합니다.
- 표 속 그릇 아이콘의 크기 차이와 범례의 크기 차이를 구분해서 기술해야 합니다.
- 해설의 내용은 보이지만, 이를 근거로 수학적 결론을 재구성하지는 않습니다.

[불확실한 부분]
- 각 음식 칸에 있는 그릇 아이콘의 정확한 개수는 이미지 해상도상 다소 흐릿하여, 세어야 하는 경우 오독 가능성이 있습니다.
- 작은 그릇과 큰 그릇의 상대적 비율, 선 두께, 색조는 대략적으로만 확인됩니다.
- `(정답)많이`는 화면에 보이지만, 문제 의도상 자동으로 넣는 형태로 쓰지 않는 것이 안전합니다.
- 표의 테두리 색이 연한 갈색/베이지 계열로 보이지만, 정확한 색상 명칭은 단정하지 않았습니다.

--- RAW VISION DRAFT END ---
