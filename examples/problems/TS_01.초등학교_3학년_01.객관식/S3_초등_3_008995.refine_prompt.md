# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008995.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008995

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 한 문제짜리 초등 수학/도형 판별형 문항이다.
- 상단에 문제 번호와 지시문이 있고, 중앙에는 판별해야 할 도형 그림 1개가 제시되어 있다.
- 하단에는 정답 표시용 선택 기호가 보이며, 그 아래에 해설 문장과 예시 그림이 배치되어 있다.
- 전체적으로 “문제 → 선택 → 설명/예시” 흐름으로 구성되어 있다.

# [보이는 텍스트]
- `33. 정사각형이면 ○, 정사각형이 아니면 ×를 선택하세요.`
- `○` 또는 `×` 선택을 유도하는 문구가 보임
- `(정답)` 표시
- `(해설)네 각이 모두 직각이고 네 변의 길이가 모두 같은 사각형을 정사각형이라고 합니다.`
- 해설 아래에는 정사각형 예시 그림이 있음

# [반복되는 시각 패턴]
- 도형이 2개 보인다.
  - 위쪽 중앙: 회색 테두리의 사각형 1개
  - 아래쪽 왼쪽: 파란 표시가 있는 정사각형 예시 1개
- 선택 표시로 보이는 원형 기호가 2곳에 나타난다.
  - 왼쪽 중간 부근에 파란색 원형 표시
  - 중앙 위 도형 아래에 파란색 `×` 표식
- 해설 문장과 예시 도형이 함께 반복적으로 “정사각형의 조건”을 시각적으로 강조한다.

# [수식 단계]
- 해당 없음.  
- 수식 변형식, 등호 연결, 계산 과정은 보이지 않는다.

# [등호별 변환 의미]
- 해당 없음.  
- 등호가 포함된 식이나 단계적 변환이 보이지 않는다.

# [세부 내용]
- 상단 문제 문장은 왼쪽에서 오른쪽으로 한 줄에 배치되어 있다.
- 문제 번호 `33.` 다음에 지시문이 이어진다.
- 지시문 문장 중 `○`, `×`는 선택해야 할 기호로 보이며, 글자 크기와 색이 본문과 비슷하게 표시된다.
- 중앙의 도형은 정사각형/직사각형처럼 보일 수 있는 사각형 1개이며, 테두리는 연한 회색이고 내부는 비어 있다.
- 도형 아래에는 파란색 `×` 기호가 따로 표시되어 있어, 이 도형이 정사각형이 아님을 선택하는 형식의 화면처럼 보인다.
- 왼쪽 중간에는 파란색 원형 표시가 하나 있고, 그 아래에 `(정답)` 텍스트와 함께 빈 원이 보인다.  
  - 이 영역은 정답 표기 또는 선택 결과를 나타내는 UI처럼 보이지만, 정확한 의미는 화면만으로 단정하기 어렵다.
- `(해설)` 문장은 왼쪽 아래에 큰 글씨로 두 줄에 걸쳐 배치되어 있다.
- 해설 아래 예시 도형은 아래쪽 왼쪽에 위치하며, 사각형의 네 꼭짓점 부근에 작은 파란 표시가 붙어 있어 직각임을 암시하는 시각 요소처럼 보인다.
- 예시 도형은 위쪽 문제 도형보다 아래로 충분히 떨어져 있어, 문제 그림과 해설 예시가 명확히 구분된다.

# [Python DSL 작성 힌트]
- 문제 텍스트를 먼저 배치하고, 그 아래에 판별 대상 도형 1개를 중앙 정렬에 가깝게 놓는 구성이 적절하다.
- 선택 기호 `○`, `×`는 텍스트 요소로 따로 표시하고, 정답/오답 영역처럼 보이는 원형 마커가 있으면 별도 객체로 분리하는 것이 좋다.
- 해설은 문제 본문과 시각적으로 분리된 하단 영역에 배치한다.
- 해설 문장 아래의 예시 도형은 별도 도형 객체로 만들고, 꼭짓점 근처의 파란 표시도 작은 장식 요소로 분리해 표현하면 좋다.
- 상단 도형은 단순 사각형 윤곽선 1개로 표현하고, 내부 채움은 없는 상태로 설정한다.
- 전체 레이아웃은 세로 흐름형이며, 상단 문제 영역 / 중간 선택 영역 / 하단 해설 영역으로 나누면 구현하기 쉽다.

# [주의할 점]
- 정답이 무엇인지 추론하여 채우지 말아야 한다.
- `○`와 `×`의 실제 선택 여부를 확정적으로 해석하지 말아야 한다.
- 위쪽 도형이 정사각형인지 아닌지를 시각 분석에서 단정하지 말고, 보이는 형태만 묘사해야 한다.
- 해설 문장의 의미를 요약할 수는 있지만, 문제 풀이 결과를 대신 제시하면 안 된다.
- 아래쪽 예시 도형의 파란 표시는 직각 표시로 추정되지만, 화면만 보고 정확한 기호 의미를 단정하지 않는 것이 좋다.

# [불확실한 부분]
- 왼쪽 중간의 파란 원형 표시와 `(정답)` 옆 빈 원의 관계는 화면만으로는 정확히 확정하기 어렵다.
- 위쪽 도형이 실제로는 정사각형인지, 직사각형인지, 혹은 정사각형이 아닌 다른 사각형인지 화면 해상도만으로는 단정하기 어렵다.
- 아래쪽 예시 도형의 파란 표시가 정확히 어떤 편집 기호인지(직각 표시, 선택 표시, 강조 표시)는 명확하지 않다.
- 문제 화면 전체가 일부 잘려 보이므로, 좌우/상하 여백의 정확한 범위는 알 수 없다.

--- RAW VISION DRAFT END ---
