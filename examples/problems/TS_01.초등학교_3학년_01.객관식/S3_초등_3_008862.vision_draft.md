[전체적인 구성]
- 화면 상단 왼쪽에 문항 번호가 있고, 그 오른쪽으로 문제 지시문이 한 줄로 이어집니다.
- 중앙에는 굵은 선으로 둘러싸인 큰 사각형의 **보기 영역**이 있습니다.
- 보기 영역 안에는 제목처럼 `<보기>`가 왼쪽 위에 배치되어 있고, 그 아래에 그림 3개가 가로로 나란히 배치되어 있습니다.
- 보기 영역 아래에는 문장형 설명이 이어지며, 괄호와 빈칸처럼 보이는 선택 구조가 있습니다.
- 하단에는 이어지는 문항 번호처럼 `(2)`가 다시 나타나고, 그 옆에 `드럼통`이 보입니다.

[보이는 텍스트]
- `□ 8. <보기>에서 물건을 선택하여 문장을 완성해 보세요.`
- `<보기>`
- 그림 아래 글자:
  - `우유갑`
  - `주스병`
  - `드럼통`
- ` (2) ( 우유갑, 주스병, 드럼통 )의 들이는 약 100L입니다.`
- `(해설)큰 그릇의 들이는 L를, 작은 그릇의 들이는 mL를 사용하여 나타내는 것이 좋습니 다.`
  - 줄바꿈 때문에 `좋습니 다.`처럼 띄어 보이나, 원문상 문장 이어짐으로 보입니다.
- 하단에 다시:
  - `(2) 드럼통`

[반복되는 시각 패턴]
- 보기 영역 안의 3개 물건이 **좌→우** 방향으로 균등하게 배치되어 있습니다.
- 각 물건은 **그림 위/가운데**, **이름 아래**의 형태로 반복됩니다.
- 보기 영역 밖의 문장에서도 `( )` 괄호 구조가 사용되어 선택지를 넣는 형식이 반복됩니다.
- `(2)` 표기가 위아래로 두 번 보이며, 아래쪽은 정답/선택된 항목처럼 보이는 독립 줄입니다.
- 큰 용기 그림은 원통형 몸체와 가로 띠가 반복되어 드럼통의 입체감을 나타냅니다.

[수식 단계]
- 해당 없음: 화면에는 등호 `=`가 포함된 수식 변형 단계가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음: 등호가 나타나는 수식 단계가 보이지 않습니다.

[세부 내용]
- 보기 상자:
  - 테두리가 있는 큰 직사각형 내부에 `<보기>`가 좌상단에 위치합니다.
  - 내부 중앙에는 세 개의 아이콘이 있고, 각 아이콘은 서로 다른 크기와 형태입니다.
- 왼쪽 그림:
  - 작은 사각형 우유팩 형태.
  - 아래 라벨은 `우유갑`.
- 가운데 그림:
  - 길쭉한 병 모양.
  - 아래 라벨은 `주스병`.
- 오른쪽 그림:
  - 큰 원통형 통 그림.
  - 아래 라벨은 `드럼통`.
- 보기 아래 문장:
  - `(2)`가 먼저 나오고, 이어서 괄호 `(` `)` 안에 `우유갑, 주스병, 드럼통`이 쉼표로 나열됩니다.
  - 그 뒤에 `의 들이는 약 100L입니다.`가 이어져 하나의 문장이 됩니다.
  - 이 구조상 괄호 안에서 보기 항목 중 하나를 선택해 문장을 완성하는 형태로 보입니다.
- 해설 문장:
  - `큰 그릇`과 `작은 그릇`을 구분하여, 들이의 단위로 `L`와 `mL`를 쓰는 것이 좋다고 안내합니다.
  - 이는 문제의 선택 기준을 설명하는 보조 문장으로 보입니다.
- 하단 `(2) 드럼통`:
  - 별도의 답안 표기 영역처럼 보이며, 문항 번호와 선택된 물건 이름만 적혀 있습니다.
  - 다만 이것이 인쇄된 정답인지, 편집상 표시인지 확정할 수는 없습니다.

[Python DSL 작성 힌트]
- 큰 사각형 보기 박스를 먼저 그립니다.
- 박스 내부 좌상단에 `<보기>` 텍스트를 배치합니다.
- 박스 안에 3개의 그림을 좌우로 정렬합니다:
  1. 왼쪽 작은 우유갑 그림 + 아래 `우유갑`
  2. 가운데 주스병 그림 + 아래 `주스병`
  3. 오른쪽 큰 드럼통 그림 + 아래 `드럼통`
- 각 그림은 아래 라벨과 수직으로 묶어서 배치하는 것이 자연스럽습니다.
- 보기 박스 아래에는 문제 문장을 한 줄로 배치하고, 괄호 안의 선택지 텍스트를 그대로 살립니다.
- 하단의 `(2) 드럼통`은 별도 줄 텍스트로 분리해 배치합니다.
- 문장 중 `L`, `mL`는 영문 대문자/소문자 구분을 유지해야 합니다.
- 줄바꿈 위치 때문에 문장이 깨져 보이는 부분은 시각적으로만 분리하고, 텍스트는 원문 흐름을 고려해 배치합니다.

[주의할 점]
- 문제를 풀어서 답을 확정하지 마세요.
- 괄호 안의 항목이 무엇을 의미하는지 추론해도, 최종 선택값을 단정하지 마세요.
- 하단의 `(2) 드럼통`이 정답 표기인지 별도 보기인지 확정하지 말고, 보이는 그대로만 다루세요.
- `좋습니 다.`처럼 보이는 부분은 줄바꿈으로 인한 분리일 수 있으므로, 임의로 문장을 재작성할 때 주의하세요.
- 그림의 실제 색감이나 세부 아이콘 형태는 참고용으로만 적고, 정확한 도안 재현이 어렵다면 단정하지 마세요.

[불확실한 부분]
- 보기 아래 `(2) ( 우유갑, 주스병, 드럼통 )의 들이는 약 100L입니다.`에서 앞의 `(2)`가 문항 번호의 일부인지, 문장 번호인지 화면만으로 완전히 확정하기 어렵습니다.
- 하단의 `(2) 드럼통`이 인쇄된 답안 표시인지, 별도의 연습용 표기인지 불명확합니다.
- 보기 영역의 정확한 내부 여백과 각 그림의 픽셀 위치는 이미지 해상도상 대략적으로만 파악됩니다.
- 해설 문장의 정확한 줄바꿈은 이미지에서 끊겨 보여서, 원문 전체 띄어쓰기와 개행을 100% 확정하기는 어렵습니다.
