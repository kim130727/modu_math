# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008990.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008990

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 한 장의 학습 문제 화면으로 보입니다.
- 상단에는 문제 번호와 지시문이 있고, 가운데에는 도형 선택용 예시가 2개 배치되어 있습니다.
- 왼쪽에는 원형 표시, 오른쪽에는 도형 그림과 그 아래 선택 표시가 있으며, 아래쪽에는 정답/해설 영역이 따로 보입니다.
- 최하단에는 개념 설명 문장이 크게 배치되어 있습니다.
- 전체적으로 “도형이 직사각형인지 아닌지 판단하여 기호를 고르는” 선택형 문제 형식입니다.

# [보이는 텍스트]
보이는 텍스트를 위에서 아래 순서로 정리하면 다음과 같습니다.

- `28.`
- `직사각형이면 ○, 직사각형이 아니면 ×를 선택하세요.`
- 왼쪽 중간에 파란색 원 안의 기호처럼 보이는 `◎` 비슷한 표시가 있음
- 오른쪽 도형 아래에 파란색 원 안의 `×` 표시
- `(정답) ×`
- `(해설)`
- `네 각이 모두 직각인 사각형을 직사각형이라고 합니다.`

주의: 일부 기호는 인쇄/해상도 때문에 정확한 글자 모양이 다소 흐릴 수 있습니다. 다만 전체 의미는 “○ 또는 × 선택” 안내로 보입니다.

# [반복되는 시각 패턴]
- 파란색 원형 선택 표시가 반복적으로 등장합니다.
- 오른쪽 도형 아래에는 파란색 원 속 `×`가 있고, 왼쪽 중간에는 파란색 원 속 다른 기호가 배치되어 있습니다.
- 아래쪽 해설 영역에는 `(정답)`과 `(해설)`이 괄호로 구분되어 반복적인 문제 풀이 구성 요소처럼 보입니다.
- 도형 그림은 선으로만 그린 단순 윤곽선이며, 내부 채움은 없습니다.
- 직각을 나타내는 작은 사각형 표시가 해설용 도형에만 강조되어 있습니다.

# [수식 단계]
- 해당 없음.  
- 이 화면에는 등호를 포함한 수식 변형 단계가 보이지 않습니다.

# [등호별 변환 의미]
- 해당 없음.  
- 등호가 있는 연산식이나 단계별 변형식은 보이지 않습니다.

# [세부 내용]
- 상단 문제 문장은 “직사각형이면 ○, 직사각형이 아니면 ×를 선택하세요.”로, 조건에 따라 기호를 고르는 지시입니다.
- 오른쪽 위의 도형은 네 변으로 이루어진 사각형 형태이지만, 직사각형인지 판단하는 기준과 연결되는 예시로 보입니다.
- 왼쪽 아래에는 `(정답) ×`가 표시되어 있어, 선택 결과가 틀림표시임을 보여주는 구조입니다.
- `(해설)` 아래의 도형은 직각 표시가 들어간 사각형 형태로, 직사각형의 성질을 설명하는 예시처럼 보입니다.
- 최하단 문장은 직사각형의 정의를 설명하는 문장입니다.
  - 보이는 사실: `네 각이 모두 직각인 사각형을 직사각형이라고 합니다.`
  - 의미 추론: 직사각형을 판단할 때 “네 각이 모두 직각인지”가 기준임을 설명합니다.
- 도형 배치상, 위쪽은 선택 대상, 아래쪽은 정답 및 개념 설명 역할로 나뉘어 있습니다.

# [Python DSL 작성 힌트]
- 문제 영역을 먼저 크게 캡처한 뒤, 상단 지시문과 중간 도형 2개, 하단 정답/해설 영역을 각각 분리해서 표현하면 좋습니다.
- 도형은 선분 기반 다각형으로 처리하고, 직각 표시용 작은 사각형도 별도 도형 객체로 넣는 것이 좋습니다.
- 파란색 원형 선택 표시는 텍스트가 아니라 아이콘/마커로 따로 배치하는 방식이 적합합니다.
- `(정답)`, `(해설)`은 텍스트 라벨로 분리해 하단에 정렬합니다.
- 최하단 개념 문장은 한 줄의 큰 텍스트로 처리하면 됩니다.
- 좌우 배치가 중요하므로, 상단 안내문, 중앙 선택 도형, 하단 설명문을 세로 순서대로 정리하는 것이 좋습니다.

# [주의할 점]
- 정답으로 표시된 기호를 답안으로 추론해서 따로 채우지 마세요.
- 도형의 정확한 기울기나 변 길이는 이미지에서 보이는 윤곽만 기준으로 기술하세요.
- 파란색 원 안의 기호는 해상도 때문에 일부 अस्प명확할 수 있으므로, 정확한 문자 판독은 보수적으로 다루세요.
- 해설용 도형의 직각 표시가 일부만 보여도, 보이는 범위 내에서만 설명하세요.
- 문제를 풀거나 정답을 재계산하는 방식으로 해석하지 마세요.

# [불확실한 부분]
- 왼쪽 중간의 파란색 원 안 기호는 해상도 때문에 정확히 `◎`인지, 다른 선택 표식인지 단정하기 어렵습니다.
- 오른쪽 위 도형의 변 각도와 정확한 사각형 분류는 그림만 보고 보조적으로 추정할 수는 있으나, 여기서는 단정하지 않는 것이 좋습니다.
- `(정답) ×` 표시는 선명하지만, 이것이 최종 출력용 정답인지, 채점 예시인지 화면 맥락상 추가 확인이 필요할 수 있습니다.
- 하단 해설 도형의 일부 변은 화면 가장자리와 겹쳐 보일 수 있어 완전한 외곽선을 100% 확정하기 어렵습니다.

--- RAW VISION DRAFT END ---
