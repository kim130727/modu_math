# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009021.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009021

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 문제 번호가 왼쪽 위에 있고, 그 오른쪽에 문제 지문이 한 줄로 이어지다가 줄바꿈되어 있습니다.
- 중앙 상단에는 연보라색 배경의 큰 직사각형 박스가 있으며, 그 안에 보기 3개가 세로로 나열되어 있습니다.
- 박스 오른쪽 아래 바깥쪽에는 선택지 표기처럼 보이는 `( ㄱ , ㄴ , ㄷ )` 형태가 가로로 배치되어 있습니다.
- 하단 왼쪽에는 `(정답)`과 `(해설)`이 따로 표시되어 있고, 해설 부분에는 짧은 선분/괄호 같은 표시와 함께 문장 설명이 2줄 이상 이어집니다.
- 전체적으로 “보기 선택형 + 해설 제시형” 레이아웃입니다.

[보이는 텍스트]
- `59. 반직선을 읽는 방법에 대한 설명으로 옳은 것을 찾아 기호를 선택하세요.`
- 보라색 박스 안:
  - `ㄱ 반직선은 두 점 중 어느 점을 먼저 읽어도 상관없습니다.`
  - `ㄴ 반직선 ㄴㄱ이라고 읽을 수 있습니다.`
  - `ㄷ 반직선은 반드시 시작점을 먼저 읽습니다.`
- 박스 오른쪽 아래:
  - `( ㄱ , ㄴ , ㄷ )`
- 하단 왼쪽:
  - `(정답)ㄷ`
  - `(해설)반직선 ㄱㄴ`
  - `반직선 ㄴㄱ`
  - `ㄱ 반직선은 반드시 시작점을 먼저 읽습니다.`
  - `ㄴ 반직선 ㄴㄱ은 반직선 ㄴㄱ으로 읽을 수 없습니다.`
  - `따라서 옳은 것은 ㄷ입니다.`
- 해설 중간에는 `ㄱㄴ`, `ㄴㄱ`을 나타내는 듯한 짧은 선/화살표 비슷한 표시가 각각 따로 보입니다.
- 해설 맨 아래 문장 끝에 `ㄷ`가 동그라미처럼 강조된 형태로 들어가 있습니다.

[반복되는 시각 패턴]
- 보기 기호 `ㄱ, ㄴ, ㄷ`가 반복적으로 사용됩니다.
- `반직선`이라는 단어가 보기와 해설에서 여러 번 반복됩니다.
- 해설에서는 같은 형태의 짧은 선분 표시가 두 번 반복되어, 서로 다른 읽기 방향을 비교하는 구조로 보입니다.
- 선택지 표기 `( ㄱ , ㄴ , ㄷ )`는 보기의 기호를 다시 한 번 묶어 제시하는 반복 패턴입니다.

[수식 단계]
- 해당 없음.  
- 숫자 계산식이나 등호를 이용한 변형 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.  
- 등호가 있는 식 변형은 보이지 않습니다.

[세부 내용]
- 문제는 반직선을 읽는 방법에 대한 설명 중 옳은 것을 찾는 객관식입니다.
- 보라색 박스 안의 각 항목은 서로 독립된 진술이며, 각 항목 앞에 `ㄱ`, `ㄴ`, `ㄷ` 기호가 붙어 있습니다.
- `ㄱ` 항목은 “두 점 중 어느 점을 먼저 읽어도 상관없다”는 취지의 문장입니다.
- `ㄴ` 항목은 반직선을 `ㄴㄱ`이라고 읽을 수 있다는 내용처럼 보입니다.
- `ㄷ` 항목은 반직선은 반드시 시작점을 먼저 읽는다는 취지의 문장입니다.
- 하단 해설에는 두 개의 반직선 표기가 나란히 제시되고, 그 아래에 `ㄱ`, `ㄴ` 항목에 대한 판단이 문장으로 정리되어 있습니다.
- 마지막에 `따라서 옳은 것은 ㄷ입니다.`라고 결론이 제시되어 있습니다.
- 해설 왼쪽의 짧은 선분 모양 표시는 반직선의 방향 또는 시작점 표시를 시각적으로 나타내는 장치로 보입니다.

[Python DSL 작성 힌트]
- 상단 문제문 텍스트, 중앙의 연보라색 박스, 하단 해설 영역을 분리해서 배치하면 좋습니다.
- 보기 박스는 하나의 큰 직사각형으로 그리고, 내부에 3개의 문장을 세로 정렬로 배치합니다.
- 각 보기 앞에는 `ㄱ`, `ㄴ`, `ㄷ` 기호를 원형 또는 동그라미 느낌의 강조와 함께 넣을 수 있습니다.
- 박스 바깥의 `( ㄱ , ㄴ , ㄷ )`는 오른쪽 아래에 따로 배치합니다.
- 하단 해설은 `(정답)`, `(해설)` 라벨을 따로 두고, 그 아래에 설명 문장을 줄바꿈하여 넣습니다.
- 해설에 있는 반직선 표시가 필요하다면, 짧은 선분과 한쪽 끝의 점/표시를 이용해 방향성을 나타내는 도형으로 구성하면 됩니다.
- 글자 줄바꿈이 여러 군데 있으므로, 한 줄에 너무 길지 않게 좌우 여백을 고려해 배치하는 것이 좋습니다.

[주의할 점]
- 정답을 새로 추론해서 쓰지 말고, 화면에 보이는 값만 그대로 반영해야 합니다.
- 보기 문장은 유사해 보여도 단어 순서가 다르므로 그대로 옮겨야 합니다.
- 해설의 도형/표시는 정확한 기하 요소라기보다 설명용 삽화일 수 있으므로 과도하게 해석하지 말아야 합니다.
- `ㄴ` 항목의 문장은 이미지에서 다소 붙어 보이므로, 텍스트 분리에 주의해야 합니다.
- 보라색 박스의 배경색과 직사각형 형태는 명확하지만, 정확한 색상 값은 단정하지 않는 것이 좋습니다.

[불확실한 부분]
- 하단 해설의 짧은 선분/점 표시는 반직선의 정확한 방향 표식인지, 단순한 강조선인지 완전히 확정하기 어렵습니다.
- `ㄴ` 보기의 문장 끝부분이 이미지 해상도상 약간 붙어 보여, 띄어쓰기나 기호 위치를 100% 확정하기 어렵습니다.
- `(정답)ㄷ`에서 정답 기호 주변의 원형 강조는 인쇄 스타일에 따른 것으로 보이지만, 정확히 어떤 도형인지 단정하기 어렵습니다.

--- RAW VISION DRAFT END ---
