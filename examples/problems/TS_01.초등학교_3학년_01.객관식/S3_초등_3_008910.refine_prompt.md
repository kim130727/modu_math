# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008910.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008910

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 번호가 있고, 그 오른쪽으로 긴 문장형 질문이 2줄에 걸쳐 배치되어 있습니다.
- 중앙에는 **표**가 크게 배치되어 있으며, 표 위에 제목이 있습니다.
- 표 아래 오른쪽에는 보기처럼 보이는 동물 이름 목록이 괄호와 함께 한 줄로 적혀 있습니다.
- 화면 하단 왼쪽에는 `(정답)` 표시가 있고, 그 옆에 정답이 텍스트로 제시되어 있습니다.
- 그 아래에는 `(해설)`이 있으며, 한두 줄의 문장으로 풀이 설명이 이어집니다.
- 전체적으로 “문제 → 표 → 정답/해설” 순서의 구성입니다.

[보이는 텍스트]
- `□ 76.`  
- `태희네 반 학생들이 기르고 싶은 동물을 조사하여 표로 나타냈습니다.`
- `가장 많은 여학생들이 기르고 싶은 동물을 선택해 보세요.`
- 표 제목: `기르고 싶은 동물별 학생 수`
- 표 머리글:
  - `동물`
  - `강아지`
  - `고양이`
  - `햄스터`
  - `앵무새`
  - `합계`
- 표의 행 이름:
  - `남학생 수(명)`
  - `여학생 수(명)`
- 표 안의 수치:
  - 남학생 수: `5, 1, 4, 3, 13`
  - 여학생 수: `6, 8, 2, 1, 17`
- 표 아래 괄호 안 텍스트:
  - `( 강아지 , 고양이 , 햄스터 , 앵무새 )`
- 하단:
  - `(정답) 고양이`
  - `(해설) 여학생 수에서 가장 큰 수를 찾으면 8이므로 가장 많은 여학생들이 기르고 싶은 동물은 고양이입니다.`

[반복되는 시각 패턴]
- 표는 열 머리글이 맨 위에 있고, 그 아래로 두 개의 데이터 행이 반복되는 전형적인 격자 구조입니다.
- 동물 이름이 열 방향으로 나열되어 있고, 각 동물에 대해 남학생 수와 여학생 수가 각각 한 칸씩 대응됩니다.
- 마지막 열 `합계`는 각 행의 총합처럼 보이는 값이 들어가 있어, 다른 열들과 성격이 조금 다릅니다.
- 표 바깥 아래에는 동물 이름들이 괄호로 다시 한 번 묶여 있으며, 순서가 표의 열 순서와 대응됩니다.
- 정답과 해설이 문제 본문과 분리되어 아래쪽에 따로 배치되어 있습니다.

[수식 단계]
- 이 문제 화면에는 등호를 포함한 수식 변형 단계가 보이지 않습니다.
- 계산식 대신 표의 수치 비교를 통해 답을 찾는 형식으로 보입니다.

[등호별 변환 의미]
- 해당 없음. 화면에 연쇄적인 등호 식이 제시되어 있지 않습니다.

[세부 내용]
- 문제 지시문은 “가장 많은 여학생들이 기르고 싶은 동물”을 찾으라고 되어 있으므로, 표의 **여학생 수** 행이 핵심 정보로 보입니다.
- 표는 동물별로 수치를 비교할 수 있도록 열 단위로 정리되어 있습니다.
- `합계` 열은 남학생 수와 여학생 수를 합친 값처럼 보이지만, 이 초안에서는 실제 계산을 확정하지 않습니다.
- 표의 아래쪽 괄호 목록 `( 강아지 , 고양이 , 햄스터 , 앵무새 )`는 선택지 또는 참조용 나열처럼 보입니다.
- `(정답) 고양이`와 해설 문장은 이미 결과가 인쇄된 상태이며, 문제 영역과 구분되어 있습니다.

[Python DSL 작성 힌트]
- 표를 먼저 큰 박스로 만들고, 상단에 제목을 가운데 정렬로 배치하는 구성이 적절합니다.
- 표는 6열 구조로 보이며, 첫 열은 행 이름, 나머지 5열은 `강아지/고양이/햄스터/앵무새/합계` 순서입니다.
- 행은 3줄 구성으로 보입니다: 머리글 1줄 + 데이터 2줄.
- 각 칸은 테두리가 있는 셀로 표현하고, 숫자는 가운데 정렬로 배치하는 것이 자연스럽습니다.
- 표 아래의 괄호 목록은 별도의 텍스트 줄로 처리하면 됩니다.
- 하단의 `(정답)`과 `(해설)`은 문제 본문과 분리된 설명 영역으로 따로 배치하세요.

[주의할 점]
- 정답을 추론해서 다시 쓰는 과정은 피해야 하지만, 이 화면 자체에는 이미 정답과 해설이 인쇄되어 있으므로 “보이는 텍스트”로만 옮겨 적어야 합니다.
- 표 안 숫자 `13`, `17`은 합계처럼 보이더라도, 이 분석 초안에서는 계산 과정을 새로 만들지 마세요.
- 문제에서 요구하는 핵심은 “가장 많은 여학생”과 관련된 비교이지만, 여기서는 답을 논리적으로 확정해 설명하지 말고 시각적으로만 언급하세요.
- `□ 76.`의 작은 사각형 기호는 문제 표시용 아이콘처럼 보입니다.

[불확실한 부분]
- 표의 선 색과 정확한 선 두께는 이미지 해상도상 세밀하게 단정하기 어렵습니다.
- 괄호 안 목록이 선택지인지 보조 표기인지 시각적으로는 보이지만, 의도는 문맥상 추정일 뿐입니다.
- `(정답)`과 `(해설)` 영역의 글자 크기 차이는 보이지만, 정확한 폰트 종류까지는 확정할 수 없습니다.

--- RAW VISION DRAFT END ---
