# [문제 개요]
- 문제 ID: `S3_초등_3_008992`
- 문제 번호 `30.`이 왼쪽 상단에 보입니다.
- 지시문은 `직각삼각형을 찾아 기호를 선택하세요.`입니다.
- 오른쪽에는 연한 파란색 격자 배경 위에 삼각형 3개가 가로로 배치되어 있습니다.
- 각 삼각형 내부에는 각각 `가`, `나`, `다`라는 글자가 들어 있습니다.
- 아래쪽에는 선택지처럼 `( 가, 나, 다 )`가 보입니다.
- 왼쪽 아래에는 `(정답)가`와 `(해설) 한 각이 직각인 삼각형은 가입니다.`가 인쇄되어 있습니다.

# [원본에 보이는 요소]
## 보이는 사실
- 상단 왼쪽: `30.`
- 상단 문장: `직각삼각형을 찾아 기호를 선택하세요.`
- 도형 영역: 연한 파란색 격자 직사각형
- 격자 안의 삼각형 3개
  - 첫 번째 삼각형 내부: `가`
  - 두 번째 삼각형 내부: `나`
  - 세 번째 삼각형 내부: `다`
- 도형 아래: `( 가, 나, 다 )`
- 하단 왼쪽: `(정답)가`
- 하단 왼쪽 해설: `(해설) 한 각이 직각인 삼각형은 가입니다.`

## 추론되는 수학적 의미
- 문제는 삼각형의 모양을 보고 직각삼각형을 고르는 형식입니다.
- 격자 배경은 삼각형의 변 방향과 각의 형태를 비교하도록 돕는 보조 요소로 보입니다.
- 하단의 해설 문장은 `가`를 정답으로 제시하는 인쇄된 문장입니다.

# [수식/관계 구조]
- 수식 전개나 등호 연쇄는 보이지 않습니다.
- 관계 구조는 다음처럼 해석할 수 있습니다.
  - 삼각형 3개가 제시됨
  - 각 삼각형은 기호 `가, 나, 다` 중 하나와 1:1로 대응됨
  - 그중 직각삼각형을 찾는 구조

# [등호별 변환 의미]
- 해당 없음.
- 등호를 포함한 변형식이 보이지 않습니다.
- 단계적 계산 변환도 없습니다.

# [빈칸 정책]
- 이 문제에는 빈칸 형태의 답란은 보이지 않습니다.
- 다만 DSL 작성 시에도 보이지 않는 값은 추론으로 채우지 않고, 원본에 보이는 텍스트만 반영해야 합니다.
- 정답이 하단에 인쇄되어 보이지만, 이것도 원본에 보이는 사실로만 기록하고 별도로 추론해서 확장하지 않습니다.

# [반복 패턴]
- 삼각형 도형이 3개 반복됩니다.
- 각 도형은 모두 선분 3개로 이루어진 삼각형 외곽선입니다.
- 각 도형 내부에는 하나씩 글자가 들어가며, 도형과 글자의 대응이 반복됩니다.
- 격자 배경이 전체 도형 영역에 반복적으로 깔려 있습니다.
- 세 도형은 좌→우로 배열되어 있어 비교형 문제로 읽힙니다.

# [DSL 작성 힌트]
- 상단 텍스트 영역과 오른쪽 도형 영역, 하단 정답/해설 영역을 분리해서 배치하는 구성이 적절합니다.
- 도형 영역은 연한 파란색 격자 직사각형으로 먼저 표현한 뒤, 그 위에 삼각형 3개를 좌→우 순서로 배치합니다.
- 삼각형 내부의 `가`, `나`, `다`는 각각 도형 중앙 근처에 넣되, 실제 원본처럼 약간 비대칭일 수 있으므로 중앙 정렬만 과하게 강제하지 않는 편이 좋습니다.
- 아래의 `( 가, 나, 다 )`는 도형 아래 중앙에 있는 선택지/기호 목록 텍스트로 처리하면 됩니다.
- 하단 왼쪽의 `(정답)가`, `(해설) ...`는 본문과 분리된 텍스트 블록으로 두는 것이 좋습니다.
- 격자선 위에 삼각형 변이 놓이므로, 꼭짓점과 변의 상대 위치를 격자 기준으로 재현하면 시각적 유사성이 높아집니다.

# [semantic/solvable 힌트]
- 문제 문구는 `직각삼각형`을 찾으라고 요구합니다.
- 원본 하단 해설에는 `한 각이 직각인 삼각형은 가입니다.`라고 적혀 있습니다.
- 따라서 원본 인쇄물에는 정답이 `가`로 표시되어 있습니다.
- 다만 DSL 작성 단계에서는 문제 풀이를 새로 만들기보다, 인쇄된 정답 표기를 사실로 옮기는 방식이 안전합니다.

# [주의 및 TODO]
- 정답을 시각 분석만으로 새로 추론해 서술하지 말고, 원본에 인쇄된 내용만 사실로 기록하세요.
- 각 삼각형의 정확한 꼭짓점 좌표는 이미지 해상도상 다소 불명확하므로, 필요 시 TODO로 남기고 세부 좌표를 단정하지 마세요.
- 첫 번째 삼각형은 왼쪽 변이 거의 세로, 아래 변이 거의 가로로 보여 직각처럼 보입니다. 다만 이는 관찰 표현으로만 두고 단정적 해설은 원문 문장에 맞추세요.
- 두 번째, 세 번째 삼각형의 내부 글자 위치는 약간씩 치우쳐 보일 수 있으므로 중앙 정렬을 지나치게 강하게 가정하지 마세요.
- 선택지 `( 가, 나, 다 )`는 입력칸이 아니라 기호 목록으로 보이므로, 빈칸 요소로 바꾸지 마세요.
- 격자선과 삼각형 변의 겹침 여부는 확대 상태에 따라 달라 보일 수 있으므로, 세부 렌더링은 TODO로 남겨도 됩니다.
