[문제 개요]
- 문제 ID는 `15.`로 보인다.
- 서진이네 학교 3학년 학생들이 좋아하는 색깔을 조사한 표가 제시되어 있다.
- 조건은 “초록색을 좋아하는 학생 수가 빨간색을 좋아하는 학생 수의 3배일 때”이다.
- 질문은 “가장 많은 남학생이 좋아하는 색깔은 무엇인가요?”이다.
- 화면에는 문제 본문, 표, 보기 형태의 텍스트, `(정답)`, `(해설)`이 함께 배치되어 있다.

[원본에 보이는 요소]
- 상단 왼쪽에 번호 `15.`가 있고, 그 오른쪽으로 긴 문제 문장이 2줄 정도로 이어진다.
- 표 제목은 가운데에 `좋아하는 색깔별 학생 수`로 보인다.
- 표는 1개이며 직사각형 격자 형태이다.
- 표의 열 제목은 `색깔`, `파란색`, `초록색`, `빨간색`, `보라색`, `합계`이다.
- 표의 행 제목은 `여학생 수(명)`, `남학생 수(명)`이다.
- 여학생 수 행에는 다음 숫자가 보인다.
  - 파란색 `18`
  - 초록색 `14`
  - 빨간색 `9`
  - 보라색 `25`
  - 합계 `66`
- 남학생 수 행에는 다음 숫자가 보인다.
  - 파란색 `17`
  - 빨간색 `6`
  - 합계 `67`
- 남학생 수 행의 초록색 칸과 보라색 칸은 비어 있다.
- 표 아래에는 괄호로 된 텍스트 `( 파란색 , 초록색 , 빨간색 , 보라색 )`가 있다.
- 그 아래 왼쪽에는 `(정답) 초록색`이 인쇄되어 보인다.
- 해설은 `(해설)`로 시작하며 여러 줄의 문장으로 이어진다.

[수식/관계 구조]
- 표 자체는 계산식이 아니라 주어진 값과 빈칸으로 구성된다.
- 해설에는 다음 식들이 보인다.
  - `9+6=15`
  - `15×3=45`
  - `45-14=31`
  - `67-17-31-6=13`
  - `31>17>13>6`
- 의미상 구조는 다음처럼 읽힌다.
  - 빨간색 전체 학생 수를 여학생 수와 남학생 수의 합으로 나타냄
  - 초록색 전체 학생 수를 빨간색 전체 학생 수의 3배로 둠
  - 초록색 남학생 수를 전체 초록색 수에서 여학생 수를 뺀 값으로 표현
  - 보라색 남학생 수를 남학생 합계에서 다른 색 남학생 수를 빼서 구함
  - 남학생 수들을 비교하여 가장 큰 색을 고름
- 표의 빈칸은 계산 결과를 직접 적는 칸처럼 보이지만, 여기서는 비워 둬야 한다.

[등호별 변환 의미]
- `9+6=15`
  - 빨간색을 좋아하는 여학생 수 `9`와 남학생 수 `6`을 더해 빨간색 전체 학생 수 `15`를 만든 것으로 보인다.
- `15×3=45`
  - 빨간색 전체 학생 수 `15`에 3배를 적용해 초록색 전체 학생 수 `45`를 나타낸 것으로 보인다.
- `45-14=31`
  - 초록색 전체 학생 수 `45`에서 여학생 수 `14`를 빼서 초록색 남학생 수 `31`을 구한 형태로 보인다.
- `67-17-31-6=13`
  - 남학생 전체 합계 `67`에서 파란색 `17`, 초록색 `31`, 빨간색 `6`을 차례로 빼서 보라색 남학생 수 `13`을 구한 형태로 보인다.
- `31>17>13>6`
  - 남학생 수를 색깔별로 비교한 순서로 보인다.
- 위 식들은 해설에 인쇄된 변형을 읽은 것이며, 새로 계산해 답을 채운 것은 아니다.
- `a/b ÷ n -> a/(b×n)` 형태의 변환은 보이지 않는다.
  - 따라서 문장 `나누기 n은 분모의 ×n으로 반영된다.`는 이 문항에는 해당 사항이 없다.

[빈칸 정책]
- 남학생 수 행의 초록색 칸과 보라색 칸은 원본에서도 비어 있으므로, DSL에서도 빈칸으로 유지해야 한다.
- 빈칸에 추론한 값을 넣지 말아야 한다.
- 표의 빈칸은 시각적으로도 빈 셀로 남겨 두는 것이 안전하다.
- 정답이 화면에 보이더라도, 표의 빈칸 자체는 정답 칸이 아니라 미완성 정보 칸으로 취급해야 한다.
- 해설에서 구해지는 값이 있더라도, 여기서는 관찰 사실과 구조만 정리하고 빈칸 채우기는 하지 않는다.

[반복 패턴]
- 표는 열 방향으로 색깔 항목이 반복되는 구조이다.
- 행 방향으로는 여학생 수와 남학생 수가 같은 형식으로 반복된다.
- 숫자는 각 칸의 중앙에 비교적 균등하게 배치되어 있다.
- 표 아래 괄호 텍스트는 쉼표로 항목을 나열하는 반복 패턴이다.
- 해설 문장은 `수식 → 결과 → 문장` 형태가 반복된다.
- 계산식은 한 줄 안에서 `연산식=결과`의 형태로 반복 등장한다.
- 비교식 `31>17>13>6`처럼 부등호를 이용한 나열도 한 번 보인다.

[DSL 작성 힌트]
- 표는 큰 직사각형 테이블 1개로 잡는 것이 좋다.
- 구조상 열은 `색깔` 라벨 열 1개와 색깔 열 4개, `합계` 열 1개로 읽힌다.
- 행은 헤더 1개와 데이터 행 2개로 보는 편이 자연스럽다.
- 셀 병합은 화면에서 분명히 보이지 않으므로, 기본 격자 형태로 처리하는 것이 안전하다.
- 여학생 수 행의 숫자들은 각 색깔 칸에 그대로 배치한다.
- 남학생 수 행에서는 보이는 숫자 `17`, `6`, `67`만 넣고, 나머지 칸은 비워 둔다.
- 표 제목은 표 위 중앙에 따로 배치한다.
- 보기 텍스트 `( 파란색 , 초록색 , 빨간색 , 보라색 )`는 표 아래쪽 오른편에 독립 텍스트로 두는 편이 원본과 비슷하다.
- `(정답) 초록색`은 표와 해설 사이의 별도 텍스트 블록으로 분리하는 것이 좋다.
- 해설은 여러 줄 텍스트 블록으로 배치하고, 각 계산식은 문장 안에 자연스럽게 포함시키면 된다.
- 렌더링 시 표의 테두리, 내부 선, 숫자 중앙 정렬, 제목 중앙 정렬을 특히 신경 쓰는 것이 좋다.

[semantic/solvable 힌트]
- 이 문항은 표 정보와 조건을 이용해 남학생 수를 비교하는 문제로 읽힌다.
- 다만 여기서는 풀이를 다시 수행하지 않고, 화면에 보이는 해설 구조만 정리한다.
- 표와 해설이 함께 존재하므로, DSL에서는 `표 데이터`와 `설명 텍스트`를 분리해 표현하는 것이 유리하다.
- 정답 표기가 화면에 인쇄되어 보이지만, 빈칸 채우기와는 별개로 관찰 사실로만 취급한다.
- 해설의 비교 결과가 최종적으로 어떤 색을 가리키는지는 화면에 보이는 텍스트로만 기록하고, 추가 추론은 하지 않는다.

[주의 및 TODO]
- TODO: 표 아래의 괄호 텍스트가 정확히 보기인지, 아니면 안내 문구인지 화면만으로는 완전히 단정하지 말 것.
- TODO: 문제 본문과 해설 사이의 정확한 간격, 글자 크기, 줄바꿈 위치는 원본 PNG 기준으로 최종 확인이 필요하다.
- TODO: 해설의 줄바꿈은 OCR 초안과 다를 수 있으므로, 실제 이미지 기준으로 문장 단위 배치를 재확인할 것.
- TODO: 남학생 수의 빈칸 두 곳은 절대 숫자로 채우지 말 것.
- TODO: 표 제목, `(정답)`, `(해설)`의 상대 위치는 원본 배치에 맞춰 조정할 수 있다.
- TODO: 선택지 텍스트와 정답 표시는 본문과 분리된 독립 요소로 다루는 것이 안전하다.
