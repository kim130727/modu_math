# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009018.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009018

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 문제 번호 **56번**이 왼쪽 위에 있고, 그 오른쪽에 문제 지시문 **“직선 ㄷ ㄹ을 선택하세요.”**가 있습니다.
- 오른쪽에는 **연보라색 테두리의 큰 직사각형 박스**가 있으며, 그 안에 직선 형태의 그림이 **위아래로 2개** 배치되어 있습니다.
- 아래쪽 왼편에는 **(정답)** 표시가 있고, 그 옆에 하나의 선 그림이 보입니다.
- 그 아래에는 **(해설)** 문장이 한 줄로 제시되어 있습니다.
- 전체적으로 “보기에서 올바른 직선을 고르는” 형식의 선택 문제로 보입니다.  
  - 다만 **실제 정답 선택은 여기서 하지 않습니다.**

# [보이는 텍스트]
- `56.`
- `직선 ㄷ ㄹ을 선택하세요.`
- `ㄷ`
- `ㄹ`
- `(정답)`
- `(해설)`
- `점 ㄷ과 점 ㄹ을 모두 지나는 직선을 직선 ㄷㄹ이라고 합니다.`

# [반복되는 시각 패턴]
- 큰 박스 안에 **가로 방향의 선분/직선처럼 보이는 도형**이 2개 반복됩니다.
- 두 도형 모두:
  - 가운데에 가까운 위치에 **작은 검은 점 2개**가 찍혀 있고,
  - 각 점 위쪽에 각각 **‘ㄷ’**, **‘ㄹ’** 글자가 배치되어 있습니다.
- 위아래 두 도형은 거의 같은 형태이며,  
  **위쪽 도형과 아래쪽 도형의 차이는 위치만 다르고 구조는 동일**해 보입니다.
- 아래의 `(정답)` 영역에도 비슷한 형태의 선 그림이 있어,  
  **정답 예시/선택 결과를 별도로 표시한 것처럼 보입니다.**

# [수식 단계]
- 해당 없음.  
- 수식 변형이나 등호(`=`)가 포함된 전개는 보이지 않습니다.

# [등호별 변환 의미]
- 해당 없음.

# [세부 내용]
- 오른쪽 큰 직사각형 박스는 **보기 영역**처럼 보입니다.
  - 내부에 두 개의 후보 그림이 위아래로 배치되어 있어,  
    **둘 중 하나를 선택하는 구조**로 추정됩니다.
- 각 후보 그림은:
  - 가로로 뻗은 한 줄의 검은 선,
  - 선 위에 찍힌 **두 개의 점**,
  - 점 위에 붙은 **‘ㄷ’, ‘ㄹ’ 표기**
  로 이루어져 있습니다.
- 점과 글자의 배치는 모두 비슷합니다.
  - 왼쪽 점 위에 `ㄷ`
  - 오른쪽 점 위에 `ㄹ`
  로 읽히는 형태입니다.
- `(정답)` 아래의 그림도 같은 구성요소를 사용하지만,  
  이 영역이 **실제 답 표시용인지, 예시용인지**는 화면만으로는 완전히 단정하기 어렵습니다.
- `(해설)` 문장은 점과 직선의 정의를 설명하는 문장으로 보이며,
  - **점 ㄷ과 점 ㄹ을 모두 지나는 직선**
  - 이를 **직선 ㄷㄹ**이라고 부른다는 내용입니다.
- 이 해설은 문제의 개념 설명이지, 계산 과정은 아닙니다.

# [Python DSL 작성 힌트]
- 전체 레이아웃을 먼저 잡는 것이 좋아 보입니다.
  1. 상단 왼쪽에 문제 번호와 지시문 텍스트
  2. 상단 오른쪽에 연보라색 테두리의 큰 직사각형
  3. 직사각형 내부에 위/아래 2개의 동일한 후보 도형
  4. 하단 왼쪽에 `(정답)` 라벨과 선 그림
  5. 그 아래 `(해설)` 텍스트 한 줄
- 도형은 다음 요소를 분리해서 그리면 좋습니다.
  - 긴 가로선
  - 선 위의 점 2개
  - 점 위의 한글 자모 텍스트 `ㄷ`, `ㄹ`
- 후보 도형은 **위아래 정렬**, 정답 예시는 **아래쪽 별도 영역**으로 배치하면 구조가 잘 드러납니다.
- 박스의 색은 연보라색 계열로 표현하면 원본 느낌과 비슷합니다.
- 텍스트와 도형의 상대 위치를 정확히 유지하는 것이 중요합니다.

# [주의할 점]
- **정답을 추론해서 표시하지 마세요.**
- `(정답)` 영역이 실제 채점용 정답인지, 교재 편집상 예시 표시인지 혼동하지 않도록 주의하세요.
- 선이 **직선인지 선분인지**는 그림만으로는 표현상 애매할 수 있으므로, 실제 DSL에서는 원본처럼 보이는 형태를 우선 반영하세요.
- 점 위의 글자 `ㄷ`, `ㄹ`은 꼭 **점과 짝지어 위치 관계를 유지**해야 합니다.
- 문제의 핵심은 계산이 아니라 **도형 인식과 이름 붙이기**에 가깝습니다.

# [불확실한 부분]
- 오른쪽 큰 박스 안의 두 도형이 각각 **완전히 같은지**, 또는 미세한 차이가 있는지는 이미지 해상도상 분명하지 않습니다.
- 아래 `(정답)` 영역의 선 그림이 **선택된 정답 표시인지, 해설용 보조 그림인지**는 화면만으로 단정하기 어렵습니다.
- 선이 실제로 **무한히 뻗는 직선**을 뜻하는지, 화면상에는 **짧게 그린 직선 표현**인지가 다소 모호합니다.

--- RAW VISION DRAFT END ---
