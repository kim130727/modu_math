# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008770.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008770

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면은 초등 수학 문제 1문항으로 보입니다.
- 왼쪽 위에 문제 번호와 지시문이 있고, 중앙에는 초록색 테두리의 큰 박스가 있습니다.
- 중앙 박스 안에는 고양이 그림이 위쪽에 배치되어 있고, 아래쪽에는 선택지 형태의 텍스트가 가로로 나열되어 있습니다.
- 화면 하단 왼쪽에는 `(정답)` 표시와 함께 정답이 보이며, 그 아래 또는 옆에 `(해설)` 문장이 길게 이어집니다.
- 전체적으로 “무게를 알맞게 어림하는 것”을 고르는 객관식 형태처럼 보입니다.

[보이는 텍스트]
- `□ 72. 무게를 알맞게 어림한 것을 찾아 선택하세요.`
- 중앙 박스 안:
  - `약 ( 3 g , 3 kg , 3 t )`
- 하단:
  - `(정답) 3 kg`
  - `(해설) 고양이의 무게는 3 g 보다 더 무겁고 3 t 보다 더 가벼우므로 약 3 kg이 알맞습니다.`

[반복되는 시각 패턴]
- 괄호 `(...)` 안에 여러 단위를 쉼표로 나열한 선택지 구조가 보입니다.
- 숫자 `3`이 여러 단위와 결합해 반복됩니다: `3 g`, `3 kg`, `3 t`.
- 중앙 박스는 얇은 초록색 선으로 둘러싸인 단일 영역이며, 그 안에서 그림과 텍스트가 위아래로 분리되어 있습니다.
- 단위 표기에서 `g`, `kg`, `t`처럼 같은 형식의 단위 기호가 반복됩니다.

[수식 단계]
- 해당 없음.  
- 화면에는 등호 `=`로 연결된 계산식이나 변형식이 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.  
- 등호가 나타나지 않아 단계별 변환 의미를 설명할 내용이 없습니다.

[세부 내용]
- 문제 번호 `72` 앞에는 작은 사각형 기호처럼 보이는 표시가 있습니다.
- 지시문은 “무게를 알맞게 어림한 것을 찾아 선택하세요.”로, 정확한 측정값이 아니라 적절한 어림값을 고르는 유형입니다.
- 중앙의 초록색 테두리 박스는 정답 선택지 영역처럼 보이며, 내부 상단에는 고양이 그림이 중앙 정렬되어 있습니다.
- 고양이 아래에는 `약`이라는 말이 먼저 나오고, 그 뒤에 괄호 안에 세 개의 단위 표현이 나열되어 있습니다.
- 괄호 안의 항목들은 `3 g`, `3 kg`, `3 t` 순서로 보이며, 쉼표로 구분되어 있습니다.
- 하단의 해설 문장은 고양이의 무게를 비교해 설명하는 문장으로, `3 g`와 `3 t`를 기준으로 상대적 크기를 말하고 있습니다.
- `(정답)` 표시는 해설보다 앞쪽에 따로 표기되어 있습니다.

[Python DSL 작성 힌트]
- 상단 문제 문구와 중앙 선택지 박스를 별도 요소로 나누어 배치하는 구성이 필요합니다.
- 중앙 박스는 초록색 테두리의 직사각형으로 표현하고, 내부 요소를 세로 정렬합니다.
- 그림 요소는 박스 상단 중앙에 위치시키고, 텍스트 `약 ( 3 g , 3 kg , 3 t )`는 아래 중앙에 배치합니다.
- 선택지 텍스트는 괄호, 쉼표, 단위 간 공백까지 보이는 형태를 최대한 유지해 적는 것이 좋습니다.
- 하단의 `(정답)`과 `(해설)`은 본문과 구분되는 작은 텍스트 블록으로 따로 처리하면 좋습니다.

[주의할 점]
- 정답 `3 kg`는 화면에 보이지만, 문제 풀이를 재구성하는 용도로 임의 확정 설명을 더하지 마세요.
- 선택지에서 어느 항목이 실제로 클릭 대상인지, 화면만으로는 별도 표시가 보이지 않으므로 추정하지 마세요.
- 그림의 크기나 정확한 위치는 대략적 상대 배치로만 설명하는 것이 안전합니다.
- 해설 문장의 논리 전개를 추가 계산처럼 확장하지 말고, 보이는 문장 그대로 다루세요.

[불확실한 부분]
- 왼쪽 위의 작은 사각형 기호가 체크박스인지 단순 인쇄 기호인지 확실하지 않습니다.
- 중앙 박스 내부의 고양이 그림은 클립아트 스타일로 보이지만, 세부 표정·색감의 정확한 수치는 단정할 수 없습니다.
- 선택지 문구가 실제 문제의 보기 전체인지, 화면 캡처에서 일부만 보인 것인지 확실하지 않습니다.
- 하단 해설의 문장 끝이 화면 경계상 완전히 명확하지 않을 수 있으나, 보이는 범위에서는 위와 같이 읽힙니다.

--- RAW VISION DRAFT END ---
