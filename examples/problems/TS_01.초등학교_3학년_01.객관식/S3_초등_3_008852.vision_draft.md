[전체적인 구성]
- 가로로 긴 문제 지면이며, 상단에 문제 번호와 본문 질문이 있고, 중앙에 저울 그림 2개가 나란히 배치되어 있습니다.
- 왼쪽 저울은 파프리카와 바둑돌 50개를 비교한 그림이고, 오른쪽 저울은 브로콜리와 바둑돌 62개를 비교한 그림입니다.
- 그림 아래쪽 오른편에는 선택지 형태로 `( 파프리카 , 브로콜리 )`가 보입니다.
- 하단에는 `(정답)` 표시와 함께 답이 텍스트로 제시되어 있고, 그 아래 `(해설)` 문장이 2~3줄로 이어집니다.

[보이는 텍스트]
- `96.`
- `저울과 바둑돌을 사용하여 파프리카와 브로콜리의 무게를 비교했습니다. 파프리카와 브로콜리 중 어느 것이 더 무거운지 선택해 보세요.`
- 저울 그림 위 라벨:
  - `파프리카`
  - `바둑돌 50개`
  - `브로콜리`
  - `바둑돌 62개`
- 선택지처럼 보이는 텍스트:
  - `( 파프리카 , 브로콜리 )`
- 하단 표기:
  - `(정답)브로콜리`
  - `(해설)파프리카의 무게는 바둑돌 50개의 무게와 같고 브로콜리의 무게는 바둑돌 62개의 무게와 같습니다.`
  - `따라서 바둑돌의 수를 비교하면 50<62이므로 브로콜리가 더 무겁습니다.`

[반복되는 시각 패턴]
- 저울 그림이 2번 반복됩니다.
- 두 그림 모두 가운데 지지대와 좌우 접시가 있는 동일한 형태의 저울이며, 물체와 바둑돌이 각각 한쪽 접시에 놓여 있습니다.
- 각 저울 위에는 연한 베이지색의 작은 라벨 박스가 있고, 물체 이름과 바둑돌 개수가 짝지어 표기됩니다.
- 왼쪽 그림과 오른쪽 그림은 구조가 같고, 비교 대상만 다릅니다.
- 숫자 표기는 `50개`, `62개`처럼 동일한 형식으로 반복됩니다.

[수식 단계]
- 이 문제는 실제 계산식 전개가 아니라, 바둑돌 개수 비교를 통해 무게 관계를 판단하는 형태입니다.
- 하단 해설에 `50<62`라는 부등호 표현이 한 번 등장합니다.
- 등호가 연속되는 변형식은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.
- 수식 변형 단계나 등호 연결식은 관찰되지 않습니다.
- 다만 해설 문장에서는
  - 파프리카의 무게 = 바둑돌 50개의 무게
  - 브로콜리의 무게 = 바둑돌 62개의 무게
  라는 대응 관계를 말로 설명하고 있습니다.

[세부 내용]
- 상단 질문은 “파프리카와 브로콜리 중 어느 것이 더 무거운지”를 묻고 있습니다.
- 왼쪽 저울:
  - 왼쪽 접시에 파프리카 그림이 있고,
  - 오른쪽 접시에 검은 바둑돌 여러 개가 쌓여 있으며,
  - 위 라벨에 `바둑돌 50개`라고 적혀 있습니다.
- 오른쪽 저울:
  - 왼쪽 접시에 브로콜리 그림이 있고,
  - 오른쪽 접시에 검은 바둑돌 여러 개가 쌓여 있으며,
  - 위 라벨에 `바둑돌 62개`라고 적혀 있습니다.
- 두 저울 모두 수평에 가깝게 표현되어 있어, 그림만으로는 각각의 물체가 해당 바둑돌 개수와 무게가 같다는 뜻을 전달하는 구성으로 보입니다.
- 오른쪽 아래의 `( 파프리카 , 브로콜리 )`는 보기 또는 선택 항목처럼 배치되어 있습니다.
- 하단의 `(정답)브로콜리`는 결과 표시 영역처럼 보입니다.
- 해설 문장은 문장형 설명으로 배치되어 있으며, 마지막 줄에서 `50<62`를 사용해 비교 논리를 보여 줍니다.

[Python DSL 작성 힌트]
- 저울 그림 2개를 좌우로 나란히 배치하는 구조로 작성하면 좋습니다.
- 각 저울은:
  - 본체
  - 왼쪽 접시의 물체 1개
  - 오른쪽 접시의 바둑돌 묶음
  - 상단 라벨 박스 2개
  로 분해해 표현할 수 있습니다.
- 바둑돌은 개별 원형이 여러 개 모인 덩어리처럼 보이므로, 단일 도형 반복 또는 그룹 객체로 처리하면 좋습니다.
- 텍스트는 상단 문제문, 그림 위 라벨, 우측 선택지, 하단 정답/해설로 구역을 나누어 넣으면 좋습니다.
- 선택지 `( 파프리카 , 브로콜리 )`는 괄호와 쉼표를 포함한 문자열로 그대로 배치하는 방식이 적절합니다.
- 해설 영역은 문제 본문과 분리된 별도 텍스트 블록으로 처리하면 좋습니다.

[주의할 점]
- 정답을 다시 계산해서 적지 마세요. 이미 보이는 정답 표기를 그대로 참고만 해야 합니다.
- `50개`, `62개`를 실제 바둑돌 개수로 단정적으로 다시 세어 쓰기보다, 그림 속 라벨 텍스트로 처리하는 것이 안전합니다.
- 저울의 기울기나 바둑돌의 정확한 개별 개수를 도형 분석으로 과도하게 추론하지 마세요.
- `( 파프리카 , 브로콜리 )`는 선택지 형식처럼 보이지만, 정확히 어떤 UI 구성인지 단정하지 않는 것이 좋습니다.
- 하단 해설의 수학적 판단을 문제 풀이로 재서술하지 말고, 보이는 문장 구조만 정리하세요.

[불확실한 부분]
- 저울 그림에서 실제로 저울판이 완전히 수평인지, 약간의 각도가 있는지는 이미지 해상도상 단정하기 어렵습니다.
- 바둑돌이 각 접시에 몇 개씩 정확히 그려졌는지는 라벨의 숫자와 별개로, 그림만 보고 정확히 세기 어렵습니다.
- 오른쪽 아래의 `( 파프리카 , 브로콜리 )`가 선택지인지, 정답 입력 예시인지, 또는 편집용 표기인지는 화면만으로 확정하기 어렵습니다.
- `(정답)브로콜리`는 본문에 포함된 학습용 해설 표기로 보이지만, 원본 문제의 필수 요소인지 여부는 단정하기 어렵습니다.
