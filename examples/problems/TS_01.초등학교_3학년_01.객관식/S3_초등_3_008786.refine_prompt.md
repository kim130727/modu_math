# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008786.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008786

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 흰색 배경의 초등 수학/생활수학형 객관식 그림 문제로 보입니다.
- 상단 왼쪽에 문제 번호와 지시문이 있고, 중앙에는 **그릇(그릇 모양의 용기)** 그림이 3개 배치되어 있습니다.
- 왼쪽 아래에는 **(정답)** 표시가 붙은 그릇 그림이 1개 따로 있으며, 하단에는 **(해설)** 문장이 한 줄로 적혀 있습니다.
- 전체적으로 **그림을 보고 더 큰 크기의 그릇을 고르는 내용**으로 보입니다.
- 수식, 표, 빈칸, 화살표, 등호 변형식은 보이지 않습니다.

# [보이는 텍스트]
- `□ 11.`  
- `들이가 더 많은 것을 선택해 보세요.`  
- `(정답)`
- `(해설)크기가 큰 그릇일수록 들이가 더 많습니다.`

# [반복되는 시각 패턴]
- 중앙에 그릇 모양이 3개 있습니다.
  - 왼쪽 아래의 그릇은 비교적 크게 보입니다.
  - 가운데 위쪽의 그릇은 상대적으로 작게 보입니다.
  - 오른쪽 위쪽의 그릇은 가장 크게 보이는 편입니다.
- 그릇들은 모두 **위가 넓고 아래가 좁아지는 곡선형 입체 그림**이며, 비슷한 스타일로 그려져 있습니다.
- 색상은 서로 다르지만 모두 같은 종류의 용기 그림으로 보입니다.
- 왼쪽 아래의 정답 그림은 그림 왼쪽에 `(정답)` 텍스트가 가까이 붙어 있어, 정답 위치를 강조하는 반복 패턴처럼 보입니다.

# [수식 단계]
- 해당 사항 없음.
- 등호나 계산식, 단계별 변형식은 보이지 않습니다.

# [등호별 변환 의미]
- 해당 사항 없음.
- 등호가 나타나지 않습니다.

# [세부 내용]
- 상단 왼쪽의 `□ 11.`은 문제 번호를 나타내는 부분으로 보입니다.
- 이어서 `들이가 더 많은 것을 선택해 보세요.`라는 지시문이 있으며, 의미상 여러 그림 중에서 더 많이 담을 수 있는 그릇을 고르라는 뜻으로 읽힙니다.
- 중앙 그림 배치는 다음처럼 보입니다.
  - **가운데 위**: 연한 파란색 그릇 1개
  - **오른쪽 위**: 연한 갈색/회갈색 그릇 1개
  - **왼쪽 아래**: 연한 갈색 그릇 1개
- 왼쪽 아래 그릇은 다른 그림보다 아래쪽에 있고, `(정답)` 텍스트가 바로 왼쪽에 붙어 있습니다.
- 하단의 해설 문장은 `크기가 큰 그릇일수록 들이가 더 많습니다.`라고 되어 있어, **그릇의 크기와 들이의 많고 적음의 관계**를 설명합니다.
- 여기서 “들이”는 용기에 들어가는 양을 뜻하는 생활 속 개념으로 보입니다.
- 그림만 보고 판단하도록 구성된 문제이며, 답안 칸은 따로 보이지 않습니다.

# [Python DSL 작성 힌트]
- 배경은 흰색 단색으로 처리합니다.
- 상단 왼쪽에 문제 번호와 지시문 텍스트를 배치합니다.
- 중앙에는 그릇 그림 3개를 서로 떨어뜨려 배치합니다.
  - 가운데 위: 작은 파란 그릇
  - 오른쪽 위: 큰 회갈색 그릇
  - 왼쪽 아래: 중간 크기 정도의 갈색 그릇
- 왼쪽 아래 그릇 옆에 `(정답)` 텍스트를 배치합니다.
- 하단에는 `(해설)` 라벨과 함께 해설 문장을 한 줄로 넣습니다.
- 각 그릇은 위쪽 타원형 테두리와 아래쪽으로 볼록한 입체 음영이 있는 형태로 그리면 유사합니다.
- 문제가 그림 선택형이므로, 선택지 박스나 빈칸은 추가하지 않는 편이 자연스럽습니다.

# [주의할 점]
- 정답을 텍스트로 추론해 적지 마세요. 이 이미지는 이미 `(정답)` 표시가 있는 그림을 보여 주는 자료입니다.
- “들이가 더 많은 것”을 크기와 직접 연결하는 설명은 해설 텍스트에 이미 있으므로, 이를 바탕으로 추가 해석을 과하게 확장하지 마세요.
- 그림의 실제 상대 크기는 원근감 때문에 약간 애매할 수 있으므로, 정확한 수치 비교로 단정하지 않는 것이 좋습니다.
- 선택지 번호나 별도의 체크표시는 보이지 않습니다.
- 빈칸, 분수, 식, 표는 없습니다.

# [불확실한 부분]
- 왼쪽 아래의 그릇이 정확히 다른 두 그릇보다 얼마나 큰지, 화면만으로는 정밀 비교가 어렵습니다.
- 각 그릇의 색상은 연한 갈색/회갈색/파란색으로 보이지만, 인쇄 환경에 따라 색감이 달라질 수 있습니다.
- `(정답)`이 실제 채점용 정답 표식인지, 예시 답안 안내용 표식인지는 화면만으로는 문맥상 완전히 확정할 수 없습니다.

--- RAW VISION DRAFT END ---
