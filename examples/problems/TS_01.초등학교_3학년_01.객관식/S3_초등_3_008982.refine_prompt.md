# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008982.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008982

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
## [전체적인 구성]
- 한 장의 초등 수학 객관식/선다형 문항으로 보입니다.
- 상단 왼쪽에 문제 번호와 지시문이 있고, 오른쪽에는 각과 관련된 선분 그림이 배치되어 있습니다.
- 그림 아래에는 “점 (ㄷ, ㄹ, □)” 형태의 선택지/기입형 안내가 보이며, 하단 왼쪽에는 정답과 해설 문장이 있습니다.
- 전체적으로 텍스트 + 도형(각도 그림) + 정답/해설 영역이 함께 있는 구성입니다.

## [보이는 텍스트]
- `□ 17.`
- `각을 보고 꼭짓점을 선택하세요.`
- `점 ( ㄷ, ㄹ, □ )`
- `(정답) ㄹ`
- `(해설) 두 반직선이 만나는 점이 각의 꼭짓점입니다.`

## [반복되는 시각 패턴]
- 선택지 괄호 안에 글자 3개가 `점 ( ㄷ, ㄹ, □ )`처럼 나열되어 있습니다.
- 빈칸처럼 보이는 네모 기호 `□`가 문제 번호 앞과 선택지 안에 각각 한 번씩 보입니다.
- 도형은 두 개의 반직선이 한 점에서 만나 각을 이루는 형태로 표현되어 있습니다.
- 도형의 끝점/꼭짓점 주변에 한글 자모가 붙어 있어 점 이름을 표시한 것으로 보입니다.

## [수식 단계] (해당될 때)
- 해당 없음.
- 이 문항은 등식 변형이나 계산식 단계가 보이지 않고, 도형 판독 문제입니다.

## [등호별 변환 의미] (해당될 때)
- 해당 없음.

## [세부 내용]
- 상단의 문장 `각을 보고 꼭짓점을 선택하세요.`는 도형을 보고 각의 꼭짓점에 해당하는 점을 고르는 지시로 보입니다.
- 오른쪽 그림에는 두 개의 선이 한 점에서 만나는 각 모양이 있습니다.
  - 아래쪽에는 왼쪽에서 오른쪽으로 거의 수평인 선이 있고,
  - 그 선의 오른쪽 끝점에서 오른쪽 위 방향으로 기울어진 선이 이어집니다.
- 두 선이 만나는 지점 근처에 점 표기 `ㄹ`이 붙어 있습니다.
- 수평선의 왼쪽 쪽에는 `ㄷ`이 표시되어 있고,
- 오른쪽 위로 올라가는 선의 위쪽에는 `ㅁ`처럼 보이는 표시가 있습니다.
- 그림 아래의 `점 ( ㄷ, ㄹ, □ )`는 보기 또는 응답용 형식으로 보이며, 세 점 중에서 꼭짓점을 고르라는 의미를 담고 있는 듯합니다.
- 하단의 `(정답) ㄹ`과 `(해설) 두 반직선이 만나는 점이 각의 꼭짓점입니다.`는 화면에 인쇄된 해설 정보입니다.

## [Python DSL 작성 힌트]
- 문제 영역을 크게 다음 블록으로 나누면 좋습니다.
  1. 좌상단 번호/지시문 텍스트
  2. 우측의 각도 도형
  3. 도형 아래의 선택지/점 이름 텍스트
  4. 하단의 정답·해설 텍스트
- 도형은 다음 요소로 분해해서 표현하면 좋습니다.
  - 한 점에서 시작하는 수평 반직선
  - 같은 점에서 시작하는 우상향 반직선
  - 두 선의 교점/꼭짓점 표시
  - 각 선 주변의 점 이름 라벨
- 라벨 `ㄷ`, `ㄹ`, `ㅁ`의 위치 관계를 반영해 배치해야 합니다.
- 선택지 `점 ( ㄷ, ㄹ, □ )`는 괄호, 쉼표, 빈 사각형 기호까지 포함해 그대로 텍스트 객체로 다루는 것이 좋아 보입니다.
- 문제 번호 앞의 작은 네모 `□`도 별도 기호로 인식할 수 있습니다.

## [주의할 점]
- 정답을 본문에서 추론해 다시 채우지 마세요. 이미 정답이 화면에 적혀 있어도, DSL 작성용 초안에서는 문제 풀이를 하지 않는 방식으로 다루는 것이 안전합니다.
- `□` 기호가 실제 빈칸인지, 인쇄상의 표시인지 문맥상 혼동될 수 있으므로 단정하지 마세요.
- 오른쪽 위 점 라벨은 이미지 해상도상 `ㅁ`으로 보이지만, 다른 글자일 가능성도 완전히 배제할 수는 없습니다.
- 도형의 선은 직선이 아니라 반직선으로 해석되는 해설 문구가 있으나, 그림만으로는 선분/반직선 경계를 엄밀히 판정하기 어려울 수 있습니다.

## [불확실한 부분]
- 문제 번호 앞의 `□`가 체크박스인지, 편집용 표시인지 확실하지 않습니다.
- 선택지 괄호 안 마지막 기호 `□`가 실제 네모 문자인지, 가려진 글자인지 해상도상 다소 불명확합니다.
- 오른쪽 위 점의 이름 표기가 정확히 `ㅁ`인지 약간 흐릿합니다.
- 도형의 두 선이 화면에서 어디까지 이어지는지, 반직선의 끝점 표현이 잘려 보이는지 여부는 이미지 해상도 때문에 완전히 단정하기 어렵습니다.

--- RAW VISION DRAFT END ---
