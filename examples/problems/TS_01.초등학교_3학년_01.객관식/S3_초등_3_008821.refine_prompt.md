# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008821.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008821

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# 시각 분석 초안

[전체적인 구성]  
- 문제 번호 **55.**가 왼쪽 상단에 있고, 그 오른쪽에 문제 문장이 두 줄로 배치되어 있습니다.  
- 본문 아래쪽에는 **표 1개**가 가로로 놓여 있으며, 그 아래에 **말풍선 2개**와 각각의 **인물 그림 2개**가 좌우로 배치되어 있습니다.  
- 화면 하단 왼쪽에는 **(정답)** 표시와 이름 텍스트가 있고, 그 아래/옆에는 **(해설)** 문장이 이어집니다.  
- 전체적으로 위에서 아래로  
  1) 문제 문장  
  2) 비교 표  
  3) 두 친구의 말풍선과 캐릭터  
  4) 정답/해설 문장  
  순서로 구성되어 있습니다.

[보이는 텍스트]  
- `55.`  
- `양동이에 물을 가득 채우려면 ㉠ 컵과 ㉡ 컵으로 다음과 같이 부어야 합니다. 바르게 이야기한 친구를 선택해 보세요.`  
- 표 제목 부분:
  - `㉠ 컵`
  - `㉡ 컵`
- 표 내용:
  - `컵의 수`
  - `8개`
  - `4개`
- 왼쪽 말풍선:
  - `양동이의 들이는`
  - `㉠ 컵의 들이의`
  - `8배야.`
- 오른쪽 말풍선:
  - `㉠ 컵과 ㉡ 컵 중에`
  - `들이가 더 많은 컵은`
  - `㉠ 컵이야.`
- 이름:
  - `재석`
  - `효민`
- 하단 텍스트:
  - `(정답)재석`
  - `(해설)효민: 물을 부은 컵의 수가 적을수록 컵의 들이가 많으므로 ㉡ 컵의 들이가 더 많`
  - `습니다.`  
- 말풍선과 이름 텍스트 사이에 캐릭터 그림이 각각 1명씩 있습니다.

[반복되는 시각 패턴]  
- `㉠`, `㉡`처럼 **동그라미 친 기호가 반복**되어 두 종류의 컵을 구분합니다.  
- 표의 열 머리글이 **좌우 2칸 구조**로 반복되어, 두 컵의 정보를 나란히 비교하게 되어 있습니다.  
- 두 개의 말풍선이 **비슷한 크기와 형태**로 좌우에 배치되어 있어, 두 친구의 의견을 비교하는 형식입니다.  
- 말풍선 안의 문장도 줄바꿈이 비슷한 방식으로 들어가 있어, 각각의 발화를 강조하는 패턴입니다.

[수식 단계] (해당될 때)  
- 이 문제 화면에는 **등호(=)가 포함된 수식 변형 단계는 보이지 않습니다.**  
- 따라서 단계별 계산식 변화는 나타나지 않습니다.

[등호별 변환 의미] (해당될 때)  
- 해당 없음. 등호를 이용한 식 변형이 제시되어 있지 않습니다.

[세부 내용]  
- 표는 연한 초록색 계열의 테두리와 배경으로 표시되어 있으며, 2열 구조입니다.  
- 첫 행은 비교 대상의 이름:
  - 왼쪽 칸은 항목명 칸으로 보이며,
  - 가운데 칸에 `㉠ 컵`,
  - 오른쪽 칸에 `㉡ 컵`이 적혀 있습니다.
- 둘째 행은 `컵의 수`라는 항목명과 함께, 각 컵을 부어야 하는 횟수/개수로 보이는 값이 들어 있습니다:
  - `㉠ 컵` 쪽은 `8개`
  - `㉡ 컵` 쪽은 `4개`
- 말풍선 2개는 표 아래에 좌우로 배치되어 있습니다.
  - 왼쪽 말풍선은 남자아이가 말하는 형태이고, 내용은 `양동이의 들이는 ㉠ 컵의 들이의 8배야.`로 읽힙니다.
  - 오른쪽 말풍선은 여자아이가 말하는 형태이고, 내용은 `㉠ 컵과 ㉡ 컵 중에 들이가 더 많은 컵은 ㉠ 컵이야.`로 읽힙니다.
- 각 말풍선 아래에는 이름표처럼 테두리가 있는 작은 박스가 있으며, 왼쪽은 `재석`, 오른쪽은 `효민`입니다.
- 하단의 `(정답)`과 `(해설)`은 일반 본문보다 작은 글씨로 보이고, 화면 아래쪽에 붙어 있습니다.

[Python DSL 작성 힌트]  
- 문제 레이아웃을 만들 때는 다음 순서로 배치하면 자연스럽습니다:
  1) 상단 문제 문장 텍스트 박스
  2) 가운데 상단의 2열 표
  3) 표 아래 좌우 2개의 말풍선
  4) 말풍선 아래 인물 캐릭터와 이름표
  5) 하단 정답/해설 텍스트
- 표는 **행/열이 명확한 그리드**로 처리하고, 상단 헤더 셀과 값 셀의 구분을 유지해야 합니다.
- 동그라미 친 기호 `㉠`, `㉡`는 일반 숫자나 문자와 다르게 **원형 강조 텍스트**로 넣는 것이 좋습니다.
- 말풍선은 각각 **타원형 본체 + 아래쪽 꼬리**가 있는 구조로 보이므로, 텍스트 박스와 말풍선 도형을 함께 배치하는 방식이 적절합니다.
- 캐릭터 그림과 이름표는 말풍선 아래에서 **중앙 정렬**된 형태로 배치하면 화면과 유사합니다.
- 하단 해설은 한 줄에 끝나지 않고 줄바꿈되어 보이므로, 긴 문장은 자동 줄바꿈 또는 수동 개행을 고려해야 합니다.

[주의할 점]  
- 문제를 풀어 정답을 새로 추론하지 마세요.  
- 표의 수치 `8개`, `4개`는 **보이는 값 그대로** 사용해야 하며, 의미를 계산으로 확장하지 마세요.  
- 말풍선 내용은 실제로는 누가 옳은지 판단하는 핵심 문장이지만, 이 초안에서는 **판단 결과를 다시 설명하지 않고** 구조와 문구만 기록하세요.  
- 하단의 `(정답)재석`은 화면에 보이지만, 시각 분석 초안에서는 정답을 근거로 서술을 확장하지 않는 것이 좋습니다.  
- 표 상단의 첫 번째 칸은 내용이 비어 있는 것처럼 보일 수 있으나, 이는 레이아웃상 항목명 칸으로 보입니다.

[불확실한 부분]  
- 표의 맨 왼쪽 위 칸이 실제로 완전히 빈칸인지, 아니면 항목 구분용 셀인지 화면만으로는 100% 단정하기 어렵습니다.  
- 하단 `(정답)`과 `(해설)` 텍스트의 정확한 줄바꿈 위치는 이미지 해상도상 약간 애매합니다.  
- 말풍선 내부 글씨의 줄바꿈은 편집 원본과 다를 수 있어, DSL 작성 시 문장 단위로만 정확히 옮기고 줄 위치는 유연하게 처리하는 것이 좋습니다.

--- RAW VISION DRAFT END ---
