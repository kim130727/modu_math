# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009008.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009008

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문항 번호 **“47.”**가 있고, 그 오른쪽으로 문제 지시문 **“선을 ㄱㄴ을 바르게 그린 것을 선택하세요.”**가 한 줄로 배치되어 있습니다.
- 지시문 아래에는 **가로로 3개의 선분 그림**이 나란히 배치되어 있고, 각 그림은 검은색 선과 양 끝의 점, 그리고 점 위/근처의 기호 표시로 구성되어 있습니다.
- 왼쪽 아래에는 **“(정답)”** 표시가 있는 예시/정답 그림이 따로 있으며, 그 아래쪽에 **“(해설) 점 ㄱ과 점 ㄴ을 곧게 이어 봅니다.”**라는 설명 문장이 있습니다.
- 전체적으로 **선택형 문제 + 정답 표시 + 해설** 구조입니다.

[보이는 텍스트]
- `47.`
- `선을 ㄱㄴ을 바르게 그린 것을 선택하세요.`
- `(정답)`
- `(해설)점 ㄱ과 점 ㄴ을 곧게 이어 봅니다.`

[반복되는 시각 패턴]
- 위쪽 선택지 3개는 모두 **검은색 사선 형태의 선분**과 **양 끝의 점**으로 이루어져 있습니다.
- 각 선택지에는 점 근처에 **ㄱ, ㄴ** 기호가 붙어 있으며, 배치 방식은 비슷하지만 선의 기울기와 점의 위치가 서로 다릅니다.
- 아래의 정답 예시는 같은 유형의 선분이지만 **파란색 선**으로 강조되어 있어, 다른 선택지와 구분됩니다.
- 각 도형은 대체로 **왼쪽 점과 오른쪽 점을 연결한 형태**처럼 보이지만, 실제로 어떤 그림이 ‘바르게’ 그린 것인지는 문제의 의미에 해당하므로 여기서는 판단하지 않습니다.

[수식 단계]
- 해당 없음: 이 문항에는 등호가 연결된 수식 변형 단계가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음: 등호가 있는 식 전개나 변환 과정이 없습니다.

[세부 내용]
- 첫 번째 선택지: 화면 상단 왼쪽 쪽에 있으며, 검은색 사선이 **왼쪽 아래에서 오른쪽 위 방향**으로 놓여 있습니다. 선의 왼쪽 끝과 오른쪽 끝에 작은 검은 점이 있고, 점 위쪽에 각각 기호가 표시되어 있습니다.
- 두 번째 선택지: 화면 상단 가운데 쪽에 있으며, 역시 검은색 사선입니다. 첫 번째보다 길이와 기울기가 약간 다르게 보이고, 점과 기호의 위치도 조금씩 다릅니다.
- 세 번째 선택지: 화면 상단 오른쪽 쪽에 있으며, 검은색 사선이 더 길게 보이고 왼쪽 끝 점이 선의 중간보다 왼쪽 아래쪽에 위치한 듯한 배치입니다.
- 정답 표시 그림: 왼쪽 아래에 따로 배치되어 있고, **“(정답)”** 표기 옆/아래에 파란색 사선 선분이 그려져 있습니다. 선의 양 끝에 점이 있으며, 위쪽에 ㄱ, ㄴ 기호가 있습니다.
- 해설 문장: 정답 그림 아래에 한 줄로 있으며, 점 ㄱ과 점 ㄴ을 **곧게 잇는다**는 내용을 설명합니다.
- 선택지들은 모두 **도형 판단형**으로 보이며, 빈칸이나 계산 칸은 없습니다.

[Python DSL 작성 힌트]
- 상단에 문제 번호와 지시문 텍스트를 먼저 배치합니다.
- 그 아래 영역에 선택지 3개를 **가로 정렬**로 배치합니다.
- 각 선택지는 다음 요소로 구성하면 좋습니다:
  - `line` 또는 `segment` 형태의 사선
  - 양 끝의 `dot`
  - 점 옆의 작은 한글 기호 `ㄱ`, `ㄴ`
- 아래쪽 왼쪽에는 `(정답)` 라벨을 두고, 그 옆 또는 아래에 파란색 선분 예시를 배치합니다.
- 하단에는 `(해설)` 문장을 텍스트로 넣습니다.
- 요소 간 간격이 중요하므로, 선택지 3개는 같은 높이 기준선에 맞추고 정답 예시는 그보다 아래에 두는 방식이 자연스럽습니다.

[주의할 점]
- 이 문항은 **정답을 추론해서 설명하는 것**이 아니라, 시각 요소를 정확히 옮기는 것이 핵심입니다.
- “선분을 바르게 그린 것”이 무엇인지 판단하여 확정적으로 서술하지 마세요.
- ㄱ, ㄴ 기호가 각 점에 어떻게 연결되는지 보이는 대로만 적고, 의미를 단정하지 마세요.
- 정답 예시의 파란색 선은 **시각적으로 강조된 요소**로 보이지만, 이를 통해 풀이 결과를 서술하지는 마세요.
- 빈칸, 표, 분수, 계산식은 보이지 않습니다.

[불확실한 부분]
- 각 선택지에서 **ㄱ, ㄴ 기호가 정확히 어느 점을 가리키는지**는 이미지 해상도상 약간 모호할 수 있습니다.
- 선의 정확한 기울기와 각 점의 미세한 위치는 캡처 품질에 따라 차이가 있을 수 있습니다.
- 정답 표시가 아래 예시를 가리키는지, 혹은 편집상 별도 해설 예시인지 시각적으로는 확인 가능하지만, 그 의미까지 단정하지 않는 것이 안전합니다.

--- RAW VISION DRAFT END ---
