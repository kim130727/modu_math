# [전체적인 구성]
- 한 장짜리 초등 수학 문제 지면으로 보입니다.
- 왼쪽 위에 문제 번호와 지시문이 있고, 중앙에는 샴푸 병 그림이 배치되어 있습니다.
- 병의 아래쪽 단위 표시 부분을 확대 원으로 따로 보여 주는 구성입니다.
- 하단에는 보기/정답 영역과 해설 문장이 텍스트로 있습니다.
- 전체적으로 “그림 속 단위 읽기” 유형의 문제이며, 계산식이나 표는 보이지 않습니다.

# [보이는 텍스트]
- `58.`
- `물건에 표시되어 있는 들이의 단위를 알아보려고 합니다. 알맞은 것을 선택하세요.`
- 그림 속 샴푸 병에 적힌 글자:
  - `샴푸`
  - `1 L`
- 가운데 설명 문장:
  - `샴푸통에 표시되어 있는 단위는 ( L , mL )입니다.`
- 아래쪽:
  - `(정답)L`
  - `(해설)샴푸통에 1 L라고 쓰여 있으므로 표시되어 있는 단위는 L입니다.`

# [반복되는 시각 패턴]
- 본문 문장에 괄호가 있고, 그 안에 선택지처럼 `L , mL`가 나열되어 있습니다.
- 정답/해설 영역이 아래쪽에 따로 배치되어 있습니다.
- 그림에서 실제 병의 단위 표시 `1 L`를 확대 원으로 다시 보여 주는 구조가 반복적 강조 패턴입니다.
- 분홍색 점선이 원본의 작은 표시와 확대 원을 연결하여, 같은 정보를 두 위치에서 확인하게 합니다.

# [수식 단계]
- 해당 없음.
- 등호나 변형식은 보이지 않습니다.

# [등호별 변환 의미]
- 해당 없음.
- 등호를 포함한 식 변환 과정은 보이지 않습니다.

# [세부 내용]
- **문제 번호와 문장 위치**
  - 왼쪽 상단에 `58.`이 있고, 그 오른쪽으로 문제 문장이 한 줄에 이어집니다.
  - 문장 끝은 다음 줄로 넘어가며, 화면 폭에 맞춰 줄바꿈된 것으로 보입니다.
- **중앙 그림**
  - 초록색 계열의 샴푸 병이 정면으로 그려져 있습니다.
  - 병 몸통 중앙에 `샴푸`라는 글자가 세로 방향이 아닌 일반 가로 글씨로 들어가 있습니다.
  - 병의 아래쪽 둥근 부분 안에 작은 `1 L` 표시가 있습니다.
- **확대 표시**
  - 병의 `1 L` 부분에서 분홍색 점선이 뻗어 나가 오른쪽의 큰 원형 확대 도형으로 연결됩니다.
  - 오른쪽 원 안에는 큰 글씨로 `1 L`이 표시되어 있으며, 아랫부분은 병 몸통처럼 약간 초록색으로 채워진 듯한 형태입니다.
  - 확대 원은 병의 실제 표기 내용을 강조하는 역할입니다.
- **선택지 형식**
  - 문장 안에 `( L , mL )`처럼 두 단위가 쉼표로 구분되어 들어가 있습니다.
  - 정답을 쓰는 별도 칸은 보이지 않고, 바로 아래에 `(정답)L`이 인쇄되어 있습니다.
- **해설 영역**
  - `샴푸통에 1 L라고 쓰여 있으므로 표시되어 있는 단위는 L입니다.`라고 설명합니다.
  - 이 문장은 해설로 보이며, 학생이 직접 작성하는 칸은 따로 없는 형태입니다.

# [Python DSL 작성 힌트]
- 문제 제목, 번호, 본문 지시문, 그림 요소, 하단 문장 순으로 배치하면 좋습니다.
- 그림은 다음 요소를 분리해 표현하면 좋습니다.
  - 샴푸 병 본체
  - 병 안의 `샴푸` 텍스트
  - 병 아래 `1 L` 표시
  - 오른쪽 확대 원
  - 병과 확대 원을 잇는 분홍색 점선 연결선
- 선택지 `L, mL`는 괄호 포함 텍스트로 처리하면 됩니다.
- 정답 표시는 별도 텍스트 블록으로, 해설은 하단 문장 블록으로 나누어 작성할 수 있습니다.
- 정답을 계산하는 과정은 넣지 말고, **보이는 정보만** 구조화하세요.

# [주의할 점]
- `1 L`을 보고 정답을 확정하는 해설 문장이 보이지만, 이 초안에서는 문제 풀이를 다시 쓰지 말고 구조만 설명해야 합니다.
- `mL`는 선택지에만 보이며 그림 속에는 직접 보이지 않습니다.
- 확대 원 안의 글씨는 `1 L`로 분명히 보이지만, 병 몸통의 작은 글씨는 원본 이미지 해상도상 약간 작게 보일 수 있습니다.
- `(정답)L`은 지면에 인쇄된 결과이지, 빈칸을 학생이 직접 채운 모습은 아닙니다.
- 분홍색 점선의 정확한 시작점과 끝점은 확대 강조용 연결선으로 보이나, 세부 좌표는 단정하지 않는 것이 좋습니다.

# [불확실한 부분]
- 병 라벨의 `샴푸` 글씨는 선명하지만, 글꼴의 세부 스타일은 이미지 해상도 때문에 정확히 특정하기 어렵습니다.
- 확대 원 내부 하단의 초록색 곡선 부분은 병의 하단을 단순화해 표현한 것처럼 보이나, 실제 디자인 의도는 단정할 수 없습니다.
- 해설 문장 끝의 `입니다.`는 분명하지만, 문장 전체 줄바꿈 위치는 편집상 약간 달라질 수 있습니다.
