# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008756.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008756

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 화면 상단에는 문제 번호와 문장이 한 줄로 배치되어 있다.
- 가운데에는 비교 대상이 되는 두 그림이 좌우로 떨어져 배치되어 있다.
  - 왼쪽: 생수병 그림
  - 오른쪽: 종이컵 그림
- 각 그림 아래에 이름 라벨과 용량 표기가 있다.
- 그 아래에는 문장 속 빈칸을 채우는 선택형/서술형 안내 문장이 있고, 괄호 안에 선택지처럼 보이는 값들이 함께 제시되어 있다.
- 하단에는 `(정답)` 표시가 있는 짧은 정답 문장과 `(해설)` 문장이 이어진다.
- 전체적으로 “그림 비교 → 빈칸 채우기 → 정답/해설”의 순서로 구성되어 있다.

# [보이는 텍스트]
- `53. 500 mL 생수병을 보고 들이를 어림해 보려고 합니다. 알맞은 것을 선택하세요.`
- 그림 아래 왼쪽:
  - `생수병`
  - `500 mL`
- 그림 아래 오른쪽:
  - `종이컵`
- 본문 문장:
  - `종이컵의 들이는 500 mL 생수병의 들이보다 ( 많습니다, 적습니다 ).`
  - `→ 종이컵의 들이는 약 ( 180 mL , 180 L )입니다.`
- 하단:
  - `(정답)적습니다, 180 mL`
  - `(해설)종이컵의 들이는 500 mL 생수병의 들이보다 적습니다. 따라서 500 mL보다 적은 180 mL로 어림할 수 있습니다.`

# [반복되는 시각 패턴]
- 용량을 나타내는 표기가 그림 아래에 반복된다.
  - 왼쪽 그림에는 대상 이름과 수치가 세로로 배치됨.
  - 오른쪽 그림에는 대상 이름만 보임.
- 빈칸을 채우는 형태의 문장이 반복적으로 나타난다.
  - 첫 번째 빈칸: `많습니다, 적습니다` 중 선택
  - 두 번째 빈칸: `180 mL, 180 L` 중 선택
- 화살표 `→`가 있어 앞 문장에서 뒤 문장으로 판단을 이어 주는 구조이다.
- 정답과 해설이 아래쪽에 작은 글씨로 별도 제시되어 있다.

# [수식 단계]
- 수식 계산식 자체는 보이지 않는다.
- 다만 비교 문장과 단위 선택이 함께 제시되어 있어, 수치 비교를 통한 어림값 선택 구조로 보인다.
- `500 mL`와 `180 mL`처럼 같은 단위의 크기 비교가 문장 속에서 이루어진다.
- `180 L`은 단위는 보이지만, 본문 선택지 중 하나로 제시된 항목일 뿐 계산 과정은 나타나지 않는다.

# [등호별 변환 의미]
- 해당 없음.
- 화면에 등호 `=`를 이용한 변형 과정은 보이지 않는다.

# [세부 내용]
- 상단 문제 문장에는 `500 mL 생수병`을 기준으로 `들이`를 어림하는 과제가 제시된다.
- 가운데 그림은 실제 용기 형태를 단순화한 삽화이다.
  - 생수병은 세로로 긴 투명한 병 모양이며, 몸통에 가로 띠가 몇 개 보인다.
  - 종이컵은 작고 짧은 컵 모양이며, 아래쪽에 색이 들어간 띠가 보인다.
- 두 그림은 좌우로 떨어져 있으며, 크기 차이가 시각적으로 드러나도록 배치되어 있다.
- 본문 첫 문장은 비교 대상의 많고 적음을 고르는 형태이다.
- 두 번째 문장은 `약`이라는 표현을 사용해 근삿값을 고르게 한다.
- 선택지 중 `180 L`은 단위가 매우 크게 보이므로, 화면상으로는 `180 mL`와 대비되는 오답 선택지처럼 배치되어 있다.
- 하단의 해설은 종이컵이 생수병보다 작다는 점과, 그래서 `500 mL`보다 작은 값으로 어림한다는 논리를 글로 풀어쓴다.

# [Python DSL 작성 힌트]
- 좌측 상단 문제번호와 전체 문제 문장을 한 줄 텍스트로 배치한다.
- 가운데에는 두 개의 그림을 가로로 나란히 배치한다.
  - 왼쪽에 생수병 아이콘
  - 오른쪽에 종이컵 아이콘
- 각 그림 아래에 라벨 텍스트를 별도로 둔다.
  - 생수병 아래: `생수병`
  - 그 아래: `500 mL`
  - 종이컵 아래: `종이컵`
- 본문 비교 문장은 두 줄로 배치하고, 첫 줄과 둘째 줄 사이에 화살표 `→`를 넣는다.
- 괄호 안 선택지는 텍스트 슬롯처럼 표현한다.
  - 첫 번째 슬롯: `많습니다, 적습니다`
  - 두 번째 슬롯: `180 mL, 180 L`
- 하단에는 `(정답)`과 `(해설)`을 작은 글씨 또는 별도 영역으로 분리한다.
- 실제 정답을 DSL에서 채우지 말고, 화면에 보이는 그대로만 재현한다.
- 그림은 실제 도형보다 설명용 아이콘 수준이면 충분하다.
- 좌우 배치 간격을 넉넉히 두어 비교 대상임이 드러나게 한다.

# [주의할 점]
- 답을 추론해 빈칸을 채우지 말아야 한다.
- `180 mL`와 `180 L` 중 무엇이 맞는지 여기서 판단하지 말고, 보이는 텍스트만 기록한다.
- 해설의 논리는 설명하되, 계산이나 정답 확정은 하지 않는다.
- 그림의 정확한 세부 비율은 사진에서 완전히 확정하기 어렵다.
- `정답`과 `해설` 표시는 원문에 포함된 텍스트이므로 누락하지 않는다.
- 문장 중 `들이`는 아이의 교과서 표현으로 보이며, 일반적인 `부피`와는 다른 원문 표현을 그대로 써야 한다.

# [불확실한 부분]
- 생수병과 종이컵 그림의 정확한 색상, 테두리 선의 두께, 내부 장식선 개수는 이미지 해상도상 세부까지 단정하기 어렵다.
- `180 mL , 180 L` 사이의 쉼표 앞뒤 공백이 정확히 어느 정도인지 OCR 기준에 따라 다를 수 있다.
- 하단 `(정답)` 문구 앞의 공백과 줄바꿈 위치는 이미지에서 완전히 동일하게 재현하기 어렵다.
- 본문 첫 줄의 문장 끝 마침표와 띄어쓰기 일부는 확대 없이 미세 차이가 있을 수 있다.

--- RAW VISION DRAFT END ---
