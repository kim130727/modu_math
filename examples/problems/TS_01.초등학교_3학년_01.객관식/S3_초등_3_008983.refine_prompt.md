# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008983.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008983

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단에 문제 번호와 지시문이 있고, 그 아래에 여러 개의 각도 모양 그림이 배치되어 있습니다.
- 화면 왼쪽 상단에는 `18.` 번호가 보이고, 그 옆에 `직각을 선택하세요.`라는 문장이 있습니다.
- 문제 본문 영역에는 가로로 나란히 놓인 각도 그림 3개가 있으며, 왼쪽 아래쪽에는 `(정답)` 표기가 있는 자리와, 아래쪽에는 `(해설)` 문장 및 직각 표시가 있는 그림이 따로 배치되어 있습니다.
- 전체적으로 “보기에서 직각을 고르는 선택형 문제” 형태입니다.

[보이는 텍스트]
- `18.`
- `직각을 선택하세요.`
- `(정답)`
- `(해설)`
- `직각 삼각자의 직각 부분을 대어 보았을 때 꼭 맞게 겹쳐지는 각이 직각입니다.`

[반복되는 시각 패턴]
- 위쪽에 각을 나타내는 선분 그림이 여러 개 반복됩니다.
- 각 그림은 두 선분이 한 점에서 만나 각을 이루는 형태입니다.
- 보기들은 서로 크기와 방향이 조금씩 다르며, 하나는 ㄴ자처럼 보이는 직각 형태, 하나는 둔각처럼 벌어진 형태, 하나는 더 넓게 벌어진 V자 형태로 보입니다.
- 아래쪽 해설 그림에서는 ㄴ자 모양 각과 작은 정사각형 표시가 함께 있어 직각을 시각적으로 강조합니다.

[수식 단계]
- 해당 없음. 수식 변형이나 등호가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호를 포함한 식 전개가 보이지 않습니다.

[세부 내용]
- 문제 번호 `18.`은 좌측 상단에 있고, 문장 `직각을 선택하세요.`가 그 오른쪽에 이어집니다.
- 보기 그림은 상단 중앙에 3개가 가로 방향으로 배치되어 있습니다.
  - 왼쪽 보기: 한 선분이 왼쪽 위 방향으로 기울어져 있고, 다른 선분은 오른쪽으로 수평에 가깝게 뻗어 있습니다.
  - 가운데 보기: 한 선분이 수직으로 위로 올라가고, 다른 선분이 오른쪽으로 수평에 가깝게 뻗어 있어 ㄴ자 형태로 보입니다.
  - 오른쪽 보기: 한 꼭짓점에서 오른쪽 위와 오른쪽 아래 방향으로 두 선분이 벌어져 있습니다.
- 왼쪽 아래에는 `(정답)` 글자가 있고, 그 옆 또는 아래에 ㄴ자 모양의 각 그림이 따로 제시되어 있습니다.
- `(해설)`은 왼쪽 중간 아래에 있고, 그 오른쪽으로 설명 문장이 한 줄 이상 이어집니다.
- 해설 아래에는 직각 표시가 있는 ㄴ자 모양 그림이 있으며, 꼭짓점 근처에 작은 네모 표시가 있어 직각임을 강조합니다.
- 문장 내용상 직각삼각자의 직각 부분을 각에 맞춰보는 설명이지만, 이것은 해설 문구에 적힌 의미이며 계산식은 없습니다.

[Python DSL 작성 힌트]
- 상단 문제문: 번호 텍스트 `18.`과 지시문 `직각을 선택하세요.`를 좌상단에 배치하는 구조로 작성하면 좋습니다.
- 보기 3개는 동일한 높이선상에 좌우 간격을 두고 배치합니다.
- 각 보기는 단순 선분 2개로 이루어진 각 도형으로 표현하면 됩니다.
- `(정답)` 영역은 본문과 분리된 별도 위치에 두고, ㄴ자 모양 각 그림을 함께 배치합니다.
- `(해설)` 영역은 설명 텍스트와 예시 각 그림을 세로로 이어서 배치하면 원본 구조를 재현하기 쉽습니다.
- 직각 표시용 작은 정사각형은 해설 그림의 꼭짓점에 붙는 형태로 그리면 됩니다.
- 전체는 흰 배경 위에 검은/회색 선으로 구성된 단순한 초등 수학 문제 레이아웃입니다.

[주의할 점]
- 답을 추론해서 특정 보기 번호를 단정하지 마세요.
- `(정답)` 영역에 실제 정답 텍스트가 있는지 여부는 이미지상 잘 보이지 않으므로, 임의로 채워 넣지 마세요.
- 해설 문장을 문제 문장과 섞지 말고, 해설 영역으로 분리해서 다뤄야 합니다.
- 각도 그림은 정확한 기하학적 수치보다 시각적 형태가 중요하므로, 선의 기울기와 배치 관계를 중심으로 기록하세요.
- 오른쪽 보기의 각이 정확히 어느 종류인지 단정하지 말고, “벌어진 형태” 정도로만 묘사하세요.

[불확실한 부분]
- `(정답)` 옆에 실제로 답안 표시가 있는지 이미지 해상도상 명확하지 않습니다.
- 상단 보기들의 정확한 각도 크기와 선분 길이는 일부 흐릿하여 정밀하게 단정하기 어렵습니다.
- 왼쪽 보기와 오른쪽 보기의 정확한 각의 종류를 수치적으로 판정하지 말고, 보이는 형태 수준에서만 기록하는 것이 안전합니다.

--- RAW VISION DRAFT END ---
