# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008769.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008769

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 왼쪽 상단에 작은 네모 체크 표시처럼 보이는 기호가 있고, 그 오른쪽에 문제 번호와 지시문이 한 줄로 배치되어 있습니다.
- 중앙에는 연두색(초록색) 테두리의 큰 직사각형 박스가 있으며, 그 안에 경찰차 그림이 위쪽에, 아래쪽에 선택지 문구가 배치되어 있습니다.
- 화면 하단 왼쪽에는 `(정답)` 표시와 함께 정답이 제시되어 있고, 그 아래에는 `(해설)` 문장이 한 줄로 이어집니다.
- 전체적으로 객관식/선택형 문제처럼 보이며, 그림을 보고 무게 단위를 고르는 형태입니다.

[보이는 텍스트]
- `71. 무게를 알맞게 어림한 것을 찾아 선택하세요.`
- 박스 안: `약 ( 1 g , 1 kg , 1 t )`
- 하단: `(정답) 1 t`
- 하단 해설: `(해설) 경찰차의 무게는 1 kg보다 더 무거우므로 약 1 t이 알맞습니다.`
- 문장과 숫자 사이에 띄어쓰기가 보이며, `1 g`, `1 kg`, `1 t`는 쉼표로 구분된 목록 형태입니다.

[반복되는 시각 패턴]
- `1`이 들어간 단위 표기가 세 번 반복됩니다: `1 g`, `1 kg`, `1 t`.
- 괄호 `(` `)`가 두 군데 사용됩니다.
  - 선택지 전체를 감싸는 형태로 보이는 괄호 구조
  - 각각의 단위 항목이 쉼표로 나열된 형태
- 하단에는 `(정답)`, `(해설)`처럼 괄호로 시작하는 라벨 패턴이 반복됩니다.
- 숫자와 단위가 붙은 짧은 선택지들이 나열되어 있어, 시각적으로 비교하도록 만든 구성입니다.

[수식 단계]
- 이 문제 화면에는 등호 `=`가 포함된 수식 변형 단계가 보이지 않습니다.
- 따라서 수식 전개나 계산식 변화는 관찰되지 않습니다.

[등호별 변환 의미]
- 해당 없음.
- 화면에 등호가 없어서 단계별 변환 의미를 설명할 수 없습니다.

[세부 내용]
- 문제 번호 `71.`은 왼쪽 상단에 표시되어 있습니다.
- 지시문은 “무게를 알맞게 어림한 것을 찾아 선택하세요.”로, 그림을 바탕으로 적절한 무게 단위를 고르는 안내입니다.
- 중앙의 초록색 테두리 박스 안에는 경찰차 그림이 있으며, 그림 아래에 선택지 텍스트가 있습니다.
- 선택지 텍스트는 `약 ( 1 g , 1 kg , 1 t )`로 보이며, `약` 뒤에 괄호가 열리고 세 단위가 쉼표로 나열됩니다.
- 박스의 역할은 그림과 선택지를 묶어 보여주는 것으로 보입니다.
- 하단의 `(정답) 1 t`는 답안 표시로 보이며, 별도 강조 없이 텍스트로 제시되어 있습니다.
- `(해설)` 문장에서는 경찰차가 `1 kg`보다 더 무겁다는 점을 근거로 적절한 값이 `1 t`라고 설명합니다.  
  - 다만 이 설명은 문제를 푸는 결과가 아니라 화면에 이미 제시된 해설 문구입니다.

[Python DSL 작성 힌트]
- 전체 레이아웃은 상단 문항 텍스트 + 중앙 이미지 박스 + 하단 정답/해설 텍스트의 3단 구성으로 잡으면 좋습니다.
- 중앙 박스는 연두색 테두리의 사각형으로 표현하고, 내부에 경찰차 이미지를 위쪽 중앙에 배치한 뒤 아래쪽에 선택지 문구를 넣는 형태가 적절합니다.
- 선택지 문자열 `약 ( 1 g , 1 kg , 1 t )`는 한 줄 텍스트로 처리하되, 괄호와 쉼표의 간격을 원문에 가깝게 유지하는 것이 좋습니다.
- 하단의 `(정답)`, `(해설)`은 별도 텍스트 블록으로 분리해서 배치하면 구조를 재현하기 쉽습니다.
- 이미지가 핵심 요소이므로, 박스 내부에서 그림과 문구의 세로 정렬 관계를 유지하도록 좌표를 나누는 방식이 좋습니다.

[주의할 점]
- 정답을 다시 계산해서 넣지 마세요. 화면에 보이는 정답 표기만 참고해야 합니다.
- `1 g`, `1 kg`, `1 t`의 실제 비교를 추론하여 서술하더라도, 문제 풀이 과정으로 확장하지는 마세요.
- 해설 문장은 화면에 적힌 내용을 그대로 옮기는 수준으로만 다루고, 추가 해석은 최소화하세요.
- 선택지 박스의 정확한 크기나 좌표는 이미지 해상도상 오차가 있을 수 있으므로 단정하지 마세요.
- 경찰차 그림의 세부 색상이나 장식은 눈에 띄는 요소만 언급하고, 과도하게 세밀한 묘사는 피하세요.

[불확실한 부분]
- 중앙 박스의 테두리 색은 연두색 또는 초록색 계열로 보이지만, 정확한 색상명은 단정하기 어렵습니다.
- 경찰차 그림의 세부 문양과 차체 색 분포는 일부 작게 보여서 완전한 정확성은 보장하기 어렵습니다.
- 상단의 작은 네모 기호는 체크박스처럼 보이지만, 실제 의미는 문제 이미지 만으로는 확정하기 어렵습니다.
- `(정답) 1 t`와 해설이 화면 하단 왼쪽에 배치되어 있으나, 원본 문서의 정확한 정렬 방식은 다소 달라질 수 있습니다.

--- RAW VISION DRAFT END ---
