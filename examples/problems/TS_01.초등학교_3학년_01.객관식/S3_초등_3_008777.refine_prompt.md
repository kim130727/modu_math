# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008777.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008777

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 문제짜리 초등 수학/과학 상식형 선택 문제처럼 보입니다.
- 상단 왼쪽에 문제 번호와 지시문이 있고, 그 아래 중앙 왼쪽에 주전자 그림이 배치되어 있습니다.
- 그림 오른쪽에는 문장형 문항이 있으며, 괄호 안에 단위 선택형 빈칸이 있습니다.
- 하단 왼쪽에는 정답 표시와 해설 문장이 따로 제시되어 있습니다.
- 전체적으로 “그림 + 문장 + 괄호 안 선택” 구조입니다.

[보이는 텍스트]
- `□ 90. 알맞은 단위를 선택하세요.`
- `주전자의 들이는 약 1 ( mL , L )입니다.`
- `(정답)L`
- `(해설)주전자의 들이는 약 1 L입니다.`

[반복되는 시각 패턴]
- 숫자 `1`이 문장 안에 단위 선택 대상과 함께 반복적으로 보입니다.
- 괄호 `(` `)` 안에 두 단위가 나란히 적혀 있으며, 쉼표로 구분되어 있습니다.
- 하단에는 `(정답)`과 `(해설)`처럼 괄호를 사용한 설명 라벨이 반복됩니다.
- 선택형 보기 형식이 문자로만 제시되어 있고, 동그라미 체크표시는 보이지 않습니다.

[수식 단계]
- 해당 없음.
- 숫자와 단위가 포함된 문장형 문제이지만, 등호를 사용한 수식 전개는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.
- 등호가 있는 식 변형 과정은 나타나지 않습니다.

[세부 내용]
- 문제 번호 `90`은 왼쪽 상단에 있고, 그 앞에 작은 네모 기호가 있습니다.
- 지시문 `알맞은 단위를 선택하세요.`가 번호 뒤에 이어집니다.
- 주전자 그림은 문장 왼쪽에 배치되어 있으며, 그림이 단위 선택의 맥락을 시각적으로 지원합니다.
- 문장 `주전자의 들이는 약 1 ( mL , L )입니다.`에서
  - `1`은 수치로 보이고,
  - 괄호 안의 `mL`와 `L`은 선택지처럼 배치되어 있습니다.
- 아래쪽에는 정답이 따로 표기되어 있고, 그 아래 해설 문장이 한 줄로 제시됩니다.
- 해설 문장은 같은 의미를 반복해서 적은 형태로 보입니다.

[Python DSL 작성 힌트]
- 상단 문제번호와 지시문을 한 줄 텍스트로 배치합니다.
- 주전자 이미지는 문장 왼쪽에 삽입하고, 텍스트는 이미지 오른쪽에 가로로 정렬합니다.
- 문항 문장은 `1` 뒤에 괄호 슬롯을 두고, 그 안에 `mL`, `L`을 텍스트 옵션처럼 나란히 배치합니다.
- 하단에는 작은 글씨로 `(정답)`과 `(해설)` 라벨을 각각 분리해 넣습니다.
- 정답 표시는 원문에 맞춰 별도 영역으로 두되, 빈칸 채움으로 보이지 않게 처리합니다.
- 전체 레이아웃은 위에서 아래로: 문제번호/지시문 → 그림+문항문장 → 정답/해설 순으로 구성하면 됩니다.

[주의할 점]
- 정답 `L`은 보이는 텍스트에 이미 포함되어 있지만, 이것을 문제 풀이 결과로 재계산하거나 추론 과정으로 재구성하지 않도록 주의합니다.
- `mL`과 `L`은 선택지이며, 둘 중 하나를 임의로 강조하는 방식으로 바꾸지 마세요.
- 그림 자체는 주전자 모양만 보이며, 크기나 용량 수치를 그림에서 직접 읽어내는 구조는 아닙니다.
- `(정답)L` 표시는 화면에 그대로 보이는 정보로만 다루고, 별도의 풀이 설명으로 확대 해석하지 않습니다.

[불확실한 부분]
- 주전자 그림의 세부 색상과 음영은 보이지만, DSL에서 어느 정도까지 세밀하게 표현할지는 불확실합니다.
- 문제 번호 왼쪽의 작은 네모 기호가 체크박스인지 단순 장식인지 화면만으로는 확정하기 어렵습니다.
- 글꼴 크기 차이는 보이지만, 정확한 폰트 종류나 수치는 이미지 만으로 단정할 수 없습니다.

--- RAW VISION DRAFT END ---
