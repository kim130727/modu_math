# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008875.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008875

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 번호 **30.**이 있고, 그 뒤로 긴 문장형 문제 설명이 한 줄 이상에 걸쳐 배치되어 있습니다.
- 중앙에는 **두 개의 그릇 정보를 비교하는 표**가 있으며, 왼쪽이 **가 그릇**, 오른쪽이 **나 그릇**으로 나뉘어 있습니다.
- 표 아래쪽 왼편에는 **아이 그림**과 이름표처럼 보이는 **‘준혁’** 표시가 있습니다.
- 아이 오른쪽에는 연한 연두색 배경의 **말풍선/설명 상자**가 있고, 그 안에 방법 설명이 적혀 있습니다.
- 왼쪽 하단에는 선택 표시로 보이는 **원형 기호 두 개**와 **(정답) ○** 표시가 따로 있습니다.
- 맨 아래에는 **(해설)** 문장과 함께 계산식이 텍스트로 제시되어 있습니다.

[보이는 텍스트]
- `30.`
- `가 그릇과 나 그릇의 들이를 나타낸 것입니다. 주혁이 가 그릇과 나 그릇을 사용하여 양동이에 물 3 L를 담는 방법을 바르게 이야기했으면 ○, 그렇지 않으면 ×를 선택하세요.`
- 표의 머리글:
  - `가 그릇`
  - `나 그릇`
- 표의 내용:
  - `4 L 200 mL`
  - `1 L 200 mL`
- 말풍선 안 문장:
  - `가 그릇에 물을 가득 담은 후 그 물을 나 그릇이 가득 찰 때까지 붓고 남는 물을 양동이에 부으면 돼.`
- 이름표:
  - `준혁`
- 선택 표시로 보이는 기호:
  - `○`
  - `×`
  - `(정답) ○`
- 해설 문장:
  - `(해설)4 L 200 mL-1 L 200 mL = 3 L이므로 바르게 이야기했습니다.`

[반복되는 시각 패턴]
- **그릇 이름이 원형 문자 표식 안에 들어간 형태**가 반복됩니다.
  - `가`, `나`가 각각 동그라미 안에 들어가 있어 표 제목과 말풍선 본문에서 같은 표식으로 재사용됩니다.
- **표 구조가 좌우 2칸으로 반복**됩니다.
  - 왼쪽 칸: 가 그릇
  - 오른쪽 칸: 나 그릇
- **단위 표기 L, mL가 반복**되어 용량 정보를 나타냅니다.
- 문제에서 요구하는 응답 방식이 **○ / × 선택형**으로 반복적으로 제시됩니다.
- 해설에서도 원래 수치와 차이값이 **등식 형태**로 한 번 제시됩니다.

[수식 단계]
- 화면 하단의 해설에 수식이 1단계로 제시되어 있습니다.
- 표시된 식:
  - `4 L 200 mL - 1 L 200 mL = 3 L`
- 이 식은 계산 과정을 한 줄로 보여주는 형태이며, 중간 변형식은 따로 이어지지 않습니다.

[등호별 변환 의미]
- 첫 번째 등호 앞의 식 `4 L 200 mL - 1 L 200 mL`는 두 그릇의 들이 차이를 나타내는 뺄셈 형태로 보입니다.
- 등호 뒤의 `3 L`는 계산 결과로 제시되어 있습니다.
- 다만 이 항목은 **해설에 적힌 결과를 옮겨 적는 수준**으로만 보이며, 실제 계산 과정을 단계별로 나눠 쓰지는 않습니다.

[세부 내용]
- 문제는 **가 그릇과 나 그릇의 들이**를 알려 주고, 이를 이용해 **양동이에 물 3 L를 담는 방법**에 대한 설명이 맞는지 판단하게 합니다.
- 표에서 **가 그릇은 4 L 200 mL**, **나 그릇은 1 L 200 mL**로 제시되어 있습니다.
- 말풍선 속 준혁의 말은:
  - 가 그릇에 물을 가득 담는다.
  - 그 물을 나 그릇이 가득 찰 때까지 붓는다.
  - 남는 물을 양동이에 붓는다.
- 시각적으로는 이 설명이 **문장형 선택 근거**로 제시되어 있고, 그 아래에 **○/× 선택**이 연결됩니다.
- 하단 해설은 위 설명이 맞다는 취지의 문장과 함께, 두 들이의 차이를 계산식으로 보여 줍니다.

[Python DSL 작성 힌트]
- 상단 문제 문장은 **좌측 정렬 텍스트 블록**으로 배치하면 됩니다.
- 가운데 표는 **2열 표**로, 상단 행은 연한 파란색 배경의 머리글, 하단 행은 흰색 배경의 값 칸으로 구성하면 됩니다.
- 표의 각 열 제목은 `가 그릇`, `나 그릇`처럼 **동그라미 문자 + 한글 명칭** 형태를 유지하세요.
- 표 아래에는 **캐릭터 이미지 + 이름표 + 말풍선**이 한 덩어리로 보이도록 배치합니다.
- 말풍선은 연한 연두색 배경의 **둥근 사각형 또는 말풍선 도형**으로 처리하고, 내부 문장을 줄바꿈 포함 텍스트로 넣으면 됩니다.
- 좌하단의 `○`, `×`, `(정답) ○`는 별도 선택 영역처럼 세로로 배치하는 것이 자연스럽습니다.
- 맨 아래 해설은 `해설` 라벨과 함께 **식 1줄 + 문장 1줄** 형태로 구성하면 좋습니다.
- 수식은 `4 L 200 mL - 1 L 200 mL = 3 L`를 그대로 텍스트로 표현하되, 단위 사이 띄어쓰기를 이미지와 비슷하게 유지하세요.

[주의할 점]
- **정답을 추론해서 채우지 마세요.** 화면에는 `(정답) ○`가 보이지만, 이것을 별도 정답 정보로 해석해도 되고, DSL에 넣을 때는 문제 풀이처럼 보이지 않게 주의해야 합니다.
- 해설의 계산 결과를 기반으로 **문제 답을 서술형으로 재구성하지 마세요.**
- `준혁` 이름은 말풍선 화자 표시로 보이므로, 표 제목이나 문제 핵심 인물과 혼동하지 않도록 하세요.
- `가`, `나`는 각각 **원형 기호 안의 문자**이며, 단순한 괄호가 아닙니다.
- 표의 수치는 각각 **용량 단위가 함께 있는 복합 표기**이므로 숫자와 단위를 분리해 잘못 배치하지 않도록 주의하세요.
- 해설 식은 **뺄셈 하나가 있는 단일 식**이며, 여러 단계의 변형식으로 확장해서 쓰지 마세요.

[불확실한 부분]
- 좌하단의 `○`, `×` 기호는 실제 선택지인지, 화면 캡처 과정에서 일부가 잘 보인 것인지 약간 애매합니다.
- `(정답) ○` 표시는 실제 정답 표시인지, 해설용 보조 표식인지 화면만으로는 완전히 단정하기 어렵습니다.
- 말풍선 도형의 정확한 꼬리 방향과 세부 곡선은 이미지 해상도상 완전히 명확하지 않습니다.
- 표의 테두리 색과 굵기는 보이지만, 정확한 RGB 값까지는 확정할 수 없습니다.

--- RAW VISION DRAFT END ---
