[전체적인 구성]
- 한 페이지형 수학 문제 이미지로 보입니다.
- 상단에 문제 번호와 지시문이 있고, 그 아래에 도형들이 가로로 나열되어 있습니다.
- 상단의 첫 줄은 **문제에서 선택해야 할 도형들**이 큰 격자 배경 위에 배치되어 있습니다.
- 상단 도형 아래에는 괄호로 묶인 보기 목록 `( 가 , 나 , 다 , 라 , 마 )`가 따로 적혀 있습니다.
- 그 아래에는 `정답` 표기가 있고, 이어서 아래쪽에 `해설` 영역이 별도로 있습니다.
- 해설 영역은 다시 같은 도형들을 가로로 배열해 놓았으며, 각 도형에 직각 표시가 추가되어 있습니다.
- 전체적으로 “직각이 있는 사각형”을 찾는 유형의 문제로 보이지만, 여기서는 실제 정답을 판단하지 않고 시각 구조만 설명합니다.

[보이는 텍스트]
- `2. 직각이 있는 사각형을 모두 선택하세요.`
- 보기 표기: `( 가 , 나 , 다 , 라 , 마 )`
- `정답)나, 다, 라`
- `해설`
- 도형 내부 글자: `가`, `나`, `다`, `라`, `마`

[반복되는 시각 패턴]
- 같은 종류의 도형 5개가 가로 방향으로 반복 배치되어 있습니다.
- 각 도형은 내부에 한 글자씩 들어 있으며, 왼쪽부터 `가`, `나`, `다`, `라`, `마` 순서입니다.
- 상단 문제 영역과 하단 해설 영역에 거의 동일한 도형 배열이 반복됩니다.
- 하단 해설에서는 각 도형의 꼭짓점 근처에 작은 직각 표시가 추가된 점이 반복 패턴의 차이입니다.
- 격자 배경이 반복적으로 사용되어 도형의 변의 방향과 길이를 비교하기 쉽게 되어 있습니다.

[수식 단계]
- 해당 없음.
- 계산식, 등호 연결식, 변형 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.
- 등호가 있는 수식 전개가 보이지 않습니다.

[세부 내용]
- 상단 문제 영역:
  - 왼쪽 위에 문제 번호 `2.`가 있습니다.
  - 그 오른쪽에 지시문이 한 줄로 배치되어 있습니다.
  - 지시문 아래에는 연한 파란색 격자 위에 5개의 사각형 모양이 가로로 놓여 있습니다.
  - 각 도형은 모두 내부에 한 글자씩 표시되어 있습니다.
- 도형의 배치:
  - 첫 번째 `가`는 좌우가 약간 벌어진 사다리꼴처럼 보입니다.
  - 두 번째 `나`는 위쪽이 기울어진 사각형 형태로 보입니다.
  - 세 번째 `다`는 세로로 긴 직사각형처럼 보입니다.
  - 네 번째 `라`는 가로로 긴 직사각형처럼 보입니다.
  - 다섯 번째 `마`는 왼쪽이 기울어진 사다리꼴처럼 보입니다.
- 보기 표기:
  - 상단 도형 아래 중앙 쪽에 `( 가 , 나 , 다 , 라 , 마 )`가 괄호 안에 적혀 있습니다.
  - 쉼표로 항목이 구분되어 있습니다.
- 정답/해설 영역:
  - 상단과 중간 사이 왼쪽에 `정답)`이 보이고, 그 뒤에 결과 문자열이 적혀 있습니다.
  - 하단에는 `해설`이라는 표기가 왼쪽에 있고, 그 오른쪽에 같은 5개 도형이 다시 나열됩니다.
  - 해설용 도형에는 일부 꼭짓점에 작은 네모 모양의 직각 표식이 들어가 있어 직각의 위치를 강조합니다.
  - 각 도형 내부 글자 위치는 상단과 거의 동일합니다.

[Python DSL 작성 힌트]
- 캔버스 상단에 문제 번호와 지시문 텍스트를 먼저 배치합니다.
- 그 아래 연한 격자 배경 영역을 만들고, 가로로 5개의 도형을 일정 간격으로 정렬합니다.
- 각 도형은 외곽선만 있는 형태로 그리되, 내부에 `가`, `나`, `다`, `라`, `마`를 각각 중앙에 넣습니다.
- 상단 도형 아래에는 괄호로 묶인 보기 문자열을 따로 배치합니다.
- 중간 왼쪽에 `정답)` 텍스트를 작게 넣고, 하단 왼쪽에 `해설` 텍스트를 배치합니다.
- 해설 영역의 도형들은 상단 도형과 같은 위치 관계를 유지하되, 각 직각 위치에 작은 직각표시를 추가합니다.
- 직각 표시는 작은 ㄱ자 형태의 선분 또는 작은 사각 모서리 표시로 구현하면 좋습니다.
- 전체적으로 가로 정렬과 균등 간격이 중요합니다.

[주의할 점]
- 정답 문자열 `정답)나, 다, 라`는 보이지만, 이를 근거로 문제 풀이 과정을 새로 만들지 마세요.
- `가`, `나`, `다`, `라`, `마`의 정확한 도형 분류를 여기서 다시 판정하지 말고, 보이는 외형만 기록하세요.
- 일부 도형의 변이 격자선과 정확히 얼마나 맞닿는지, 또는 각도가 몇 도인지까지는 이미지상 정밀 판독이 어렵습니다.
- 하단 해설의 직각 표식 위치는 도형마다 조금씩 다를 수 있으므로, “꼭짓점 근처” 정도로 설명하는 것이 안전합니다.
- 격자 칸 수를 정확히 세어 수치화하는 것은 불확실할 수 있습니다.

[불확실한 부분]
- 상단의 각 도형 변 길이와 각도의 정확한 수치.
- 하단 해설 도형에 표시된 직각 표식의 정확한 개수와 각 꼭짓점의 세부 위치.
- `정답)` 뒤의 표기가 인쇄체인지 손글씨체인지, 또는 굵기 차이가 있는지.
- 격자 배경의 정확한 칸 수와 페이지 내 위치 비율.
