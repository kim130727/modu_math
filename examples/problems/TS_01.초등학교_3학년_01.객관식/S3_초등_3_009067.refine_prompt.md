# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009067.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009067

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 문제짜리 도형 선택형 문제로 보입니다.
- 상단에는 문제 번호와 지시문이 있고, 그 아래 파란 점선 격자 위에 5개의 도형이 가로로 나열되어 있습니다.
- 각 도형에는 가운데쯤 한 글자씩 라벨이 들어가 있습니다.
- 도형 아래에는 설명 문장이 한 줄로 적혀 있습니다.
- 그 아래에 `(정답)` 표기가 따로 있고, 정답으로 보이는 선택 결과가 텍스트로 제시되어 있습니다.
- 하단에는 `(해설)` 표기와 함께 같은 5개 도형이 다시 배열되어 있으며, 일부 도형에는 모서리 표시처럼 보이는 작은 표시가 추가되어 있습니다.

[보이는 텍스트]
- `8. 도형을 보고 알맞은 기호를 모두 선택하세요.`
- 도형 라벨: `가`, `나`, `다`, `라`, `마`
- 설명 문장: `네 변의 길이가 모두 같은 사각형은 ( 가, 나, 다, 라, 마 )입니다.`
- `정답) 다, 마`
- `해설)`

[반복되는 시각 패턴]
- 위쪽과 아래쪽에 같은 종류의 도형 5개가 반복되어 나타납니다.
- 도형의 배치는 왼쪽에서 오른쪽으로 일렬이며, 각 도형 중앙에 한 글자 라벨이 들어가 있습니다.
- 도형들은 모두 파란 격자 배경 위에 놓여 있어 크기 비교나 변의 길이 비교를 돕는 형태입니다.
- 아래 해설 영역에서도 위와 같은 5개 도형 배열이 다시 반복됩니다.
- 반복 도형의 종류는 대체로 직사각형, 사다리꼴/사각형 변형, 마름모 형태로 보입니다.

[수식 단계]
- 해당 없음. 수식 변형이나 등호 연결식은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호를 포함한 계산 단계가 보이지 않습니다.

[세부 내용]
- 상단의 문제 문장은 도형의 성질을 보고 알맞은 기호를 고르는 형식입니다.
- 괄호 `( 가, 나, 다, 라, 마 )` 부분은 선택지 목록처럼 보이며, 쉼표로 구분된 문자들이 한 줄로 나열되어 있습니다.
- 첫 번째 도형 `가`:
  - 네 변이 직각에 가까운 직사각형 형태로 보입니다.
  - 좌우 폭이 일정하고 위아래 변도 수평입니다.
- 두 번째 도형 `나`:
  - 왼쪽 변은 수직에 가깝고, 오른쪽 변은 비스듬한 선입니다.
  - 위아래 변이 수평이며, 전체적으로 사다리꼴처럼 보입니다.
- 세 번째 도형 `다`:
  - 네 변이 같은 길이처럼 보이도록 그려진 정사각형 형태입니다.
- 네 번째 도형 `라`:
  - 위아래 변이 수평이고 좌우 변이 비스듬해 보이는 사다리꼴 형태입니다.
  - 윗변보다 아랫변이 더 길어 보입니다.
- 다섯 번째 도형 `마`:
  - 마름모처럼 생긴 도형입니다.
  - 위아래 꼭짓점이 뾰족하고 좌우로 벌어진 형태입니다.
- 하단 해설 영역의 도형들에는 각 꼭짓점 근처에 작은 표시가 보이는데, 이는 변의 길이나 꼭짓점 위치를 설명하는 보조 표시일 가능성이 있습니다.
- 다만 그 작은 표시가 정확히 어떤 의미인지, 실제로 무엇을 강조하는지는 이미지 해상도상 확실하지 않습니다.

[Python DSL 작성 힌트]
- 문제 본문 텍스트와 선택지 문구를 별도 요소로 분리해서 배치하면 좋겠습니다.
- 상단 도형 5개는 동일한 기준선 위에 가로 정렬된 도형들로 처리하면 됩니다.
- 각 도형 중앙의 한글 라벨은 텍스트 객체로 따로 넣는 방식이 적절합니다.
- 파란 점선 격자는 배경 격자 요소로 표현하면 원본 느낌을 살리기 쉽습니다.
- 하단의 `(정답)`과 `(해설)`은 본문과 구분되는 주석/보조 텍스트로 배치하세요.
- 해설 영역의 도형 반복은 상단과 거의 같은 구성이므로 재사용 가능한 도형 정의를 쓰는 것이 좋습니다.
- 도형의 실제 정확한 좌표나 비율은 이미지 기준으로 근사 배치가 필요합니다.

[주의할 점]
- 정답을 직접 풀이해서 유도하지 말고, 이미지에 이미 적힌 `정답) 다, 마`만 사실로 기록하세요.
- `가, 나, 다, 라, 마` 각각이 정확히 어떤 도형인지 단정할 때는 이미지상 보이는 외형만 기준으로 서술하세요.
- `나`, `라`의 기울어진 변이나 사다리꼴 형태는 해상도에 따라 약간 다르게 보일 수 있습니다.
- 하단 해설 도형의 작은 모서리 표시의 의미는 추론만 가능하므로 단정하지 마세요.
- 선택지 괄호 안의 문자는 항목 나열로 보이며, 빈칸 형태는 따로 보이지 않습니다.

[불확실한 부분]
- 하단 해설 영역의 작은 표시가 직각 표시인지, 변의 길이 비교 표시인지, 또는 단순한 강조선인지 확실하지 않습니다.
- `나`와 `라` 도형의 정확한 분류는 이미지 해상도상 약간 애매할 수 있습니다.
- 상단 문장의 괄호 표기는 선택지처럼 보이지만, 실제로는 문제 서술의 일부일 수도 있습니다.
- 도형의 정확한 기하학적 종류명은 문제 의도와 다를 수 있으므로, 시각적으로 보이는 형태 중심으로 기술하는 것이 안전합니다.

--- RAW VISION DRAFT END ---
