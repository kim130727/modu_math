# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009040.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009040

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 지시문이 있고, 그 아래 왼쪽에는 도형 그림 1개가 배치되어 있습니다.
- 오른쪽에는 연한 살구색/주황색 계열의 긴 직사각형 영역이 있으며, 그 안에 선택지 텍스트가 가로로 나열되어 있습니다.
- 하단 왼쪽에는 `(정답)` 표시가 붙은 짧은 답안 예시가 있고, 그 아래에 `(해설)` 문장이 1줄로 제시되어 있습니다.
- 전체적으로 “도형을 보고 해당 도형 이름을 고르는” 객관식/선택형 형태의 문제 화면입니다.

[보이는 텍스트]
- `□ 78. 도형의 이름을 모두 선택하세요.`
- 선택지 영역 안:
  - `직사각삼각형`
  - `직사각형`
  - `정사각형`
- 하단:
  - `(정답)직사각형, 정사각형`
  - `(해설)정사각형은 직사각형이라고 할 수 있습니다.`

[반복되는 시각 패턴]
- 선택지 텍스트 3개가 같은 크기와 비슷한 간격으로 한 줄에 배치되어 있습니다.
- 아래쪽에 `(정답)`, `(해설)`처럼 괄호가 붙은 안내 문구가 반복적으로 사용되어 있습니다.
- 왼쪽의 도형 그림은 테두리선만 있는 단순 사각형 형태로, 내부 색은 채워져 있지 않습니다.
- 문제 번호 앞에는 작은 네모 모양 기호가 보이며, 체크박스처럼 보이지만 실제 선택 여부는 표시되어 있지 않습니다.

[수식 단계]
- 해당 없음. 수식이나 등호 변형이 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호 기호가 포함된 변형식이 보이지 않습니다.

[세부 내용]
- 왼쪽 도형:
  - 세로로 긴 흰색 내부의 사각형 1개가 있으며, 바깥쪽 테두리는 회색/검은색 선입니다.
  - 일반적인 직사각형처럼 보이며, 정사각형처럼 보일 수도 있는 비율로 보이지만, 화면만으로는 정확한 가로세로 비를 단정하지는 않습니다.
- 오른쪽 선택지 박스:
  - 연한 배경색의 큰 직사각형 영역 안에 3개의 도형 이름이 가로로 배치되어 있습니다.
  - 각 텍스트는 별도의 체크박스나 동그라미가 없이 글자만 보입니다.
- 하단 정답/해설:
  - `(정답)`은 괄호와 함께 굵게 보이는 듯한 접두어 역할을 하며, 그 뒤에 정답 텍스트가 이어집니다.
  - `(해설)` 다음에는 한 문장 설명이 이어집니다.
- 해설 문장 의미상, 정사각형과 직사각형의 포함 관계를 설명하는 내용으로 보입니다.

[Python DSL 작성 힌트]
- 문제 제목 영역, 도형 그림, 선택지 박스, 정답/해설 텍스트를 각각 별도 오브젝트로 분리해서 배치하는 것이 좋습니다.
- 선택지 3개는 같은 y축 선상에 가로 정렬하고, 각 텍스트 간 간격을 일정하게 두세요.
- 왼쪽 도형은 단순 사각형 1개로 표현하고, 내부 채움 없이 외곽선만 그리면 됩니다.
- 하단의 `(정답)`과 `(해설)`은 별도 텍스트 줄로 처리하는 편이 자연스럽습니다.
- 선택지 박스의 배경색은 연한 살구색/주황색 계열로 처리하면 원본 분위기와 유사합니다.
- 문제 번호 앞의 작은 네모 기호는 필요하면 체크박스 모양의 장식 요소로 둘 수 있습니다.

[주의할 점]
- 정답을 빈칸에 추론해서 넣는 방식으로 처리하지 마세요. 화면에 이미 보이는 정답 표시는 별도 텍스트로만 옮기면 됩니다.
- 도형의 정확한 가로세로 비율은 이미지 해상도에 따라 다르게 보일 수 있으므로, 단정적인 수치화는 피하세요.
- 선택지가 “모두 선택” 형식이지만, 실제 체크 표시가 보이지 않으므로 선택 상태를 임의로 추가하지 마세요.
- 해설 문장은 문제 풀이 과정이 아니라 설명 문구로만 취급하세요.

[불확실한 부분]
- 왼쪽 도형이 정사각형인지 직사각형인지 화면상 비율만으로는 약간 애매하게 보일 수 있습니다. 다만 하단 정답 표시는 화면에 그대로 존재합니다.
- 문제 번호 앞의 작은 네모 기호가 실제 체크박스인지 장식용 기호인지 확정하기 어렵습니다.
- 선택지 박스의 정확한 색상 값과 위치는 이미지 처리 기준에 따라 약간 달라질 수 있습니다.

--- RAW VISION DRAFT END ---
