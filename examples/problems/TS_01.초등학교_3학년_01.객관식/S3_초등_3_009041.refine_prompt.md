# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009041.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009041

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 왼쪽 위에 문제 번호와 지시문이 있고, 그 아래 중앙에는 보라색 테두리의 큰 직사각형 박스가 배치되어 있습니다.
- 큰 박스 안에는 3개의 보기 그림이 가로 방향으로 나란히 놓여 있으며, 각 보기 위쪽에는 동그라미 안의 기호가 붙어 있습니다.
- 각 보기에는 점이 찍힌 선분처럼 보이는 도형이 하나씩 있습니다.
- 박스 아래 왼쪽에는 `(정답)` 표시와 동그라미 기호가 함께 보이고, 그 아래에는 `(해설)` 문장 형태의 설명이 이어집니다.
- 전체적으로 “보기 3개 중 선분을 고르는 문제” 형식으로 보입니다.

[보이는 텍스트]
- `79. 선분을 찾아 선택하세요.`
- 보기 기호: 동그라미 안에 `ㄱ`, `ㄴ`, `ㄷ`
- 첫 번째 보기의 양 끝 쪽 라벨: `ㄱ`, `ㄴ`
- 두 번째 보기의 위/아래 점 옆 라벨: `ㄷ`, `ㄹ`
- 세 번째 보기의 왼쪽/오른쪽 점 옆 라벨: `ㅁ`, `ㅂ`
- `(정답) ㄴ`
- `(해설) 선분은 두 점을 곧게 이은 선이므로 ㄴ입니다.`

[반복되는 시각 패턴]
- 세 개의 보기 모두 동일한 크기의 기호 원형 표시가 위쪽에 붙어 있고, 그 아래에 도형이 하나씩 배치됩니다.
- 각 보기에는 점 2개가 포함되어 있으며, 점 사이를 잇는 선의 방향만 다릅니다.
- 첫 번째와 세 번째 보기는 대각선 방향의 선처럼 보이고, 두 번째 보기는 세로 방향의 선처럼 보입니다.
- 점 옆의 한글 기호는 도형의 양 끝을 표시하는 용도로 반복 사용된 것으로 보입니다.

[수식 단계]
- 해당 없음. 수식 변형이나 계산 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호로 이어진 식 변환은 보이지 않습니다.

[세부 내용]
- 첫 번째 보기:
  - 약간 왼쪽 아래에서 오른쪽 위로 기울어진 선처럼 보입니다.
  - 선의 양쪽 근처에 점이 각각 하나씩 있고, 각 점 옆에 `ㄱ`, `ㄴ`이 표시되어 있습니다.
- 두 번째 보기:
  - 거의 정확한 세로선 형태입니다.
  - 위쪽 점 옆에 `ㄷ`, 아래쪽 점 옆에 `ㄹ`이 있습니다.
- 세 번째 보기:
  - 왼쪽 아래에서 오른쪽 위로 올라가는 대각선 형태입니다.
  - 왼쪽 점 근처에 `ㅁ`, 오른쪽 점 근처에 `ㅂ`이 있습니다.
- `(정답)` 표시 옆에는 동그라미 안의 `ㄴ`이 있어, 해설상 선택 대상이 어느 보기인지 알려주는 구조입니다.
- 해설 문장은 “두 점을 곧게 이은 선”이라는 정의를 텍스트로 제시하고 있습니다.

[Python DSL 작성 힌트]
- 상단에 문제 번호와 지시문 텍스트를 먼저 배치합니다.
- 그 아래에 보라색 테두리의 큰 직사각형 박스를 그리고, 박스 내부를 3개의 동일한 영역처럼 보이도록 가로로 나눠 배치합니다.
- 각 영역에는:
  - 원형 기호 1개
  - 점 2개가 찍힌 선분 형태의 도형 1개
  - 점 옆의 한글 라벨 2개
  를 포함하도록 구성합니다.
- 박스 아래에는 `(정답)`과 `(해설)` 텍스트를 왼쪽 정렬로 배치합니다.
- 도형은 선의 방향, 점의 위치, 라벨 위치를 개별 요소로 분리해서 표현하면 좋습니다.
- 텍스트와 도형 간 간격이 꽤 있으므로, 세로 간격을 넉넉히 잡는 편이 맞습니다.

[주의할 점]
- 정답을 추론하여 새로 쓰지 마세요. 화면에 이미 보이는 `(정답) ㄴ`만 사실로 다루면 됩니다.
- `ㄴ`이 답으로 표시되어 있더라도, 초안에서는 “어느 보기인지”를 설명하는 수준만 유지하세요.
- 첫 번째와 세 번째 도형이 단순한 직선처럼 보여도, 실제로는 점의 배치와 기울기를 정확히 구분해서 서술해야 합니다.
- 해설의 문장은 문제의 정의를 설명하는 텍스트이므로, 계산식으로 해석하지 말아야 합니다.

[불확실한 부분]
- 보라색 박스의 정확한 크기와 각 보기 사이의 정확한 간격은 이미지 해상도상 대략적으로만 파악됩니다.
- 각 점 옆의 한글 라벨이 선분의 끝점 표시인지, 보기 식별용 표기인지 화면만으로는 완전히 단정하기 어렵습니다.
- 첫 번째 보기의 선이 아주 약간 아래로 기울었는지, 거의 수평에 가까운지 경계가 다소 흐립니다.

--- RAW VISION DRAFT END ---
