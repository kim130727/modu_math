# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008807.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008807

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 문장이 한 줄로 배치되어 있습니다.
- 상단 중앙에는 저울 그림이 있고, 저울 위에 동물 그림 3개가 올라가 있습니다.
- 상단 오른쪽에는 선택지 형태로 보이는 괄호 안 텍스트 `( g , kg )`가 크게 표시되어 있습니다.
- 하단 왼쪽에는 `(정답)g`가 따로 보이며, 그 아래에 `(해설)` 문장이 한 줄로 제시되어 있습니다.
- 전체적으로 “무게 단위 선택” 문제 형식이며, 그림을 통해 상황을 보여주고 답 단위를 고르게 하는 구성입니다.

[보이는 텍스트]
- `□ 36.`
- `필통의 무게를 나타내는 데 알맞은 단위를 선택하세요.`
- `( g , kg )`
- `(정답)g`
- `(해설)1 kg보다 가벼운 물건의 무게는 g으로 나타냅니다.`

[반복되는 시각 패턴]
- 괄호 안에 두 단위가 나란히 제시된 형식 `( g , kg )`이 보입니다.
- 문제 번호 `36.` 앞에 작은 네모 표시가 있습니다.
- 하단에 `(정답)`과 `(해설)`이 라벨처럼 반복적으로 붙어 있습니다.
- 그림은 단일 사물 대신 여러 개의 물체가 저울 위에 놓인 형태로, 크기 비교를 시각적으로 돕는 패턴입니다.

[수식 단계]
- 해당 없음. 화면에는 등호를 포함한 식 변형 단계가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호가 이어진 변형식이 없습니다.

[세부 내용]
- 문제 문장은 “필통의 무게를 나타내는 데 알맞은 단위를 선택하세요.”로 읽힙니다.
- 상단의 그림은 저울 모양이며, 위쪽 받침대 위에 동물 그림 3개가 놓여 있습니다.
  - 왼쪽과 오른쪽에는 비슷한 색의 거북/동물 그림이 보이고,
  - 가운데에는 초록색 작은 동물 그림이 있습니다.
- 저울 받침대는 연한 노란색 계열이며, 중앙 아래쪽에 받침 구조가 두 줄 정도 표현되어 있습니다.
- 오른쪽의 `( g , kg )`는 보기처럼 보이며, 두 단위 중 하나를 고르는 형식입니다.
- 하단 해설 문장은 “1 kg보다 가벼운 물건의 무게는 g으로 나타냅니다.”라고 적혀 있어, 단위 선택의 기준을 설명하는 보조 문장으로 보입니다.
- 다만 이 해설은 화면에 보이는 문장일 뿐이며, 문제를 푸는 과정으로 확장해 쓰지는 않습니다.

[Python DSL 작성 힌트]
- 상단 좌측: 문제 번호, 본문 문장을 한 줄 텍스트로 배치합니다.
- 중앙 상단: 저울 그림을 넣고, 그 위에 동물 3개의 아이콘을 가로로 배치합니다.
- 우측 상단: `( g , kg )`를 큰 글씨로 표시합니다.
- 하단 좌측: `(정답)`과 `(해설)` 라벨을 별도 텍스트로 분리하여 배치합니다.
- 선택지 표현은 두 단어를 괄호와 쉼표로 묶은 텍스트로 구현하면 됩니다.
- 그림 요소는 실제 물체 이름을 단정하지 말고, 보이는 대로 동물 아이콘 3개와 저울로 처리하는 것이 안전합니다.

[주의할 점]
- 문제를 실제로 풀어서 정답을 계산하거나 설명을 완성하지 마세요.
- `(정답)g`는 화면에 이미 보이는 텍스트이지만, 이를 바탕으로 답을 추론해 다른 빈칸에 확장하지 마세요.
- 그림 속 동물의 정확한 종류를 단정하지 말고, 보이는 형태 중심으로만 묘사하세요.
- 저울이 실제로 어떤 상태인지(평형, 기울어짐 등)는 화면만으로 명확하지 않으므로 단정하지 않는 것이 좋습니다.
- `g`와 `kg`의 의미 설명은 해설 문장에 있는 범위까지만 다루고, 추가 설명은 자제하세요.

[불확실한 부분]
- 상단 그림의 동물이 정확히 거북인지, 다른 동물인지 확정하기 어렵습니다.
- 저울이 실제 측정 장면인지, 단순한 삽화인지 화면만으로는 명확하지 않습니다.
- 왼쪽의 작은 네모 표시는 체크박스인지 인쇄상의 장식인지 단정하기 어렵습니다.
- 하단의 `(정답)g`는 편집용 표시처럼 보이지만, 실제 학생용 지면에 포함된 요소인지 여부는 불확실합니다.

--- RAW VISION DRAFT END ---
