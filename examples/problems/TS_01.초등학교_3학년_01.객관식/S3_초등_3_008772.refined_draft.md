[문제 개요]
- 초등 수학/생활수학 형태의 그림 보고 고르기 문제이다.
- 주어진 상황: `가` 물병에 물을 가득 채운 후 `나` 물병에 옮겨 담았을 때의 그림을 보고, 더 작은 들이의 물병을 선택하는 문제이다.
- 원본 하단에는 `(정답)`과 `(해설)`이 인쇄되어 있으며, 이들은 **보이는 사실**로만 기록한다.

[원본에 보이는 요소]
- 문제 번호: `□ 82.`
- 본문 문장:
  - `가 물병에 물을 가득 채운 후 나 물병에 옮겨 담았습니다.`
  - `그림과 같이 물을 채웠을 때에 들이가 더 작은 물병을 선택해 보세요.`
- 그림 속 라벨:
  - 위쪽 기울어진 물병 안에 `가`
  - 아래쪽 세워진 물병 안에 `나`
- 선택지 형태:
  - `( 가 물병 , 나 물병 )`
- 하단 인쇄 문구:
  - `(정답) 가 물병`
  - `(해설) 가 물병의 물이 나 물병에 다 들어가므로 가 물병의 들이가 더 적습니다.`

[수식/관계 구조]
- 등호나 계산식은 보이지 않는다.
- 수학적 관계는 문장형 조건으로만 제시된다.
- 관계의 핵심은 다음과 같이 읽힌다:
  - `가 물병`의 물을 `나 물병`에 옮겨 담을 수 있음
  - 이때 `가 물병`의 들이가 `나 물병`보다 더 작다고 판단하는 문제 구성
- 단, 위 관계는 문제 문장의 의미를 정리한 것이며, 정답을 새로 추론하는 단계는 아니다.

[등호별 변환 의미]
- 해당 없음.
- 등호 연쇄 변형, 분수 변형, 계산 과정은 보이지 않는다.

[빈칸 정책]
- 이 문제에는 답을 적는 빈칸이 직접적으로 보이지 않는다.
- 선택지는 `( 가 물병 , 나 물병 )` 형태로 제시되어 있으며, 원본에 인쇄된 정답 `(정답) 가 물병`은 **보이는 사실**로만 기록한다.
- 만약 DSL에서 빈칸/응답 슬롯을 만들더라도, 추론값을 채우지 말고 원본에 보이는 형태 그대로 비워 두는 정책을 따른다.
- 보이지 않는 정답을 임의로 채우지 않는다.

[반복 패턴]
- `가`, `나` 표기가 문장, 그림, 선택지, 해설에서 반복된다.
- `물병`이라는 명사가 여러 위치에 반복되어 같은 대상군을 연결한다.
- 괄호 선택지 형식 `( 항목1 , 항목2 )`가 사용된다.
- 하단의 `(정답)`, `(해설)` 라벨이 같은 형식으로 분리 배치된다.

[DSL 작성 힌트]
- 상단 텍스트는 2줄 문장으로 분리 배치하는 것이 자연스럽다.
- 그림은 중앙 상단에 배치하고, 두 개의 물병을 각각 독립된 도형 요소로 표현하면 좋다.
- 위쪽 물병:
  - 약간 비스듬히 기울어진 상태
  - 입구가 오른쪽 아래를 향함
  - 내부에 `가` 원형 라벨이 있음
- 아래쪽 물병:
  - 세워진 상태
  - 내부에 물이 채워진 파란 음영이 있음
  - 내부에 `나` 원형 라벨이 있음
- 라벨 `가`, `나`는 원형 배지처럼 보이므로, 원 안의 한글 텍스트로 처리하는 편이 맞다.
- 오른쪽 아래 선택지는 한 줄 텍스트로 두 항목을 괄호와 쉼표로 감싸 배치한다.
- 하단의 `(정답)`, `(해설)`은 별도의 텍스트 블록으로 분리해 배치한다.
- 전체 스타일은 흰 배경, 검은 글자, 하늘색/파란색 윤곽선과 물 채움 표현 중심이다.

[semantic/solvable 힌트]
- 문제의 의미는 “그림을 보고 들이가 더 작은 물병을 고르기”이다.
- 하단 해설 문구가 인쇄되어 있지만, 이를 근거로 추가 계산을 하거나 답을 재도출하지 않는다.
- 그림에서 `가` 물병의 물이 `나` 물병에 들어가는 상황이 표현되어 있다.
- 다만 본 항목은 문제 의미를 설명하는 수준이며, 빈칸을 채우거나 새 답을 만드는 용도는 아니다.

[주의 및 TODO]
- 정답 `(정답) 가 물병`은 원본에 인쇄되어 **보이는 사실**이지만, 이를 바탕으로 답을 새로 추론하거나 확정 계산처럼 다루지 않는다.
- `가`와 `나`의 위치를 뒤바꾸지 않도록 주의한다: 위쪽 기울어진 병이 `가`, 아래쪽 세워진 병이 `나`로 보인다.
- 왼쪽 맨 앞의 작은 네모 기호 `□`는 체크박스인지 인쇄용 기호인지 확정하지 말고 TODO로 남긴다.
- 물병 윤곽, 물의 경계, 음영은 해상도상 다소 흐릿하므로 세부 형상은 단정하지 않는다.
- 이 문제는 수식형 변형이 없으므로 분수/등호 관련 변환 설명은 추가하지 않는다.
