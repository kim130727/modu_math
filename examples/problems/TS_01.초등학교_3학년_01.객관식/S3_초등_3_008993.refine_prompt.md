# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008993.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008993

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 문제 번호 `31.`이 왼쪽 상단에 있고, 그 오른쪽에 문제 문장이 한 줄로 배치되어 있습니다.
- 본문 아래쪽에는 연한 파란색 격자 배경 위에 도형 4개가 가로로 나열되어 있습니다.
- 각 도형 내부에는 각각 글자 `가, 나, 다, 라`가 하나씩 들어 있습니다.
- 도형 배열 아래에는 선택지 묶음처럼 `( 가, 나, 다, 라 )`가 가운데 정렬에 가깝게 표시되어 있습니다.
- 더 아래 왼쪽에는 `(정답)`과 `(해설)`이 각각 라벨처럼 표시되어 있습니다.  
- 화면 하단에는 해설 문장이 한 줄로 이어져 있습니다.

[보이는 텍스트]
- `31. 직사각형을 모두 찾아 기호를 선택하세요.`
- 도형 안의 글자: `가`, `나`, `다`, `라`
- 아래 괄호 표기: `( 가, 나, 다, 라 )`
- `정답) 나, 라`
- `해설) 네 각이 모두 직각인 사각형을 직사각형이라고 합니다.`

[반복되는 시각 패턴]
- 네 개의 도형이 같은 높이 선상에 가로로 반복 배치되어 있습니다.
- 각 도형은 하나의 기호를 대표하는 듯하며, 내부 글자 위치도 모두 비슷한 중심 배치입니다.
- 왼쪽 두 도형과 오른쪽 두 도형이 모두 같은 격자 배경 위에 놓여 있어 비교형 문제처럼 보입니다.
- 도형 외곽선은 모두 회색 계열의 단선으로 그려져 있고, 내부 글자는 굵은 검은색입니다.

[수식 단계]
- 해당 없음. 이 문제 화면에는 등호가 이어지는 수식 변형 과정이 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 첫 번째 도형 `가`:
  - 격자 위에 놓인 사각형 계열 도형입니다.
  - 왼쪽 변과 오른쪽 변이 기울어져 있고, 위아래 변은 거의 수평입니다.
  - 전체적으로 평행사변형처럼 보이며, 내부에 `가`가 들어 있습니다.
- 두 번째 도형 `나`:
  - 네 변이 직각으로 보이는 직사각형 형태입니다.
  - 세로가 더 긴 직사각형처럼 보이며, 내부에 `나`가 있습니다.
- 세 번째 도형 `다`:
  - 윗부분이 넓고 아래로 갈수록 좁아지는 형태로 보입니다.
  - 좌우 변이 기울어져 있고, 직각형은 아닌 사각형처럼 보입니다.
  - 내부에 `다`가 있습니다.
- 네 번째 도형 `라`:
  - 가로로 긴 직사각형 형태입니다.
  - 네 모서리가 모두 직각처럼 보이며, 내부에 `라`가 있습니다.
- 도형 아래의 `( 가, 나, 다, 라 )`는 보기 또는 선택 대상 기호를 나열한 것으로 보입니다.
- 문제의 의도는 여러 사각형 중 직사각형에 해당하는 것을 고르는 형식으로 읽힙니다.  
  - 다만 이는 문제 문장과 해설을 바탕으로 한 의미 설명이며, 실제 정답 판단 자체는 여기서 하지 않습니다.

[Python DSL 작성 힌트]
- 상단 문제 문장을 텍스트 객체로 배치합니다.
- 그 아래에 연한 파란 격자 배경 사각형 영역을 하나 두고, 그 안에 도형 4개를 가로 정렬합니다.
- 각 도형은 개별 폴리곤 또는 사각형 객체로 그리되, 외곽선만 있고 채움색은 없는 형태로 표현합니다.
- 각 도형의 중심에 `가`, `나`, `다`, `라` 텍스트를 각각 넣습니다.
- 도형 아래에는 `( 가, 나, 다, 라 )` 텍스트를 중앙 배치합니다.
- 하단 좌측에는 `(정답)`, `(해설)` 라벨과 해설 문장을 별도 텍스트 블록으로 배치합니다.
- 격자 배경은 작은 정사각형 격자선이 반복되는 패턴으로 처리하면 좋습니다.
- 도형들의 상대적 기울기와 너비 차이를 살려서 서로 다른 사각형처럼 보이게 구성합니다.

[주의할 점]
- `가, 나, 다, 라`를 정답으로 단정해서는 안 됩니다. 화면에 보이는 선택 대상 문자로만 다루어야 합니다.
- 도형의 정확한 좌표나 각도는 이미지 해상도상 오차가 있을 수 있으므로, 너무 세밀하게 단정하지 않는 것이 좋습니다.
- `정답)`과 `해설)`은 화면에 이미 인쇄된 텍스트이므로, 문제 풀이 결과와 혼동하지 않도록 주의해야 합니다.
- 해설 문장은 보이지만, 이를 바탕으로 빈칸이나 선택지를 임의로 채우지 마세요.

[불확실한 부분]
- 첫 번째 도형 `가`와 세 번째 도형 `다`의 정확한 사각형 유형은 이미지 해상도와 기울기 때문에 약간의 해석 차이가 있을 수 있습니다.
- 격자선의 정확한 개수와 각 도형의 정확한 위치는 원본 이미지의 픽셀 수준 판독이 아니므로 근사적으로만 설명했습니다.
- 하단의 `(정답) 나, 라`는 화면에 표시된 텍스트이지만, Python DSL 작성 시에는 별도 해설 영역으로 처리하는 것이 적절한지 여부가 도구 설계에 따라 달라질 수 있습니다.

--- RAW VISION DRAFT END ---
