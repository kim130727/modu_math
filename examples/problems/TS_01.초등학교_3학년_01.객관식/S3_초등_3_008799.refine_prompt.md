# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008799.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008799

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 장의 단일 문제 화면이다.
- 좌측 상단에 문제 번호와 지시문이 있고, 그 아래 넓은 흰 여백에 그림 선택형 문항이 배치되어 있다.
- 화면 중앙 부근에는 가위 그림 1개가 있고, 좌측 아래와 우측 상단에 책가방 그림이 각각 1개씩 배치되어 있다.
- 좌측 아래 책가방 그림 옆에는 `(정답)` 표시가 붙어 있어 정답 예시로 제시된 선택지를 뜻한다.
- 하단에는 `(해설)`로 시작하는 문장이 한 줄로 제시되어 있다.
- 수식이나 연산식은 보이지 않고, 그림 비교를 통해 더 무거운 물체를 고르는 유형이다.

[보이는 텍스트]
- `28. 무게가 더 무거운 것을 선택하세요.`
- `(정답)`
- `(해설) 양손으로 들었을 때 힘이 더 드는 책가방이 더 무겁습니다.`

[반복되는 시각 패턴]
- 같은 종류의 물체 그림이 2개 이상 반복되지는 않지만, 비교 대상이 서로 다른 위치에 분산 배치되어 있다.
- 책가방 그림은 좌측 아래와 우측 상단에 각각 하나씩 있으며, 형태가 거의 동일하게 보인다.
- 비교 대상 외에 가위 그림이 가운데에 배치되어 있어, 2가지 물체 중 더 무거운 쪽을 고르는 구조로 읽힌다.
- `(정답)` 표시는 그림 바로 옆에 붙는 형식으로, 선택된 항목을 시각적으로 강조한다.

[수식 단계]
- 해당 없음.  
- 등호, 계산식, 단계별 변형은 보이지 않는다.

[등호별 변환 의미]
- 해당 없음.  
- 수식 변형이 없는 그림 선택 문제이다.

[세부 내용]
- 좌측 상단에는 작은 빈 사각형 표시와 함께 문제 번호 `28.`이 있다.
- 그 오른쪽에 문제 지시문이 이어진다.
- 그림 영역:
  - 중앙 상단 쪽에 가위 그림이 있다.
  - 좌측 하단 쪽에 책가방 그림이 있다.
  - 우측 상단 쪽에 책가방 그림이 있다.
- 좌측 하단 책가방 근처에 `(정답)` 텍스트가 있으며, 해당 책가방이 선택된 답안임을 나타낸다.
- 하단 문장 `(해설) 양손으로 들었을 때 힘이 더 드는 책가방이 더 무겁습니다.`는 선택 기준을 설명한다.
- 배경은 흰색이며, 별도의 표, 칸, 화살표, 분수틀, 도형 연결선은 없다.
- 각 그림은 독립적으로 배치되어 있어, 클릭/선택형 문항의 보기처럼 보인다.

[Python DSL 작성 힌트]
- 문제 제목 영역과 그림 영역, 해설 영역을 분리해서 배치하면 좋다.
- 그림은 좌측/우측/중앙의 상대 위치를 유지해 배치하는 것이 핵심이다.
- `(정답)` 텍스트는 좌측 아래 책가방 그림의 인근에 작은 라벨처럼 붙여 표현하면 된다.
- 해설 문장은 하단에 별도 텍스트 블록으로 넣는 방식이 적절하다.
- 선택지 번호나 보기 박스가 따로 보이지 않으므로, 그림 객체만 독립 배치하는 구성이 자연스럽다.

[주의할 점]
- 문제를 풀어 답을 추론해 쓰면 안 된다.
- 보이는 그림이 모두 책가방처럼 생겼더라도, 본문에 따라 정답 표시가 있는 위치를 그대로 기록해야 한다.
- `(정답)` 표시는 결과값이 아니라 화면에 인쇄된 라벨로 취급해야 한다.
- 해설 문장은 추측하지 말고, 보이는 그대로 옮겨 적어야 한다.
- 가위와 책가방의 실제 크기 비교는 시각 자료만으로 단정하지 말고, 위치와 종류만 기술하는 것이 안전하다.

[불확실한 부분]
- 우측 상단 책가방이 정답인지 여부는 화면상 `(정답)` 라벨이 좌측 아래 책가방에 붙어 있으므로, 그 기준을 따르되 추가 해석은 하지 않는 것이 좋다.
- 가위와 책가방의 정확한 색상 톤, 외곽선 두께, 세부 질감은 이미지 해상도에 따라 다르게 보일 수 있다.
- 좌측 상단의 작은 빈 사각형은 인쇄상의 장식인지 체크박스인지 화면만으로는 단정하기 어렵다.

--- RAW VISION DRAFT END ---
