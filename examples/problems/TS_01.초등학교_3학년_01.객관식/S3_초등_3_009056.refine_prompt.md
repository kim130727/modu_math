# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009056.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009056

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 페이지 상단 왼쪽에 문제 번호와 지시문이 있고, 그 아래와 오른쪽에 연한 파란색 격자 위 도형들이 배치되어 있습니다.
- 가운데 왼쪽에는 정답 표기처럼 보이는 괄호 텍스트가 따로 있고, 그 아래에 해설 문장이 한 줄 있습니다.
- 하단에는 같은 도형들을 다시 한 번 격자 배경에 배치한 예시 그림이 들어 있습니다.
- 전체적으로 “위의 도형들 중 직각삼각형을 고르는 문제” 형태로 보입니다.

[보이는 텍스트]
- `94. 직각삼각형을 모두 선택하세요.`
- `(정답) 나, 바`
- `(해설) 한 각이 직각인 삼각형을 직각삼각형이라고 합니다.`
- 도형 내부 라벨:
  - `가`
  - `나`
  - `다`
  - `라`
  - `마`
  - `바`

[반복되는 시각 패턴]
- 격자 배경 위에 삼각형 6개가 각각 하나씩 분리되어 배치되어 있습니다.
- 각 삼각형 내부에 한글 한 글자 라벨이 들어가 있습니다.
- 아래쪽 해설용 그림에서는 위쪽 문제 그림과 거의 같은 배치가 반복되며, 일부 도형에는 직각 표시처럼 보이는 작은 네모 표시가 추가되어 있습니다.
- 같은 스타일의 파란 격자, 검은 외곽선, 내부 문자 배치가 반복됩니다.

[수식 단계]
- 해당 없음: 수식 변형이나 등호가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음: 등호가 사용된 식이 보이지 않습니다.

[세부 내용]
- 상단 문제 그림:
  - 격자 위에 삼각형 6개가 서로 떨어져 있습니다.
  - 각 삼각형은 검은 선으로만 그려져 있고, 내부에 각각 `가, 나, 다, 라, 마, 바`가 표시되어 있습니다.
  - 삼각형의 방향이 서로 다릅니다.
  - 일부 도형은 한 변이 수직/수평처럼 보이고, 일부는 기울어진 변들로 이루어져 있습니다.
- 도형 배치 감각:
  - `가`는 왼쪽 위 영역에 있으며 위 꼭짓점이 뾰족한 형태입니다.
  - `나`는 상단 중앙 부근에 있고 왼쪽 변이 수직에 가깝게 보이며 위쪽 변이 수평에 가깝게 보입니다.
  - `다`는 상단 오른쪽에 길게 뻗은 형태입니다.
  - `라`는 왼쪽 아래 쪽에 있고 좌우로 벌어진 형태입니다.
  - `마`는 하단 중앙에 세로로 길어 보입니다.
  - `바`는 오른쪽 아래에 있으며 한 변이 수평, 한 변이 수직처럼 보이는 작은 삼각형입니다.
- 정답 표시는 문제 그림 아래쪽 왼편에 `(정답) 나, 바`로 따로 적혀 있습니다.
- 해설 문장은 직각삼각형의 정의를 설명하는 일반 문장으로 보입니다.
- 하단 재배치 그림에서는 `바` 도형 근처에 작은 직각 표시 모양의 네모가 추가되어 있어, 직각이 있는 부분을 강조한 것으로 보입니다.

[Python DSL 작성 힌트]
- 문제 텍스트와 해설 텍스트를 별도 영역으로 분리해 배치하면 좋습니다.
- 상단 문제 그림은 큰 격자 사각형 안에 삼각형 6개를 각각 독립 도형으로 그리는 방식이 적합합니다.
- 각 삼각형은 `polygon` 또는 `line` 조합으로 표현하고, 내부 라벨은 도형 중앙 근처에 텍스트로 넣으면 됩니다.
- 하단 해설 그림은 상단 도형 배치를 거의 그대로 복제하되, 특정 도형의 직각 위치에 작은 직각 표시를 추가하는 구조가 어울립니다.
- 정답 문구 `(정답) 나, 바`는 본문과 분리된 텍스트 블록으로 넣는 것이 좋습니다.
- 격자는 배경선으로 반복 생성하고, 삼각형 외곽선은 더 진한 색으로 오버레이하는 방식이 자연스럽습니다.

[주의할 점]
- 정답을 새로 추론해서 쓰지 말고, 보이는 텍스트에 있는 정보와 이미지 구조만 옮겨 적어야 합니다.
- `나`, `바`에 대한 정답 표시는 이미지에 이미 보이는 내용이므로, 이것을 시각 사실로만 기록해야 합니다.
- 직각 여부를 설명하는 해설은 보이지만, 각 도형이 왜 해당되는지 수학적으로 풀어 쓰지는 않아야 합니다.
- 도형의 정확한 기울기나 픽셀 수준 좌표는 불확실하므로, 필요 이상으로 단정하지 않는 것이 좋습니다.
- 하단 그림의 작은 네모 표시는 직각 표시로 보이지만, 세부 크기와 정확한 위치는 약간 불확실합니다.

[불확실한 부분]
- 각 삼각형의 꼭짓점 좌표와 정확한 각도는 이미지 해상도상 정밀하게 판독하기 어렵습니다.
- 하단 해설 그림에서 일부 도형 내부 라벨과 직각 표시의 상대 위치는 약간 흐릿해 보일 수 있습니다.
- `가`, `다`, `라`, `마` 도형이 정확히 어떤 변 길이를 가지는지까지는 이미지 정보만으로 확정하기 어렵습니다.
- 격자 칸 수를 정확히 세어 좌표화하는 것은 이 이미지 만으로는 오차가 있을 수 있습니다.

--- RAW VISION DRAFT END ---
