# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008825.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008825

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 흰 배경의 초등 수학/과학 연계형 객관식 또는 단답형 문제 화면처럼 보입니다.
- 화면 상단 왼쪽에 문제 번호와 문제 문장이 있고, 중앙에는 물통/우유팩 그림이 배치되어 있습니다.
- 그림 오른쪽에는 분홍색 원형 확대경이 연결되어 있어, 용기 표면의 표시를 확대해서 보여 줍니다.
- 하단에는 단위 선택을 유도하는 설명 문장과, 그 아래에 `(정답)` 및 `(해설)`이 따로 표시되어 있습니다.
- 전체적으로 “그림 속 표기 읽기 → 단위 판단” 구조입니다.

# [보이는 텍스트]
- `59.`
- `물건에 표시되어 있는 들이의 단위를 알아보고, 알맞은 것을 선택하세요.`
- 확대된 표시에 `200 mL`
- `두유팩에 표시되어 있는 단위는 ( L , mL )입니다.`
- `(정답)mL`
- `(해설)두유팩에 200 mL라고 쓰여 있으므로 표시되어 있는 단위는 mL입니다.`
- 괄호 안에 선택지처럼 `L , mL`가 보입니다.
- 물건 그림 자체에도 작은 글씨로 `200 mL`가 보입니다.

# [반복되는 시각 패턴]
- 같은 정보가 두 번 제시됩니다.
  - 원본 물체의 작은 표기
  - 오른쪽 원형 확대 그림 안의 큰 표기
- 확대 영역은 분홍색 점선/점선 곡선으로 원본 그림과 연결되어 있습니다.
- 하단 해설에서 다시 한 번 `200 mL`가 문자로 반복됩니다.
- 단위 선택은 `( L , mL )`처럼 괄호 안에 두 항목을 나란히 놓는 형식입니다.

# [수식 단계]
- 이 문제에는 등호로 이어지는 수식 변형 단계는 보이지 않습니다.
- 숫자와 단위가 함께 표기된 읽기 문제 형태입니다.

# [등호별 변환 의미]
- 해당 없음.
- 등호를 사용한 변형식이 화면에 나타나지 않습니다.

# [세부 내용]
- 중앙의 물체는 초록색 계열의 두유팩/우유팩처럼 보이는 직육면체 모양입니다.
- 물체 앞면 하단에 `200 mL`가 인쇄되어 있습니다.
- 오른쪽 확대 원은 분홍색 테두리의 원형이며, 확대 대상의 일부만 크게 보여 줍니다.
- 확대 원 안에는 윗부분의 초록색 그림 일부와 `200 mL` 텍스트가 크게 보입니다.
- 원본 그림과 확대 원은 분홍색 점선으로 연결되어 있어 “이 부분을 확대했다”는 의미가 시각적으로 드러납니다.
- 문제 문장은 상단 왼쪽에서 시작해 한 줄로 이어지다가 줄바꿈되어 보입니다.
- 하단의 `(정답)mL`는 정답 표기 영역처럼 따로 떨어져 배치되어 있습니다.
- `(해설)`은 정답 아래에 위치하고, 해설 문장은 한 줄로 제시됩니다.

# [Python DSL 작성 힌트]
- `문항번호`, `문제문장`, `그림영역`, `확대원`, `하단설명`을 분리해서 구성하면 좋습니다.
- 그림 영역에는 다음 요소를 따로 배치하면 좋습니다.
  - 직육면체 형태의 두유팩 그림
  - 물체 표면의 `200 mL` 작은 텍스트
  - 오른쪽의 원형 확대경
  - 원본과 확대경을 잇는 점선 연결선
- 단위 선택 부분은 `"( L , mL )"`처럼 괄호와 쉼표 포함 텍스트로 처리할 수 있습니다.
- 정답 칸처럼 보이는 `(정답)mL`는 실제 입력칸인지 단순 표기인지 구분해서 배치해야 합니다.
- 해설 문장은 본문 텍스트와 시각적으로 구분되는 별도 텍스트 블록으로 두는 것이 좋습니다.

# [주의할 점]
- `200 mL`는 보이는 사실로만 적고, 그 의미를 문제 풀이 과정처럼 확장해서 쓰지 마세요.
- `정답` 표기는 화면에 보이지만, 이를 근거로 다른 값을 추론해 빈칸을 채우지 마세요.
- 두유팩의 정확한 그림 종류는 사진만으로 단정하지 말고, “두유팩처럼 보이는 용기” 정도로 완화해도 됩니다.
- 확대 원의 연결선이 점선인지 점선 곡선인지 약간 애매할 수 있으므로, “분홍색 점선 연결” 정도로 표현하는 것이 안전합니다.
- 선택지 `( L , mL )`의 띄어쓰기와 괄호 형태는 실제 화면과 약간 다르게 보일 수 있습니다.

# [불확실한 부분]
- 작은 물체 그림이 실제로는 두유팩인지 우유팩인지, 또는 다른 음료 용기인지 확정하기 어렵습니다.
- 상단 문제 문장의 줄바꿈 위치는 화면 폭에 따라 달라질 수 있어 정확한 줄 단위 재현은 불확실합니다.
- `(정답)mL`의 괄호 위치와 띄어쓰기는 화면 캡처상 다소 밀착되어 보여, 정확한 텍스트 간격은 불확실합니다.
- 확대 원 내부의 상단 초록색 도형은 제품 디자인의 일부로 보이지만, 구체적 의미는 확정할 수 없습니다.

--- RAW VISION DRAFT END ---
