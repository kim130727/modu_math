# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009013.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009013

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호가 있고, 그 오른쪽으로 문제 지시문이 한 줄 이상 이어집니다.
- 본문 중앙에는 가로로 놓인 선분 후보 그림이 3개 세로 배열로 배치되어 있습니다.
- 왼쪽 아래에는 `(정답)` 표시와 함께 짧은 선분 예시가 따로 제시되어 있고, 그 아래에 `(해설)` 문장이 있습니다.
- 전체적으로 “문장 지시 + 보기 그림 + 정답 예시 + 해설” 구조입니다.

[보이는 텍스트]
- `51.`
- `직선을 알아보려고 합니다. 점 ㄱ과 점 ㄴ을 지나 양쪽으로 길게 늘인 직선으로 알맞은 것을 선택하세요.`
- 보기 그림의 각 선분 위/근처에 `ㄱ`, `ㄴ` 표기가 보입니다.
- 왼쪽 아래에 `(정답)`
- `(해설)직선은 선분을 양쪽으로 끝없이 늘인 곧은 선입니다.`

[반복되는 시각 패턴]
- 가운데 영역에 비슷한 형태의 가로선 3개가 반복됩니다.
- 각 가로선은 끝점처럼 보이는 작은 점 표시가 있고, 그 위 또는 근처에 `ㄱ`, `ㄴ` 글자가 붙어 있습니다.
- 3개의 그림은 모두 같은 방향의 수평선이며, 차이는 양쪽 끝이 선으로 더 이어져 있는지, 점이 선의 안쪽에 있는지 등입니다.
- 왼쪽 아래의 `(정답)` 예시는 보기와 유사한 가로선 하나를 따로 제시합니다.

[수식 단계]
- 해당 없음. 수식 변형이나 등호가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호가 없습니다.

[세부 내용]
- 문제 지시문은 “직선”을 고르는 내용이며, “점 ㄱ과 점 ㄴ을 지나 양쪽으로 길게 늘인 직선”이라는 설명이 들어 있습니다.
- 가운데 보기 3개는 모두 가로 방향의 선으로 보이지만, 각 보기의 끝 처리와 점의 위치가 서로 다릅니다.
- 첫 번째 보기:
  - 수평선이 길게 그려져 있고,
  - 왼쪽 끝 위쪽에 `ㄱ`, 오른쪽 쪽 위에 `ㄴ`이 보입니다.
  - 왼쪽 끝은 선이 조금 더 이어지는 듯한 표현이 있고, 오른쪽 끝도 점을 지나 약간 더 이어진 형태처럼 보입니다.
- 두 번째 보기:
  - 수평선이 가운데 길게 있고,
  - 왼쪽에 `ㄱ`, 오른쪽에 `ㄴ`이 각각 점 근처에 표시됩니다.
  - 양쪽 끝이 비교적 딱 끊긴 선처럼 보입니다.
- 세 번째 보기:
  - 수평선이 길게 있고,
  - 왼쪽 쪽에 `ㄱ`, 오른쪽 쪽에 `ㄴ`이 있으며,
  - 왼쪽 끝과 오른쪽 끝 중 적어도 한쪽은 점 바깥으로 더 이어진 듯한 표현이 보입니다.
- 왼쪽 아래 `(정답)` 옆에는 짧은 수평선이 따로 그려져 있는데, 이 역시 예시 형태로 보입니다.
- 해설 문장은 직선의 정의를 설명하는 서술형 문장입니다.

[Python DSL 작성 힌트]
- 문제 상단의 문장은 텍스트 박스로 배치하면 됩니다.
- 중앙의 3개 보기 그림은 같은 스타일의 가로선 오브젝트를 세로로 간격을 두고 정렬하면 됩니다.
- 각 보기에는 작은 점 2개와 그 위의 문자 `ㄱ`, `ㄴ`을 별도의 텍스트 요소로 배치해야 합니다.
- 선의 끝이 점을 지나 더 연장되는지 여부를 표현하는 것이 중요합니다.
- 왼쪽 아래 `(정답)` 레이블과 옆의 짧은 선분 예시는 별도의 소형 도형 묶음으로 처리하면 됩니다.
- `(해설)` 문장은 하단 텍스트 박스로 넣고, 문제 본문과 구분되도록 좌측 정렬하세요.
- 보기 3개는 폭은 비슷하게, 세로 간격만 일정하게 맞추면 원본과 유사한 구성이 됩니다.

[주의할 점]
- 직선이 무엇인지 문제에서 설명하지만, 답을 직접 선택하거나 특정 보기를 단정하지 마세요.
- 점 ㄱ, 점 ㄴ의 실제 위치 관계를 임의로 보정하지 마세요.
- 선이 “끝까지 이어지는지” “점에서 끊기는지”는 그림 그대로만 반영해야 합니다.
- `(정답)` 표시는 화면에 실제로 보이지만, 이것이 정답 선택 결과를 의미한다고 단정적으로 해석하지는 마세요.
- 해설 문장은 문제 풀이가 아니라 설명 문장으로만 다루세요.

[불확실한 부분]
- 중앙의 세 번째 보기에서 선이 점 바깥으로 어느 정도 더 이어지는지, 이미지 해상도상 경계가 다소 अस्प명확합니다.
- 첫 번째 보기와 세 번째 보기의 끝 처리 차이는 확대하지 않으면 정확히 구분이 어려울 수 있습니다.
- 왼쪽 아래 `(정답)` 옆의 짧은 선분은 예시인지 실제 정답 표시인지 화면만으로는 100% 단정하기 어렵습니다.

--- RAW VISION DRAFT END ---
