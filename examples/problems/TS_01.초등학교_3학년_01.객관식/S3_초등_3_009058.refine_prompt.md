# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009058.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009058

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 왼쪽 위에 문제 번호와 지시문이 있고, 그 아래에 점선 격자 배경 위로 여러 개의 삼각형 도형이 가로로 나열되어 있습니다.
- 각 도형 안에는 한글 글자 1개씩이 들어가 있으며, 도형마다 모양이 조금씩 다릅니다.
- 도형 배열 아래에는 선택지처럼 보이는 글자 묶음이 괄호로 표시되어 있습니다.
- 더 아래에는 `(정답)` 표기와 함께 정답이 글자로 적혀 있고, 그 아래에 짧은 해설 문장이 있습니다.
- 화면 하단에는 같은 도형 묶음이 다시 한 번 작은 크기로 제시되어 있습니다.

[보이는 텍스트]
- `96. 직각삼각형을 모두 선택하세요.`
- 도형 안의 글자: `가`, `나`, `다`, `라`, `마`
- 선택지 형태의 표기: `( 가 , 나 , 다 , 라 , 마 )`
- `(정답)나, 라`
- `(해설)한 각이 직각인 삼각형은 나, 라입니다.`

[반복되는 시각 패턴]
- 점선 격자 배경 위에 삼각형들이 연속적으로 배치되어 있습니다.
- 각 삼각형 내부에는 한글 라벨이 중앙 부근에 배치되어 있습니다.
- 위쪽의 도형 배열과 아래쪽의 도형 배열이 거의 같은 구성을 반복합니다.
- 삼각형마다 직각을 나타내는 작은 네모 표시가 있는 것처럼 보이는 도형이 일부 있습니다.
- 하나의 행 안에서 도형들이 좌우로 일정 간격을 두고 나열되어 있어, 비교형 문제처럼 보입니다.

[수식 단계]
- 해당 없음. 수식 변형식이나 등호 연쇄가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 문제 지시문은 “직각삼각형을 모두 선택”하라는 형태입니다.
- 화면 상단의 그림에는 총 5개의 삼각형이 좌→우로 배열되어 있습니다.
- 각 삼각형은 외곽선만 그려져 있고 내부는 비어 있으며, 각 도형마다 글자 `가`, `나`, `다`, `라`, `마`가 하나씩 들어 있습니다.
- 일부 도형은 한 꼭짓점이 직각처럼 보이도록 한 변이 수직, 다른 변이 수평으로 놓여 있습니다.
- 특히 `나`와 `라` 도형에는 직각 표시처럼 보이는 작은 네모 모양이 근처에 있습니다.
- `가`, `다`, `마`는 일반 삼각형처럼 보이지만, 직각 표시가 뚜렷하지 않습니다.
- 문제 아래쪽의 `(정답)나, 라`와 해설 문장은 정답 정보를 직접 제공하고 있습니다. 다만 이는 시각적 정보로 존재하는 텍스트일 뿐, 여기서 새로 추론한 것은 아닙니다.
- 하단에 반복 제시된 도형 묶음은 위쪽 그림과 동일 계열의 재배치로 보이며, 학습용 예시 또는 해설용 재표시처럼 보입니다.

[Python DSL 작성 힌트]
- 상단에 문제 번호와 지시문 텍스트를 배치합니다.
- 그 아래에 연한 파란색 점선 격자 배경 영역을 크게 만듭니다.
- 격자 위에 삼각형 5개를 가로로 정렬해 배치합니다.
- 각 삼각형은 단순 외곽선 도형으로 그리되, 내부에 한글 라벨 `가`, `나`, `다`, `라`, `마`를 각각 넣습니다.
- 일부 도형에는 직각 표시용 작은 네모 마커를 추가합니다.
- 도형 묶음 아래에 괄호로 감싼 선택지 표기 `( 가 , 나 , 다 , 라 , 마 )`를 가운데 정렬에 가깝게 배치합니다.
- 그 아래에는 `(정답)나, 라`, `(해설)한 각이 직각인 삼각형은 나, 라입니다.` 텍스트를 줄바꿈하여 표시합니다.
- 하단에도 동일한 스타일의 격자 영역과 삼각형 묶음을 한 번 더 배치합니다.
- 전체적으로 좌측 정렬된 학습지 스타일을 유지합니다.

[주의할 점]
- 도형의 정확한 기울기나 꼭짓점 좌표는 이미지 해상도에 따라 달라질 수 있으므로, 너무 세밀하게 단정하지 않는 것이 좋습니다.
- 직각 표시가 있는지 여부는 작은 네모 마커의 존재를 기준으로 다루되, 확대 없이 단정적으로 과장하지 않도록 주의합니다.
- `(정답)`과 `(해설)`은 문제 해결 과정이 아니라 화면에 이미 인쇄된 텍스트로 취급합니다.
- 위쪽과 아래쪽 도형 묶음이 완전히 동일한지, 또는 일부만 같은지는 제작 시 다시 확인이 필요합니다.

[불확실한 부분]
- 각 삼각형의 꼭짓점 위치와 변의 정확한 각도는 이미지상 선명도가 제한되어 있어 완전히 확정하기 어렵습니다.
- `나`, `라` 도형에 보이는 직각 표시의 크기와 정확한 위치는 확대에 따라 다르게 보일 수 있습니다.
- 하단에 반복된 도형 배열이 해설용인지, 단순 재인쇄인지 화면만으로는 단정하기 어렵습니다.

--- RAW VISION DRAFT END ---
