# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009026.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009026

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 흰색 배경의 수학 문제 이미지이며, 위아래로 같은 형태의 문제 문장이 두 번 반복되어 보입니다.
- 각 문제 문장 아래에 삼각형 그림 4개가 가로로 나열되어 있고, 각 그림 위쪽에 선택지처럼 보이는 한글 기호가 붙어 있습니다.
- 왼쪽 아래에는 (2)번 문장과 그 아래에 짧은 답안처럼 보이는 텍스트가 별도로 배치되어 있습니다.
- 전체적으로 “그림을 보고 알맞은 기호를 고르는” 선택형 문제 구성입니다.

[보이는 텍스트]
- 상단 문제 문장: `□ 64. 삼각형을 보고 알맞은 기호를 모두 선택하세요.`
- 같은 문제 문장이 한 번 더 반복됨: `□ 64. 삼각형을 보고 알맞은 기호를 모두 선택하세요.`
- 각 그림 위에 보이는 기호:
  - 첫 번째 그림 위: `가`
  - 두 번째 그림 위: `나`
  - 세 번째 그림 위: `다`
  - 네 번째 그림 위: `라`
- 하단 설명 문장:
  - `(2) 한 각이 직각인 삼각형은 나, 라입니다.`
- 맨 아래 답안처럼 보이는 텍스트:
  - `(2) 나, 라`

[반복되는 시각 패턴]
- 동일한 문제 문장이 상하로 2회 반복되어 있습니다.
- 각 반복마다 삼각형 4개가 동일한 순서로 왼쪽에서 오른쪽으로 배열됩니다.
- 각 삼각형 위에는 한글 기호 1개씩 배치되어 있어, 그림-기호 대응 구조가 반복됩니다.
- 반복된 두 문제 블록은 거의 같은 레이아웃이지만, 아래쪽 블록에서는 하단 설명 문장과 답안 텍스트가 추가로 보입니다.

[수식 단계]
- 해당 없음. 등호를 포함한 수식 변형은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호 변환 단계가 보이지 않습니다.

[세부 내용]
- 첫 번째 블록의 네 삼각형은 모두 윤곽선만 있는 단순한 도형입니다.
  - 첫 번째 삼각형: 위쪽 꼭짓점이 뾰족한 모양으로, 아래변이 수평에 가깝게 보입니다.
  - 두 번째 삼각형: 왼쪽 변이 수직에 가깝고, 아래변이 수평에 가깝고, 오른쪽으로 기울어진 변이 있습니다.
  - 세 번째 삼각형: 한 변이 아래쪽에서 왼쪽으로 길게 뻗고, 위쪽 꼭짓점이 오른쪽 위에 있는 비대칭 삼각형입니다.
  - 네 번째 삼각형: 위쪽 변이 수평에 가깝고 오른쪽 변이 수직에 가깝게 보이는 직각 형태의 삼각형처럼 보입니다.
- 두 번째 블록의 네 삼각형도 동일한 배치이지만, 내부 각도나 기울기가 첫 블록과 약간 다르게 그려져 있습니다.
- 아래쪽 문장 `(2) 한 각이 직각인 삼각형은 나, 라입니다.`는 특정 삼각형 기호를 언급하는 설명문으로 보입니다.
- 그 바로 아래 `(2) 나, 라`는 답을 적어 놓은 형태처럼 보이지만, 시각적으로는 인쇄된 텍스트인지 작성된 답인지 구분이 어렵습니다.

[Python DSL 작성 힌트]
- 문제를 두 개의 큰 블록으로 나누어 배치하면 좋습니다.
- 각 블록은 다음 요소로 구성하면 됩니다:
  - 문제 번호와 지시문 텍스트
  - 가로 4열로 배열된 삼각형 도형
  - 각 도형 위의 라벨 `가, 나, 다, 라`
- 삼각형은 단순 선분 도형으로 표현하고, 내부 채움 없이 외곽선만 그리면 됩니다.
- 아래쪽 블록에는 추가 텍스트 문장 `(2) 한 각이 직각인 삼각형은 나, 라입니다.`를 별도 줄에 넣습니다.
- 맨 아래의 `(2) 나, 라`는 답안 예시인지 인쇄문인지 불확실하므로, 필요 시 주석 처리하거나 별도 텍스트 요소로 분리하는 것이 안전합니다.
- 블록 간 세로 간격을 충분히 두고, 각 삼각형은 같은 높이 기준선에 맞춰 정렬하는 편이 좋습니다.

[주의할 점]
- 문제를 풀어 정답을 도출하지 마세요.
- 빈칸에 답을 채우는 형식이 아니므로, 추론으로 답을 생성하지 마세요.
- 첫 번째와 두 번째 삼각형 세트의 도형 모양이 미세하게 다를 수 있으므로, 두 블록을 같은 도형으로 단정하지 않는 것이 좋습니다.
- `□ 64.`의 사각형은 체크박스처럼 보일 수 있으나, 실제로는 문제 번호 앞의 표시인지 불명확합니다.
- 하단의 `(2) 나, 라`는 학생 작성 답안인지, 인쇄된 정답 표시인지 시각만으로 확정하기 어렵습니다.

[불확실한 부분]
- 맨 앞의 `□` 기호가 실제 체크박스인지, 편집용 표시인지 확실하지 않습니다.
- 아래쪽 `(2) 나, 라` 텍스트가 정답란인지 해설용 표기인지 명확하지 않습니다.
- 두 번째 블록의 삼각형들은 첫 번째 블록과 비슷하지만 선의 기울기와 각도가 다르게 보이므로, 정확히 동일한 도형인지 단정할 수 없습니다.
- 이미지 해상도 때문에 각 삼각형의 꼭짓점 위치와 직각 여부를 세밀하게 판독하는 데 한계가 있습니다.

--- RAW VISION DRAFT END ---
