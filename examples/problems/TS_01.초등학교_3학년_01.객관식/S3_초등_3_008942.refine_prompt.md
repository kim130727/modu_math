# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008942.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008942

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 전체 화면은 서술형 선택지형 문제 1개와, 그 아래에 표, 보기 4개, 정답 표시, 해설 문장으로 구성되어 있습니다.
- 상단에는 문제 번호 `10.`과 함께 긴 질문 문장이 왼쪽에서 오른쪽으로 이어져 있습니다.
- 가운데에는 제목 `학생들이 좋아하는 음료`가 있고, 그 아래에 2행 구성의 표가 배치되어 있습니다.
- 표 아래 왼쪽과 가운데 영역에 보기 ①~④가 세로/가로로 나뉘어 배치되어 있습니다.
- 하단에는 `(정답) ③`이 보이고, 이어서 `(해설)` 아래에 각 음료별 학생 수를 계산식 형태로 정리한 문장이 줄바꿈으로 나열되어 있습니다.
- 맨 아래에는 회색 화살표 기호와 함께 비교 결과를 문장으로 설명하는 해설이 있습니다.

[보이는 텍스트]
- `10. 서윤이는 반 학생들이 좋아하는 음료를 조사하여 표로 나타내었습니다.`
- `서윤이네 반 학생들을 위해 음료를 한 가지 만 준비한다면 어떤 음료를 준비하는 것이 좋을까요?`
- 표 제목: `학생들이 좋아하는 음료`
- 표 머리글: `음료 / 우유 / 탄산음료 / 주스 / 식혜 / 합계`
- 첫째 행: `여학생 수(명) / 3 / 7 / 2 / 4 / 16`
- 둘째 행: `남학생 수(명) / 5 / 2 / 9 / 1 / 17`
- 보기:
  - `① 우유`
  - `② 탄산음료`
  - `③ 주스`
  - `④ 식혜`
- `(정답) ③`
- `(해설) 우유: 3 + 5 = 8(명)`
- `탄산음료: 7 + 2 = 9(명)`
- `주스: 2 + 9 = 11(명)`
- `식혜: 4 + 1 = 5(명)`
- `11>9>8>5이므로 가장 많은 학생이 좋아하는 주스를 준비하는 것이 좋습니다.`

[반복되는 시각 패턴]
- 표는 열 방향으로 음료 종류가 나열되고, 행 방향으로 `여학생 수(명)`과 `남학생 수(명)`이 반복되는 구조입니다.
- 각 음료 항목마다 두 개의 숫자가 위아래로 대응되며, 해설에서는 같은 형식의 덧셈식이 반복됩니다.
- 보기 번호 ①~④는 파란색 원형 숫자 아이콘과 텍스트 조합으로 반복되어 있습니다.
- 해설의 각 줄은 `음료이름: 수 + 수 = 결과(명)` 형식을 따릅니다.
- 마지막 비교 문장은 `>` 기호를 연속해서 사용하여 상대적 크기를 나열합니다.

[수식 단계]
- 표 자체에는 수식 변형 단계가 직접 보이지 않습니다.
- 해설에서는 각 음료별로 여학생 수와 남학생 수를 더하는 계산식이 따로 제시되어 있습니다.
- 이어서 계산 결과들을 크기 비교 형식으로 한 줄에 정리한 문장이 있습니다.
- 등호가 포함된 계산식은 각각 독립적으로 보이며, 연쇄적인 등호 변형은 보이지 않습니다.

[등호별 변환 의미]
- `3 + 5 = 8(명)` : 우유에 대해 두 성별의 수를 더한 결과를 나타내는 식으로 보입니다.
- `7 + 2 = 9(명)` : 탄산음료에 대한 계산식입니다.
- `2 + 9 = 11(명)` : 주스에 대한 계산식입니다.
- `4 + 1 = 5(명)` : 식혜에 대한 계산식입니다.
- 마지막 문장의 `11>9>8>5`는 계산 결과들 사이의 크기 비교를 나타냅니다.
- 다만 이 비교를 통해 어떤 선택이 적절하다는 설명은 해설 문장에 직접 적혀 있을 뿐, 여기서는 그 결론을 풀이하지는 않습니다.

[세부 내용]
- 표는 연한 보라색 계열의 테두리와 머리글 배경을 가진 형태입니다.
- 첫 행 머리글에는 음료 종류가 `우유`, `탄산음료`, `주스`, `식혜`, `합계` 순으로 배치되어 있습니다.
- 첫 열에는 `여학생 수(명)`, `남학생 수(명)`이 행 제목처럼 들어가 있습니다.
- 각 셀의 숫자는 가운데 정렬된 형태로 보이며, 합계 열에는 `16`, `17`이 들어 있습니다.
- 보기 영역은 표 아래쪽에 넓게 떨어져 있으며, 왼쪽에 ①과 ③, 오른쪽에 ②와 ④가 배치되어 있습니다.
- `(정답) ③`은 보기 번호만 따로 표시되어 있어, 선택지 번호를 가리키는 형식입니다.
- 해설의 계산식들은 세로로 한 줄씩 내려가며 정렬되어 있고, 마지막 비교 문장은 회색 화살표 아이콘 뒤에 이어집니다.

[Python DSL 작성 힌트]
- 상단 문제 문장과 표 제목은 별도의 텍스트 블록으로 처리하면 좋습니다.
- 표는 2행 이상의 격자 구조로 만들고, 첫 행은 열 머리글, 첫 열은 행 머리글로 구성하는 방식이 적합합니다.
- 각 칸의 숫자는 텍스트 중심 정렬로 배치하고, 합계 열은 다른 열과 같은 크기의 셀로 유지합니다.
- 보기 ①~④는 표 아래에 2열 정도의 분산 배치로 두되, 원형 번호 아이콘과 텍스트를 함께 그리는 구조가 좋습니다.
- 해설 영역은 계산식 4줄과 비교 문장 1줄로 분리하여 작성하면 화면 구성이 유사해집니다.
- 계산식과 비교 문장은 들여쓰기나 줄간격으로 구분하여 시각적으로 묶는 것이 좋습니다.
- 표와 보기 사이에 충분한 여백을 두어 문제 풀이 영역과 선택지 영역을 구분하세요.

[주의할 점]
- 빈칸이나 미지수 슬롯은 보이지 않습니다.
- 등호가 연속된 변형식은 없으므로, 각 계산식을 독립된 라인으로 처리해야 합니다.
- 표의 숫자를 이용해 답을 추론하여 DSL에 결론을 넣지 않도록 주의해야 합니다.
- `③`이 정답으로 표시되어 있지만, 시각 분석 초안에서는 정답 내용을 재생성하지 말고 위치 정보만 참고하세요.
- `11>9>8>5`의 순서를 그대로 옮기되, 의미 해석을 과도하게 확장하지 마세요.

[불확실한 부분]
- 표의 정확한 테두리 두께와 색상은 이미지 해상도상 아주 세밀하게는 단정하기 어렵습니다.
- 보기 ①~④의 좌우 배치가 엄밀히 같은 간격인지, 혹은 약간 비대칭인지 이미지 만으로는 완전히 확정하기 어렵습니다.
- 하단 화살표 아이콘의 정확한 도형 스타일은 단순한 회색 화살표로 보이지만, 세부 벡터 형태는 불확실합니다.
- 표 제목과 표 사이의 정확한 간격은 눈대중으로 보이는 수준만 설명할 수 있습니다.

--- RAW VISION DRAFT END ---
