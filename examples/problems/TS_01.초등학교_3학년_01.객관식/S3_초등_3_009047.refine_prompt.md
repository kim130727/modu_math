# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_009047.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_009047

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 장짜리 초등 수학 문제 화면이다.
- 왼쪽 상단에 문제 번호와 지시문이 있고, 그 아래에 선분/반직선 모양의 그림이 여러 개 배치되어 있다.
- 가운데 영역에는 비교 대상처럼 보이는 두 개의 긴 수평 선분형 그림이 좌우로 나란히 놓여 있다.
- 아래쪽 왼편에는 `(정답)` 표시와 함께 더 짧은 수평 그림이 있고, 맨 아래에는 `(해설)` 문장이 한 줄로 적혀 있다.
- 전체적으로 “보기에서 알맞은 것을 고르는” 형식이며, 그림을 보고 기호 `ㄱ`, `ㄴ`의 관계를 판단하는 문제로 보인다.

[보이는 텍스트]
- `□ 85.`  
  - 번호 앞에 작은 빈 사각형이 보이고, 그 뒤에 `85.`가 있다.
- `반직선 ㄴ ㄱ으로 알맞은 것을 고르세요.`
- 그림 위의 기호: `ㄱ`, `ㄴ`
- `(정답)`  
- `(해설)반직선 ㄴ 그은 점 ㄴ에서 시작하여 점 ㄱ을 지나는 반직선입니다.`  
  - 해설 문장은 붙어 쓰여 있으며, 띄어쓰기 상태는 이미지 그대로 다소 자연스럽지 않을 수 있다.

[반복되는 시각 패턴]
- 수평선 위에 점이 찍혀 있고, 그 점 위 또는 근처에 한글 기호 `ㄱ`, `ㄴ`이 표시된 형태가 반복된다.
- 긴 수평선의 양 끝 또는 특정 위치에 검은 점이 있으며, 점 사이에 선이 이어져 있다.
- 위쪽의 두 그림은 서로 비슷한 구조로, 같은 유형의 선 그림을 좌우로 비교하도록 배치되어 있다.
- 아래의 `(정답)` 영역에도 같은 유형의 수평선 그림이 한 번 더 나오는데, 위쪽 그림보다 더 짧게 그려져 있다.
- 해설 문장은 그림 아래 텍스트 설명으로 연결되어 있어, 그림의 방향성과 시작점/지나는 점을 말로 설명하는 패턴이다.

[수식 단계]
- 해당 없음.  
  - 이 화면에는 등호가 있는 수식 변형이나 계산 단계가 보이지 않는다.

[등호별 변환 의미]
- 해당 없음.  
  - 등호를 포함한 단계적 변환식이 보이지 않는다.

[세부 내용]
- 문제 문장은 `반직선 ㄴ ㄱ`처럼 기호 두 개를 순서대로 제시하고, 그에 맞는 그림을 고르라는 형태이다.
- 상단의 두 개 그림은 모두 수평 방향의 선분/반직선 표현으로 보이며, 각 그림 위에 `ㄱ`, `ㄴ` 표시가 붙어 있다.
- 왼쪽 상단 그림은 왼쪽에 `ㄱ`, 오른쪽 끝 쪽에 `ㄴ`이 표시된 긴 수평선처럼 보인다.
- 오른쪽 상단 그림은 왼쪽 쪽에 `ㄱ`, 오른쪽 끝 쪽에 `ㄴ`이 표시된 긴 수평선처럼 보인다.
- 아래 `(정답)` 옆의 그림은 왼쪽 점 위에 `ㄱ`, 오른쪽 끝 점 쪽에 `ㄴ`이 있는 짧은 수평선이다.
- 해설에서는 “점 ㄴ에서 시작하여 점 ㄱ을 지나는 반직선”이라는 식으로 시작점과 지나가는 점을 설명하고 있다.
- 따라서 이 문제는 그림의 방향과 시작점을 읽는 시각적 판독 문제로 이해된다.  
  - 단, 실제로 어떤 그림이 정답인지 본문만으로 확정해서 쓰지는 않는다.

[Python DSL 작성 힌트]
- 문제 상단 텍스트는 왼쪽 정렬로 배치하고, 번호와 지시문을 한 줄로 구성한다.
- 그림 영역은 가로 방향으로 넓게 잡고, 위쪽에 좌우로 두 개의 선 그림을 배치한다.
- 각 선 그림은:
  - 가는 수평선
  - 선 위의 검은 점 1개 또는 2개
  - 점 위의 작은 기호 `ㄱ`, `ㄴ`
  로 구성된 단순 도형으로 표현하면 된다.
- 아래 `(정답)` 영역은 왼쪽에 라벨을 두고, 그 오른쪽 또는 아래에 짧은 수평선 그림을 배치한다.
- `(해설)` 문장은 별도 텍스트 블록으로 하단에 한 줄 배치한다.
- 도형의 상대 위치가 중요하므로, `x`, `y` 좌표 기준으로 점과 기호의 정렬을 맞추는 방식이 좋다.
- 화살표나 색상은 보이지 않으므로 기본 검은색 선과 점만 사용하면 된다.

[주의할 점]
- 정답을 미리 확정해서 서술하지 않는다.
- 보이는 기호 `ㄱ`, `ㄴ`의 위치를 바꾸어 해석하지 않도록 주의한다.
- 위쪽 두 그림이 보기인지, 예시인지, 비교 대상인지 단정적으로 쓰지 말고 “나란히 배치된 그림” 정도로 기술하는 것이 안전하다.
- 해설 문장의 의미를 요약할 수는 있지만, 그 내용을 바탕으로 답을 채워 넣지는 않는다.
- `반직선 ㄴ ㄱ`의 의미를 수학적으로 완결해 설명하려고 하지 말고, 화면에 적힌 해설 문구 중심으로만 기술한다.

[불확실한 부분]
- 상단 두 그림이 정확히 어떤 선택지 번호와 연결되는지는 이미지에서 명확히 보이지 않는다.
- 왼쪽 상단 그림과 오른쪽 상단 그림의 세부 점 위치가 약간 흐릿하여, 정확히 끝점인지 중간점인지를 단정하기 어렵다.
- `(정답)` 옆의 그림이 실제로 답을 표시한 것인지, 예시로 따로 제시한 것인지 화면 구조상 완전히 확정하기는 어렵다.
- 첫 줄의 빈 사각형 기호가 체크박스인지, 인쇄 요소인지 명확하지 않다.

--- RAW VISION DRAFT END ---
