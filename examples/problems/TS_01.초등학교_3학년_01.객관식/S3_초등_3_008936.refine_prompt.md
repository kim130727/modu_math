# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008936.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008936

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단에 문제 번호와 질문 문장이 있고, 그 아래 중앙에는 **제목이 있는 표/그림 영역**이 배치되어 있습니다.
- 표 아래쪽에는 **그림의 기호 뜻(범례)**가 있고, 그 아래에는 **객관식 보기 4개**가 2줄로 나뉘어 배치되어 있습니다.
- 맨 아래에는 **정답 표시**와 **해설 문장**이 인쇄되어 있습니다.
- 전체적으로 “목장별 우유 생산량”을 그림기호로 비교하는 **표 + 범례 + 보기** 구조입니다.

[보이는 텍스트]
- 문제 번호: `4.`
- 문제 문장: `연서네 마을의 목장에서 일주일 동안 생산한 우유의 양을 조사하여 그림그래프로 나타내었습니다. 우유 생산량이 가장 많은 목장은 어디인가요?`
- 표 제목: `목장별 우유 생산량`
- 표 머리글: `목장`, `우유 생산량`
- 행 이름: `가`, `나`, `다`, `라`
- 범례:
  - 큰 원 기호 옆: `10 L`
  - 작은 원 기호 옆: `1 L`
- 보기:
  - `① 가 목장`
  - `② 나 목장`
  - `③ 다 목장`
  - `④ 라 목장`
- 정답 표기: `(정답) ②`
- 해설 문장: `(해설) 큰 그림 수가 가장 많은 목장은 나 목장입니다.`

[반복되는 시각 패턴]
- 각 목장 행마다 **원 모양 그림기호가 반복**되어 있습니다.
- 원 기호는 두 종류가 반복됩니다.
  - **큰 원**: 안쪽이 비어 보이는 원, 범례에 따라 `10 L`를 의미하는 기호로 보임
  - **작은 원**: 상대적으로 작은 원, 범례에 따라 `1 L`를 의미하는 기호로 보임
- 각 행은 왼쪽에 목장 이름, 오른쪽에 우유 생산량 그림이 가로로 이어지는 동일한 형식입니다.
- 각 행에서 큰 원과 작은 원의 개수가 다르게 배치되어 있어, 시각적으로 비교하도록 만든 그림그래프 형태입니다.

[수식 단계]
- 해당 없음.  
- 이 문제 화면에는 등호(`=`)로 이어지는 수식 변형이 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.  
- 수식 단계가 없으므로 등호별 변환 의미를 설명할 요소가 없습니다.

[세부 내용]
- 표는 주황색 테두리로 둘러싸여 있고, 내부도 행과 열이 선으로 나뉘어 있습니다.
- 첫 번째 열은 목장 이름을 적는 좁은 칸, 두 번째 열은 우유 생산량 그림이 들어가는 넓은 칸입니다.
- 각 행의 그림은 왼쪽에서 오른쪽으로 이어지며, **큰 원 여러 개 뒤에 작은 원 여러 개가 붙는 방식**으로 보입니다.
- 범례는 표의 아래쪽 오른편에 따로 배치되어 있어, 그림기호의 의미를 확인하게 합니다.
- 보기 영역은 표 아래 왼쪽과 오른쪽에 나뉘어 배치되어 있어, 2개씩 세로로 정렬된 선택지처럼 보입니다.
- 정답과 해설은 맨 아래에 작게 표시되어 있습니다.
- 화면 속 문제는 “가장 많은 목장”을 묻는 문장이므로, 시각적으로는 각 행의 **큰 원 개수와 작은 원 개수의 합을 비교하도록 유도**하는 구조입니다.  
  - 다만 이는 문제 해결을 위한 의미 설명이며, 실제 값 계산은 하지 않습니다.

[Python DSL 작성 힌트]
- 상단 문제 문장을 별도 텍스트 블록으로 배치하세요.
- 중앙의 표는 `2열 5행` 구조처럼 보이게 그리되, 첫 행은 제목 행, 그 아래 4행은 `가/나/다/라`입니다.
- 표의 오른쪽 열에는 각 행마다 원형 기호들을 가로 배열로 넣으세요.
- 범례는 표 아래쪽 오른편에 `큰 원 + 10L`, `작은 원 + 1L` 형태로 따로 배치하세요.
- 보기 4개는 좌우 2열 배치로, 각 보기 앞에 동그라미 숫자(①~④)를 넣으세요.
- 정답 표시와 해설은 페이지 하단의 텍스트로 처리하세요.
- 원 기호는 크기가 다른 두 종류이므로, DSL에서 크기 차이를 명확히 구분해 주세요.
- 표의 각 행에서 원의 개수와 배열 간격이 핵심이므로, 동일한 간격으로 반복 배치되도록 구현하는 것이 좋습니다.

[주의할 점]
- 그림기호의 **개수는 보이지만, 이를 계산해 답을 직접 확정하지는 마세요.**
- 범례의 큰 원과 작은 원을 혼동하지 않도록 주의하세요.
- 보기 중 어떤 번호가 정답인지 화면에 이미 표시되어 있지만, 시각 분석 초안에서는 **그 사실만 기록**하고 문제를 새로 풀지는 마세요.
- 표의 테두리 색은 주황색 계열이며, 내부 배치가 비교적 정돈되어 있으므로 셀 정렬을 신경 써야 합니다.
- “가장 많은 목장”이라는 질문은 해석 요소이지만, 초안에서는 문제 해결 대신 레이아웃 설명에 집중하세요.

[불확실한 부분]
- 큰 원과 작은 원이 정확히 몇 개씩 들어 있는지, 화면 해상도상 경계가 다소 겹쳐 보이는 부분이 있어 세밀한 판독이 어려울 수 있습니다.
- 표의 선 두께와 정확한 색상은 이미지 품질에 따라 달라질 수 있어, DSL 작성 시 대략적인 시각 구조를 우선 반영하는 것이 좋습니다.
- 보기와 정답 표시는 인쇄된 원본 형식에 맞춰 배치되어 보이지만, 실제 좌표나 간격은 이미지 기준으로 추정해야 합니다.

--- RAW VISION DRAFT END ---
