[문제 개요]
- 문제 ID: `S3_초등_3_008998`
- 상단에 문제 번호 `36.`과 지시문 `그림을 보고 알맞은 말을 선택하세요.`가 보인다.
- 중앙 상단에 도형 2개가 나란히 제시된다.
  - 왼쪽: 직사각형
  - 오른쪽: 정사각형
- 각 도형 아래에 이름표가 있다.
- 도형 아래에는 괄호 선택형 빈칸이 들어간 문장 2개가 있다.
- 하단 왼쪽에는 `(정답) 두, 네`가 인쇄되어 보이고, 그 아래에는 `(해설)` 문장이 있다.
- 본문은 도형의 성질을 말로 고르는 문제이며, 계산식은 없다.

[원본에 보이는 요소]
- 보이는 제목/지시문: `36. 그림을 보고 알맞은 말을 선택하세요.`
- 도형 라벨:
  - `직사각형`
  - `정사각형`
- 본문 문장:
  - `직사각형은 마주 보는 ( 두, 네 ) 변의 길이가 같습니다.`
  - `정사각형은 ( 두, 네 ) 변의 길이가 모두 같습니다.`
- 하단 표기:
  - `(정답) 두, 네`
  - `(해설) 직사각형은 마주 보는 두 변의 길이가 같고, 정사각형은 네 변의 길이가 모두 같습니다.`
- 도형에는 분홍색의 작은 직각 표시가 각 꼭짓점 근처에 있다.
- 왼쪽 상단 `□` 모양 기호가 문제 번호 앞에 보인다.

[수식/관계 구조]
- 수식은 없다.
- 구조적으로는 다음과 같은 문장-선택 구조이다.
  - 직사각형의 성질: 마주 보는 `두/네` 변 중 선택
  - 정사각형의 성질: `두/네` 변 중 선택
- 의미상 비교 대상은 두 도형이며, 각 도형의 변의 수/같음 여부를 묻는다.
- 보기 자체는 따로 제시되지 않고, 문장 안 괄호에 선택어가 들어가는 형태다.

[등호별 변환 의미]
- 해당 없음.
- 등호가 포함된 식이나 변형 단계가 보이지 않는다.

[빈칸 정책]
- 괄호 `(` `)` 안의 단어는 빈칸/선택 영역으로 취급한다.
- 빈칸에는 추론한 값을 임의로 채우지 않는다.
- 원본에 `(정답)`이 인쇄되어 보이더라도, 본문 빈칸 자체는 빈칸으로 유지하는 정책이 우선이다.
- 즉, DSL 작성 시 본문 문장과 정답 표기를 분리해서 다루고, 본문 내부 빈칸은 그대로 둔다.

[반복 패턴]
- 같은 선택 구조 `( 두, 네 )`가 본문 문장 2개에 반복된다.
- 하단 해설에서도 `두 변`, `네 변` 표현이 다시 반복된다.
- 도형 2개가 좌우로 대칭적으로 배치되어 비교 학습 패턴을 만든다.
- 두 도형 모두 각 꼭짓점 근처에 분홍색 직각 표시가 반복된다.
- 각 도형 아래의 이름표도 동일한 스타일로 반복 배치된다.

[DSL 작성 힌트]
- 상단 텍스트 `□ 36. 그림을 보고 알맞은 말을 선택하세요.`는 한 줄 제목으로 배치한다.
- 도형 2개는 수평 정렬로 배치한다.
  - 왼쪽: 직사각형 외곽선
  - 오른쪽: 정사각형 외곽선
- 각 도형의 네 꼭짓점 근처에 분홍색 L자 모양의 작은 직각 표식을 넣는다.
- 각 도형 아래에 라벨을 각각 `직사각형`, `정사각형`으로 둔다.
- 본문 문장은 도형 아래에 2줄로 배치한다.
  - 첫째 줄: `직사각형은 마주 보는 ( 두, 네 ) 변의 길이가 같습니다.`
  - 둘째 줄: `정사각형은 ( 두, 네 ) 변의 길이가 모두 같습니다.`
- 하단에는 `(정답) 두, 네`와 `(해설)` 문장이 따로 보이도록 배치한다.
- 하단 해설은 본문보다 작은 글씨로 분리해 두는 것이 자연스럽다.
- 도형과 본문 사이, 본문과 하단 설명 사이의 세로 간격이 비교적 크다.
- 정렬은 전체적으로 가운데 정렬에 가깝지만, 하단 정답/해설은 왼쪽 정렬로 보인다.

[semantic/solvable 힌트]
- 이 문제는 도형의 성질을 읽고 괄호 안 표현을 선택하는 이해형 문항이다.
- 직사각형과 정사각형의 이름이 그림 아래에 직접 붙어 있어, 도형-문장 대응이 핵심이다.
- 해설 영역이 보이므로 문항 의도는 확인 가능하지만, 본문 빈칸은 그대로 유지해야 한다.
- 정답 표기가 보이는 것은 관찰 사실로만 기록할 수 있고, 본문 빈칸에 복원해 넣지는 않는다.

[주의 및 TODO]
- `□` 기호의 의미는 체크박스인지 장식인지 확정하지 말고 TODO로 남긴다.
- 분홍색 직각 표식은 직각 표시로 해석되지만, 인쇄 보조 마크일 가능성도 완전히 배제하지 말고 사실과 추론을 구분한다.
- `(정답) 두, 네`가 화면에 보이지만, DSL에서 반드시 포함할지 여부는 출력 목적에 따라 검토가 필요하다.
- 도형의 정확한 비율은 이미지와 렌더링 환경에 따라 달라질 수 있으므로, 직사각형/정사각형의 상대적 차이만 반영하는 것이 안전하다.
- 본문 괄호 안의 순서 `( 두, 네 )`는 원본 그대로 유지한다.
- 해설 문장은 본문과 분리된 보조 설명 영역으로 취급한다.
