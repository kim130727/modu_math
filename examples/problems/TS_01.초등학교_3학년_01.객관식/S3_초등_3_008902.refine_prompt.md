# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008902.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008902

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# 시각 분석 초안

## [전체적인 구성]
- 화면 상단에는 안내 문장이 왼쪽 정렬로 배치되어 있습니다.
- 그 아래 중앙 부근에 **표**가 하나 있으며, 표의 제목이 표 위쪽 중앙에 따로 표시되어 있습니다.
- 표 아래에는 번호가 붙은 선택형 문제가 이어지고, 오른쪽 아래 쪽에 보기 형태의 선택지 `(표, 그림그래프)`가 있습니다.
- 화면 하단에는 정답과 해설 문장이 괄호로 구분되어 제시되어 있습니다.
- 전체적으로 **문제 문장 → 표 → 선택 질문 → 정답/해설** 순서의 세로 배치입니다.

## [보이는 텍스트]
- 상단 문장:
  - `준희네 모둠 학생들이 모은 붙임딱지 수를 조사하여 표로 나타내었습니다.`
  - `붙임딱지 수를`
  - `세요.`  
  - 위 문장은 줄바꿈 때문에 화면에서 `세요.`가 다음 줄 앞쪽에 따로 보입니다. 전체 문장은 아마도 “표로 나타내었어요.”로 이어지는 형태로 보입니다.
- 표 제목:
  - `모은 붙임딱지 수`
- 표 머리글:
  - `이름`
  - `준희`
  - `영우`
  - `주연`
  - `성준`
  - `합계`
- 표 값:
  - `붙임딱지 수(명)`  
    - 이 표현은 왼쪽 첫 칸에 세로로 긴 라벨처럼 들어가 있습니다.
  - `15`
  - `32`
  - `19`
  - `93`
  - 한 칸은 비어 있음
- 문제 번호와 질문:
  - `□ 68.`  
  - `준희네 모둠 학생들이 모은 붙임딱지가 모두 몇 장인지 알아보려면 표와 그림그래프 중 어느 것이 더 편리할까요? 알맞은 것을 선택해 보세요.`
- 선택지:
  - `( 표 , 그림그래프 )`
- 아래쪽 텍스트:
  - `(정답)표`
  - `(해설)표는 각각의 자료의 수와 합게를 쉽게 알 수 있고, 그림그래프는 각각의 자료의 크기를 쉽게 비교할 수 있습니다`
  - 해설 문장에는 `합게`로 보이는 철자 오기가 있습니다. 원문 그대로 보입니다.

## [반복되는 시각 패턴]
- 표는 **가로 방향 2행 구조**로 보이며, 위 행은 이름 항목, 아래 행은 수량 항목입니다.
- 왼쪽 첫 열은 다른 열보다 넓고, 배경색이 연한 노란색 계열로 채워져 있습니다.
- 나머지 열들은 흰색 배경에 회색 선으로 구획되어 있습니다.
- 표 제목은 표 본문과 분리되어 있고, 가운데 정렬되어 있습니다.
- 수치 칸들은 각 열 아래에 하나씩 배치되어 있으며, 가운데 정렬된 숫자들입니다.
- 비어 있는 칸이 하나 있어, 값이 빠진 상태임이 시각적으로 드러납니다.
- 문제 본문 아래의 선택지는 괄호 안에 두 개의 항목이 나열된 단순 선택형 패턴입니다.

## [수식 단계] (해당될 때)
- 해당 없음
- 화면에는 등호(`=`)를 포함한 수식 변형 단계가 보이지 않습니다.

## [등호별 변환 의미] (해당될 때)
- 해당 없음
- 등호가 포함된 계산식이나 변형식이 표시되어 있지 않습니다.

## [세부 내용]
- 표의 전체 제목은 `모은 붙임딱지 수`이며, 학생별로 모은 붙임딱지 수를 정리한 표로 보입니다.
- 표의 첫 번째 열은 항목명 역할을 하고, 두 번째 행의 첫 칸에는 `붙임딱지 수(명)`가 들어 있습니다.
- 이름 열에는 `준희`, `영우`, `주연`, `성준`, `합계`가 가로로 배치되어 있습니다.
- 숫자 칸에는 `준희` 아래 `15`, `주연` 아래 `32`, `성준` 아래 `19`, `합계` 아래 `93`이 보이고, `영우` 아래 칸은 비어 있습니다.
- 문제의 핵심 질문은 “모두 몇 장인지 알아보려면”이라는 상황에서 **표와 그림그래프 중 어떤 것이 더 편리한지 선택**하게 하는 형식입니다.
- 아래의 `(정답)표`와 `(해설)`은 문제 본문과는 별도로 인쇄된 설명으로 보입니다.  
  - 해설은 표가 수와 합계를 쉽게 알 수 있다는 점, 그림그래프는 크기 비교에 유리하다는 점을 설명합니다.

## [Python DSL 작성 힌트]
- 먼저 표 전체를 하나의 큰 직사각형 영역으로 잡고, 상단에 제목 텍스트를 가운데 정렬로 배치하는 방식이 적절합니다.
- 표 내부는 **열 6개, 행 2개** 형태로 구성된 그리드로 보이므로, 셀 경계선을 명확히 나누어 표현하면 좋습니다.
- 왼쪽 첫 열은 일반 셀보다 넓게 설정하고, 배경색을 연한 노랑 계열로 지정하면 원본과 비슷합니다.
- 각 셀의 텍스트는 가운데 정렬에 가깝게 배치합니다.
- 숫자 셀 중 빈칸은 텍스트를 넣지 않고 빈 셀로 유지해야 합니다.
- 문제 문장은 표 아래에 왼쪽 정렬로, 여러 줄로 끊어진 상태를 반영해 줄바꿈 위치를 고려하면 좋습니다.
- 선택지 `(표 , 그림그래프)`는 하나의 텍스트 블록으로 처리하되, 괄호와 쉼표의 간격을 원문처럼 유지하는 것이 좋습니다.
- 하단의 `(정답)표`, `(해설)...`은 별도 설명 영역으로 분리하여 배치하는 것이 안전합니다.

## [주의할 점]
- 빈칸 칸의 값은 추론해서 넣지 마세요.
- 문제를 풀어서 정답 이유를 계산해 넣지 마세요.
- 상단 문장의 줄바꿈 때문에 `표로 나타내었습니다.`가 `세요.`처럼 잘려 보이므로, 원문 전체를 임의로 보정할 때 주의가 필요합니다.
- 표 안의 `붙임딱지 수(명)`는 수량 단위가 다소 어색해 보일 수 있으나, 보이는 그대로 옮기는 것이 중요합니다.
- 해설의 `합게`는 오타 가능성이 있지만, 시각 자료에서는 그렇게 보입니다.
- 선택지의 순서는 `(표, 그림그래프)`이며, 이를 바꾸지 마세요.

## [불확실한 부분]
- 상단 문장의 마지막 부분은 화면에서 줄바꿈 때문에 일부가 분리되어 보여, 정확한 원문이 완전히 명확하지 않습니다. 다만 전체 의미상 표로 나타내었다는 문장으로 보입니다.
- 표 제목 `모은 붙임딱지 수`의 정확한 글자 크기와 위치는 화면상 약간 애매하지만, 표 위 중앙에 놓인 것은 분명합니다.
- 표의 선 굵기와 색조는 이미지 해상도에 따라 차이가 있을 수 있습니다.
- `붙임딱지 수(명)`의 괄호 안 단위가 의도된 표현인지, 인쇄상 혼동인지 단정할 수 없습니다.

--- RAW VISION DRAFT END ---
