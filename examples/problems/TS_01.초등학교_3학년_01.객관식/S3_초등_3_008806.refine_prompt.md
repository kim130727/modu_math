# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008806.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008806

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 지시문이 한 줄로 배치되어 있습니다.
- 중앙 상단 쪽에 회색 테두리의 큰 사각형 박스가 있고, 그 안에 `<보기>`와 예시 그림, 그리고 `100 g` 표기가 함께 들어 있습니다.
- 박스 오른쪽에는 선택지처럼 보이는 과일 그림 2개가 떨어져 배치되어 있습니다. 하나는 딸기, 다른 하나는 파인애플입니다.
- 화면 왼쪽 하단에는 `(정답)` 표시와 함께 파인애플 그림이 다시 크게 제시되어 있습니다.
- 화면 맨 아래에는 `(해설)` 문구 뒤로 문장 형태의 설명이 이어집니다.
- 전체적으로 “보기 박스 + 그림 선택 요소 + 정답 표시 + 해설 문장”의 구조입니다.

[보이는 텍스트]
- `□ 35. 무게가 100 g 보다 더 가벼운 것을 선택하세요.`
- 박스 안: `<보기>`
- 박스 안 하단: `100 g`
- 왼쪽 하단: `(정답)`
- 맨 아래 해설: `(해설) 무게가 100 g 보다 더 무거운 것은 파인애플입니다.`
- 박스 안 예시 그림의 몸통에는 작은 글씨가 들어가 있는데, 해상도가 낮아 정확한 문구는 판독이 어렵습니다.
- 숫자 표기는 `100 g`처럼 공백이 있는 형태로 보입니다.

[반복되는 시각 패턴]
- 과일 그림이 두 종류 이상 반복적으로 등장합니다.
  - 딸기 1개
  - 파인애플 2개 정도가 보이며, 하나는 선택지 위치, 하나는 정답 표시 옆에 있습니다.
- `100 g`라는 수치가 보기 박스 안에서 기준값처럼 제시됩니다.
- 같은 종류의 그림이 크기나 위치를 달리해 반복되므로, 선택지 그림과 정답 예시 그림을 구분해야 합니다.
- 왼쪽 아래 `(정답)`은 텍스트와 그림이 묶인 안내 패턴으로 보입니다.

[수식 단계]
- 해당 없음.  
- 등호를 포함한 수식 변형이나 계산 과정은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.  
- 화면에는 등호가 있는 식 전개가 나타나지 않습니다.

[세부 내용]
- 상단 문장은 선택형 지시문이며, 빈 사각형 기호 `□`가 문제 번호 앞에 있습니다.
- 보기 박스는 회색 선으로 된 정사각형에 가까운 테두리이며, 내부에 `<보기>`가 좌상단 쪽에 배치되어 있습니다.
- 보기 박스 안에는 녹색의 튜브형 물체 그림이 대각선 방향으로 놓여 있고, 아래쪽에 `100 g`가 크게 적혀 있습니다.
- 박스 바깥 오른쪽에는 딸기와 파인애플 그림이 좌우로 떨어져 있으며, 파인애플이 더 오른쪽에 있습니다.
- 왼쪽 아래의 `(정답)`은 파인애플 그림 옆에 붙어 있어, 정답 예시를 시각적으로 강조합니다.
- 해설 문장은 하단 왼쪽부터 시작하며, 본문 텍스트는 한 줄로 이어집니다.
- 문장 내용상 보기의 기준 무게와 비교해 어떤 것이 더 무거운지 설명하는 형식입니다. 다만 이 의미 설명은 화면에 적힌 문장을 바탕으로 한 해석일 뿐, 문제를 푸는 내용은 아닙니다.

[Python DSL 작성 힌트]
- 전체 레이아웃을 상단 지시문, 중앙 보기 박스, 우측 선택 그림, 좌하단 정답 표시, 하단 해설의 5개 영역으로 나누어 배치하면 좋습니다.
- 보기 박스는 사각형 테두리로 표현하고, 내부에 `<보기>` 텍스트와 `100 g` 텍스트를 별도로 배치하세요.
- 박스 안의 녹색 물체 그림은 대각선으로 기울어진 상태를 반영해 회전된 이미지 요소로 처리하면 자연스럽습니다.
- 딸기와 파인애플은 각각 독립된 이미지 객체로 두고, 서로 겹치지 않게 오른쪽에 배열하세요.
- `(정답)` 라벨과 아래 파인애플 그림은 묶음 요소로 처리하되, 정답 값을 텍스트로 추정해 채우지는 마세요.
- 해설 문장은 별도 텍스트 박스로 넣되, 문제 본문과 혼동되지 않게 하단에 배치하세요.

[주의할 점]
- 문제를 실제로 풀어 정답을 추론하면 안 됩니다.
- `(정답)` 옆의 파인애플을 보고 답을 확정적으로 서술하지 마세요. 이는 화면에 제시된 정답 표시일 뿐입니다.
- 박스 안 녹색 물체의 작은 글씨는 해상도 한계로 판독이 불명확하므로 임의로 단정하지 마세요.
- 하단 해설 문장의 내용은 보이지만, 이를 근거로 문제 풀이를 진행하지 마세요.
- 선택지처럼 보이는 과일 그림들의 정확한 의미 관계는 화면 배치에 근거해 설명하되, 답을 계산하거나 추론하지 마세요.

[불확실한 부분]
- 보기 박스 안 녹색 물체의 내부 글씨는 정확히 읽기 어렵습니다.
- 왼쪽 아래 `(정답)` 표시 옆의 파인애플이 실제로 정답을 가리키는지, 아니면 예시용 재표시인지 화면만으로는 단정하기 어렵습니다.
- 상단의 빈 사각형 기호 `□`는 선택 표시인지 단순 문제 번호 장식인지 문맥상 해석이 필요하지만, 정확한 기능은 이미지 만으로 완전히 확정할 수 없습니다.

--- RAW VISION DRAFT END ---
