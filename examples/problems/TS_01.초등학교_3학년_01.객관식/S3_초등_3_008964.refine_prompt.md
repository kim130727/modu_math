# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008964.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008964

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 화면 상단에 문제 번호 **34.**와 함께 긴 서술형 질문이 한 줄 이상으로 배치되어 있다.
- 그 아래 중앙에 **그림그래프** 형태의 표가 있고, 표 제목은 **“모둠 학생들이 접은 종이꽃 수”**로 보인다.
- 표는 **2열 구성**이며, 왼쪽 열은 **이름**, 오른쪽 열은 **종이꽃 수**를 나타낸다.
- 표 아래쪽 오른편에는 **그림 1개가 몇 송이를 뜻하는지**를 설명하는 범례가 있다.
- 범례 아래에는 **객관식 보기 5개**가 2줄로 흩어져 배치되어 있다.
- 그 아래에 **정답 표시**와 **해설 문장**이 이어진다.

# [보이는 텍스트]
- 문제 번호: **34.**
- 문제 문장:
  - **재경이네 모둠 학생들이 접은 종이꽃의 수를 조사하여 그림그래프로 나타내었습니다.**
  - **재경이와 채미가 접은 종이꽃이 모두 67송이라면 채미와 은서가 접은 종이꽃은 모두 몇 송이일까요?**
- 표 제목: **모둠 학생들이 접은 종이꽃 수**
- 표의 열 제목:
  - **이름**
  - **종이꽃 수**
- 행 이름:
  - **재경**
  - **은서**
  - **채민**
- 범례:
  - 큰 꽃 그림 옆에 **:10송이**
  - 작은 꽃 그림 옆에 **:1송이**
- 보기:
  - **① 49송이**
  - **② 52송이**
  - **③ 55송이**
  - **④ 57송이**
  - **⑤ 59송이**
- 정답 표시: **(정답) ⑤**
- 해설 일부에 보이는 식:
  - **67 - 33 = 34(송이)**
  - **34 + 25 = 59(송이)**

# [반복되는 시각 패턴]
- 표의 각 이름 행 오른쪽에는 **꽃 그림 아이콘이 여러 개 반복**되어 있다.
- 꽃 그림은 **큰 꽃**과 **작은 꽃** 두 종류로 반복되며, 범례에서 각각의 수량 의미를 알려 준다.
- 재경과 은서의 행에는 **큰 꽃 여러 개 + 작은 꽃 여러 개**가 같은 형식으로 나열되어 있다.
- 같은 행 안에서 꽃 아이콘들은 **가로 방향으로 일정 간격**을 두고 배열되어 있다.
- 보기 항목은 **동그라미 숫자(①~⑤)**와 함께 송이 수가 제시되는 반복 형식이다.

# [수식 단계]
- 해설 부분에 등호를 이용한 식 변형이 보인다.
- 보이는 식의 흐름:
  1. **(채민이가 접은 종이꽃의 수)**
  2. **= (재경이와 채민이가 접은 종이꽃의 수) - (재경이가 접은 종이꽃의 수)**
  3. **= 67 - 33 = 34(송이)**
  4. **은서가 접은 종이꽃이 25송이이므로 채민이와 은서가 접은 종이꽃은 모두 34 + 25 = 59(송이)**

# [등호별 변환 의미]
- 첫 번째 등호:
  - **채민이가 접은 종이꽃의 수**를, **전체(재경+채민)**에서 **재경의 수**를 뺀 값으로 나타내고 있다.
- 두 번째 등호:
  - 앞의 문장식을 **숫자식 67 - 33**으로 바꾸어 계산한 모습이다.
- 세 번째 등호:
  - **67 - 33의 결과를 34송이**로 적고 있다.
- 마지막 식:
  - **채민과 은서의 수를 더하는 단계**를 보여 주며, **34 + 25 = 59(송이)**로 마무리되어 있다.
- 주의: 위 등호 해석은 **문제 해설에 적힌 내용을 기준으로 한 시각적 읽기**이며, 문제 자체를 푸는 과정 설명은 아니다.

# [세부 내용]
- 표의 왼쪽 열은 이름이 들어가는 단순 텍스트 칸이고, 오른쪽 열은 그림그래프 칸이다.
- 재경 행:
  - 큰 꽃 그림이 **3개**
  - 작은 꽃 그림이 **3개**
  - 오른쪽으로 나란히 배열됨
- 은서 행:
  - 큰 꽃 그림이 **2개**
  - 작은 꽃 그림이 **5개**
  - 재경 행보다 작은 꽃 수가 더 많아 보이도록 배치됨
- 채민 행:
  - 오른쪽 칸이 **비어 있음**
  - 그림이 아직 그려지지 않은 상태로 보임
- 범례:
  - 큰 꽃 그림 아래/옆에 **10송이**
  - 작은 꽃 그림 아래/옆에 **1송이**
  - 범례는 표 오른쪽 아래에 따로 배치되어 있어, 그림의 크기와 수량 대응을 설명한다.
- 보기들은 표 아래에서 좌우로 나뉘어 배치되어 있어, 한 줄로 연속되지 않고 여러 칸에 흩어져 있다.
- 해설 문장은 표 아래부터 시작해 여러 줄로 이어지며, 식과 문장이 섞여 있다.

# [Python DSL 작성 힌트]
- 먼저 **상단 문제 문장**, **중앙 표**, **범례**, **보기**, **해설 텍스트**를 서로 분리해 배치하는 구조로 작성하면 좋다.
- 표는 **2열 3행(헤더 포함)** 형태로 생각하고, 각 행의 오른쪽 칸에는 꽃 아이콘들을 개별 객체로 반복 배치한다.
- 재경 행과 은서 행은 **큰 꽃/작은 꽃 아이콘의 반복 패턴**을 그대로 옮기고, 채민 행 오른쪽 칸은 **빈 칸**으로 둔다.
- 범례는 표 아래 오른쪽에 **아이콘 2개와 텍스트 라벨 2개**를 묶어서 배치한다.
- 보기 영역은 **①~⑤ 번호 + “송이” 단위 텍스트**를 각각 독립 요소로 처리하면 좋다.
- 해설 부분은 수식 줄과 문장 줄이 섞여 있으므로, **텍스트 블록 여러 개**로 나누어 배치하는 것이 안전하다.
- 식 안의 숫자와 괄호는 그대로 옮기고, 계산 결과를 새로 만들지 말고 **보이는 그대로** 사용한다.

# [주의할 점]
- **빈 채민 행에 값을 추론하여 넣지 말 것.**
- **큰 꽃 = 10송이, 작은 꽃 = 1송이**라는 범례를 임의로 바꾸지 말 것.
- 보기의 정답 번호를 기준으로 내용을 재구성하지 말 것.
- 해설에 있는 식은 **보이는 그대로** 옮기되, 문제를 푸는 방식으로 다시 설명하지 말 것.
- 표의 꽃 개수는 그림상 해상도 때문에 세부 경계가 다소 흐릴 수 있으므로, **아이콘의 개수만 보수적으로 인식**해야 한다.
- 문제 문장 끝의 물음표와 숫자 단위를 빠뜨리지 말 것.

# [불확실한 부분]
- 표 안의 꽃 개수는 그림 해상도 때문에 일부 아이콘의 겹침이 있어 **정확한 개수 판독이 어려울 수 있다**.
- 해설 문장 중 일부는 줄바꿈 때문에 문장이 이어지는 위치가 다소 애매할 수 있다.
- 채민 행은 비어 있지만, 이것이 **의도적으로 공란인 것인지 / 원래 그림이 생략된 것인지**는 시각만으로는 추가 판단이 필요하다.
- 해설의 첫 줄에서 **“재경이가 접은 종이꽃이 33송이”**라는 표현은 보이지만, 이 값이 그림그래프에서 어떻게 읽힌 결과인지는 여기서 단정하지 않는다.

--- RAW VISION DRAFT END ---
