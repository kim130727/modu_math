# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008826.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008826

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에는 문제 번호와 문제 지시문이 한 줄로 배치되어 있습니다.
- 중앙 왼쪽에는 아이 그림이 있고, 그 옆에 말풍선이 있습니다.
- 중앙 오른쪽에는 보라색 계열의 주전자/병 모양 그림이 따로 있습니다.
- 아래쪽에는 (1)번 문장 형태의 보기/선택 설명이 길게 이어집니다.
- 그 아래에는 해설처럼 보이는 문장이 한 줄 더 있고, 맨 아래에는 짧은 답안/기입용으로 보이는 텍스트가 일부 보입니다.
- 전체적으로 “그림 + 말풍선 + 문장형 문제 + 선택지/설명” 구조입니다.

[보이는 텍스트]
- 상단 문제 문구:
  - `□ 60. 포도주스의 양을 어림해 보고 답합니다. 알맞은 것을 선택해 보세요.`
- 말풍선 안:
  - `1L와 200 mL 우유갑으로 포도주스의 양을 어림해 볼까?`
- (1)번 문장:
  - `포도주스의 양은`
  - 중간에 우유갑 그림이 끼워져 있으며, 그림 옆 문장은 이어져
  - `과 비슷하므로 약 ( 1 L , 3 L )입니다.`
- 하단 설명처럼 보이는 문장:
  - `포도주스의 양은 1L짜리 우유와 비슷하므로 약 1L로 어림할 수 있습니다.`
- 맨 아래 왼쪽에 일부만 보이는 텍스트:
  - `(1) 1 L`
  - 앞부분이 잘려 있거나 다음 단계와 연결된 것으로 보입니다.

[반복되는 시각 패턴]
- `1L`가 여러 위치에서 반복됩니다.
  - 말풍선의 `1L`
  - (1)번 문장의 선택지 `1 L`
  - 하단 설명의 `1L짜리`
  - 맨 아래의 `1 L`
- `우유갑`을 뜻하는 그림이 문장 사이에 끼워 넣어진 형태로 보이며, 숫자와 단위 텍스트를 시각적으로 연결하는 패턴이 있습니다.
- 선택지 `( 1 L , 3 L )`처럼 괄호와 쉼표로 구분된 두 값이 나열되어 있습니다.
- 말풍선과 본문 문장이 모두 “어림”이라는 표현을 사용해, 그림을 기준으로 양을 비교하도록 유도하는 구성입니다.

[수식 단계]
- 화면에 전형적인 수식 전개식이나 등호가 이어지는 단계는 보이지 않습니다.
- 따라서 수식 변형 단계는 없는 것으로 보입니다.
- 다만 `약 ( 1 L , 3 L )`처럼 선택형 수치가 제시되어 있어, 계산식보다 비교/판단형 구성입니다.

[등호별 변환 의미]
- 등호 `=`가 포함된 변형식은 보이지 않습니다.
- 따라서 등호별 변환 의미를 설명할 대상이 없습니다.

[세부 내용]
- 문제는 포도주스의 양을 직접 재는 것이 아니라, 제시된 그림과 비교하여 대략적인 양을 고르는 형태입니다.
- 말풍선은 어린이가 말하는 장면처럼 표현되어 있으며, “1L와 200mL 우유갑”을 기준으로 포도주스의 양을 어림해 보자는 대화 문장입니다.
- 오른쪽의 주전자/병 그림은 포도주스 용기처럼 보이며, 내용물의 양을 상징하는 시각 요소입니다.
- (1)번 문장 중간에는 실제 우유갑 그림이 삽입되어 있고, 이 그림이 `1L` 표기를 가진 우유갑으로 보입니다.
- 문장 끝의 `( 1 L , 3 L )`는 선택지처럼 보이며, 둘 중 알맞은 것을 고르게 하는 의도입니다.
- 하단 설명은 정답 방향을 암시하는 해설 문장처럼 보이지만, 본 작업에서는 그 의미만 서술하고 답은 확정하지 않습니다.
- 화면 하단의 `1 L` 텍스트는 일부가 잘려 있어 문장 전체를 정확히 복원하기 어렵습니다.

[Python DSL 작성 힌트]
- 상단 제목 문장은 한 줄 텍스트로 배치하세요.
- 아이 그림은 왼쪽 상단 중앙쯤, 말풍선은 그 오른쪽에 둥근 타원형으로 배치하세요.
- 말풍선 내부 텍스트는 2줄 정도로 나누어 중앙 정렬에 가깝게 배치하세요.
- 오른쪽 상단의 병/주전자 그림은 단독 아이콘처럼 배치하세요.
- (1)번 문장은 왼쪽에서 시작해 긴 한 줄 텍스트로 놓고, 중간에 우유갑 이미지를 인라인 요소처럼 끼워 넣는 구성을 고려하세요.
- 선택지 `( 1 L , 3 L )`는 괄호와 쉼표 간격이 눈에 띄도록 텍스트 객체로 배치하세요.
- 하단 설명 문장은 문제 문장보다 조금 아래에 별도 텍스트 블록으로 넣으세요.
- 마지막에 일부만 보이는 `1 L` 텍스트는 잘림 상태를 반영해 별도 텍스트 요소로 처리하는 것이 좋습니다.

[주의할 점]
- 포도주스의 실제 양을 계산해서 답으로 확정하지 마세요.
- 선택지 중 어느 것이 정답인지 단정하지 마세요.
- 그림의 용도는 추론할 수 있어도, 화면에 없는 정보까지 추가하지 마세요.
- 하단의 문장이 해설인지 정답 표기인지 단정하지 말고, “설명처럼 보인다” 정도로만 표현하세요.
- 우유갑 그림이 정확히 어떤 위치에 삽입되는지, 텍스트와 겹치는 정도는 화면 구현 시 약간 차이가 날 수 있습니다.

[불확실한 부분]
- 맨 아래 왼쪽의 `(1) 1 L` 앞부분은 화면이 잘려 있어 정확한 문장 전체를 알기 어렵습니다.
- (1)번 문장 안에서 우유갑 그림 앞뒤로 어떤 조사나 띄어쓰기가 정확히 들어가는지 일부 식별이 어렵습니다.
- 하단 설명 문장의 정확한 줄바꿈 위치는 이미지 해상도상 완전히 확정하기 어렵습니다.
- 오른쪽의 보라색 병/주전자는 포도주스를 나타내는지 시각적으로는 그렇게 보이지만, 명시 텍스트로 직접 적혀 있지는 않습니다.

--- RAW VISION DRAFT END ---
