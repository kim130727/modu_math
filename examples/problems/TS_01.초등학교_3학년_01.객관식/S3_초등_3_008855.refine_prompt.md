# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008855.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008855

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호 `1.`이 있고, 그 뒤로 긴 문장형 문제가 1~2줄로 줄바꿈되어 배치되어 있습니다.
- 문제 본문 아래쪽 오른편에는 괄호로 된 선택지 형태 `（사과주스, 오렌지주스, 포도주스）`가 따로 배치되어 있습니다.
- 그 아래 왼쪽에는 `정답` 표시가 있고, 이어서 답안 단어 `사과주스`가 붙어 있습니다.
- 하단에는 `(해설)`이 붙은 계산 과정이 여러 줄로 제시되어 있으며, 마지막 줄에 비교 결과와 결론 문장이 함께 있습니다.
- 전체적으로 “문제 → 선택지 → 정답 → 해설”의 순서로 구성된 서술형/객관식 혼합 형태입니다.

[보이는 텍스트]
- `1. 민정이네 냉장고에 사과주스가 3 L 50 mL, 오렌지주스가 3500 mL, 포도주스가 5 L와 30 mL 있습니다. 사과주스, 오렌지주스, 포도주스 중 가장 적은 것은 어느 것인가요?`
- `（사과주스, 오렌지주스, 포도주스）`
- `(정답)사과주스`
- `(해설)`
- `3500 mL = 3000 mL + 500 mL = 3 L + 500 mL = 3 L 500 mL`
- `5 L와 30 mL = 5 L 30 mL`
- `5 L 30 mL > 3 L 500 mL > 3 L 50 mL이므로 가장 적은 것은 사과주스입니다.`

[반복되는 시각 패턴]
- 용량 표기가 `L`와 `mL`를 함께 쓰는 형태로 반복됩니다.
  - 예: `3 L 50 mL`, `3500 mL`, `5 L와 30 mL`, `5 L 30 mL`
- 해설에서 같은 값을 다른 단위 표현으로 바꾸는 등식 패턴이 연속적으로 나타납니다.
  - `3500 mL = 3000 mL + 500 mL = 3 L + 500 mL = 3 L 500 mL`
- 비교 기호 `>`가 연속해서 사용되어 크기 비교 순서를 보여줍니다.
  - `5 L 30 mL > 3 L 500 mL > 3 L 50 mL`
- 괄호 안에 보기 3개가 나열되는 전형적인 선택지 패턴이 있습니다.

[수식 단계]
- `3500 mL = 3000 mL + 500 mL = 3 L + 500 mL = 3 L 500 mL`
  - 같은 양을 여러 방식으로 바꿔 적은 단계가 이어져 있습니다.
- `5 L와 30 mL = 5 L 30 mL`
  - 문장형 표현 `5 L와 30 mL`를 단위 병기형 표현 `5 L 30 mL`로 정리한 것으로 보입니다.
- `5 L 30 mL > 3 L 500 mL > 3 L 50 mL`
  - 세 양의 대소 관계를 비교한 식이 제시되어 있습니다.
  - 각 값의 크기를 직접 계산해 적지는 않고, 비교 결과만 나열되어 있습니다.

[등호별 변환 의미]
- `3500 mL = 3000 mL + 500 mL`
  - 보이는 사실: 3500 mL를 3000 mL와 500 mL의 합으로 분해해 적었습니다.
  - 의미: mL 단위를 1000 mL 단위와 나머지로 나눈 형태로 보여 주려는 의도입니다.
- `3000 mL + 500 mL = 3 L + 500 mL`
  - 보이는 사실: 3000 mL를 3 L로 바꾸었습니다.
  - 의미: 같은 양을 L와 mL가 섞인 형태로 변환한 것입니다.
- `3 L + 500 mL = 3 L 500 mL`
  - 보이는 사실: 더하기 기호가 빠지고 한 줄 표기처럼 연결되었습니다.
  - 의미: 혼합 단위 수량을 `3 L 500 mL` 형식으로 쓴 것입니다.
- `5 L와 30 mL = 5 L 30 mL`
  - 보이는 사실: `와`가 제거되고 숫자와 단위가 붙은 형식으로 바뀌었습니다.
  - 의미: 혼합 단위를 정리한 표기입니다.

[세부 내용]
- 문제 문장은 냉장고 안의 세 음료 용량을 비교하는 상황입니다.
- 사과주스는 `3 L 50 mL`로 보입니다.
- 오렌지주스는 `3500 mL`로 제시되어 있습니다.
- 포도주스는 `5 L와 30 mL`라고 적혀 있습니다.
- 보기 괄호에는 음료 이름 3개만 나열되어 있으며, 별도의 동그라미나 체크 표시는 보이지 않습니다.
- 해설의 마지막 문장은 비교식 뒤에 이어서 “가장 적은 것은 사과주스입니다.”라고 결론을 씁니다.
- 정답 표시는 문제 아래쪽 왼편에 따로 배치되어 있어, 해설과 분리된 형태입니다.

[Python DSL 작성 힌트]
- 문제 영역과 해설 영역을 분리해서 배치하는 구조가 필요합니다.
- 상단 문제는 왼쪽 정렬의 긴 문장 2줄 정도로 처리하면 자연스럽습니다.
- 선택지는 괄호를 포함한 한 줄 텍스트로 오른쪽에 배치하는 구성이 보입니다.
- `(정답)` 라벨과 정답 단어를 붙여 표시하는 별도 텍스트 줄이 있습니다.
- 해설에서는 등식 1줄, 다음 변환 1줄, 또 다른 표현 1줄, 마지막 비교 결론 1줄의 순서로 구성하면 좋습니다.
- `=`와 `>` 기호가 포함된 수식 텍스트는 줄마다 독립된 텍스트 객체로 두는 편이 맞아 보입니다.
- 용량 표기에서 `L`, `mL`는 숫자와 같은 텍스트 덩어리로 다루는 것이 안전합니다.
- 괄호 선택지는 텍스트 박스로 만들고, 해설은 별도의 아래쪽 텍스트 블록으로 나누는 것이 적절합니다.

[주의할 점]
- 답이 무엇인지 문제를 풀어서 설명하지 말고, 화면에 이미 보이는 정답 표기만 인용해야 합니다.
- `3 L 50 mL`, `3500 mL`, `5 L와 30 mL`의 실제 크기를 계산해 서술하지 마세요.
- `5 L와 30 mL`와 `5 L 30 mL`의 차이는 표기상 변환으로만 설명하고, 값 비교를 새로 추론하지 마세요.
- 해설의 비교식은 화면에 보이는 순서를 그대로 유지해야 합니다.
- 선택지 순서와 정답 표시는 이미지와 동일하게 적어야 합니다.

[불확실한 부분]
- `정답`과 `해설`의 괄호 모양은 이미지에서 흐릿하게 보여 정확한 서체/기호 스타일은 단정하기 어렵습니다.
- 문제 번호 앞의 작은 사각형 기호는 체크박스인지 단순 표식인지 확실하지 않습니다.
- 마지막 비교식의 공백 배치는 인쇄체 특성상 일부 다르게 읽힐 수 있으나, 전체 구조는 명확합니다.

--- RAW VISION DRAFT END ---
