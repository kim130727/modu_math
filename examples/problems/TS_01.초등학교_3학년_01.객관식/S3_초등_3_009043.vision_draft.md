[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 지시문이 있고, 오른쪽에는 파란 점선 격자 위에 4개의 도형이 가로로 배치되어 있습니다.
- 각 도형 안에는 한 글자씩 들어가 있으며, 도형 아래에는 선택지 형태로 `(가, 나, 다, 라)`가 적혀 있습니다.
- 그 아래에는 `(정답)나`가 보이고, 이어서 설명 문장이 있습니다.
- 하단에는 같은 도형들이 다시 한 번 작은 크기로 제시되어, 앞의 보기와 같은 배치로 반복됩니다.
- 전체적으로 “그림에서 직각삼각형을 찾는” 단일 선택형 문제 구조입니다.

[보이는 텍스트]
- `□ 81. 직각삼각형을 찾아 선택하세요.`
- 도형 아래 표기: `( 가 , 나 , 다 , 라 )`
- ` (정답)나`
- ` (해설)직각삼각형은 한 각이 직각인 삼각형으로 나입니다.`
- 하단에도 동일한 도형들이 다시 보이며, 도형 내부 글자는 `가`, `나`, `다`, `라`입니다.

[반복되는 시각 패턴]
- 파란 점선 격자 배경 위에 도형 4개가 좌→우 순서로 배열됩니다.
- 도형들은 각각 서로 다른 형태이지만, 모두 세로 방향의 중심을 기준으로 배치되어 있습니다.
- 첫 번째와 세 번째 도형은 직사각형 계열로 보이는 네모꼴이며, 두 번째와 네 번째는 삼각형입니다.
- 아래쪽의 작은 예시 그림도 위쪽 그림과 같은 순서와 구성으로 반복됩니다.
- 각 도형 내부의 글자는 중앙 근처에 배치되어 있고, 도형 모양과 글자가 한 세트처럼 보입니다.

[수식 단계]
- 해당 없음. 수식 변형이나 등호가 나타나지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호가 보이지 않습니다.

[세부 내용]
- 문제 번호 `81`은 좌상단에 있고, 그 옆에 작은 네모 체크 표시처럼 보이는 기호가 있습니다.
- 지시문 `직각삼각형을 찾아 선택하세요.`는 한 줄로 표시됩니다.
- 첫 번째 도형 `가`:
  - 파란 격자 위에 세로로 긴 직사각형 형태입니다.
  - 내부 중앙에 `가`가 있습니다.
- 두 번째 도형 `나`:
  - 왼쪽 변이 거의 수직이고, 아래 변이 수평이며, 오른쪽 변이 사선으로 내려오는 삼각형입니다.
  - 내부에 `나`가 있습니다.
  - 밑변 쪽 왼쪽 아래에 직각을 나타내는 작은 네모 모양 표시가 보입니다.
- 세 번째 도형 `다`:
  - 정사각형 또는 직사각형처럼 보이는 네모꼴입니다.
  - 내부에 `다`가 있습니다.
- 네 번째 도형 `라`:
  - 위 꼭짓점이 뾰족한 삼각형입니다.
  - 내부에 `라`가 있습니다.
- 보기 텍스트 `( 가 , 나 , 다 , 라 )`는 도형 아래 중앙에 있고, 선택 후보를 나열하는 형식입니다.
- `(정답)나`는 해설보다 앞에 따로 표시되어 있습니다.
- 해설 문장은 괄호와 함께 시작하며, 직각삼각형의 정의를 설명합니다.

[Python DSL 작성 힌트]
- 상단 문제 문장, 보기 묶음, 정답 표시, 해설 문장을 각각 별도 텍스트 요소로 처리하면 좋습니다.
- 파란 점선 격자 배경은 큰 사각형 영역으로 먼저 깔고, 그 위에 도형 4개를 좌표 기반으로 배치하는 방식이 적합합니다.
- 각 도형은 윤곽선 도형으로 그리되, 내부 글자 `가/나/다/라`는 중앙 정렬 텍스트로 얹는 구성이 자연스럽습니다.
- 두 번째 도형 `나`에는 직각 표시용 작은 사각형 기호가 포함되어 있으므로, 삼각형 내부의 왼쪽 아래 모서리 근처에 작은 직각 표시를 별도 도형으로 추가해야 합니다.
- 아래쪽의 작은 반복 그림은 상단 그림과 동일한 요소를 축소해서 다시 배치한 것으로 보면 됩니다.
- `정답` 표시는 강조 텍스트로 따로 표시하고, 해설 문장은 일반 문장으로 이어서 배치하면 됩니다.

[주의할 점]
- 직각삼각형이 무엇인지 계산하거나 판정 과정을 서술하지 말고, 화면에 보이는 구성만 기록해야 합니다.
- `(정답)나`를 근거로 답을 추론해도 되지만, 초안에서는 문제 해결 과정이 아니라 “보이는 표시”로만 다루는 것이 좋습니다.
- 도형의 정확한 크기나 좌표는 이미지에서 완전히 정밀하지 않으므로, 상대적인 위치 관계 중심으로 작성하는 것이 안전합니다.
- `가`, `나`, `다`, `라`의 순서는 왼쪽에서 오른쪽으로 보이는 배열 순서입니다.
- 아래 해설 그림은 위 그림의 반복처럼 보이지만, 세부 크기는 약간 다를 수 있습니다.

[불확실한 부분]
- 파란 격자 한 칸의 정확한 크기와 전체 그림의 정확한 비율은 이미지 해상도상 확정하기 어렵습니다.
- `나` 도형 안의 직각 표시가 단순한 작은 네모인지, 격자선과 겹친 표현인지 약간 모호합니다.
- 하단 반복 그림이 완전히 동일한 복제본인지, 설명을 위해 약간 축소된 별도 삽화인지 단정하기 어렵습니다.
- 문제 지시문의 끝 문장부호는 화면상 선명하지만, 정확한 서체/간격까지는 확정하기 어렵습니다.
