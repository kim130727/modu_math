[문제 개요]
- 문제 ID: `S3_초등_3_008841`
- 화면 상단에 문제 번호 `83.`가 있고, 그 오른쪽에 지시문이 2줄로 배치되어 있다.
- 주제는 `대추, 바나나, 포도의 무게 비교`이며, 가장 무거운 것을 고르는 형식이다.
- 중앙에는 저울 그림 2개가 좌우로 나란히 있다.
- 아래에는 `(1)` 문항이 있고, 그 아래에 `(해설)`과 맨 아래 답처럼 보이는 텍스트가 보인다.

[원본에 보이는 요소]
- 상단 텍스트:
  - `83.`
  - `저울로 대추, 바나나, 포도의 무게를 비교하고 있습니다. 가장 무거운 것은`
  - `무엇인지 알맞은 말을 선택해 보세요.`
- 그림 위 라벨:
  - 왼쪽 그림: `가`, `대추`, `바나나`
  - 오른쪽 그림: `나`, `바나나`, `포도`
- 본문 문항:
  - `(1) 가에서 대추와 바나나 중 더 무거운 것은 ( 대추, 바나나 )이고,`
  - `나에서 바나나와 포도 중 더 무거운 것은 ( 바나나, 포도 )입니다.`
- 해설:
  - `(해설)(1) 가에서 대추와 바나나 중 바나나를 올려놓은 접시가 내려갔으므로 더 무거운 것은 바나나이고, 나에서 바나나와 포도 중 포도를 올려놓은 접시가 내려갔으므로 더 무거운 것은 포도입니다.`
- 맨 아래 답처럼 보이는 텍스트:
  - `(1) 바나나, 포도`

[수식/관계 구조]
- 이 문제는 등식이나 수식 전개가 아니라, **저울의 기울어짐을 통해 무게의 대소를 판단하는 관계 문제**이다.
- 관찰되는 비교 관계:
  - `가` 그림: `대추`와 `바나나`를 비교
  - `나` 그림: `바나나`와 `포도`를 비교
- 각 저울은 두 물체 중 어느 쪽 접시가 더 내려갔는지로 더 무거운 쪽을 나타내는 구조로 보인다.
- 수식 기호 `=`, `+`, `-`, `÷` 등은 보이지 않는다.

[등호별 변환 의미]
- 등호 연쇄 변환은 없다.
- 따라서 단계별 변형 의미를 설명할 대상이 보이지 않는다.

[빈칸 정책]
- `( 대추, 바나나 )`, `( 바나나, 포도 )`는 **선택형 빈칸**으로 보인다.
- 빈칸에는 추론값을 넣지 말고, **원문 그대로 빈칸 상태를 유지**해야 한다.
- 원본에 답이 인쇄되어 보이는 경우에만 관찰 사실로 따로 기록할 수 있다.
- 이 문제에서는 맨 아래에 `(1) 바나나, 포도`가 보이지만, DSL 작성 시에는 **정답 확정값이 아니라 보이는 텍스트 요소**로 취급하는 것이 안전하다.

[반복 패턴]
- 저울 그림 2개가 같은 형식으로 반복된다.
  - 저울 본체
  - 좌우 접시
  - 각 접시 위의 과일 1개씩
  - 상단 라벨 박스
- 문장 구조도 반복된다.
  - `가에서 ... 중 더 무거운 것은 (   )`
  - `나에서 ... 중 더 무거운 것은 (   )`
- 해설도 동일한 판단 패턴을 반복한다.
  - `어느 접시가 내려갔는가` → `더 무거운 쪽이 무엇인가`

[DSL 작성 힌트]
- 페이지 상단에 `83.`과 본문 지시문을 좌측 정렬로 배치한다.
- 중앙에는 저울 일러스트 2개를 **좌우로 나란히** 배치한다.
- 각 저울은 다음 요소로 분해하면 좋다.
  - 받침대
  - 중앙 기둥
  - 수평 막대
  - 좌우 접시
  - 과일 아이콘
  - 기울어진 막대 각도
  - 상단 라벨 박스
- 라벨 박스는 베이지색 배경의 작은 상자처럼 보이며, 각 저울 위에 따로 배치된다.
- 왼쪽 그림의 라벨 구성:
  - `가`
  - `대추`
  - `바나나`
- 오른쪽 그림의 라벨 구성:
  - `나`
  - `바나나`
  - `포도`
- 본문 문항은 그림 아래에 2줄로 배치하고, 괄호 빈칸은 실제 빈칸 형태로 남긴다.
- 해설은 본문과 분리된 작은 글씨 블록으로 배치한다.
- 맨 아래의 `(1) 바나나, 포도`는 별도 텍스트 블록처럼 처리할 수 있다.
- 과일은 텍스트가 아니라 **그림 객체**로 분리하는 것이 적절하다.
- 저울의 기울기는 회전된 막대 또는 좌우 높이 차이로 표현하면 된다.

[semantic/solvable 힌트]
- 보이는 해설에 따르면, 두 저울 모두 **더 내려간 접시 쪽이 더 무거운 것**을 뜻한다.
- `가`에서는 `바나나` 쪽 접시가 내려간 것으로 설명되어 있다.
- `나`에서는 `포도` 쪽 접시가 내려간 것으로 설명되어 있다.
- 따라서 해설 문장과 맨 아래 텍스트는 **관찰 가능한 결과**로만 참고할 수 있다.
- 다만 이 단계에서는 정답을 새로 계산하거나 추론해서 채우지 않는다.

[주의 및 TODO]
- 정답을 풀이 형태로 단정하지 말 것.
- `가`, `나`는 사람 이름이 아니라 저울 그림을 구분하는 기호로 보인다.
- `대추`, `바나나`, `포도`는 라벨과 그림 속 물체를 연결해서 읽어야 한다.
- 선택지 괄호는 예시 나열이 아니라 **고르는 자리**로 취급한다.
- 저울의 정확한 기울기 각도는 이미지 해상도상 세밀하게 단정하지 말 것.
- 상단 베이지색 라벨 박스의 세부 테두리 형태는 렌더링 시 약간 달라질 수 있다.
- 해설과 맨 아래 답처럼 보이는 텍스트는 화면에 실제로 보이지만, DSL에서는 본문과 분리해 다루는 것이 좋다.
