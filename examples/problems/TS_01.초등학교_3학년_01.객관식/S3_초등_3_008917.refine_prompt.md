# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008917.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008917

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
## [전체적인 구성]
- 화면 상단 왼쪽에 문제 번호가 붙은 서술형/선다형 문제 문장이 있고, 그 아래 중앙에 표가 배치되어 있습니다.
- 표 아래에는 보기(선택지)가 2열 배치 형태로 나열되어 있습니다.
- 더 아래쪽에는 `(정답)①`과 `(해설)` 문구가 따로 표시되어 있습니다.
- 전체적으로 “문제 문장 → 표 → 선택지 → 정답/해설” 순서의 구성입니다.

## [보이는 텍스트]
- `83. 은우이는 반 학생들이 존경하는 위인을 조사하여 표로 나타냈습니다. 가장 많은 학생들이 존경하는 위인은 누구인가요?`
- 표 제목: `존경하는 위인별 학생 수`
- 표 머리글:
  - `위인`
  - `세종대왕`
  - `김구`
  - `이순신`
  - `유관순`
  - `합계`
- 표의 두 번째 줄:
  - `학생 수(명)`
  - `10`
  - `3`
  - `8`
  - `6`
  - `27`
- 보기:
  - `① 세종대왕`
  - `② 김구`
  - `③ 이순신`
  - `④ 유관순`
- 아래쪽 문구:
  - `(정답)①`
  - `(해설)표에서 가장 큰 수를 찾으면 10이므로 가장 많은 학생들이 존경하는 위인은 세종대왕입니다.`

## [반복되는 시각 패턴]
- 표는 열이 5개 이상으로 보이며, 각 열 머리글이 연한 초록색 배경으로 구분되어 있습니다.
- 첫 번째 열은 항목명(`위인`, `학생 수(명)`)이 들어가는 행 제목 역할을 합니다.
- 나머지 열에는 위인 이름과 숫자 데이터가 1:1로 대응되어 배치되어 있습니다.
- 숫자 데이터는 각 위인 이름 아래에 정렬되어 있어 표 형태로 비교가 가능합니다.
- 선택지는 동그라미 숫자(①~④)와 텍스트가 함께 있는 형식이며, 좌우 두 줄 배치처럼 보입니다.

## [수식 단계]
- 해당 없음.  
- 화면에는 계산식이나 등호를 포함한 변형 과정이 보이지 않습니다.

## [등호별 변환 의미]
- 해당 없음.  
- 등호를 이용한 식 변형이 제시되지 않았습니다.

## [세부 내용]
- 문제의 핵심은 “가장 많은 학생들이 존경하는 위인”을 표에서 찾는 것입니다.
- 표 안에는 각 위인별 학생 수가 각각 숫자로 제시되어 있습니다.
- `합계` 열이 따로 있으나, 이 문제에서는 개별 값들 사이의 비교가 중심으로 보입니다.
- 해설 문장에 “가장 큰 수”라는 표현이 보이며, 이는 표의 숫자 중 최대값을 찾는 형태의 사고를 유도합니다.
- 다만 이 초안에서는 실제 정답을 계산하거나 확정하지 않고, 표 구조와 비교 관계만 기술합니다.

## [Python DSL 작성 힌트]
- 표를 먼저 구성하는 방식이 적절합니다.
  - 제목 텍스트 1개
  - 2행 × 6열 정도의 표
- 표의 첫 행에는 `위인`과 각 위인 이름, `합계`를 배치합니다.
- 둘째 행에는 `학생 수(명)`과 각 숫자 데이터를 배치합니다.
- 표의 셀 배경색은 첫 행/첫 열에 연한 초록색 계열을 주는 형태로 표현할 수 있습니다.
- 보기 영역은 표 아래에 2열 배열로 배치하면 원본과 유사합니다.
- 정답 표시와 해설 문구는 표보다 아래에 따로 텍스트 블록으로 처리합니다.

## [주의할 점]
- 숫자값을 보고 정답을 직접 서술할 때도, 초안 단계에서는 결과를 확정적으로 풀어 쓰지 않는 것이 좋습니다.
- `합계 27`은 보이지만, 이 문제의 의도상 각 위인별 수를 비교하는 표로 읽히므로 합계 칸의 역할을 혼동하지 않도록 주의해야 합니다.
- 선택지의 순서와 번호를 바꾸지 않도록 해야 합니다.
- 표의 색상, 테두리, 정렬을 가능한 한 원본처럼 반영해야 합니다.
- 문제 문장과 해설 문장을 서로 섞지 말고 분리해서 기록해야 합니다.

## [불확실한 부분]
- 표의 정확한 선 두께나 배경색의 세부 RGB 값은 이미지상에서 단정하기 어렵습니다.
- 제목 `존경하는 위인별 학생 수`의 글자 크기와 정확한 중앙 정렬 여부는 약간의 차이가 있을 수 있습니다.
- `은우이`로 보이는 첫 문장의 이름 표기는 이미지 해상도상 약간 흐릴 수 있어, 실제 원문 표기와 다를 가능성이 있습니다.

--- RAW VISION DRAFT END ---
