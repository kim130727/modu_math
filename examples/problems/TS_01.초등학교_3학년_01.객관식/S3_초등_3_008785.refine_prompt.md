# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008785.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008785

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에는 9번 문제 문장이 가로로 길게 배치되어 있다.
- 본문 중앙에는 2행 4열 형태의 표가 있으며, 왼쪽 첫 칸은 항목명, 오른쪽 3칸은 학용품 종류와 숫자로 구성되어 있다.
- 표 아래쪽 오른편에는 보기처럼 보이는 괄호 안 텍스트가 따로 있다.
- 화면 하단 왼쪽에는 `(정답)` 표시와 함께 답이 이미 제시되어 있고, 그 아래에는 `(해설)`이 이어진다.
- 해설 문장은 두 줄로 나뉘어 있으며, 마지막 줄에는 부등호를 이용한 비교식이 포함되어 있다.

[보이는 텍스트]
- `□ 9. 같은 무게의 쇠구슬과 저울을 사용하여 다음 표의 무게를 알아본 것입니다. 가장 무거운 학용품은 어느 것인지 선택해 보세요.`
- 표의 첫째 행:
  - `학용품`
  - `공책`
  - `크레파스`
  - `가위`
- 표의 둘째 행:
  - `쇠구슬의 수(개)`
  - `11`
  - `17`
  - `13`
- 표 오른쪽 아래 근처:
  - `( 공책 , 크레파스 , 가위 )`
- 하단:
  - `(정답)크레파스`
  - `(해설)쇠구슬의 수가 많을수록 더 무겁습니다.`
  - `17>13>11이므로 크레파스가 가장 무겁습니다.`

[반복되는 시각 패턴]
- 표 구조가 2행 4열로 반복되는 규칙적인 칸 배열이다.
- 윗행은 대상 이름, 아랫행은 수량 정보로 대응되는 형태이다.
- 숫자 3개가 가로 방향으로 나란히 배치되어 있고, 각 숫자는 각 학용품 칸 아래에 1:1로 대응된다.
- 하단 해설에서 `17>13>11`처럼 세 숫자를 연속된 부등호로 비교하는 패턴이 보인다.
- 보기로 보이는 `( 공책 , 크레파스 , 가위 )`는 쉼표로 항목을 나열한 형태이다.

[수식 단계]
- 화면에는 별도의 여러 단계의 식 변형은 보이지 않는다.
- 다만 하단 해설에 `17>13>11`이라는 비교식이 한 번 제시되어 있다.
- 이 식은 숫자들의 크기 관계를 나타내는 형태로 보인다.

[등호별 변환 의미]
- 해당 없음.
- 화면에 `=` 등호를 이용한 연속 변형식은 보이지 않는다.

[세부 내용]
- 문제의 핵심은 “같은 무게의 쇠구슬”의 개수를 기준으로 학용품의 무게를 비교하도록 구성된 것으로 보인다.
- 표에서 각 학용품 아래에 적힌 수는 직접적인 무게 단위가 아니라 쇠구슬 개수로 표시되어 있다.
- 숫자 배치는 다음과 같이 보인다:
  - 공책 아래: `11`
  - 크레파스 아래: `17`
  - 가위 아래: `13`
- 표의 셀 경계는 얇은 선으로 구분되어 있으며, 상단 행의 셀은 연한 배경색으로 채워져 있다.
- 왼쪽 첫 열의 `학용품`, `쇠구슬의 수(개)`는 설명용 라벨 역할을 한다.
- 표 아래 오른쪽의 `( 공책 , 크레파스 , 가위 )`는 선택지 또는 참고용 항목 나열로 보인다.
- `(정답)크레파스`와 `(해설)`은 문제 풀이 결과와 설명을 구분해 주는 형식이다.

[Python DSL 작성 힌트]
- 표를 먼저 만들고, 2행 4열 구조로 배치하는 방식이 적절해 보인다.
- 첫 행은 제목/항목명, 둘째 행은 대응 숫자를 넣는 레이아웃으로 구성하면 된다.
- 각 숫자 `11`, `17`, `13`은 각각 해당 학용품 칸의 아래쪽 중앙에 정렬된 형태로 표현하면 좋다.
- 하단의 `(정답)`, `(해설)`은 표와 분리된 텍스트 블록으로 좌측 정렬 배치가 자연스럽다.
- 보기 `( 공책 , 크레파스 , 가위 )`는 표 오른쪽 아래의 별도 텍스트로 처리하면 된다.
- 연한 배경색의 상단 행, 테두리선, 셀 구분선을 DSL에서 지정할 가능성이 있다.
- 부등호 비교식 `17>13>11`은 해설 영역의 마지막 줄에 넣되, 실제 의미 해석은 별도로 다루는 것이 좋다.

[주의할 점]
- 정답을 새로 추론해 쓰지 말고, 화면에 이미 보이는 내용을 그대로 옮겨야 한다.
- `17>13>11`은 보이는 비교식일 뿐, 이 내용을 다른 식으로 바꾸어 쓰지 말아야 한다.
- 표의 숫자가 의미하는 바를 설명할 수는 있지만, 계산 과정이나 정답 도출은 하지 말아야 한다.
- 문제 본문은 `같은 무게의 쇠구슬`과 `저울`을 언급하므로, 숫자 자체를 무게 단위로 오해하지 않도록 주의한다.
- `( 공책 , 크레파스 , 가위 )`는 실제 보기인지, 편집 과정의 표시인지 화면만으로는 단정하기 어렵다.

[불확실한 부분]
- 표 아래의 `( 공책 , 크레파스 , 가위 )`가 실제 선택지인지, 편집용 메모인지 화면만으로는 확정하기 어렵다.
- 좌상단의 작은 `□` 표시는 문제 번호 앞의 체크 박스처럼 보이나, 실제 기능은 단정할 수 없다.
- 표의 배경색과 테두리 두께는 이미지 해상도상 정확한 색상값까지는 판별하기 어렵다.
- 정답과 해설이 원본 이미지에 포함된 것인지, 예시용으로 추가된 것인지도 화면만으로는 확실하지 않다.

--- RAW VISION DRAFT END ---
