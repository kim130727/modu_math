# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008899.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008899

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 좌측 상단에 문제 번호 `65.`가 있고, 그 오른쪽으로 긴 지시문이 한 줄 이상에 걸쳐 배치되어 있습니다.
- 중앙에는 제목처럼 보이는 문구 `가고 싶은 장소별 남녀 학생 수`가 표 위에 가운데 정렬로 놓여 있습니다.
- 그 아래에 2행×5열 형태의 표가 있으며, 표는 `장소`와 `남학생 수(명)`, `여학생 수(명)` 관련 정보를 담고 있습니다.
- 표 아래에는 오른쪽을 가리키는 화살표 `→`와 함께 장소를 나열한 문장이 있습니다.
- 하단 왼쪽에는 보기 선택을 위한 원형 표시들이 세로로 배치되어 있고, `○`, `×`, `(정답) ○` 형태로 보입니다.
- 맨 아래에는 `(해설)` 표시와 함께 짧은 설명 문장이 이어집니다.

[보이는 텍스트]
- `65.`
- `현수네 학교 학생들이 가고 싶은 체험 학습장소를 남학생과 여학생으로 나누어 표로 나타내었습니다. 많은 학생이 가고 싶은 장소부터 순서대로 바르게 나열하였으면 ○표, 그렇지 않으면 ×표를 선택하세요.`
- `가고 싶은 장소별 남녀 학생 수`
- 표의 열 머리글:
  - `장소`
  - `놀이공원`
  - `식물원`
  - `박물관`
  - `과학관`
  - `합계`
- 표의 행 머리글:
  - `남학생 수(명)`
  - `여학생 수(명)`
- 표 안의 숫자:
  - 남학생 수: `46`, `23`, `17`, `19`, `105`
  - 여학생 수: `25`, `31`, `38`, `27`, `121`
- 표 아래 문장:
  - `→ 박물관, 식물원, 과학관, 놀이공원`
- 선택 표식:
  - `○`
  - `×`
  - `(정답) ○`
- 해설 문구:
  - `(해설) 장소별 여학생의 수를 비교합니다.`

[반복되는 시각 패턴]
- 표의 구조가 규칙적입니다.
  - 왼쪽 첫 열은 항목명(행 제목)이고, 나머지 열은 장소별 수치가 들어가는 방식입니다.
  - 상단 행은 연한 색 배경으로 강조되어 있고, 행/열 경계선이 일정하게 그어져 있습니다.
- 숫자 데이터가 남학생, 여학생 두 행에 걸쳐 각 장소별로 나열되어 있어 비교형 자료임을 드러냅니다.
- 표 아래의 화살표 뒤에는 `장소명 4개`가 쉼표로 연결되어 반복 나열됩니다.
- 아래쪽 선택 영역에는 원형 기호가 반복되어 있어 보기 선택 문제 형식임을 보여 줍니다.

[수식 단계]
- 해당 없음. 표와 문장 중심의 자료이며, 등호로 연결된 계산식이나 변형식은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 문제의 핵심은 여러 장소에 대해 남학생 수와 여학생 수가 각각 주어져 있다는 점입니다.
- 표의 열은 `놀이공원`, `식물원`, `박물관`, `과학관`, `합계` 순서로 보입니다.
- `합계` 열에는 각 성별의 총인원처럼 보이는 수가 들어 있으며, 다른 장소 열과 구분됩니다.
- 표 아래의 `→ 박물관, 식물원, 과학관, 놀이공원`은 장소의 순서를 제시하는 문장입니다.
- 이 순서가 맞는지 판단하는 유형으로 보이며, 아래에 ○/× 선택 표시가 있는 것으로 보입니다.
- `(해설) 장소별 여학생의 수를 비교합니다.`라는 문구가 있어, 비교 기준이 여학생 수임을 암시합니다.
- 다만 이 초안에서는 실제 정답 판단이나 순서의 타당성 평가는 하지 않습니다.

[Python DSL 작성 힌트]
- 표 구조를 먼저 잡는 것이 중요합니다.
  - 2행 5열 표로 구성하고, 첫 행은 열 제목, 첫 열은 행 제목으로 처리합니다.
- 각 셀의 텍스트를 정확히 분리해 입력합니다.
  - 예: 상단 행의 `장소`, `놀이공원`, `식물원`, `박물관`, `과학관`, `합계`
  - 좌측 열의 `남학생 수(명)`, `여학생 수(명)`
- 표 상단의 제목 `가고 싶은 장소별 남녀 학생 수`는 표의 위쪽 가운데에 배치합니다.
- 표 아래 화살표와 이어진 문장은 별도의 텍스트 박스로 배치하는 것이 자연스럽습니다.
- 하단의 `○`, `×`, `(정답) ○`는 세로로 떨어진 선택 표식처럼 배치합니다.
- `(해설)`은 작은 주석 영역 또는 하단 설명 텍스트로 넣을 수 있습니다.
- 색상 정보가 필요하면 표 머리글 배경색을 연한 베이지 계열로 표현할 수 있습니다.
- 숫자와 문구의 상대적 위치를 유지하면 원본 레이아웃을 잘 재현할 수 있습니다.

[주의할 점]
- 문제를 풀어 순서를 판단하거나 정답을 확정하지 마세요.
- `박물관, 식물원, 과학관, 놀이공원`이 맞는지 여부를 단정하지 마세요.
- `합계` 열의 의미를 임의로 확장하지 마세요. 보이는 숫자와 라벨만 기록하세요.
- 표 아래의 `(정답) ○`는 원문에 보이는 표식일 뿐, 여기서 정답 근거를 해석하지 마세요.
- 해설 문구가 보이더라도 실제 계산이나 비교 결과를 적지 마세요.

[불확실한 부분]
- 표의 선 굵기와 정확한 색상은 이미지 해상도상 대략적으로만 보입니다.
- `(정답) ○`의 의미는 문제 화면의 표시로 보이나, 선택지 배치가 정확히 어떤 형식인지 세부는 다소 अस्प명확합니다.
- 하단의 원형 표시들이 실제 클릭용 선택지인지, 인쇄된 보기인지 화면만으로는 완전히 확정하기 어렵습니다.
- 표 아래 화살표 문장의 정확한 줄바꿈 위치는 이미지상 약간 흐릿하여 DSL에서는 유연하게 배치하는 것이 좋습니다.

--- RAW VISION DRAFT END ---
