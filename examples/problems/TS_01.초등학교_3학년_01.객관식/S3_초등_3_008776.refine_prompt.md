# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008776.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008776

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 줄짜리 선택형 문제처럼 보이는 구성이다.
- 좌측 상단에 작은 네모 표시와 함께 문항 번호 `89.`가 있고, 그 오른쪽에 문제 지시문이 있다.
- 본문 중앙에는 파란색 그림 1개가 배치되어 있으며, 그 오른쪽에 문장형 빈칸/선택 문장이 있다.
- 하단에는 `(정답)`과 `(해설)`이 각각 따로 제시되어, 정답 안내와 해설 문장을 구분해 보여준다.
- 전체적으로 위에서 아래로 `문항 번호 → 그림과 문장 → 정답/해설` 순서로 구성되어 있다.

[보이는 텍스트]
- `89. 알맞는 단위를 선택하세요.`
- `대접의 높이는 약 250 ( mL , L )입니다.`
- `(정답)mL`
- `(해설)대접의 높이는 약 250 mL입니다.`

[반복되는 시각 패턴]
- 괄호 안에 단위 후보가 두 개 `mL , L`로 나열되어 있다.
- 하단에 `(정답)`과 `(해설)`이 같은 형식으로 반복되어 배치되어 있다.
- 문제 문장 안에서 숫자 `250` 뒤에 단위 선택 괄호가 붙는 형태가 반복적으로 나타나는 단위 선택 문제 패턴이다.
- 본문과 해설에서 같은 수치 `250`이 반복되지만, 해설에는 선택된 단위가 포함된 완성 문장이 다시 제시된다.

[수식 단계]
- 해당 없음. 등호나 계산식, 변형 단계는 보이지 않는다.

[등호별 변환 의미]
- 해당 없음. 등호가 있는 수식 변형은 보이지 않는다.

[세부 내용]
- 문제 번호 `89.`는 왼쪽 상단에 있으며, 그 앞에 작은 빈 네모 모양 기호가 있다.
- 지시문 `알맞는 단위를 선택하세요.`는 굵은 글씨처럼 보이며 문제의 유형을 설명한다.
- 중앙의 그림은 파란색의 그릇/대접 모양이다.
  - 그림은 오른쪽 문장보다 왼쪽에 위치한다.
  - 그릇 안쪽에 옅은 음영과 물결 모양 장식이 있어 액체가 담긴 용기처럼 보인다.
- 문장 `대접의 높이는 약 250 ( mL , L )입니다.`에서
  - `250`은 숫자로 제시되어 있다.
  - 단위 후보는 괄호 안에 있으며, `mL`와 `L`이 쉼표로 구분되어 있다.
  - 괄호 앞뒤에 공백이 있어 선택지처럼 보이는 레이아웃이다.
- 하단 `(정답)mL`는 정답만 따로 표시한 영역으로 보인다.
- 하단 `(해설)대접의 높이는 약 250 mL입니다.`는 선택된 단위를 사용한 완성 문장 형태다.

[Python DSL 작성 힌트]
- 상단 문항 번호와 지시문을 한 줄 또는 두 줄 텍스트로 배치하는 구조로 작성할 수 있다.
- 중앙에는 왼쪽에 그림, 오른쪽에 문장 텍스트가 오도록 수평 배치를 고려하면 된다.
- 문장 안의 `( mL , L )`는 선택지 괄호 텍스트로 표현하고, 실제 답은 빈칸으로 채우지 말고 별도 정답 영역으로 분리하는 방식이 적절하다.
- 하단에는 `(정답)`과 `(해설)`을 각각 독립 텍스트 블록으로 두는 구성이 좋다.
- 그림은 단순 아이콘/삽화로 처리하고, 구체적인 수치나 의미는 텍스트에서만 다루는 방식이 안전하다.

[주의할 점]
- 단위를 문제 해결 과정으로 추론해 설명하지 말고, 화면에 보이는 텍스트만 기준으로 기술해야 한다.
- `mL`와 `L` 중 무엇이 맞는지 분석 초안에서 선택 근거를 길게 쓰지 말고, 정답 칸에 이미 표시된 사실만 분리해서 적어야 한다.
- 그림의 정확한 용도나 재질을 단정하지 말고, 보이는 형태를 기준으로 `대접` 또는 `그릇 모양` 정도로만 표현하는 것이 좋다.
- 해설 문장의 의미를 추가 추론하지 말고, 화면에 적힌 문장을 그대로 옮기는 수준으로 정리해야 한다.

[불확실한 부분]
- 그림이 실제로는 `대접`인지, 일반적인 `그릇`인지 시각적으로 완전히 단정하기는 어렵다. 문제 본문 텍스트에 `대접`이라고 적혀 있으므로 그 표현을 우선 따르되, 그림 자체만으로는 형태를 엄밀히 구분하기 어렵다.
- 그림의 세부 색감과 음영은 보이지만, 정확한 도형 스타일이나 테두리 선 처리 방식은 이미지 해상도상 세부 확인이 제한된다.
- `(정답)mL` 표기는 줄 간격이 매우 좁아 보이므로, 실제 레이아웃에서는 괄호와 정답 텍스트가 붙어 있는 하나의 라벨처럼 처리될 수 있다.

--- RAW VISION DRAFT END ---
