# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008836.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008836

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# 시각 분석 초안

## [전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 문제 문장이 있고, 그 아래 왼쪽에는 양팔저울 그림이 배치되어 있습니다.
- 화면 오른쪽에는 연한 분홍색 배경의 선택지 박스가 있으며, 2열 2행 형태로 보기 4개가 정리되어 있습니다.
- 하단 왼쪽에는 `(정답)` 표시와 그 옆의 보기 기호가 보이고, 그 아래에 `(해설)` 문장이 이어집니다.
- 전체적으로 **문제 문장 + 그림 + 선택지 + 정답/해설** 순서의 객관식 구성입니다.

## [보이는 텍스트]
- `78. 윗접시저울로 물건의 무게를 재는 데 사용할 수 있는 단위가 아닌 것을`
- `찾아 선택해 보세요.`
- 선택지 박스 안:
  - `ㄱ 클립`
  - `ㄴ 바둑돌`
  - `ㄷ 밤`
  - `ㄹ 100원짜리 동전`
- 아래쪽:
  - `(정답) ㄷ`
  - `(해설) ㄷ 밤은 무게가 일정하지 않으므로 무게를 재는 데 단위로 사용하기에 적당하지 않습니다.`

## [반복되는 시각 패턴]
- 선택지는 각 항목 앞에 **동그라미 안 한글 자모 기호**가 붙어 있습니다.
- 보기들이 **2행 2열**로 일정하게 정렬되어 있습니다.
- 기호와 텍스트 사이에 간격이 있고, 각 보기의 시작 위치가 좌우로 균형 있게 배치되어 있습니다.
- 정답과 해설에서도 `ㄷ` 기호가 다시 반복되어 나타납니다.

## [수식 단계]
- 해당 없음. 수식 변형이나 등호 연결식은 보이지 않습니다.

## [등호별 변환 의미]
- 해당 없음.

## [세부 내용]
- 문제 문장에서는 `윗접시저울`이라는 도구를 기준으로, **무게를 재는 데 사용할 수 있는 단위가 아닌 것**을 찾으라고 요청합니다.
- 왼쪽 그림은 양쪽 접시가 있는 저울의 삽화로, 실제 측정 도구의 예시를 보여 줍니다.
- 오른쪽 선택지 박스는 연한 분홍색 바탕 위에 검은 글씨로 항목이 배치되어 있습니다.
- 각 선택지는 다음과 같은 형태로 보입니다.
  - 왼쪽 위: `ㄱ` + `클립`
  - 오른쪽 위: `ㄴ` + `바둑돌`
  - 왼쪽 아래: `ㄷ` + `밤`
  - 오른쪽 아래: `ㄹ` + `100원짜리 동전`
- 하단의 `(정답)` 및 `(해설)`은 문제의 해설 정보로 보이며, 실제 문제 풀이용 시각 요소라기보다 정답 표시 영역입니다.

## [Python DSL 작성 힌트]
- 문제 영역을 크게 다음처럼 나누면 좋습니다.
  1. 상단 문제 문장 텍스트
  2. 좌측 저울 그림
  3. 우측 선택지 박스
  4. 하단 정답/해설 텍스트
- 선택지 박스는 **배경색이 있는 큰 사각형**으로 만들고, 그 안에 2행 2열 그리드를 배치하는 구성이 적절합니다.
- 각 보기 항목은 `원형 기호 + 텍스트`의 조합으로 표현하면 됩니다.
- `ㄱ, ㄴ, ㄷ, ㄹ`은 각각 별도의 작은 원형 마커처럼 보이므로, 텍스트와 분리해서 좌표를 잡는 방식이 유리합니다.
- 저울 그림은 세부 기계 구조가 많지만, DSL에서는 삽화 전체를 한 개의 이미지 객체로 처리해도 무방합니다.
- 하단 해설은 작은 글씨로 한 줄 또는 두 줄에 걸쳐 배치하면 됩니다.

## [주의할 점]
- 답을 추론해서 채우지 말고, 화면에 보이는 그대로만 기록해야 합니다.
- 정답 표시 `ㄷ`이 보이더라도, 그것을 숨은 답으로 해석하여 문제 풀이 과정에 반영하지 않도록 주의해야 합니다.
- 선택지 `ㄱ~ㄹ`의 순서는 바꾸지 말고, 보이는 배치 그대로 유지해야 합니다.
- 그림 속 저울은 실제 측정값을 나타내는 도식이 아니라, 문제 주제와 관련된 삽화로 보입니다.
- 해설 문장의 의미를 이용해 추가 내용을 덧붙이지 말고, 보이는 문장만 옮겨 적는 수준으로 정리하는 것이 좋습니다.

## [불확실한 부분]
- 저울 그림의 세부 부품 명칭은 해상도상 정확히 구분하기 어렵습니다.
- 선택지 박스의 정확한 배경색 값이나 테두리 유무는 이미지에서 완전히 정밀하게 판독하기 어렵습니다.
- 문제 번호 왼쪽의 아주 작은 사각형 기호는 체크박스처럼 보이지만, 정확한 기능은 단정하기 어렵습니다.

--- RAW VISION DRAFT END ---
