# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008883.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008883

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 문제 번호 `47.`이 왼쪽 상단에 있고, 그 오른쪽에 긴 문장형 지시문이 한 줄 이상으로 배치되어 있습니다.
- 중앙에는 표가 크게 배치되어 있으며, 표 위에 제목 문구가 따로 있습니다.
- 표 아래에는 선택지를 유도하는 문장과, `(정답)` 표시가 붙은 정답 표기, 그리고 `(해설)`로 시작하는 설명 문장이 이어집니다.
- 전체적으로 **문장형 문제 → 표 → 결론 문장 → 정답/해설** 순서의 구성입니다.

[보이는 텍스트]
- `47. 연구네 학교 학생들이 방과 후 수업으로 배우고 싶어 하는 운동을 조사하여 나타낸 표입니다. 표를 보고 알맞은 말을 선택해 보세요.`
- 표 제목: `여학생과 남학생이 배우고 싶어 하는 운동`
- 표의 열 제목: `운동`, `축구`, `골프`, `야구`, `스쿼시`, `합계`
- 표의 행 제목: `여학생 수(명)`, `남학생 수(명)`
- 여학생 수(명) 행의 값: `18`, `10`, `27`, `35`, `90`
- 남학생 수(명) 행의 값: `32`, `20`, `28`, `15`, `95`
- 표 아래 문장: `여학생이 가장 많이 배우고 싶어 하는 운동은 ( 축구, 골프, 야구, 스쿼시 )입니다.`
- 아래쪽에 `(정답)스쿼시`
- 해설 문장: `(해설)여학생 수를 살펴보면 가장 큰 수가 35이므로 여학생이 가장 많이 배우고 싶어 하는 운동은 스쿼시입니다.`

[반복되는 시각 패턴]
- 표는 **가로로 열이 나뉜 격자 형태**이며, 위쪽에 항목명이 있고 아래에 수치가 들어가는 구조입니다.
- 행은 2개(여학생/남학생)이고, 각 행마다 4개 운동 항목과 1개의 합계가 반복 배치되어 있습니다.
- 숫자들은 표 가운데 정렬에 가깝게 들어가 있으며, 항목명은 왼쪽 첫 칸에 문장처럼 길게 들어갑니다.
- 선택지 문장은 괄호 안에 보기들이 **쉼표로 나열**되어 있습니다.
- 정답 표기는 문제 본문과는 분리되어 아래쪽에 `(정답)` 라벨이 붙어 있습니다.

[수식 단계]
- 해당 없음.  
- 이 문제 화면에는 등호가 이어지는 수식 변형 과정이 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 표의 구조:
  - 첫 번째 열은 행 이름을 적는 열입니다.
  - 그 오른쪽으로 `축구`, `골프`, `야구`, `스쿼시`, `합계`가 열 제목으로 배치되어 있습니다.
  - 표의 테두리는 연한 붉은색 선으로 보입니다.
  - 표 내부에도 같은 색 계열의 선으로 행과 열이 구분되어 있습니다.
- 숫자 배치:
  - 여학생 행: `18`, `10`, `27`, `35`, `90`
  - 남학생 행: `32`, `20`, `28`, `15`, `95`
- 표 위의 제목은 표 가운데에 정렬되어 있고, 표 본문보다 위쪽에 약간 떨어져 있습니다.
- 표 아래의 문장은 가운데 정렬된 형태로 보이며, 괄호 안 보기들이 한 줄에 나열되어 있습니다.
- 정답과 해설은 본문보다 아래쪽에 따로 있으며, 설명용 텍스트로 처리된 듯합니다.

[Python DSL 작성 힌트]
- 표를 만들 때는 먼저 **2행 × 6열** 구조를 기준으로 생각하면 좋습니다.
- 첫 칸은 행 제목, 나머지 칸은 운동 항목과 합계로 채우는 형태입니다.
- 표 제목은 표 상단 중앙 정렬 텍스트로 따로 배치하는 방식이 مناسب합니다.
- 표 아래의 선택지 문장은 하나의 문장 텍스트로 두고, 괄호 안 보기는 쉼표 포함 문자열로 표현하면 좋습니다.
- `(정답)`과 `(해설)`은 본문과 분리된 별도 텍스트 블록으로 배치하는 것이 자연스럽습니다.
- 표의 경계선 색이 연분홍/연붉은색 계열이므로, DSL에서 색상이나 스타일 속성을 줄 수 있다면 유사하게 지정할 수 있습니다.

[주의할 점]
- `연구네 학교`라는 학교 이름은 실제 텍스트 그대로 보이는 대로 적어야 하며, 임의로 교정하지 않는 것이 좋습니다.
- 표 안의 숫자는 **그대로 시각 정보로만 기록**해야 하며, 합계를 이용해 계산한 값으로 해석해 덧붙이지 말아야 합니다.
- 보기 문장에 있는 괄호 속 항목들을 정답으로 미리 확정하여 본문에 섞어 쓰지 않도록 주의해야 합니다.
- 해설 문장은 화면에 이미 적힌 설명이므로, 별도 풀이로 재구성하지 말고 보이는 대로만 옮겨 적는 것이 좋습니다.

[불확실한 부분]
- 문제 상단의 학교 이름 `연구네`는 글꼴 특성상 `연구네`로 보이지만, 실제 의도한 표기가 다른지 여부는 화면만으로는 확정하기 어렵습니다.
- 표 제목의 정확한 줄바꿈 위치는 이미지 해상도 때문에 약간 다르게 보일 수 있습니다.
- `(정답)스쿼시` 부분은 붙여 쓴 형태로 보이지만, 실제 편집에서는 `(정답)`과 `스쿼시` 사이에 미세한 간격이 있을 수 있습니다.

--- RAW VISION DRAFT END ---
