# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008808.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008808

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 문제짜리 선택형/빈칸형 학습지 화면입니다.
- 왼쪽 상단에 문제 번호와 지시문이 있고, 중앙에는 식탁 그림이 크게 배치되어 있습니다.
- 오른쪽에는 단위 선택을 위한 괄호 표기 `( g , kg )`가 따로 보입니다.
- 왼쪽 아래에는 `(정답)kg`가 표시되어 있고, 그 아래에 해설 문장이 있습니다.
- 전체적으로 “그림을 보고 무게 단위를 고르는” 단순 구성입니다.

[보이는 텍스트]
- `□ 37. 식탁의 무게를 나타내는 데 알맞은 단위를 선택하세요.`
- `( g , kg )`
- `(정답)kg`
- `(해설)1 kg 보다 무거운 물건의 무게는 kg으로 나타냅니다.`

[반복되는 시각 패턴]
- 선택지 단위가 괄호 안에 `g`, `kg` 형태로 제시됩니다.
- 본문 아래쪽에 정답 표시와 해설 표시가 각각 괄호 형식으로 들어가 있습니다.
- 숫자 대신 단위 선택을 요구하는 문제 형식입니다.
- 그림 중심의 문제 배치가 반복적으로 사용된 형태로 보입니다.

[수식 단계]
- 해당 없음.
- 등호가 포함된 변형식이나 연산 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.
- 등호 연결식이 없으므로 변환 의미를 설명할 부분이 없습니다.

[세부 내용]
- 문제 번호 `37` 앞에는 작은 네모 모양 기호 `□`가 함께 보입니다.
- 문제 문장은 한 줄로 이어지며, 식탁 그림을 보고 알맞은 무게 단위를 선택하라고 안내합니다.
- 중앙의 식탁 그림은 나무색 계열의 입체 그림입니다.
  - 상판이 직사각형으로 보입니다.
  - 다리가 여러 개 보이며 원근감이 있는 3D 형태입니다.
- 오른쪽의 선택지는 `( g , kg )`처럼 쉼표로 구분된 두 단위가 괄호 안에 놓여 있습니다.
- 왼쪽 아래 `정답` 표시는 이미 `kg`로 적혀 있지만, 이는 화면에 보이는 정보로만 기록해야 합니다.
- 해설 문장은 `1 kg 보다 무거운 물건의 무게는 kg으로 나타냅니다.`로, 무거운 물건의 단위를 설명합니다.

[Python DSL 작성 힌트]
- 문제 영역을 상단 문장, 중앙 그림, 우측 선택지, 하단 정답/해설의 4개 블록으로 나누어 배치하면 좋습니다.
- 식탁 그림은 정답 유도 요소가 아니라 시각 자료이므로, 도형/이미지 객체로 따로 배치합니다.
- 선택지는 텍스트 객체로 `(`, `g`, `,`, `kg`, `)`를 한 줄에 정렬합니다.
- 하단의 `(정답)`과 `(해설)`은 별도 텍스트 줄로 작성합니다.
- 문제 번호 `37`과 지시문은 같은 첫 줄 또는 인접한 상단 영역에 배치합니다.
- 선택지와 정답 표시는 서로 다른 위치에 있으므로 좌우 정렬 관계를 유지해 주세요.

[주의할 점]
- 정답을 추론해서 설명문에 섞지 마세요. 이 초안에서는 화면에 보이는 단위 표기만 기록합니다.
- 그림의 실제 무게를 수치로 판단하지 마세요.
- `kg`와 `g`의 의미를 설명하는 것은 가능하지만, 문제의 답을 풀어 단정하는 표현은 피해야 합니다.
- 하단에 적힌 `(정답)kg`는 이미지에 보이는 사실로만 다뤄야 합니다.
- 해설 문장은 인쇄된 텍스트 그대로 옮기는 데 집중하고, 그 내용으로 추가 계산을 하지 마세요.

[불확실한 부분]
- 왼쪽 상단의 작은 네모 `□` 기호의 정확한 용도는 이미지상만으로는 확정하기 어렵습니다.
- 식탁 그림의 세부 선분 수나 다리 구조는 일부 겹쳐 보여 정확한 개수 판독이 다소 어렵습니다.
- `(정답)kg`는 화면에 선명하지만, 이것이 원본 학습지의 편집 의도인지 시스템 표기인지는 이미지 만으로 단정하기 어렵습니다.

--- RAW VISION DRAFT END ---
