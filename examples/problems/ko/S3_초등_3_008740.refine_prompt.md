# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008740.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008740

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 전체는 초등 수학 문제 1개와, 그 아래에 **비교를 위한 도식(나무 구조)**, 그리고 **정답/해설** 영역이 함께 배치된 화면이다.
- 상단에는 문제 번호와 지시문이 왼쪽 정렬로 있고, 중앙에는 큰 비교 도식이 있다.
- 도식은 위에서 아래로 내려가는 **계층형 분기 구조**로 되어 있으며, 맨 위에 ① 표시가 들어간 상자 1개가 있고, 그 아래에 좌우로 두 갈래로 나뉜다.
- 각 갈래 아래에는 다시 두 개씩의 후보 분수/대분수 카드가 놓여 있어, 비교 관계를 시각적으로 보여준다.
- 화면 중간 왼쪽에는 **(정답)**, **(해설)** 문구와 함께 분수/대분수 비교가 텍스트로 제시되어 있다.
- 하단에는 또 한 번 같은 주제의 작은 도식이 반복되며, 마지막에 문장형 설명이 이어진다.

[보이는 텍스트]
- 문제 번호: `26.`
- 지문: `두 분수의 크기를 비교하여 더 큰 분수를 빈칸에 써 넣으려 합니다. ①에 들어갈 분수로 알맞은 것을 가장 아래 칸의 분수들 중에 선택해 보세요.`
- 도식 상단 상자 안: `①`
- 아래 후보 상자들:
  - `23/6`
  - `21/6`
  - `3 2/6`
  - `3 4/6`
- 중간 왼쪽:
  - `(정답) 23/6`
  - `(해설) 23/6 > 21/6, 3 2/6 < 3 4/6`
- 하단 문장:
  - `23/6 과 3 4/6의 크기를 비교하면 23/6 = 3 5/6 이므로 23/6 > 3 4/6입니다.`
  - `따라서 ①에 알맞은 수는 23/6 입니다.`
- 참고로 화면에는 아래쪽에 또 다른 작은 동일 구조의 도식이 보이며, 그 안의 일부 상자에는 파란색으로 `23/6`, `3 4/6`가 강조 표시되어 있다.

[반복되는 시각 패턴]
- 같은 형태의 **사각형 카드**가 반복 사용된다.
- 카드들은 연한 보라색 테두리와 옅은 채움색을 가진다.
- 각 카드의 아래쪽에는 작은 보라색 띠처럼 보이는 강조 부분이 있다.
- 도식은 항상
  1. 위의 한 상자
  2. 아래로 연결선
  3. 좌우 두 개의 중간 상자
  4. 다시 아래로 연결선
  5. 맨 아래의 네 개 후보 상자
  구조를 가진다.
- 상자 사이 연결은 직선 형태의 **계단형/분기형 선**으로 표현된다.
- 하단의 예시 도식에서는 선택된 값처럼 보이는 두 상자 안의 숫자가 **파란색**으로 표시되어 있어 강조 패턴이 있다.
- 분수 표기는 일반적인 가로선 분수와 대분수 형식이 섞여 반복된다.
- 텍스트 설명에서는 비교 기호 `>`와 `<`가 반복 사용되어 두 수의 크기 관계를 나타낸다.

[수식 단계]
- 화면 하단 설명에 비교 과정이 텍스트로 제시되어 있다.
- 보이는 식의 형태:
  - `23/6 과 3 4/6의 크기를 비교하면`
  - `23/6 = 3 5/6 이므로`
  - `23/6 > 3 4/6입니다.`
- 해설 줄에는 또 다른 비교식이 함께 제시되어 있다:
  - `23/6 > 21/6, 3 2/6 < 3 4/6`
- 이 부분은 문제 풀이 자체를 요구하는 게 아니라, **비교 근거를 단계적으로 보여주는 형식**으로 보인다.

[등호별 변환 의미]
- `23/6 = 3 5/6`
  - 보이는 정보 기준으로는, 같은 값을 **가분수 형태에서 대분수 형태로 바꿔 쓴 것**처럼 표현되어 있다.
  - 단정적으로 계산 과정을 확장하지 않고, 화면에 표시된 변환 결과만 읽으면 된다.
- `23/6 > 3 4/6`
  - 두 수의 크기 비교 결과를 문장으로 연결한 것이다.
  - 등호가 여러 번 연달아 있는 형태는 아니며, 한 번의 `=`와 한 번의 `>`가 각각 다른 의미로 쓰였다.
- `23/6 > 21/6, 3 2/6 < 3 4/6`
  - 콤마로 두 개의 비교식을 나란히 적어 놓은 형태이다.
  - 각 비교식은 서로 독립적인 관계를 나타내는 것으로 보인다.

[세부 내용]
- 상단 문제 문장 왼쪽에는 작은 네모 모양 기호가 있고, 그 오른쪽에 문제 번호 `26.`이 붙어 있다.
- 본문 문장은 두 줄로 나뉘어 있고, 첫 줄 끝에 `①에 들어가`가 보이며 다음 줄로 이어진다.
- 중앙 도식의 최상단 상자는 정사각형에 가까운 흰색 박스이며, 내부에 원형 테두리 안의 `①`이 들어 있다.
- 그 아래에는 가로 연결선이 있고, 좌우로 두 개의 큰 빈 사각형 상자가 배치된다.
- 각 중간 상자 아래에도 다시 짧은 연결선이 있어, 아래쪽 두 개씩의 후보 상자로 이어진다.
- 아래 후보 상자들은 4개가 한 줄에 나란히 놓여 있으며, 각각 분수 또는 대분수 텍스트가 들어 있다.
- 왼쪽 두 상자는 `23/6`, `21/6`이고, 오른쪽 두 상자는 `3 2/6`, `3 4/6`으로 보인다.
- 중간 왼쪽에 있는 `(정답)` 표시는 정답 값 바로 아래에 작게 배치되어 있고, `(해설)`은 그 아래 줄에 이어진다.
- 하단에는 동일한 문제를 다시 설명하는 듯한 문장이 있으며, 수식은 본문보다 큰 글씨로 보인다.
- 아래쪽 작은 도식은 상단 ① 상자와 하단 후보 상자들의 배치가 원형 도식과 유사하나, 일부 값이 파란색으로 강조되어 있어 예시/정답 표시처럼 보인다.

[Python DSL 작성 힌트]
- 도식은 **상단 1개 → 중간 2개 → 하단 4개**의 트리 구조로 표현하면 좋다.
- 각 상자는 동일한 크기의 사각형으로 통일하고, 내부 텍스트만 다르게 넣는 방식이 적합하다.
- 상자 색은 연한 보라색 계열, 강조된 텍스트는 파란색 계열로 구분하면 화면과 유사하다.
- 연결선은 각 상자 중심을 기준으로 수직/수평 분기선으로 그리면 된다.
- 텍스트 영역과 도식 영역은 위아래로 크게 분리되어 있다.
- 해설 텍스트는 별도의 줄 블록으로 배치하고, `>`와 `<`, `=` 기호를 그대로 유지하는 것이 좋다.
- 분수는 `23/6`처럼 슬래시 표기와 `3 4/6` 같은 대분수 표기를 모두 지원해야 한다.
- 정답 표시 `(정답)`은 본문 왼쪽에 작게 두고, 실제 답안은 그 오른쪽 또는 아래에 분리 배치하는 형태가 자연스럽다.

[주의할 점]
- 빈칸 `①`에 들어갈 값을 추론해 채우지 말아야 한다.
- 화면에 보이는 정답/해설 문구가 있어도, 초안에서는 그것을 **시각 정보로만 기록**하고 문제 풀이 과정으로 확장하지 말아야 한다.
- `23/6 = 3 5/6` 같은 변환은 화면에 적힌 그대로만 적고, 추가 계산을 덧붙이지 않는다.
- 아래쪽의 작은 도식이 완전히 동일한 의미인지, 예시인지, 정답 표시용인지 단정하지 않는다.
- 파란색으로 보이는 숫자는 강조 표시로만 기록하고, 특별한 의미를 확정하지 않는다.
- 도식의 정확한 선 굵기나 색상 값은 이미지 해상도상 오차가 있을 수 있으므로 대략적인 스타일로 기술한다.

[불확실한 부분]
- 상단 문제 문장의 줄바꿈 위치는 이미지상 보이지만, 원본 편집 시 정확히 같은 줄 분할인지 확정할 수 없다.
- 도식 상단의 ①이 단순 빈칸 표시인지, 선택 대상의 번호인지 문맥상 해석은 가능하지만 단정은 피해야 한다.
- 하단의 작은 도식은 정답 설명용 예시로 보이지만, 정확한 역할은 이미지 만으로 완전히 확정하기 어렵다.
- 일부 분수의 글자 크기와 위치는 확대 없이 판독한 것이므로, 실제 DSL에서는 약간의 여백 차이가 있을 수 있다.

--- RAW VISION DRAFT END ---
