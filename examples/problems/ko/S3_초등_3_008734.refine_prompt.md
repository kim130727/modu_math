# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008734.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008734

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 번호 `89.`와 함께 문장이 한 줄~두 줄로 배치되어 있다.
- 중앙에는 분홍색 테두리의 큰 표가 있고, 가로 4칸 × 세로 2칸 구조로 보인다.
- 각 칸의 윗부분에는 그림 아이콘, 아랫부분에는 이름과 분수 표기가 세로로 배치되어 있다.
- 표 아래 오른쪽에는 괄호로 묶인 보기 형태의 텍스트가 있고, 더 아래 왼쪽에는 `(정답)`과 `(해설)` 표시가 보인다.
- 하단 해설 영역에는 분수 두 개와 화살표, 그리고 `진분수`라는 단어가 이어져 있다.

[보이는 텍스트]
- `89. 블루베리 즙 2잔을 만드는 데 필요한 재료입니다. 빈 칸에 진분수인 재료를 모두 선택해 보세요.`
- 표 안의 항목:
  - `블루베리`
  - `물`
  - `우유`
  - `꿀`
  - `7/4 컵`
  - `6/6 컵`
  - `3/8 컵`
  - `5/14 컵`
- 표 오른쪽 아래 쪽:
  - `( 블루베리 , 물 , 우유 , 꿀 )`
- 하단 왼쪽:
  - `(정답) 우유, 꿀`
  - `(해설) 3/8, 5/14 → 진분수`

[반복되는 시각 패턴]
- 4개의 재료가 같은 형식으로 반복 배치되어 있다.
  - 위: 그림
  - 중간: 재료 이름
  - 아래: 분수와 단위 `컵`
- 분수 표기는 모두 같은 크기와 스타일로 제시되어 있으며, 각 항목 아래에 단독으로 놓여 있다.
- 표 전체는 동일한 칸 너비로 4개가 나란히 배열된 균등 구조이다.
- 아래 해설에서는 두 개의 분수가 쉼표로 나열되고, 오른쪽 화살표 뒤에 분류 결과가 한 단어로 제시된다.

[수식 단계]
- 화면에 수식 변형 단계가 직접 여러 번 이어진 형태는 보이지 않는다.
- 다만 해설 부분에 `3/8, 5/14 → 진분수`처럼 분수 예시와 분류 결과가 연결되어 있다.
- 이는 계산 과정이라기보다, 특정 분수들을 기준에 따라 분류해 결과를 표시한 구성으로 보인다.

[등호별 변환 의미]
- 등호 기호는 보이지 않는다.
- 화살표 `→`는 왼쪽의 분수들이 오른쪽의 분류 결과 `진분수`에 해당함을 보여주는 표시로 보인다.
- 보이는 사실만 말하면, `3/8`과 `5/14`가 화살표 뒤의 결과와 연결되어 있다.
- 추론 의미로는, 해설이 어떤 분수가 진분수인지 예시로 보여주는 역할을 하는 것으로 해석된다.

[세부 내용]
- 문제는 “블루베리 즙 2잔”을 만드는 데 필요한 재료를 고르는 내용이다.
- 표에는 네 가지 재료가 제시되어 있다.
  1. 블루베리: `7/4 컵`
  2. 물: `6/6 컵`
  3. 우유: `3/8 컵`
  4. 꿀: `5/14 컵`
- 각 재료명은 그림 바로 아래에 있고, 분수는 그 아래에 분리되어 적혀 있다.
- 표의 바깥쪽 오른쪽 아래에 보기처럼 `( 블루베리 , 물 , 우유 , 꿀 )`이 따로 적혀 있어, 선택형 문항임을 보여준다.
- 하단의 `(정답)` 표시는 인쇄물에 포함된 해설 정보로 보이며, 이후에 `우유, 꿀`이 적혀 있다.
- 단, 이 정답 표시는 시각적으로 보이는 요소이므로 기록은 가능하지만, 작성 단계에서는 답을 채워 넣지 않는 것이 좋다.

[Python DSL 작성 힌트]
- 표는 4열 2행 구조로 구현하면 된다.
- 각 열은 다음과 같이 구성해 분리 배치하는 것이 좋다.
  - 첫 행: 아이콘 이미지
  - 둘째 행: 재료명 + 분수 표기
- 분수는 일반 텍스트가 아니라 분수 슬롯 또는 상하 분리 표기로 표현하면 원본과 유사하다.
- `컵` 단위는 분수 오른쪽에 붙어 있으므로, 분수와 단위를 한 덩어리로 배치한다.
- 표 테두리는 분홍색 계열의 선으로 표현한다.
- 표 아래 오른쪽의 보기 텍스트는 괄호 안 항목들을 한 줄로 배치한다.
- 하단 해설 영역은 문제 본문과 떨어진 별도 텍스트 블록으로 넣는다.
- 화살표 기호는 텍스트 또는 도형 요소로 중앙 정렬해 배치할 수 있다.
- 아이콘은 블루베리, 물컵, 우유팩, 꿀병 순으로 배치되어 있다.

[주의할 점]
- 빈칸에 들어갈 답을 추론하여 채우지 말아야 한다.
- `정답`으로 보이는 부분이 있어도, DSL 작성 시에는 보기/해설 요소로만 분리해 다루는 것이 안전하다.
- `3/8`, `5/14`는 해설에 보이는 분수 예시이며, 이것을 이용해 문제 답을 다시 계산하거나 서술하지 않는다.
- `6/6`은 화면에 그대로 보이는 값이므로 다른 값으로 바꾸지 않는다.
- 표의 각 재료명과 분수는 서로 같은 열 안에 정확히 대응되도록 배치해야 한다.
- 보기 괄호 `(... )`는 단순 텍스트이므로 체크박스나 선택칸과 혼동하지 않는다.

[불확실한 부분]
- 표 아래의 `( 블루베리 , 물 , 우유 , 꿀 )`는 보기 목록인지, 별도의 안내 문구 일부인지 레이아웃상 애매할 수 있다.
- 하단의 `(해설)` 영역은 원문 문제지의 일부인지, 편집용 주석인지 이미지 만으로는 완전히 단정하기 어렵다.
- `우유, 꿀`의 위치는 화면에서 정답 표기처럼 보이지만, 실제 시험지 구성인지 해설 캡처인지 맥락이 조금 불명확하다.
- 글꼴 크기와 줄바꿈 위치는 이미지 해상도에 따라 약간 달라질 수 있다.

--- RAW VISION DRAFT END ---
