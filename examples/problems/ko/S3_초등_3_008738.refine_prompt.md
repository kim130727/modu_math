# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008738.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008738

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에는 문제 번호와 지시문이 한 줄로 배치되어 있습니다.
- 중앙에는 연한 파란색 테두리의 4열 표가 있고, 각 열에 재료 이름과 분수 형태의 양이 아래위로 나뉘어 표시되어 있습니다.
- 표 아래 오른쪽에는 괄호로 묶인 선택 목록이 따로 적혀 있습니다.
- 하단 왼쪽에는 `(정답)` 표시와 함께 선택된 항목이 텍스트로 제시되어 있고, 그 아래에는 `(해설)`이 붙은 분류 설명이 있습니다.
- 맨 아래에는 결론 문장이 한 줄로 제시되어 있습니다.

[보이는 텍스트]
- `□ 15. 블루베리 주스 2잔을 만드는 데 필요한 재료입니다. 필요한 양이 진분수인 재료를 모두 선택하세요.`
- 표 안의 항목:
  - `블루베리`
  - `물`
  - `우유`
  - `꿀`
- 각 항목 아래의 분수:
  - `7/4 컵`
  - `5/5 컵`
  - `7/9 컵`
  - `6/13 컵`
- 표 아래 오른쪽:
  - `( 블루베리 , 물 , 우유 , 꿀 )`
- 하단:
  - `(정답)우유, 꿀`
  - `(해설)진분수 → 7/9, 6/13; 가분수 → 7/4, 5/5`
  - `따라서 필요한 양이 진분수인 재료는 우유, 꿀입니다.`

[반복되는 시각 패턴]
- 4개의 열이 같은 너비로 나뉜 표 구조입니다.
- 각 열은 위쪽에 그림 아이콘, 아래쪽에 재료 이름, 맨 아래쪽에 분수 양이 배치되어 있습니다.
- 분수는 모두 세로로 놓인 분자/분모 형식이며, 각 열마다 동일한 형식으로 반복됩니다.
- 재료 이름과 양이 각각 해당 열의 중앙에 정렬되어 있어, 시각적으로 “아이콘-이름-수치” 패턴이 반복됩니다.

[수식 단계]
- 해설 부분에 분수 분류가 한 번에 제시되어 있습니다.
- `진분수 → 7/9, 6/13`
- `가분수 → 7/4, 5/5`
- 등호를 이용한 연쇄 변형은 보이지 않고, 화살표로 분류 결과를 묶어 보여주는 형태입니다.

[등호별 변환 의미]
- 해당 없음.
- 화면에는 등호를 이용한 수식 변형 단계가 나타나지 않습니다.
- 대신 `→` 기호로 “진분수에 해당하는 분수들”과 “가분수에 해당하는 분수들”을 나누어 적어 놓았습니다.

[세부 내용]
- 문제는 “블루베리 주스 2잔을 만드는 데 필요한 재료”를 보고, 그중 “필요한 양이 진분수인 재료”를 고르는 형식입니다.
- 표의 각 재료는 다음과 같이 보입니다.
  - 블루베리: 그림 아이콘 위에 이름, 아래에 `7/4 컵`
  - 물: 컵 그림 위에 이름, 아래에 `5/5 컵`
  - 우유: 우유팩 그림 위에 이름, 아래에 `7/9 컵`
  - 꿀: 꿀병 그림 위에 이름, 아래에 `6/13 컵`
- 표 아래의 괄호 안에는 선택지처럼 `블루베리, 물, 우유, 꿀`이 한 줄로 나열되어 있습니다.
- 하단의 `(정답)`과 `(해설)`은 문제의 정답 및 분수 분류를 텍스트로 보여주는 참고 영역처럼 보입니다.
- 그림 아이콘은 각 재료를 직관적으로 구분하도록 배치되어 있으며, 실제로 답을 고르는 위치와는 별도로 설명용 시각 요소로 보입니다.

[Python DSL 작성 힌트]
- 상단 문제 문장은 텍스트 박스로 처리하고, 번호 `15.`와 지시문을 한 줄 또는 두 줄 텍스트로 배치하면 됩니다.
- 중앙의 4열 표는 바깥 테두리와 내부 세로 구분선을 포함한 표 형태로 구성하는 것이 자연스럽습니다.
- 각 칸은 위: 그림, 중간: 재료명, 아래: 분수 문자열 순서로 세로 정렬합니다.
- 분수는 일반 문자열이 아니라 분자/분모가 위아래로 보이는 수직 분수 슬롯 형태로 표현하는 것이 좋습니다.
- 표 아래의 `( 블루베리 , 물 , 우유 , 꿀 )`은 별도의 텍스트 라인으로 우측 정렬에 가깝게 배치하면 원본과 유사합니다.
- 하단의 `(정답)`와 `(해설)`은 문제 본문과 구분되는 작은 텍스트 블록으로 나누어 배치합니다.
- 그림 아이콘은 정확한 도형 재현보다 “블루베리 송이, 물컵, 우유팩, 꿀병” 정도의 간단한 아이콘 placeholder로 구현해도 됩니다.

[주의할 점]
- 정답을 추론하여 빈칸처럼 보이는 부분에 직접 채우지 마세요.
- 이 자료는 문제 풀이가 아니라 시각 구조 분석이므로, “진분수”의 실제 판정 기준을 설명하거나 계산 결과를 전개할 필요는 없습니다.
- 표의 각 수치는 문제에 이미 제시된 정보로만 다루고, 추가 계산을 포함하지 마세요.
- `(정답)`과 `(해설)`은 원본 이미지에 이미 적힌 텍스트이므로, 생성형 답안처럼 오해하지 않도록 분리해서 다루는 것이 좋습니다.
- `5/5`처럼 분자와 분모가 같은 형태는 보이지만, 이를 해석하거나 판정하는 서술은 최소화하세요.

[불확실한 부분]
- 표 상단의 아이콘은 실제 사진인지, 단순화된 그림인지 해상도상 완전히 명확하지 않습니다.
- 분수 표기 `7/4`, `5/5`, `7/9`, `6/13`의 서체와 크기는 이미지가 작아 세부 글꼴까지는 단정하기 어렵습니다.
- 표 아래 오른쪽의 괄호 텍스트는 선택지 표시처럼 보이지만, 정확한 용도는 이미지 문맥에 따라 다소 해석 여지가 있습니다.
- 상단 첫 줄 맨 앞의 작은 사각형은 체크박스처럼 보이지만, 실제 편집 기호인지 문제 요소인지는 확정하기 어렵습니다.

--- RAW VISION DRAFT END ---
