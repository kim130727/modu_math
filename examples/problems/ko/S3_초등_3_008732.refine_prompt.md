# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008732.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008732

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
## [전체적인 구성]
- 전체적으로 **분수 3개를 보고, 각각이 가분수인지 진분수인지 분류하는 문제**이다.
- 화면 상단에는 문제 지시문이 있고, 그 아래에 **분수 3개가 세로로 박스 안에 제시**되어 있다.
- 각 분수 아래에는 **분류 선택 칸처럼 보이는 분홍색 박스**가 있으며, 글자로 `가분수`, `가분수`, `진분수`가 적혀 있다.
- 각 분홍 박스 아래에는 `(○, ×)` 형식의 표기가 있어, **맞는지 아닌지 표시하는 선택 영역**처럼 보인다.
- 중간 부분에는 `(답)`과 `(해설)`이 따로 있으며, 해설 줄에는 분수들이 화살표로 이어진 **설명 흐름**이 있다.
- 하단에는 같은 분수 3개와 함께, **파란 점선/실선 연결선으로 분류 관계를 시각화한 도식**이 다시 배치되어 있다.
- 상단 도식과 하단 도식은 같은 내용을 다른 방식으로 보여주는 구성으로 보인다.

## [보이는 텍스트]
- 문제 번호: `71.`
- 지시문: `사다리를 타고 내려가 도착한 곳이 참이면 ○표, 거짓이면 ×표 하세요.`
- 분수 표기:
  - `2/8`
  - `9/10`
  - `4/4`
- 분홍 박스 안 글자:
  - `가분수`
  - `가분수`
  - `진분수`
- 선택 기호 표기:
  - `(○, ×)` 가 각 분홍 박스 아래에 있음
- 정답 표기처럼 보이는 줄:
  - `(정답) ○, ×, ○`
- 해설 줄:
  - `(해설) 2/8 → 진분수, 9/10 → 진분수, 4/4 → 가분수`
- 하단 분류 박스:
  - `가분수`
  - `가분수`
  - `진분수`

## [반복되는 시각 패턴]
- **파란색 테두리의 분수 박스**가 3개 반복된다.
- 각 분수 박스 아래에는 **세로 연결선**이 내려오고, 가운데 구간에서 **가로 연결선**이 서로 이어진다.
- **분홍색 라벨 박스** 3개가 나란히 반복된다.
- 각 라벨 아래에는 `(○, ×)`가 반복되어 있어, **판정 항목마다 동일한 선택 형식**을 사용한다.
- 하단 도식에서도 동일한 3개 분수와 3개 라벨이 다시 반복되며,
  - 실선,
  - 점선,
  - 꺾인 연결선
  이 섞여 있어 경로를 강조하는 방식으로 보인다.

## [수식 단계] (해당될 때)
- 이 문항은 일반적인 계산식 전개보다는, **분수의 종류를 판정하는 분류형 문제**이다.
- 해설 줄에서 보이는 흐름은 다음과 같은 **판정 결과 나열 형식**이다.
  - `2/8 → 진분수`
  - `9/10 → 진분수`
  - `4/4 → 가분수`
- 등호가 이어진 수식 변형은 보이지 않는다.
- 따라서 이 섹션에서는 **수식 변형 단계가 아니라 분류 결과의 연결 표시**가 핵심이다.

## [등호별 변환 의미] (해당될 때)
- 화면에는 `=` 기호로 이어진 연산 변형은 보이지 않는다.
- 대신 `→` 화살표가 사용되어, **각 분수에서 판정명으로 이동하는 의미**를 나타낸다.
- 해설의 화살표는 계산 결과를 바꾸는 것이 아니라, **분수와 그 분류를 연결**하는 역할로 보인다.
- 따라서 각 화살표는 “이 분수는 어떤 종류로 분류되는가”를 보여주는 안내 표식으로 해석할 수 있다.

## [세부 내용]
- 상단의 3개 분수는 왼쪽부터:
  1. `2/8`
  2. `9/10`
  3. `4/4`
- 각 분수는 **하늘색 배경의 정사각형 박스** 안에 들어 있으며, 분수선이 중앙에 가로로 그어져 있다.
- 분수 박스 아래에는 각자 **세로선이 아래로 내려가고**, 가운데에서 서로 연결되는 **가로선**이 있어 사다리/경로처럼 보인다.
- 각 분수 아래 대응하는 분홍 박스의 글자는:
  - 왼쪽: `가분수`
  - 가운데: `가분수`
  - 오른쪽: `진분수`
- 그 아래의 `(○, ×)`는 **참/거짓 표시를 하도록 만든 선택칸**으로 보인다.
- 하단 도식은 위의 상단 도식을 변형한 형태로,
  - 파란 실선과 점선이 박스 사이를 연결하고,
  - 점선은 특정 경로를 강조하는 장식 또는 안내선처럼 보인다.
- 하단의 분홍 박스도 상단과 같은 순서로 배치되어 있으며, 각 박스의 텍스트는 동일하다.
- `(정답)`과 `(해설)`은 본문과 떨어져 왼쪽 아래에 배치되어 있어, 문제 영역과 해설 영역이 구분되어 있다.

## [Python DSL 작성 힌트]
- 분수 3개를 **가로로 균등 배치된 박스 3개**로 표현하면 좋다.
- 각 분수 박스는:
  - 상단에 `2`, `9`, `4`
  - 하단에 `8`, `10`, `4`
  - 가운데 분수선
  형태로 구성한다.
- 분수 아래에는 **분홍색 라벨 박스**를 하나씩 두고, 각 라벨 아래에 `(○, ×)`를 추가한다.
- 상단 연결선은:
  - 왼쪽 분수 → 가운데 분수 쪽으로 이어지는 수평선,
  - 가운데 분수 → 오른쪽 분수 쪽으로 이어지는 수평선
  같은 식의 사다리형 레이아웃을 반영한다.
- 하단 도식은 같은 요소를 다시 배치하되, **실선/점선 스타일 차이**를 넣어 강조하면 된다.
- 텍스트 요소는 정답 여부를 직접 채우지 말고, **문제에 보이는 그대로만 배치**한다.
- 해설 줄의 `→` 기호는 텍스트 객체와 화살표 객체를 함께 사용해 표현할 수 있다.
- 상단의 `(정답)` 표기는 별도 작은 텍스트로, 해설 영역과 분리해 두는 것이 좋다.

## [주의할 점]
- 이 문제는 **정답을 추론해서 채우는 작업이 아니다**.
- `가분수`, `진분수`의 판정이 맞는지 여부를 여기서 판단해 넣지 말아야 한다.
- `(○, ×)` 중 무엇을 선택해야 하는지 직접 결정하지 말고, **보이는 배치만 기록**해야 한다.
- 상단의 사다리형 연결선은 단순 장식이 아니라, **분류 관계를 보여주는 도식**일 수 있으므로 선의 연결 위치를 정확히 기록해야 한다.
- 하단에 있는 점선은 상단과 다르게 보이므로, **선 종류 차이**를 구분해 적는 것이 좋다.
- `정답`, `해설` 영역은 문제 본문과 분리해서 처리해야 한다.

## [불확실한 부분]
- 상단의 연결선이 정확히 어떤 의미로 설계되었는지는 화면만으로 완전히 단정하기 어렵다. 다만 **분수와 분류 라벨을 연결하는 사다리형 구조**로 보인다.
- 하단의 파란 점선 일부는 실제 연결선인지, 강조용 보조선인지가 약간 애매하다.
- `(○, ×)`의 정확한 선택 의도는 화면에 명시되어 있으나, 여기서는 **선택 결과를 확정하지 않고** 텍스트 구조만 기록했다.

--- RAW VISION DRAFT END ---
