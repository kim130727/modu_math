# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008730.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008730

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 한 페이지짜리 초등 수학 문제 화면입니다.
- 상단에는 문제 번호와 지시문이 있고, 그 아래에는 **그림 중심의 거리 비교 도식**이 배치되어 있습니다.
- 가운데에는 **학교–상구네 집–놀이터**의 위치 관계를 나타내는 수평선 그림이 있으며, 각 구간 길이가 분수/대분수로 표시되어 있습니다.
- 하단에는 **정답 표시**와 **풀이 설명 문장**이 따로 인쇄되어 있습니다.
- 전체적으로 “두 장소 중 더 가까운 곳을 고르기” 유형의 시각 자료입니다.

# [보이는 텍스트]
- 상단 문제 문구:
  - `□ 49. 학교와 놀이터 중 상구네 집에서 더 가까운 곳을 선택해 보세요.`
- 그림 위의 라벨:
  - `학교`
  - `상구네 집`
  - `놀이터`
- 거리 표기:
  - `13/11 km`
  - `1 3/11 km`
- 오른쪽 중간의 선택 표기:
  - `( 학교 , 놀이터 )`
- 하단 정답 표기:
  - `(정답)학교`
- 하단 해설:
  - `(해설) 1 3/11 = 14/11 이므로 13/11 < 1 3/11 입니다.`
  - `상구네 집에서 더 가까운 곳은 학교입니다.`

# [반복되는 시각 패턴]
- **점 3개와 선 2구간**이 일렬로 연결된 거리 도식이 보입니다.
  - 왼쪽 점: 학교
  - 가운데 점: 상구네 집
  - 오른쪽 점: 놀이터
- 점들 사이를 잇는 **주황색 수평 직선**이 하나 있고, 그 아래에 **파란 점선 곡선 화살표처럼 보이는 표시**가 각각 한 구간씩 연결되어 있습니다.
- 두 거리 표시는 모두 `km` 단위로 적혀 있으며, **왼쪽 구간은 진분수 형태**, **오른쪽 구간은 대분수 형태**입니다.
- 선택지 표기 `( 학교 , 놀이터 )`처럼 **괄호 안에 두 후보를 나란히 제시**하는 형식입니다.

# [수식 단계]
- 화면에 보이는 수식 변형은 다음과 같습니다.
  - `1 3/11 = 14/11`
- 이어서 비교식이 나타납니다.
  - `13/11 < 1 3/11`
- 즉, **대분수를 가분수로 바꾸는 과정이 한 번 보이고**, 그 결과를 이용해 두 거리의 크기를 비교하는 흐름입니다.
- 다만 이 자료는 풀이를 완전히 전개한 식이라기보다, **비교 결론을 설명하기 위한 짧은 해설식** 형태입니다.

# [등호별 변환 의미]
- `1 3/11 = 14/11`
  - 보이는 의미: 대분수 `1 3/11`을 가분수 `14/11`로 바꾸어 같은 값임을 나타냅니다.
  - 변환 방식 자체는 화면에 직접 계산 과정이 모두 나오지는 않고, 결과만 제시되어 있습니다.
- 이 등호 다음의 비교식 `13/11 < 1 3/11`
  - 보이는 의미: 앞에서 같은 값으로 바꾼 결과를 이용해, 두 거리 중 어느 쪽이 더 작은지 비교합니다.
  - 단, 이 비교의 최종 판단 자체는 이미 해설 문장으로 제시되어 있으므로, 여기서는 **관계식이 보인다**는 사실만 기록하면 됩니다.

# [세부 내용]
- 문제 상황은 상구네 집을 기준으로 **학교와 놀이터 중 더 가까운 곳**을 찾는 것입니다.
- 그림에서 상구네 집은 가운데에 있고, 학교는 왼쪽, 놀이터는 오른쪽에 배치되어 있습니다.
- 거리 정보는 다음처럼 각각 다른 구간에 붙어 있습니다.
  - 학교 ↔ 상구네 집 구간: `13/11 km`
  - 상구네 집 ↔ 놀이터 구간: `1 3/11 km`
- 구간을 설명하는 파란 점선은 각 거리 표기와 대응되어 있습니다.
- 하단에는 정답이 이미 표시되어 있으며, 풀이 문장까지 함께 인쇄되어 있습니다.
- 화면 오른쪽의 `( 학교 , 놀이터 )`는 선택지처럼 보이며, 정답은 아래 해설에서 학교로 제시됩니다.

# [Python DSL 작성 힌트]
- 상단 문제 문구는 좌측 상단에 배치합니다.
- 중앙에는 **3개의 점을 수평으로 정렬**하고, 각 점 위에 `학교`, `상구네 집`, `놀이터` 라벨을 올립니다.
- 점들 사이에는 **직선 선분 2개**를 이어서 하나의 긴 수평선처럼 보이게 구성합니다.
- 왼쪽 구간 아래에는 `13/11 km`, 오른쪽 구간 아래에는 `1 3/11 km`를 각각 배치합니다.
- 각 거리 표기 아래/주변에는 **파란색 점선 곡선 연결선** 또는 곡선 화살표 스타일을 넣어 구간을 강조합니다.
- 우측 중간에는 `( 학교 , 놀이터 )` 형태의 선택지 텍스트를 둡니다.
- 하단에는 `(정답)학교`, `(해설)` 문장, 그리고 등호가 포함된 수식 `1 3/11 = 14/11` 및 비교식 `13/11 < 1 3/11`을 순서대로 배치합니다.
- 텍스트 블록은 문제 본문과 해설 영역이 시각적으로 구분되도록 아래쪽에 따로 두는 것이 좋습니다.

# [주의할 점]
- 이 문제는 **정답을 새로 계산하는 작업이 아니라, 이미 있는 시각 정보를 분석하는 작업**입니다.
- 빈칸을 추론해서 채우지 마세요.
- `1 3/11`은 화면에 보이는 그대로 **대분수 형태**로 적어야 하며, 임의로 다른 표기로 바꾸지 마세요.
- 그림 속 거리 관계를 설명할 때는 **보이는 선분 길이와 라벨 위치**를 기준으로 서술하고, 문제 해결 추론은 최소화하세요.
- 해설 문장에 있는 정답 표기는 이미 제시된 것이므로, 이를 새로 계산한 결과처럼 쓰지 마세요.

# [불확실한 부분]
- 파란 점선 곡선의 정확한 끝점 처리 방식은 이미지 해상도상 약간 모호합니다.
- 선택지 `( 학교 , 놀이터 )`의 정확한 폰트 크기와 좌표는 이미지에서 세밀하게 판독하기 어렵습니다.
- 상단의 문제 번호 왼쪽에 있는 작은 네모 기호 `□`의 의도된 의미는 도식상 장식인지 체크박스 형태인지 단정하기 어렵습니다.
- 하단 해설의 수식 배치는 한 줄로 이어져 보이지만, 실제 편집 기준에서는 줄바꿈이 있을 수 있습니다.

--- RAW VISION DRAFT END ---
