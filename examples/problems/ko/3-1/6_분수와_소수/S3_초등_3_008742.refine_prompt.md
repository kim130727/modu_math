# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008742.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008742

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 세로로 긴 한 장의 수학 문제 지면입니다.
- 상단에는 문제 번호와 본문 안내 문장이 있고, 그 아래에 **분수 비교용 나무형 도식**이 2개 배치되어 있습니다.
- 첫 번째 도식은 위에 물음표(또는 빈칸 표시)가 들어간 상위 박스가 있고, 그 아래 좌우로 두 개의 빈 박스가 연결되며, 다시 아래쪽에 각각 두 개씩의 분수/대분수 카드가 연결됩니다.
- 도식 아래에는 **정답 표기**와 **풀이 설명 문장**이 이어집니다.
- 하단에는 두 번째 도식이 다시 한 번 제시되며, 이번에는 일부 박스에 빨간색/파란색으로 분수가 들어간 상태가 보입니다.
- 맨 아래에는 비교 결과를 바탕으로 한 결론 문장이 있습니다.

[보이는 텍스트]
- 문제 번호: `28.`
- 본문:
  - `두 분수의 크기를 비교하여 빨간색 선을 만나면 큰 분수를, 파란색 선을 만나면 작은 분수를 빈칸에 써넣으려고 합니다. ㉠에 들어갈 분수로 알맞은 것을 가장 아래칸의 분수들 중에 선택해 보세요.`
- 첫 번째 도식 내부:
  - 맨 위 박스: `㉠`처럼 보이는 원형 기호/표시
  - 아래쪽 가장 왼쪽 카드: `1 3/15`
  - 아래쪽 왼쪽에서 두 번째 카드: `1 1/15`
  - 아래쪽 오른쪽에서 두 번째 카드: `23/15`
  - 아래쪽 가장 오른쪽 카드: `26/15`
- 첫 번째 도식 아래쪽 왼편에:
  - `(정답) 23/15`
- 해설 문장:
  - `(해설) 1 3/15 > 1 1/15 이므로 빨간색 선을 만나서 큰 분수인 1 3/15이 빈칸으로 올라갑니다.`
  - `23/15 < 26/15 이므로 파란색 선을 만나서 작은 분수인 23/15이 빈칸으로 올라갑니다.`
- 두 번째 도식 내부:
  - 맨 위 박스: `㉠`처럼 보이는 원형 기호/표시
  - 왼쪽 중간 박스에 빨간 글씨로 `1 3/15`
  - 오른쪽 중간 박스에 파란 글씨로 `23/15`
  - 아래쪽 카드들:
    - `1 3/15`
    - `1 1/15`
    - `23/15`
    - `26/15`
- 하단 설명 문장:
  - `1 3/15과 23/15의 크기를 비교하면 23/15 = 1 8/15이므로 1 3/15 < 23/15입니다.`
  - `따라서 빨간색 선을 만나서 큰 분수가 빈칸으로 올라가므로 ㉠에 알맞은 수는 23/15입니다.`
- 문장 중간에 `1 8/15`가 보이지만, 이 값이 정확히 어떤 계산 결과인지 도식만으로는 직접 확인되지 않습니다. 텍스트상으로는 비교를 위한 변환 결과처럼 제시되어 있습니다.

[반복되는 시각 패턴]
- **상하 계층 구조**가 반복됩니다.
  - 맨 위 1개 박스
  - 그 아래 2개 박스
  - 그 아래 4개의 분수 카드
- 상위 박스와 하위 박스는 **직선 연결선**으로 이어져 있습니다.
- 연결선 색이 의미를 구분합니다.
  - **빨간색 선**: 큰 분수가 올라가는 경로로 설명됨
  - **파란색 선**: 작은 분수가 올라가는 경로로 설명됨
- 아래쪽 카드들은 모두 연한 민트색 배경의 사각형 카드 형태이며, 각 카드 안에 분수가 중앙 정렬되어 있습니다.
- 첫 번째 도식은 위쪽과 중간 박스가 비어 있고, 아래 카드만 주어진 상태입니다.
- 두 번째 도식은 중간 박스에 결과 분수가 채워진 상태를 보여주어, 첫 번째 도식의 진행 결과를 시각적으로 다시 제시합니다.

[수식 단계] (해당될 때)
- 화면에는 등호가 직접 이어지는 다단계 변형식은 많지 않지만, 비교를 위해 다음과 같은 표현이 텍스트로 보입니다.
  - `23/15 = 1 8/15`
- 또한 부등호를 사용한 비교식이 제시됩니다.
  - `1 3/15 > 1 1/15`
  - `23/15 < 26/15`
  - `1 3/15 < 23/15`
- 이 부분은 계산 과정을 보여준다기보다, **비교 기준을 설명하기 위한 식**으로 보입니다.

[등호별 변환 의미] (해당될 때)
- `23/15 = 1 8/15`
  - 보이는 의미: `23/15`를 대분수 형태로 바꿔 적은 것으로 보입니다.
  - 단, 실제 계산 절차는 화면에 상세히 전개되어 있지 않습니다.
- 부등호 비교식들:
  - `1 3/15 > 1 1/15`
    - 보이는 의미: 두 대분수의 크기 비교 결과를 제시한 것입니다.
  - `23/15 < 26/15`
    - 보이는 의미: 같은 분모의 두 진분수/가분수의 크기 비교 결과를 제시한 것입니다.
  - `1 3/15 < 23/15`
    - 보이는 의미: 위의 비교 결과를 종합하여 최종적으로 어느 쪽이 큰지 설명하는 문장 안에 들어 있습니다.

[세부 내용]
- 첫 번째 도식:
  - 최상단 박스는 정답을 넣을 대상인 `㉠`을 나타내는 빈 상자처럼 보입니다.
  - 그 아래 양쪽으로 두 갈래가 나뉘고, 다시 각 갈래 아래에 두 개씩의 분수 카드가 있습니다.
  - 왼쪽 하위 카드 쌍은 `1 3/15`와 `1 1/15`이며, 둘 사이를 묶는 연결선은 **빨간색**으로 표현됩니다.
  - 오른쪽 하위 카드 쌍은 `23/15`와 `26/15`이며, 둘 사이를 묶는 연결선은 **파란색**으로 표현됩니다.
- 문제의 의도:
  - 아래 두 개씩의 분수를 비교해서, 각 쌍에서 **큰 분수** 또는 **작은 분수**가 중간 박스로 올라가고,
  - 다시 그 결과를 이용해 최종적으로 `㉠`에 들어갈 분수를 고르는 구조로 보입니다.
- 화면에 이미 제시된 해설은:
  - 왼쪽 쌍에서는 큰 분수 쪽이 선택되어 중간으로 올라간다고 설명하고,
  - 오른쪽 쌍에서는 작은 분수 쪽이 선택되어 중간으로 올라간다고 설명합니다.
- 두 번째 도식:
  - 첫 도식의 중간 결과가 실제로 색깔 글씨로 채워진 예시처럼 보입니다.
  - 왼쪽 중간 박스에는 빨간색 `1 3/15`,
  - 오른쪽 중간 박스에는 파란색 `23/15`가 들어 있습니다.
- 하단 결론:
  - 두 중간 결과를 비교하여 최종적으로 `㉠`에 들어갈 수를 고르는 방식입니다.
  - 본문과 해설에 의해 어떤 수가 정답인지 텍스트로 제시되어 있지만, 여기서는 그 값을 새로 추론해 적지 않겠습니다.

[Python DSL 작성 힌트]
- 도식 요소를 계층적으로 배치하면 좋습니다.
  - 상단 1개 박스
  - 중간 2개 박스
  - 하단 4개 카드
- 각 박스와 카드의 위치 관계를 명확히 표현하세요.
  - 가운데 정렬
  - 좌우 대칭
  - 위에서 아래로 연결선
- 선 색상을 구분해야 합니다.
  - 빨간색 연결선
  - 파란색 연결선
- 텍스트 박스와 분수 카드는 다른 스타일로 처리하는 것이 좋습니다.
  - 상단/중간 박스는 빈칸 박스 형태
  - 하단은 숫자가 들어간 카드 형태
- 해설 문장은 도식과 분리해 아래쪽에 별도 텍스트 블록으로 두는 구성이 적절합니다.
- 두 번째 도식은 첫 번째 도식의 확장/결과 예시처럼 별도 그룹으로 처리하세요.
- 분수 표기는 정수부와 분수부를 분리한 대분수 형식과 일반 분수 형식을 모두 지원하는 것이 좋습니다.

[주의할 점]
- 정답을 빈칸에 미리 채워 넣지 마세요.
- `㉠`이 원형 기호인지, 동그라미 표시인지, 또는 특별한 빈칸 표시인지 정확히 단정하지 마세요.
- `1 8/15`는 화면에 보이지만, 그 유도 과정을 임의로 보강해 쓰지 마세요.
- 빨간색/파란색의 의미는 본문 설명에 근거해 적어야 하며, 다른 의미로 해석하지 마세요.
- 아래 카드의 분수 순서와 좌우 배치를 바꾸지 마세요.
- 두 번째 도식의 색 글씨는 단순 장식이 아니라, 첫 번째 도식의 결과를 시각적으로 보여주는 요소로 보이므로 별도 강조가 필요합니다.

[불확실한 부분]
- 맨 위의 `㉠` 표시는 화면상 작은 원 안의 글자처럼 보이는데, 실제로는 어떤 특수 기호인지 선명하게 판독하기 어렵습니다.
- 하단 설명에 있는 `1 8/15`는 비교를 위한 변환 결과로 보이지만, 정확한 계산 맥락은 이미지 만으로 완전히 확인되지는 않습니다.
- 연결선의 정확한 접점 위치나 박스의 미세한 크기는 이미지 해상도상 일부 불명확합니다.
- `(정답) 23/15`는 화면에 보이지만, 이 초안에서는 정답 확정을 서술하지 않고 배치 정보만 참고했습니다.

--- RAW VISION DRAFT END ---
