# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008743.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008743

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단 왼쪽에 문제 번호와 문장이 있고, 그 오른쪽 아래에 **사각형 테두리 안의 도형 묶음**이 크게 배치되어 있습니다.
- 도형 묶음 아래쪽에는 **선택지 5개**가 번호 동그라미와 함께 가로로/세로로 분산 배치되어 있습니다.
- 왼쪽 아래에는 **(정답 ④)** 표시가 있고, 더 아래에는 **해설 문장**이 한 줄로 적혀 있습니다.
- 전체적으로 “그림을 보고 분수 고르기” 유형의 객관식 문제 형식입니다.

[보이는 텍스트]
- `36. 그림을 보고 색칠한 부분을 분수로 바르게 나타낸 것을 고르세요.`
- 선택지:
  - `① 1/24`
  - `② 1/12`
  - `③ 1/6`
  - `④ 1/4`
  - `⑤ 1/3`
- ` (정답 ④ )`
- ` (해설) 24를 똑같이 6씩 4묶음으로 나누면 1묶음은 전체의 1/4입니다.`

[반복되는 시각 패턴]
- 중앙 그림은 **마름모(다이아몬드) 모양**이 여러 개 반복된 배열입니다.
- 같은 크기의 마름모가 **여러 줄로 규칙적으로 정렬**되어 있으며, 왼쪽부터 일부만 주황색으로 색칠되어 있습니다.
- 색칠된 마름모는 모두 같은 색(주황색)이고, 나머지는 내부가 비어 있는 윤곽선만 보입니다.
- 반복 배열은 **가로 방향으로 동일한 칸이 연속**되는 형태이며, 행도 여러 줄로 반복됩니다.

[수식 단계]
- 이 이미지에는 문제 풀이용 수식 전개식이나 등호가 이어지는 단계는 보이지 않습니다.
- 다만 해설 문장 안에 `24`, `6`, `4`, `1/4`가 포함되어 있어, 분할 관계를 말로 설명하고 있습니다.

[등호별 변환 의미]
- 해당 없음.
- 등호로 연결된 변환식이 시각적으로 제시되어 있지 않습니다.

[세부 내용]
- 중앙 도형은 연한 파란색의 큰 직사각형 테두리 안에 들어 있습니다.
- 그 안에는 마름모가 **3줄**로 배열되어 보입니다.
- 각 줄마다 왼쪽에서 **처음 2개 마름모가 주황색으로 칠해져 있고**, 나머지는 흰색 내부와 검은 윤곽선으로만 표시됩니다.
- 마름모들은 서로 일정한 간격으로 배치되어 있으며, 각 줄은 거의 같은 형태로 반복됩니다.
- 선택지 영역은 그림 아래쪽에 있고, 각 숫자 앞에는 **파란색 원 안의 번호**가 붙어 있습니다.
- 분수는 세로로 위아래에 수가 놓인 일반적인 분수 표기입니다.
- 왼쪽 아래의 정답 표시는 괄호 안에 번호가 들어가는 형태입니다.
- 해설은 문제 아래쪽 가장자리 근처에 작게 표시되어 있습니다.

[Python DSL 작성 힌트]
- 중앙 그림은 `box` 또는 `rect` 형태의 외곽 사각형 1개 안에, 동일한 크기의 마름모들을 **행렬처럼 반복 배치**하는 구조로 작성하면 좋습니다.
- 색칠된 도형과 비색칠 도형을 구분해서 표현해야 합니다.
- 마름모는 회전된 정사각형처럼 보이므로, DSL에서 `diamond` 또는 `polygon` 계열 도형으로 표현할 수 있습니다.
- 각 행의 도형 개수와 색칠 여부를 개별 객체로 나누어 기록하는 방식이 적합합니다.
- 선택지는 `label + fraction` 조합으로 배치하고, 번호 원형 마커도 함께 표현하면 좋습니다.
- 정답 표시는 문제 데이터용 메타 정보로 처리하고, 그림 자체와는 분리하는 것이 좋습니다.

[주의할 점]
- 색칠된 마름모의 개수를 **답으로 추론해 적지 마세요**.
- `1/4`가 정답처럼 보이더라도, 시각 분석 초안에서는 **정답을 확정하는 서술보다 배치와 텍스트 사실** 위주로 적는 것이 안전합니다.
- 그림 속 총 개수나 색칠 개수는, DSL 작성에 필요하더라도 **보이는 형태 그대로만** 기록하세요.
- 해설 문장은 실제 인쇄 텍스트이므로, 문제 본문과 분리해서 기록하는 것이 좋습니다.

[불확실한 부분]
- 중앙 그림의 마름모 **정확한 총 개수**는 이미지 해상도상 세부 확인이 필요합니다.
- 각 행마다 마름모가 정확히 몇 개인지, 혹은 오른쪽 끝 일부가 잘려 보이는지 여부는 확대 확인이 필요합니다.
- 색칠된 영역이 정확히 어떤 수학적 의미를 갖는지는 이 초안에서 단정하지 않습니다.

--- RAW VISION DRAFT END ---
