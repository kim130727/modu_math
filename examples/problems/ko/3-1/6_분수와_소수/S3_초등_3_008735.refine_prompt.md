# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008735.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008735

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 왼쪽 위에 문제 번호와 문장이 있고, 가운데에는 2행 4열 형태의 표가 크게 배치되어 있습니다.
- 표의 윗줄에는 재료 그림과 이름이 들어 있고, 아랫줄에는 각 재료의 양이 분수로 제시되어 있습니다.
- 표 아래 오른쪽에는 선택지처럼 보이는 괄호 묶음이 있으며, 아래쪽 왼편에는 `(정답)`, `(해설)` 표기가 따로 있습니다.
- 전체적으로 “그림-이름-분수”를 대응시키는 시각 자료와, 그중 조건에 맞는 항목을 고르는 문제 형식입니다.

[보이는 텍스트]
- 문제 번호: `90.`
- 본문 문장:
  - `블루베리 주스 2잔을 만드는 데 필요한 재료입니다. 필요한 양이 가분수인 재료를 모두 선택해 보세요.`
- 표 안의 재료 이름:
  - `블루베리`
  - `물`
  - `우유`
  - `꿀`
- 표 안의 분수 표기:
  - `7/4 컵`
  - `6/6 컵`
  - `3/8 컵`
  - `5/14 컵`
- 표 아래의 괄호 묶음:
  - `( 블루베리 , 물 , 우유 , 꿀 )`
- 아래쪽 텍스트:
  - `(정답) 블루베리, 물`
  - `(해설) 7/4, 6/6 → 가분수`

[반복되는 시각 패턴]
- 표는 4개의 열로 나뉘고, 각 열마다 위에는 그림, 아래에는 이름과 분수값이 반복되는 구조입니다.
- 각 분수 표기는 분자와 분모가 세로로 놓인 일반적인 분수 형식이며, 단위 `컵`이 함께 붙습니다.
- 네 개의 재료가 같은 크기의 칸에 균등하게 배치되어 있어 비교 대상이 여러 개임을 시각적으로 강조합니다.
- 아래 해설 부분에서는 분수 두 개 `7/4`, `6/6`이 나란히 제시되고, 오른쪽 화살표 다음에 `가분수`가 연결되어 있습니다.

[수식 단계]
- 표 안에는 계산 과정이 직접적으로 보이지 않고, 각 재료의 양이 분수로 제시되어 있습니다.
- 하단 해설에서 `7/4, 6/6 → 가분수`처럼 분수 예시와 분류 결과가 연결되어 있습니다.
- 등호로 이어진 변형식은 보이지 않습니다.

[등호별 변환 의미]
- 화면에 등호 `=`를 사용한 식 변형은 보이지 않습니다.
- 따라서 단계별 계산 변형 의미를 따로 설명할 수 있는 부분은 없습니다.

[세부 내용]
- 문제는 블루베리 주스 2잔을 만들 때 필요한 재료들 중에서, 필요한 양이 `가분수`인 재료를 고르는 형태입니다.
- 표의 네 재료는 다음 순서로 배치되어 있습니다.
  - 1열: 블루베리 그림, 아래에 `블루베리`, 그 아래 `7/4 컵`
  - 2열: 물 그림, 아래에 `물`, 그 아래 `6/6 컵`
  - 3열: 우유 그림, 아래에 `우유`, 그 아래 `3/8 컵`
  - 4열: 꿀 그림, 아래에 `꿀`, 그 아래 `5/14 컵`
- 표의 테두리와 내부 구분선은 분홍색 계열로 보이며, 각 칸의 경계가 분명하게 나뉘어 있습니다.
- 표 아래쪽의 괄호 안에는 네 재료명이 쉼표로 구분되어 있어, 선택지처럼 활용될 수 있는 형태입니다.
- 하단 `(정답)`과 `(해설)`은 문제 본문과 떨어져 별도로 배치되어 있어, 학습용 예시 또는 해설 표시로 보입니다.

[Python DSL 작성 힌트]
- 큰 상자 1개 안에 2행 4열 표를 먼저 배치하는 구성이 적합합니다.
- 각 열은 다음과 같은 하위 요소를 가지도록 분리하면 좋습니다.
  - 상단: 작은 그림 아이콘
  - 중단: 재료 이름 텍스트
  - 하단: 분수와 단위 텍스트
- 분수는 일반 텍스트가 아니라 분자/분모가 위아래로 쌓인 슬롯 형태로 표현하면 좋습니다.
- 표의 각 열은 동일 너비, 동일 높이로 맞추는 것이 중요합니다.
- 표 아래에는 별도의 텍스트 블록으로 괄호 묶음 `( 블루베리 , 물 , 우유 , 꿀 )`를 배치합니다.
- 화면 하단 왼쪽에는 작은 글씨로 `(정답)`과 `(해설)` 영역을 따로 두는 구성이 보입니다.
- 화살표가 있는 해설 부분은 텍스트와 화살표 아이콘을 한 줄에 배치하는 방식으로 작성하면 됩니다.

[주의할 점]
- 정답을 빈칸에 채워 넣는 방식으로 해석하지 마세요.
- `가분수`의 판정 결과를 시각 분석 초안에서 단정적으로 설명하기보다, 화면에 보이는 분류 문구만 기록하세요.
- `7/4`, `6/6`, `3/8`, `5/14`의 수학적 성질을 여기서 판단하여 서술하지 마세요.
- 표의 내용은 이미지와 텍스트가 함께 있는 구성이라, 그림만이 아니라 이름과 분수의 위치 관계도 함께 기록해야 합니다.
- 하단의 `(정답)` 표기는 원문에 보이는 추가 정보이므로, 문제 본문과 구분해서 적어야 합니다.

[불확실한 부분]
- 각 그림의 세부 모양은 작은 아이콘 수준으로만 보이므로, 실제 식품 포장 디자인까지는 정확히 단정하기 어렵습니다.
- 표의 분홍색 테두리와 선 굵기는 화면 해상도에 따라 다르게 인식될 수 있습니다.
- 하단 해설의 문구가 정확히 어떤 편집 의도로 삽입된 것인지, 문제 본문 일부인지 별도 예시인지 시각만으로는 확정하기 어렵습니다.

--- RAW VISION DRAFT END ---
