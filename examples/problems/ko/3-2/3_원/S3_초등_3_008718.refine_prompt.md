# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008718.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008718

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호 `21.`이 있고, 그 뒤에 문장형 지시문이 한 줄로 배치되어 있습니다.
- 중앙에는 연한 파란색 테두리의 큰 직사각형 박스가 있으며, 그 안에 4개의 선택 그림이 2행 2열로 배치되어 있습니다.
- 각 선택지는 작은 원 안에 한글 자모처럼 보이는 기호로 표시되어 있습니다.
- 박스 오른쪽 아래 바깥쪽에는 `( ㉠, ㉡, ㉢, ㉣ )` 형태의 선택지 나열이 보입니다.
- 화면 하단 왼쪽에는 `(정답)` 표시와 함께 동그라미 친 기호가 있고, 그 아래에 `(해설)` 문장이 있습니다.

[보이는 텍스트]
- `21. 원의 중심을 옮겨 가며 그린 모양이 아닌 것을 찾아 기호를 고르세요.`
- 선택 그림의 기호:
  - `ㄱ`처럼 보이는 표시
  - `ㄴ`처럼 보이는 표시
  - `ㄷ`처럼 보이는 표시
  - `ㄹ`처럼 보이는 표시
- 오른쪽 아래 괄호 안 표기:
  - `( ㄱ , ㄴ , ㄷ , ㄹ )`처럼 보이는 배열
- 하단:
  - `(정답) ㉡`처럼 보이는 표시
  - `(해설) 원의 중심은 모두 같고 반지름을 늘려 가며 그린 것입니다.`

[반복되는 시각 패턴]
- 네 개의 선택지가 모두 `원` 또는 `원의 겹침` 형태로 이루어져 있습니다.
- 첫 번째 선택지는 서로 일부 겹치는 원 3개가 모여 있는 형태입니다.
- 두 번째 선택지는 좌우로 겹치는 원 3개가 가로 방향으로 나란히 배치된 형태입니다.
- 세 번째 선택지는 동심원 3개가 중심을 공유하는 형태입니다.
- 네 번째 선택지는 큰 원 2개와 그 안쪽 아래에 작은 원 1개가 들어간 형태입니다.
- 각 선택지 왼쪽에는 동일한 크기의 작은 원형 기호가 하나씩 붙어 있어, 보기 번호처럼 기능하는 구조입니다.

[수식 단계]
- 해당 없음. 수식이나 등호 변형식은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 문제 문장은 “원의 중심을 옮겨 가며 그린 모양이 아닌 것”을 찾으라는 뜻의 선택형 문제입니다.
- 중앙의 파란 테두리 박스 안에는 4개의 그림이 2×2 배열로 정렬되어 있습니다.
- 좌상단 그림:
  - 원 3개가 서로 겹쳐 있고, 가운데에 삼각형처럼 보이는 겹침 영역이 생깁니다.
- 우상단 그림:
  - 원 3개가 가로로 배치되어 있으며 서로 일부씩 겹칩니다.
  - 전체적으로 좌우로 긴 형태입니다.
- 좌하단 그림:
  - 중심이 같은 원 3개가 동심원처럼 배치되어 있습니다.
  - 안쪽에서 바깥쪽으로 크기만 달라지는 구조입니다.
- 우하단 그림:
  - 위쪽에 큰 원 2개가 겹치며,
  - 아래쪽에는 더 작은 원 1개가 포함된 형태입니다.
- 각 그림 옆의 작은 원형 기호는 선택지 식별용 표식으로 보입니다.
- 오른쪽 아래의 `( ㄱ , ㄴ , ㄷ , ㄹ )` 표기는 보기 목록을 문자 순서대로 제시한 것으로 보입니다.
- 하단의 해설 문장은 “원의 중심은 모두 같고 반지름을 늘려 가며 그린 것입니다.”라고 적혀 있어, 어떤 그림이 조건에 맞는지에 대한 설명이 따로 붙어 있습니다.
- 다만 이 초안에서는 정답 판단이나 선택지 대응을 확정하지 않습니다.

[Python DSL 작성 힌트]
- 큰 파란 테두리 직사각형을 먼저 그린 뒤, 내부를 2행 2열 영역처럼 나누어 4개의 그림을 배치하면 좋습니다.
- 각 선택지별로:
  - 왼쪽에 작은 원형 기호 1개,
  - 오른쪽에 해당 도형 그림 1개
  구조를 유지하면 원본 레이아웃을 잘 반영할 수 있습니다.
- 원들은 모두 얇은 선으로 그려져 있으며, 채움 없이 윤곽선만 있습니다.
- 동심원은 중심을 같은 점에 두고 반지름만 다르게 설정해야 합니다.
- 겹치는 원들은 서로 부분적으로만 교차하도록 배치되어 있습니다.
- 전체적으로 선택지 사이 간격이 넓지 않으므로, 한 박스 안에서 상대 위치를 맞춰 그리는 방식이 적합합니다.
- 하단의 `(정답)`과 `(해설)`은 별도 텍스트 영역으로 처리하고, 해설 문장은 한 줄로 배치하는 것이 자연스럽습니다.

[주의할 점]
- 정답을 추론해 채우지 마세요.
- 보기 기호를 임의로 다른 문자로 바꾸지 마세요.
- 원의 중심, 겹침 정도, 동심 여부를 섣불리 단정하지 말고 보이는 대로만 기술하세요.
- 하단 해설 문장은 보이기는 하지만, 이를 근거로 답을 계산해 확정하는 서술은 피해야 합니다.
- 작은 원형 보기 표시와 실제 도형 그림을 혼동하지 않도록 주의해야 합니다.
- 파란 테두리 박스의 정확한 색상은 연한 파랑 계열로 보이나, 구현 시 세부 색상값은 달라질 수 있습니다.

[불확실한 부분]
- 왼쪽의 작은 원 안 문자들이 `ㄱ, ㄴ, ㄷ, ㄹ`인지, 또는 다른 기호체계인지 화면 해상도상 약간 흐릴 수 있습니다.
- 오른쪽 아래 괄호 안의 기호 순서가 실제로는 `ㄱ, ㄴ, ㄷ, ㄹ`인지 일부 글자가 비슷하게 보일 수 있습니다.
- 하단 `(정답)` 옆의 동그라미 친 기호가 `㉡`처럼 보이지만, 실제 인쇄체에 따라 판독이 달라질 수 있습니다.
- 해설 문장의 일부 글자 경계가 다소 흐려 보여 세부 띄어쓰기나 조사 정확성은 약간 다를 수 있습니다.

--- RAW VISION DRAFT END ---
