# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008737.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008737

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 문제 지면 안에 문장 설명, 수직선 2개, 비교 결과 문장, 그리고 보기처럼 보이는 짧은 수식/표기 영역이 함께 배치되어 있습니다.
- 좌상단에 문제 번호가 있고, 그 오른쪽으로 긴 서술형 문장이 이어집니다.
- 중앙에는 두 개의 수직선이 위아래로 세로 정렬되어 있으며, 각각 다른 길이를 나타내는 것으로 보입니다.
- 각 수직선 아래쪽에는 비교를 돕는 문장이 이어지고, 하단에는 괄호로 묶인 표시가 보입니다.
- 전체적으로 “두 수를 수직선에서 비교하는” 형식의 초등 수학 문제 레이아웃입니다.

[보이는 텍스트]
- 문제 번호: `94.`
- 본문 문장:
  - `재영이는 노란색 털실을 9/7 m 가지고 있고 보라색 털실을 1 3/7 m 가지고 있습니다.`
  - `더 긴 털실은 무슨 색인지 알아보려고 합니다. 물음에 답하세요.`
- 문항:
  - `(1) 수직선을 보고, 더 큰 수를 선택해 보세요.`
- 수직선 주변 표기:
  - 위 수직선 왼쪽: `9/7 m`
  - 아래 수직선 왼쪽: `1 3/7 m`
  - 두 수직선 모두 눈금 아래에 `0`, `1`, `2(m)` 표시가 있음
  - 각 수직선의 `1` 근처 위쪽에 `7/7` 표기가 있음
- 아래쪽 문장:
  - `(1) 수직선에 나타난 부분을 비교하면 9/7 < 1 3/7 입니다.`
- 맨 아래쪽에 일부만 보이는 표기:
  - `1 3/7`처럼 보이는 숫자 표기가 화면 하단에 걸쳐 있어 전체가 완전히 보이지 않음

[반복되는 시각 패턴]
- 같은 형태의 수직선이 2개 반복됩니다.
- 두 수직선 모두:
  - 왼쪽 시작점에 `0`
  - 가운데 근처에 `1`
  - 오른쪽 끝에 `2(m)`
  - 0과 1, 1과 2 사이에 여러 개의 등간격 눈금
- 두 수직선에서 파란색 선분이 왼쪽부터 어떤 위치까지 채워져 있으며, 검은색 눈금과 대비됩니다.
- `7/7` 표기가 두 수직선 모두 같은 세로 위치에 반복되어 있어, 분수와 관련된 기준 눈금을 강조하는 구성으로 보입니다.

[수식 단계]
- 화면에 별도의 연속 등식 변형은 보이지 않습니다.
- 다만 아래쪽 문장에 비교식이 한 번 제시됩니다:
  - `9/7 < 1 3/7`
- 수직선에서의 위치 비교를 바탕으로 부등호 방향을 제시하는 형태로 보입니다.

[등호별 변환 의미]
- 별도의 등식 사슬은 보이지 않습니다.
- 보이는 비교식 `9/7 < 1 3/7`은
  - 왼쪽 수와 오른쪽 수의 크기를 수직선 위치로 비교한 결과를 적은 것으로 해석됩니다.
- 이 부분은 “변환 과정”이라기보다 “비교 결론 표기”에 가깝습니다.

[세부 내용]
- 첫 번째 수직선:
  - 왼쪽에 `9/7 m`가 적혀 있습니다.
  - 파란색 선이 0에서 시작해 1을 지나 1과 2 사이의 어느 지점까지 이어집니다.
  - `1` 위쪽에 `7/7`이 표시되어 있어, 1을 분수 형태 기준으로 강조하는 것처럼 보입니다.
- 두 번째 수직선:
  - 왼쪽에 `1 3/7 m`가 적혀 있습니다.
  - 파란색 선의 시작점과 눈금 구조는 첫 번째와 동일합니다.
  - 역시 `1` 위에 `7/7`이 표기되어 있습니다.
- 두 수직선 아래에는 각 길이를 나타내는 수치가 왼쪽에 따로 적혀 있어, 두 값의 비교 대상임을 알 수 있습니다.
- 하단의 비교 문장은 수직선에서 보이는 위치를 비교하라는 안내 뒤에, 두 수가 비교된 결과를 문장으로 제시한 형태입니다.

[Python DSL 작성 힌트]
- 두 개의 수직선을 각각 별도 객체로 배치하는 구성이 필요해 보입니다.
- 각 수직선에는 다음 요소를 포함하면 좋습니다:
  - 가로 축 선분
  - 0, 1, 2 눈금 라벨
  - 0~2 사이의 여러 등간격 눈금
  - `1` 위치 위의 `7/7` 분수 라벨
  - 왼쪽에 해당 길이 표기 텍스트
  - 0에서 특정 지점까지의 파란 강조 선분
- 상단 문장과 문항 문장은 텍스트 박스로 처리하면 좋습니다.
- 하단의 비교 문장은 별도 텍스트 박스로 두고, 수식 부분은 인라인 수식처럼 배치할 수 있습니다.
- 수직선 두 개는 세로로 정렬하고, 좌측 라벨 위치와 파란 선분 길이를 동일한 기준으로 맞추는 것이 중요합니다.
- 괄호로 묶인 `(1)` 표시는 문항 번호처럼 따로 배치하는 것이 좋습니다.

[주의할 점]
- 답을 추론해 색 이름이나 최종 정답을 채우지 마세요.
- `9/7`과 `1 3/7`의 크기 비교를 설명하더라도 실제 계산은 하지 말고, 화면에 보이는 비교 문장만 기록하세요.
- `7/7` 표기가 수직선 위에 정확히 어떤 눈금과 대응하는지는 이미지상 약간 작아서, 설명할 때는 “1 근처 위쪽에 표시됨” 정도로 적는 것이 안전합니다.
- 맨 아래쪽에 일부 잘린 텍스트가 있어 전체 문장이 정확히 안 보입니다.
- 파란 선의 정확한 끝점이 어느 눈금인지도 이미지 해상도상 약간 अस्पင်း할 수 있으므로, 단정하지 않는 것이 좋습니다.

[불확실한 부분]
- 하단에 잘린 `1 3/7` 표기의 앞뒤 문맥은 화면 밖이라 완전한 문장인지 확실하지 않습니다.
- 두 수직선의 파란색 선이 정확히 어느 세부 눈금에서 끝나는지는 해상도상 완전히 분명하지 않습니다.
- 수직선 아래의 비교 문장이 원래 문제의 정답 제시인지, 예시 설명인지, 또는 화면 캡처 시 일부가 섞인 것인지 단정하기 어렵습니다.
- `7/7` 표기의 정확한 위치는 보이지만, 그 위아래 정렬이 픽셀 단위로 완전 일치하는지는 불확실합니다.

--- RAW VISION DRAFT END ---
