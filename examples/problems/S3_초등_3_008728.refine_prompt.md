# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008728.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008728

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단에는 문제 번호와 지시문이 한 줄로 배치되어 있다: “34. 그림을 보고 바르게 말한 사람을 선택해 보세요.”
- 그 아래 중앙 상단에 큰 테두리 박스가 있고, 그 안에 자동차 그림들이 두 줄로 배열되어 있다.
- 자동차 그림 영역 아래에는 말풍선 2개가 좌우로 나뉘어 배치되어 있으며, 각 말풍선 아래에 인물 그림과 이름표가 있다.
- 화면 하단에는 “(정답)진경”과 “(해설)” 문장이 별도로 적혀 있다.

[보이는 텍스트]
- “34. 그림을 보고 바르게 말한 사람을 선택해 보세요.”
- 말풍선 왼쪽:
  - “색칠한 부분은”
  - “7묶음 중에서 3묶음이므로”
  - “전체의 3/7이야.”
- 말풍선 오른쪽:
  - “색칠하지 않은 부분은”
  - “7묶음 중에서 4묶음이므로”
  - “전체의 4/7이야.”
- 인물 이름표:
  - “진경”
  - “현진”
- 하단 텍스트:
  - “(정답)진경”
  - “(해설)현진: 색칠하지 않은 부분은 7묶음 중에서 4묶음이므로 전체의 4/7입니다.”

[반복되는 시각 패턴]
- 자동차 그림이 여러 개 반복되어 있으며, 일부는 초록색으로 색칠되어 있고 일부는 흰색/연한 초록 선으로만 표현되어 있다.
- 자동차들은 작은 분홍색 테두리 박스들 안에 묶여 반복 배치되어 있다.
- 위쪽 줄과 아래쪽 줄의 자동차 묶음이 서로 구분되어 보인다.
- 말풍선 두 개가 좌우 대칭처럼 배치되어 있고, 각각 같은 형식으로 “부분 → 묶음 수 → 전체의 분수” 설명을 담고 있다.
- 인물 그림 아래에는 둥근 모서리의 이름표가 각각 하나씩 있다.

[수식 단계] (해당될 때)
- 이 문제 화면에는 등호로 이어지는 계산식 전개는 보이지 않는다.
- 분수 표현은 말풍선 문장 속에 직접 제시되어 있다:
  - 왼쪽 말풍선: “전체의 3/7”
  - 오른쪽 말풍선: “전체의 4/7”

[등호별 변환 의미] (해당될 때)
- 등호를 사용한 단계별 변환은 보이지 않는다.
- 따라서 각 단계의 변환 의미를 따로 해석할 수는 없다.
- 대신 문장형 설명에서 “7묶음 중 몇 묶음인지”가 분수의 분자/분모 의미와 연결되어 있다.

[세부 내용]
- 상단 자동차 그림 영역은 큰 연보라색 테두리 박스 안에 들어 있다.
- 그 안에는 작은 분홍색 사각형 테두리들이 여러 개 있으며, 각 사각형 안에 자동차 2대씩 배치되어 있는 형태로 보인다.
- 자동차는 초록색으로 칠해진 것과 칠하지 않은 것 두 종류로 구분된다.
- 첫 줄은 왼쪽부터 여러 묶음이 있고, 오른쪽 끝에는 흰색 자동차 2대가 있는 묶음이 보인다.
- 둘째 줄에도 흰색 자동차 묶음들이 이어져 있다.
- 말풍선 왼쪽은 진경 캐릭터 위에 위치하고, 말풍선 안 문장은 줄바꿈되어 있다.
- 말풍선 오른쪽은 현진 캐릭터 위에 위치하고, 동일한 방식으로 줄바꿈된 문장이 있다.
- 말풍선 끝의 꼬리는 각각 아래 인물 쪽으로 향한다.
- 하단의 “(정답)진경” 표기는 문제 선택 결과를 알려주는 영역처럼 보이나, 본 요청에서는 정답을 활용해 풀이하지 않아야 한다.
- 그 아래 해설 문장은 현진의 말이 왜 맞지 않는지 설명하는 형식으로 보인다.

[Python DSL 작성 힌트]
- 상단 지시문 텍스트를 한 줄로 배치하는 레이아웃이 필요하다.
- 큰 연보라색 외곽 박스와 그 내부의 작은 분홍색 묶음 박스들을 중첩 구조로 표현하면 좋다.
- 자동차 아이콘은 동일한 크기의 반복 도형/이미지 요소로 배치하는 방식이 적합하다.
- 색칠된 자동차와 색칠되지 않은 자동차를 색상 차이로 구분해야 한다.
- 말풍선 2개는 좌우 대칭 배치로 처리하고, 각 말풍선 내부 텍스트는 여러 줄로 나누어 넣는 것이 자연스럽다.
- 인물 그림과 이름표는 말풍선 아래에 세로로 배치한다.
- 하단의 정답/해설 텍스트는 별도의 텍스트 블록으로 분리해 두는 편이 좋다.
- 분수 표기는 일반 텍스트가 아니라 분수 슬롯 또는 상하 첨자 형식으로 분리해도 된다.

[주의할 점]
- 문제의 핵심은 “누가 바르게 말했는지”를 고르는 것이지만, 여기서는 정답을 직접 확정하는 서술을 피해야 한다.
- 자동차 개수나 묶음 수를 세어 최종 결론을 적지 마세요.
- 말풍선의 문장을 수식으로 바꿔 풀이하지 마세요.
- 하단의 “(정답)” 표기는 참고용으로 보이지만, 분석 초안에서는 그 사실만 언급하고 답을 재현하지 않는 것이 안전하다.
- 자동차 그림의 정확한 총개수는 이미지에서 일부 겹침/축소 때문에 오독 가능성이 있으므로 단정하지 않는 것이 좋다.

[불확실한 부분]
- 자동차 그림의 정확한 총 개수와 각 줄의 묶음 개수는 축소된 이미지 특성상 다소 불명확할 수 있다.
- 일부 자동차는 색칠 경계가 얇게 보여 초록색 채움인지 외곽선만 있는지 구분이 애매할 수 있다.
- 말풍선 텍스트의 줄바꿈 위치는 화면상 보이는 그대로이지만, 실제 편집 시 줄 수는 다르게 조정될 수 있다.
- 하단 해설 문장은 화면 하단에 일부 잘려 보이거나 작게 보여, 정확한 구두점/띄어쓰기는 약간 다를 수 있다.

--- RAW VISION DRAFT END ---
