# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008729.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008729

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문항 번호와 지시문이 있고, 그 아래 중앙에는 둥근 사각형 테두리 안에 구슬 그림이 가로로 배치되어 있습니다.
- 중앙 아래에는 분수와 문장이 포함된 완성형 문장이 크게 제시되어 있고, 괄호 안에 색 이름을 고르는 형태입니다.
- 화면 하단 왼쪽에는 `(정답)` 표시와 함께 정답 단어가 따로 보이며, 그 아래에는 `(해설)`로 시작하는 설명 문장이 이어집니다.
- 전체적으로 “그림을 보고 알맞은 말 선택” 유형이며, 그림-문장-정답-해설 순서로 구성되어 있습니다.

[보이는 텍스트]
- `37. 그림을 보고 알맞은 말을 선택하세요.`
- `12의 1/3은 ( 초록색 , 분홍색 ) 구슬입니다.`
- `(정답)초록색`
- `(해설)12의 1/3은 12를 3묶음으로 똑같이 나눈 것 중의 1묶음입니다.`
- 분수 표기는 `1/3`처럼 세로 분수 형태가 아니라, 화면상에서는 `1`과 `3`이 위아래로 배치된 분수 형태로 보입니다.
- 숫자 `12`와 분수 `1/3`가 문장 중간에 함께 배치되어 있습니다.

[반복되는 시각 패턴]
- 구슬이 일정한 간격으로 가로 한 줄에 반복 배치되어 있습니다.
- 구슬 색은 두 종류로 보이며, 앞부분에는 초록색 구슬이 연속으로 있고 뒤쪽에는 분홍색 구슬이 연속으로 있습니다.
- 각 구슬은 원형이며 내부에 하이라이트가 있어 구체처럼 보입니다.
- 그림 전체가 연한 분홍색의 둥근 사각형 테두리 안에 들어가 있습니다.
- 초록색 구슬과 분홍색 구슬이 색으로 묶여 있어, 색의 종류를 비교하거나 개수를 세는 시각 패턴으로 읽힙니다.

[수식 단계]
- 이 문제 화면에는 등호 `=`를 포함한 변형식 단계가 보이지 않습니다.
- 대신 분수 `1/3`가 문장 속에 삽입되어 있고, 해설 문장으로 의미를 풀어 설명하고 있습니다.

[등호별 변환 의미]
- 해당 없음.
- 화면에 등호가 있는 수식 변형 과정이 나타나지 않습니다.

[세부 내용]
- 상단 문항 번호 `37.` 왼쪽에는 작은 네모 표시처럼 보이는 체크 박스/문항 표식이 있습니다.
- 그림 영역:
  - 둥근 직사각형 테두리 안에 구슬이 가로로 늘어서 있습니다.
  - 왼쪽부터 초록색 구슬이 4개 정도 연속되어 보이고, 그 뒤로 분홍색 구슬이 여러 개 연속되어 보입니다.
  - 구슬들은 모두 동일한 크기와 간격으로 정렬되어 있습니다.
- 문장 영역:
  - “12의 1/3은”이 먼저 나오고, 그 다음에 괄호 `(` `)` 안에 두 색 이름 `초록색 , 분홍색`이 선택지처럼 들어 있습니다.
  - 괄호 안은 두 항목이 쉼표로 구분되어 있으며, 중간에 공백이 포함되어 있습니다.
- 정답/해설 영역:
  - `(정답)`은 왼쪽 아래에 작게 표시되어 있고, 바로 뒤에 `초록색`이 이어집니다.
  - `(해설)`은 그 아래 줄에서 시작하며, 문장 중 `12의 1/3` 부분은 본문과 같은 분수 표기를 사용합니다.
  - 해설은 “12를 3묶음으로 똑같이 나눈 것 중의 1묶음”이라는 의미를 텍스트로 설명합니다.
- 시각적으로는 그림이 문제의 핵심이며, 아래 문장과 해설이 이를 언어로 확인하는 구조입니다.

[Python DSL 작성 힌트]
- 상단 지시문은 텍스트 요소로 배치하고, 문항 번호 `37.`도 별도 텍스트로 처리하면 좋습니다.
- 중앙의 구슬 그림은:
  - 둥근 사각형 테두리 1개
  - 내부에 원형 구슬 여러 개를 가로 정렬
  - 왼쪽 구간은 초록색, 오른쪽 구간은 분홍색으로 색상 구분
  - 동일한 크기의 원 반복 배치
  로 구성하면 됩니다.
- 문장 `12의 1/3은 ( 초록색 , 분홍색 ) 구슬입니다.`는 한 줄 텍스트로 두고, 분수 `1/3`만 별도 스타일로 표현할 수 있으면 더 정확합니다.
- 정답과 해설은 하단 왼쪽에 작은 글씨로 두 줄 배치하는 형태가 자연스럽습니다.
- 괄호 선택지는 실제로 선택 UI가 아니라 인쇄된 텍스트이므로, 버튼 형태보다는 텍스트 괄호로 표현하는 편이 맞습니다.

[주의할 점]
- 초록색 구슬과 분홍색 구슬의 정확한 개수를 추정해서 적지 마세요. 그림에서 색이 바뀌는 지점만 시각적으로 설명하는 것이 안전합니다.
- 정답은 화면에 이미 제시되어 있지만, 작성용 초안에서는 문제를 푸는 방식으로 서술하지 말고 “정답 표기 있음” 정도로만 다루는 것이 좋습니다.
- 분수 `1/3`는 계산식이 아니라 문장 안의 표기이므로, 별도의 수식 단계로 오해하지 마세요.
- 그림의 테두리는 완전한 직사각형보다 모서리가 둥근 사각형에 가깝습니다.
- 하단 해설은 계산 과정이라기보다 개념 설명 문장입니다.

[불확실한 부분]
- 구슬의 총 개수는 화면상 일부는 명확하지만, 세부 개수를 정확히 셀 필요가 없고 화면 해상도상 오차 가능성이 있습니다.
- 초록색 구슬과 분홍색 구슬의 정확한 분포 비율은 보이지만, 이를 수치로 단정해 적지는 않는 것이 좋습니다.
- 문항 번호 왼쪽의 작은 네모 표식이 체크박스인지 단순 문항 표시인지 화면만으로는 확정하기 어렵습니다.

--- RAW VISION DRAFT END ---
