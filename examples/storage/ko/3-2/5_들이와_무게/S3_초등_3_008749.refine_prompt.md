# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008749.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008749

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단 왼쪽에 문제 번호 `46.`이 있고, 그 오른쪽으로 문제 문장이 한 줄 이상에 걸쳐 배치되어 있습니다.
- 본문 중앙에는 그림이 왼쪽에서 오른쪽으로 이어지는 형태로 배치되어 있습니다.
  - 왼쪽: `주전자` 그림에서 `그릇`으로 물이 붓는 모습
  - 가운데: 회색 화살표 `→`
  - 오른쪽: 물이 들어간 뒤의 `주전자`와 `그릇` 그림
- 그림 아래 또는 오른쪽 아래에 선택지처럼 `( 주전자 , 그릇 )` 표기가 보입니다.
- 하단에는 `(정답)` 표기와 함께 `주전자`가 적혀 있고, 그 아래에 `(해설)` 문장도 있습니다.

[보이는 텍스트]
- `□ 46.`
- `주전자에 물을 가득 채운 후 그릇에 옮겨 담았습니다. 그림과 같이 물을 채웠을 때에 주전자와 그릇 중 들이가 더 많은 것은 어느 것인가요?`
- 그림 속 라벨: `주전자`, `그릇`
- 선택지 형태 표기: `( 주전자 , 그릇 )`
- `(정답) 주전자`
- `(해설) 주전자의 물이 그릇에 모두 들어가지 않으므로 주전자의 들이가 더 많습니다.`

[반복되는 시각 패턴]
- 같은 종류의 용기 2개가 비교되는 구조입니다.
- 왼쪽 그림과 오른쪽 그림이 대응되며, 중간의 화살표가 상태 변화를 나타냅니다.
- 용기 내부의 물 높이가 색으로 표현되어 있고, 물의 양 비교를 시각적으로 유도합니다.
- `주전자`와 `그릇`이라는 두 대상이 반복적으로 등장하며, 괄호로 묶인 선택지 표기가 보입니다.

[수식 단계]
- 해당 없음. 수식이나 등호를 이용한 변형식은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호가 있는 계산식/변형식이 보이지 않습니다.

[세부 내용]
- 문제는 물의 **들이**를 비교하는 상황입니다.
- 첫 그림에서는 주전자의 물이 그릇으로 옮겨지는 장면이 그려져 있고, 물줄기가 그릇 안으로 들어가는 모습이 보입니다.
- 두 번째 그림에서는 주전자와 그릇이 나란히 놓인 상태로, 각각 내부에 물이 들어 있는 모습이 단순화되어 표현되어 있습니다.
- 오른쪽 아래의 `( 주전자 , 그릇 )` 표기는 답을 고르는 선택지 또는 비교 대상 표시처럼 보입니다.
- 하단 해설은 “주전자의 물이 그릇에 모두 들어가지 않음”을 근거로 들이 비교를 설명하고 있습니다.

[Python DSL 작성 힌트]
- 문제 번호와 문장을 상단 텍스트 블록으로 배치하면 좋습니다.
- 중앙 그림은 다음과 같이 요소를 나눠 표현할 수 있습니다.
  - 왼쪽 용기: 주전자 아이콘
  - 왼쪽 용기 아래 라벨: `주전자`
  - 가운데: 오른쪽 방향 화살표
  - 오른쪽 용기 2개: 주전자와 그릇의 비교 상태
- 각 용기 내부의 파란 물 채움은 도형/채움색으로 처리하면 됩니다.
- `( 주전자 , 그릇 )`은 괄호와 쉼표가 포함된 텍스트로 별도 배치하는 것이 적절합니다.
- 하단의 `(정답)`, `(해설)`은 일반 본문보다 아래쪽에 작은 텍스트 영역으로 두면 좋습니다.
- 그림의 좌우 정렬과 가운데 화살표 위치를 유지하면 원본 구조를 잘 반영할 수 있습니다.

[주의할 점]
- 정답은 추론해서 별도로 쓰지 말고, 원문에 보이는 텍스트 구조만 반영해야 합니다.
- 물의 양을 수치로 해석하거나 계산식으로 바꾸지 마세요.
- 오른쪽 그림의 물 높이 차이를 과도하게 일반화하지 말고, 보이는 범위만 설명하세요.
- 선택지처럼 보이는 `( 주전자 , 그릇 )` 표기를 실제 답안 형식으로 단정하지 말아야 합니다.
- 해설 문장은 문제 본문과 분리해서 참고 설명으로 다루는 것이 좋습니다.

[불확실한 부분]
- `( 주전자 , 그릇 )` 표기가 실제 선택지인지, 정답 표시용인지 화면상으로는 완전히 분리되어 보이지 않습니다.
- 오른쪽 그림의 두 용기 내부 물 높이는 그림 스타일상 단순화되어 있어 정확한 양 관계를 수치적으로 단정할 수는 없습니다.
- 상단 문제 문장이 줄바꿈된 정확한 위치는 이미지 해상도상 약간의 차이가 있을 수 있습니다.

--- RAW VISION DRAFT END ---
