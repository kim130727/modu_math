[전체적인 구성]
- 한 장의 초등 수학 문항 이미지로, 상단에는 문제 번호와 본문 설명이 있고, 중간에는 두 개의 물병 그림이 좌우로 배치되어 있습니다.
- 왼쪽에는 `가`라고 표시된 물병, 오른쪽에는 `나`라고 표시된 물병이 각각 하나씩 있습니다.
- 각 물병의 오른쪽에는 회색 화살표가 있고, 화살표 끝에는 해당 물병에 담긴 컵의 개수를 나타내는 작은 컵 그림들이 가로로 나열되어 있습니다.
- 하단에는 선택지를 뜻하는 표시로 보이는 `○`, `×`가 세로로 놓여 있고, 그 아래에 `(정답)` 옆의 빈 원이 있습니다.
- 맨 아래에는 `(해설)`로 시작하는 설명 문장이 있으며, 마지막 줄에는 회색 화살표 모양이 함께 보입니다.

[보이는 텍스트]
- `43.`
- `두 물병에 물을 가득 채운 후 모양과 크기가 같은 컵에 각각 옮겨 담았습니다.`
- `바르게 설명했으면 ○표, 그렇지 않으면 ×표를 선택하세요.`
- 물병 안의 표기: `가`, `나`
- 가운데 문장: `나 물병이 컵 1개만큼 물이 더 들어갑니다.`
- 선택 표시처럼 보이는 기호: `○`, `×`
- `(정답)`  
- `(해설) 가 물병: 컵 5개, 나 물병: 컵 6개`
- `→ 나 물병이 가 물병보다 컵 6 - 5 = 1(개)만큼 물이 더 들어갑니다.`

[반복되는 시각 패턴]
- 왼쪽 물병과 오른쪽 물병이 거의 같은 형식으로 반복됩니다.
- 두 물병 모두 내부에 한글 한 글자 라벨이 들어가며, 각각 `가`, `나`로 다릅니다.
- 각 물병 오른쪽에는 동일한 스타일의 작은 컵 그림들이 일정 간격으로 가로 배열되어 있습니다.
- 왼쪽 컵 묶음은 5개, 오른쪽 컵 묶음은 6개로 보이며, 컵의 모양과 색은 동일하게 반복됩니다.
- 하단 해설에서는 `컵 5개`, `컵 6개`, `6 - 5 = 1(개)`처럼 컵 개수와 차이를 다시 반복해 설명합니다.

[수식 단계]
- 하단 해설에 `6 - 5 = 1(개)` 형태의 수식이 보입니다.
- 이 수식은 문장 속 비교를 숫자로 옮겨 적은 형태로 보이며, 계산 과정을 한 번에 제시하는 방식입니다.
- 수식 앞뒤로는 화살표가 함께 있어, 해설 문장과 연결된 설명 흐름을 나타냅니다.

[등호별 변환 의미]
- `6 - 5 = 1(개)`에서 등호 왼쪽은 두 물병의 컵 개수 차이를 나타내는 식으로 보입니다.
- 등호 오른쪽의 `1(개)`는 그 차이를 수량 단위로 적은 표현입니다.
- 다만 이 문항은 변형식이 여러 단계로 이어진 전개가 아니라, 한 번의 계산 표현만 보입니다.

[세부 내용]
- 왼쪽 `가` 물병:
  - 아래에 연결된 컵 그림이 5개 나열되어 있습니다.
  - 물병 그림은 위가 좁고 아래가 둥근 병 형태이며, 윗부분에 파란색 뚜껑/마개처럼 보이는 요소가 있습니다.
- 오른쪽 `나` 물병:
  - 아래에 연결된 컵 그림이 6개 나열되어 있습니다.
  - 윗부분은 검은색 손잡이 또는 펌프처럼 보이는 구조가 추가되어 있습니다.
- 가운데 문장 `나 물병이 컵 1개만큼 물이 더 들어갑니다.`는 두 물병의 물 양 비교를 설명하는 문장입니다.
- 하단 `(해설)`에서는 각 물병이 몇 컵인지 다시 적고, 그 차이를 수식으로 정리한 뒤 문장으로 결론을 이어 갑니다.
- 좌하단의 `○`, `×`는 정답 선택용 표시로 보이며, 실제 동그라미 선택칸은 `(정답)` 옆에 따로 있습니다.

[Python DSL 작성 힌트]
- 전체 레이아웃을 상단 문제문구 / 중앙 그림 비교 / 하단 선택지와 해설의 3구역으로 나누어 배치하면 좋습니다.
- 중앙에는 좌우 대칭에 가깝게 물병 2개를 배치하고, 각 물병 오른쪽에 화살표를 둔 뒤 컵 아이콘들을 가로로 연속 배치하세요.
- 각 물병 내부에는 `가`, `나` 텍스트를 중앙 정렬로 넣는 구성이 적절합니다.
- 컵 아이콘은 동일 크기 반복 요소로 처리하고, 왼쪽은 5개, 오른쪽은 6개로 개수를 달리해 표현하면 됩니다.
- 하단 해설은 일반 문장과 수식 `6 - 5 = 1(개)`를 분리해 배치하면 원문 구조를 따르기 쉽습니다.
- 선택지 표시는 `○`, `×`를 세로로 배치하고, `(정답)` 텍스트 옆에 빈 원형 선택칸을 두는 형태로 구현할 수 있습니다.

[주의할 점]
- 정답이 `○`인지 `×`인지 이 시각 분석 단계에서는 추론해서 쓰지 마세요.
- `가`, `나`의 상대적 의미를 단정적으로 해석하지 말고, 그림과 문장에 보이는 그대로만 기술하세요.
- 물병 안의 정확한 물 높이나 내부 세부 구조는 그림 스타일상 단순화되어 있으므로 과도하게 의미를 부여하지 마세요.
- 컵의 개수는 그림에서 보이는 수를 기준으로 적되, 해설 문장을 그대로 옮길 때는 텍스트에 적힌 수를 따로 분리해서 적는 것이 안전합니다.

[불확실한 부분]
- 왼쪽 물병 윗부분의 파란 요소는 뚜껑인지 손잡이인지 그림만으로는 단정하기 어렵습니다.
- 오른쪽 물병 윗부분의 검은 요소도 펌프 장치처럼 보이지만 정확한 명칭은 확실하지 않습니다.
- 좌하단의 `○`, `×` 표시는 선택지인지 예시 표시인지 문맥상 선택지로 보이지만, 배치상 약간 독립적으로 놓여 있어 구현 시 정확한 위치 관계를 다시 확인하는 것이 좋습니다.
- `(정답)` 옆의 빈 원이 실제 답안 선택칸인지, 인쇄용 표시인지 이미지 해상도만으로는 완전히 확정하기 어렵습니다.
